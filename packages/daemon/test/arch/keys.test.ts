import { describe, it } from 'vitest';
// M1-T1 骨架期：占位让 arch 目录存在；分层与命名规则（barrel / ALLOWED_IMPORTS / kebab-case）由后续任务实装。
describe('arch', () => {
	it.todo('layering + no-barrel checks get wired in later M1 tasks');
});
