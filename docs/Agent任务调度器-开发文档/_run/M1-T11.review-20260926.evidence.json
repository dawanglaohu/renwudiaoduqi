{
  "scope": "task-contract",
  "tasks": {
    "M1-T11": {
      "contractHash": "0c7bdc162ec852eff7a351c351cb4ba882a3a037559261ab1d379dfbb86da3b9",
      "evidence": [
        "AC 1: package.json:10 builds Web before Vitest; e2e/smoke.test.ts:126 launches bootstrap.mjs with a temporary data directory and random port, captures stdout, stderr, and exit result; step 2 claims pairing-code.txt through the real HTTP route.",
        "AC 2: e2e/smoke.test.ts:520 uses Playwright Chromium on the production pairing form, enters host and code, then checks navigation and computed styles.",
        "AC 3: e2e/smoke.test.ts:337 creates an isolated fixture Git repository; step 4 waits for the imported document in the server snapshot before reloading the browser; step 5 dispatches the first task through a real worktree and fake-agent process and asserts a live SSE chunk appears in the DOM while a disconnected browser does not receive it.",
        "AC 4: e2e/smoke.test.ts:802 scans production source for test hooks and E2E source for spawnManaged replacement; the dispatched run uses the ordinary agent registry and process path.",
        "AC 5: e2e/smoke.test.ts:385 and 399 persist sanitized setup and step failure evidence; .github/workflows/desktop-ci.yml:335 runs Ubuntu E2E with artifact upload and release gate dependency; biome.json:46 excludes e2e from workspace check.",
        "This smoke verifies live SSE and a broken-connection negative control (E-10), one isolated dispatch worktree (E-31), imported-task rendering (E-108), the real agent process path (E-135), Node 22 bootstrap use (E-139), bundled styles and fonts (E-159/E-175), pairing bootstrap (E-226), and the Ubuntu release gate (E-265). Offline replay, concurrency limits, review permissions, and old-Node rejection remain covered by their owning prerequisite tests. pnpm -w check passed 202 files and 2490 tests after merging main. Local pnpm -w e2e passed 7/7 before the snapshot timing repair; the next local attempt could not start because another worktree's live daemon held the machine lock, so final-head E2E requires Ubuntu CI confirmation."
      ],
      "verdict": "pass"
    }
  }
}
