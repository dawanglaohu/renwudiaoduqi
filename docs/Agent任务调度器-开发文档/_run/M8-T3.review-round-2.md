REVIEW-ROUND 2

VERDICT: pass

第 2 轮只核第 1 轮的三条返工条目，不新增无关阻断项。

## 逐条复核

- R1 接线缺失 → 已修。`boot/container.ts:305` 装配 `DispatchService` 并放进 `services.dispatch`；`http/plugins/50-routes.ts:48` 调用 `registerBatchesRoutes`/`registerSnapshotRoute`，并把本任务 7 条端点补进 `customRegisteredPaths`；`container.ts` 的 `jobs` 由恒空改为 `[schedulerTickJob]`，`main.ts:114` 在 `listen()` 之后逐个 `start()`。回归用例 `test/unit/dispatch-contract.test.ts:819` 走真实 `createContainer` + `createHttpServer` + `inject`，断言 `container.jobs` 含 `scheduler-tick`、`services.dispatch` 存在、两个端点均非 501 且响应 message 不是占位句。
  另用一次性探针（真实容器 + 真实 server + `onRoute` 钩子）对「注册的 method+path 集合 === routes.ts 常量表」做了全量比对：**missing=[] extra=[]**，说明第 1 轮担心的「被未实现占位吞掉」在运行期已彻底消失。探针文件未入库。

- R2 跨层 SQL → 已修。runs 表的 `INSERT/SELECT/UPDATE`、预编译语句（`repo/runs.ts:192-199` 工厂闭包内 prepare 一次复用）与 Row/DTO 映射整体落到 `packages/daemon/src/repo/runs.ts`；`service/dispatch.ts` 现零 SQL 字面量、零 `db.prepare`；快照 INSERT 与 run INSERT 收进同一个 `unitOfWork.run`（`:333` 同步闭包，`:359-363` 调用），`bus.publish` 在 `:401` 的 `run()` 返回之后；并发回退改判 code（`dispatch.ts:158` → `repo/runs.ts:316` 判 `SQLITE_CONSTRAINT`/`_UNIQUE`/`_PRIMARYKEY`），`err.message` 匹配已删除。新增 `test/arch/service-sql.test.ts:10` 扫 `service/` 全目录断言无 SQL 关键字与 `db.prepare`。

- R3 agent 硬编码 → 已修。`dispatch.ts:228/235/243` 三个取值函数都走注入的注册表与可用性，硬编码 `'codex'` 已删除；tick 在 `:817-825` 逐候选取 `agentId`，取不到或不可用即 `tasksBlocked` 且 `reason='agent_unavailable'`，不落 run；`createRun` 在 `:306` 对不可用 agent 抛已登记的 `E_AGENT_UNAVAILABLE`；`agentLimitFor`（`:251`）改由注入的 `agentLimits` 或注册表 `concurrencyLimit` 推导，不再散落魔法默认值。回归用例 `dispatch-contract.test.ts:737`：空注册表下 tick 产出 0 条 run 且任务出现在 `tasksBlocked`。

## 本轮检查

- `tsc -b --noEmit` 退出 0；`biome ci .` 346 文件无问题（先清掉两个未跟踪测试残留 `test/fixtures/agents.json`、`test/fixtures/pairing-code.txt`，它们是 Windows 上既有 pairing 0600 断言失败与 agent 探测用例留下的产物，非本层文件，未入库）。
- 最小充分集：`dispatch-contract.test.ts` + `arch/` + `dispatch-snapshots` + `source-contracts` + `path-clash` + `concurrency` → 114 例中 112 通过，2 例失败均为 `arch/routes.test.ts` 的 Windows POSIX 0600 环境性断言（与 main 同源，非本层引入）。
- 全仓 `vitest run`：79 失败 / 1163 通过，失败文件与第 1 轮完全同源（probe、sse、landing、agents-service、http-plugins、devices、dsh-adapter、documents-routes、arch/routes、agents-routes、daemon-runtime、pairing-routes、run-log-routes、disk-full、events-runtime），本层 10 个改动文件无一在列。

## 范围

返工新增的 `repo/runs.ts` 与 `test/arch/service-sql.test.ts` 是 R2 直接要求的产物（runs 表仓储按 08 节只能放 `repo/`），`boot/container.ts` 与 `http/plugins/50-routes.ts` 由第 1 轮文档补丁补入有效范围，未越界。

## 回填

`图谱/任务/M8-T3.md` 的「代码位置」与「实施沉淀」已按 R1/R2/R3 后的新行号重写；第 1 段不再把幂等防重说成纯应用层逻辑（改为「UNIQUE 约束是最终兜底 + 应用层避免把冲突当正常路径」），新增第 4 段记接线与 agent 取值的坑。行号抽查一致。

## FOLLOW_UP（不阻断落地）

- `test/arch/layering.test.ts` 全仓仍不存在（只在 `02-设计/08-后端架构.md:6` 被引用），分层禁令至今没有机器断言；本轮的 `service-sql.test.ts` 只补上了「service 无 SQL」这一条，建议单开任务补齐 `ALLOWED_IMPORTS` 邻接表。
- `service/dispatch.ts` 的 tick 仍直接写批次 `done`，与 M8-T6 的 `transitionBatch()` 唯一写入口并存；`batch_wrapups` 表与 clean/fixed 有效裁定由 M8-T6 引入，请它落地时收口（E-281、E-49 的完整语义）。
