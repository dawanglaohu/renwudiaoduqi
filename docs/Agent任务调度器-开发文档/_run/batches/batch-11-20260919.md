---
batch: 11
tasks: M7-T5, M7-T7, M8-T6, M9-T10, M9-T8, M9-T9
date: 2026-09-19
verdict: fixed
tests: pass
pr: https://github.com/dawanglaohu/renwudiaoduqi/pull/116
note: 修复返工 worktree、审查续接、批次收口、日志首屏与多流档位等接缝；下一批可在这些契约上继续接线。
---

## 交付了什么

第 11 批把返工承接、审查续接、批次收口、审批卡、虚拟日志和多流甲板组成可验证的整体。本次收口同时按接线注册表覆盖第 1–10 批已落地能力，复核 backendRoutes、container、frontendRoutes、buildPipeline、sharedTypes、eventKinds、migrations、jobs 与 shellBridge 九类入口。

## 测试

- `pnpm -w check`：154/154 测试文件通过，1839 通过、32 个平台条件跳过；49 个错误码一致性、深浅主题对比度、前端禁令、473 文件 Biome、TypeScript 全部通过。
- 聚焦回归覆盖：POST `/documents` 真导入、snapshot 已发布游标、批次 landed/HEAD 判定、人工与机器收口裁定共存、events 流报告提取、审查续接失败语义、返工提示词、真实 Git worktree reuse。
- 真 SQLite 迁移：0009 从既有数据升级无丢失，`foreign_key_check` 零行，新旧库 `sqlite_master` 一致。
- 等价端到端冒烟：Web 生产构建与真实 daemon 通过；真浏览器确认 Public Sans、`.flex` 规则、非透明首层背景和 `data-style-loaded=true`，真实一次性配对 claim 返回 200 并删除配对码文件。配对后尝试打开 `#/` 会被守卫拉回 `#/pair`，实证当前 main 尚缺 M9-T26 的启动令牌装载；该缺口已由 PR #115 承接。正式 `pnpm -w e2e` 仍由 M1-T11 承接。

## 发现与修复

- S1，跨批 M3-T4 ↔ M3-T2：文档接管未导入 tasks/batches；补齐生产装配与真库路由测试。
- S1，跨批 M8-T3 ↔ M2-T5：snapshot 使用 event_seq 预留水位；改为已发布事件游标，避免 SSE 续接跳跃。
- S1，M8-T6：landed 判定被更高 attempt 的 review 行遮蔽，人工 landed 也未统一计入；新增 `batch-landing.ts`，tick、trigger、读模型共用最新 implement 语义。
- S1，M8-T6 / E-274：收口报告从厂商 raw JSONL 解析；改为跨轮转段聚合归一化 assistant message 事件，无内容事件才回落 raw。
- S1，M8-T6 / E-288：`batch_wrapups.run_id` 单列唯一阻止同轮机器裁定与人工裁定共存；0009 改为 `(run_id, is_human_verdict)` 唯一并补升级测试。
- S1，M7-T7：续接行 attempt/parent/review_round 与真库约束不一致；修正轮次持久化与恢复失败行语义，并补真库覆盖。
- S1，M7-T7 / E-330：把人工 `run.aborted` 误判为上下文撑爆；改为归一化失败 state event，人工中止不自动新开会话。
- S2，M7-T5：内置返工规则与“不得 commit/push”互相矛盾；去掉重复提交叙事，保留单一约束段。
- S1，跨批 M5-T1 / E-277：reuse worktree 丢目录时会返回坏路径或创建新分支；精确清理登记并在原分支原路径重建。
- S2，M9-T8：首屏默认读日志头部；客户端显式 `direction=backward`，保证尾部窗口与实时追加连续。
- S2，M9-T8：SSE token delta 被逐片当成整行；增加未闭合行合并，仅换行时产生新日志行。
- S3，M9-T9：甲板内第二处裸宽度断点与粗指针套用手机规则；去除重复断点，手机行为只由 phone/phone-xs 档触发。
- 文档：回填 M9-T8、M9-T9 当前代码位置行号。

## 遗留

- M9-T8 的实时段驱逐后重拉游标、按 index 保存展开态、进度行识别过宽、折叠历史清洗、截断 payload 与“系统程序打开原文件”执行端均为 S3；不阻断本批，后续应由日志/壳接线任务定点承接。
- M9-T9 的 afterBody/审批双槽、五段外壳 grid token、若干无效 utility 与运行时重挂测试为 S3；M9-T21/M9-T25 接线时复核。
- 已有明确任务承接而未在本批提前实现：M9-T25 路由装配、M9-T26 SSE/壳启动接线、M10-T6 真壳启动、M8-T10 真 spawn、M7-T9 审查与 jobs 容器装配、M2-T7/M2-T8 路由与契约收紧、M1-T11 正式端到端冒烟。这些不构成 `open` verdict。

## 给下一批的提醒

第 12 批及后续接线必须继续消费共享契约与已有 service，不得在容器、前端或壳内重造状态枚举、游标或任务判定；收口 landed 语义统一走 `batch-landing.ts`，报告文本统一走归一化事件流。
