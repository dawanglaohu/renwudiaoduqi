---
batch: 16
tasks: M9-T22
date: 2026-09-26
verdict: open
tests: fail
pr: https://github.com/dawanglaohu/renwudiaoduqi/pull/160
note: 已修旧行校验与双编辑器覆盖；真实浏览器、通用缓存及旧两键文档仍需返工。main 未含第 15 批收口记录，第 17 批不得按本记录解锁。
repair_schema: 1
---

## 交付了什么

M9-T22 将三个闸门右侧的两个流水线开关、共享二段原语及第八条设置路由接入正式页面。前端从 daemon 获取四键设置，以完整四键提交，最终选择由事件回流决定。本次收口在主干 `3af6f1f3` 上逐条复核，修复旧设置行的未知字段绕过，以及桌面顶栏、设置页两个编辑器独立持有待写状态造成的选择覆盖。未完成真实浏览器与架构缓存验收，不宣称本批干净。

## 测试

- `pnpm install --frozen-lockfile` 成功，Node 22.17.0、pnpm 10.18.3。
- 第一次 `pnpm -w check` 与 Web 构建并发，27 例因读取时 `packages/web/dist` 尚不存在而失败；197 文件通过、2485 例通过、4 例平台跳过。先保留失败，不当作产品 bug；构建完成后的串行原命令通过。
- `pnpm -w e2e` 的 Web 构建成功，真实 daemon 在 `e2e/smoke.test.ts:372` 的启动步骤失败：`C:\ProgramData\agent-scheduler\daemon.lock` 需要 Administrators/SYSTEM-only ACL，当前 Windows 进程不是管理员；套件 beforeAll 失败、7 条主流程未执行。未改系统 ACL、未用 mock 锁或测试钩子替代，未生成本轮真实服务 GIF。
- B1 修前：`pnpm exec vitest run packages/daemon/test/unit/pipeline-settings.test.ts -t "rejects a legacy-looking row"` 失败，实际返回 bughunt=1、manual，而期望四键默认。修后同文件 7/7，通过合法旧行不改库、非法行整体回默认并 warn。
- B2 修前：临时挂载与正式桌面设置路由相同的两个容器，先在顶栏开启查 bug，再在 PATCH 挂起期间在设置页切手动；断言只发一次请求，实际发两次。失败探针随后撤下，修复后保留双容器、恢复与生命周期回归。
- 修后相邻检查：`pnpm exec tsc -b --noEmit`；Vitest 选择 `pipeline-toggles.test.tsx`、`routes.test.ts`、`check-forbidden.test.ts`、`connection-store.test.ts`、`cache-invalidation.test.ts`、daemon `pipeline-settings.test.ts`、`pipeline-lanes.test.ts`，7 文件 97/97。随后新增恢复 GET 与新写入交错回归。
- 最终 `pnpm -w check`：错误码、对比度、禁令、Biome、TypeScript 全通过；203/203 文件，2517 例通过、4 例按平台条件跳过（2521 例）。`git diff --check` 通过。真实浏览器测试失败仍单独保留，因此 `tests: fail`。一次与额外 Python 生成器测试并发的中间全量运行发生 6 个 Vitest worker RPC 超时；取消长时间无输出的额外 Python 运行后，独占原命令全绿。未改生成器脚本，`--batches` 构建成功。
- `finesse-ui audit` 只作只读检查：共享二段原语有 focus-visible、null、disabled、pending、active/inactive 状态，颜色使用 token；对比度机检通过。未启动正式页面，手机 320/375/414/768 布局、实际焦点顺序与 GIF 未验收，归 B3。

## 逐任务复核

- AC1：`packages/web/src/ui/segmented-toggle.tsx:32` 提供受控原语，高度见 `:69`；闸门与流水线展示分别从 `packages/web/src/components/gate-toggles.tsx:18`、`pipeline-toggles.tsx:19` 导入；`packages/web/src/app/app.tsx:60` 在闸门右侧放开关，`:62` 的桌面分隔线为 1px；`packages/web/scripts/check-forbidden.ts:513`、`:523` 禁互相 import，正反例由 check-forbidden 测试通过。真实 computed style 仍欠 B3。
- AC2 / E-26：`packages/web/src/ui/segmented-toggle.tsx:45` 缺值显示不可点「—」；`packages/web/src/components/pipeline-toggles.tsx:85`、`:94` 发出两值；`packages/web/src/features/run-deck/use-pipeline-settings.ts:124` 的共享源提供值及 pending，`:216` 提交缓存四键。HTTP 响应不用于乐观翻转；`:172` 的回流同时更新全部挂载者。B2 修前两个挂载者各自 pending 不符合本条，现已修。
- AC3 / E-306、E-312：`packages/web/src/components/pipeline-toggles.tsx:63` 两条说明随值出现，设置页 inline，顶栏 portal 到独立宿主；`packages/web/src/app/app.tsx:71` 为说明区。无 dialog。
- AC4：`packages/web/src/app/routes.tsx:16` 登记第八条路径；`packages/web/src/app/app.tsx:27` lazy；`packages/web/src/app/routes.tsx:519` 共享 Suspense；`packages/web/src/pages/settings-pipeline-page.tsx:33` 只装配同一 settings 容器，容器 `:64` 显示来源句。顶栏 `app.tsx:56` 的 CSS 在手机档隐藏五开关，但实际 DOM 仍挂载它们；手机宽度的真实呈现与触摸验收归 B3。
- AC5：`packages/web/src/features/run-deck/use-pipeline-settings.ts:74` 按 details.stage 规范化错误，`pipeline-toggles-container.tsx:81` 用 InlineNotice 就地呈现，无 toast；pipeline-toggles 测试覆盖该分支。
- AC6：pipeline-toggles、routes、check-forbidden 新旧用例全部通过，新增 B2 回归在 `packages/web/test/pipeline-toggles.test.tsx:172`、`:252`、`:277`。
- E-157：`use-pipeline-settings.ts:178` 按事件更新，`:236` 等 HTTP 与匹配本设备四键回流；重连后丢事件只用新的 daemon GET 恢复。`packages/web/test/pipeline-toggles.test.tsx:425` 起的四键交互及后续迟到 GET、多设备事件测试通过。
- E-306 接缝：`packages/daemon/src/service/review.ts:2668` 在审查 pass 时读当前设置，仅尚未派查 bug 时走 `:2690`；`packages/daemon/src/service/settings.ts:236` 写 pipeline，`:240` 发布完整事件；流水线集成用例验证已建查 bug 行、结果、槽位与归档。开关不计算阶段链，前置层通过实际 run 列表决定节点。
- E-312 接缝：`packages/daemon/src/service/dispatch.ts:1325` 手动模式不自动 trigger；`packages/daemon/src/service/wrapup.ts:360` 为自动入口第二道拒绝，`:362` 返回阶段错误；pipeline-lanes 第 ⑫ 用例实际验证 manual 无运行、canWrapup 为 true、切 auto 下一 tick 派发和重启快照稳定。
- E-318 / E-356 接缝：`packages/daemon/src/domain/pipeline-settings.ts:7` 四键默认，`:143` 读侧不写库；`:164` 只允许恰两键旧行补默认；`packages/shared/src/api/settings.ts:92` 禁多键、`:93` required 四项；`packages/daemon/src/service/settings.ts:168` 注册表快照查 agent，`:190`、`:217` 限制不支持 effort 的家；`:236` 事务写入后 `:240` 发布完整事件并 nudge。前端补全两项指派设置，不自行计算取值链。旧文档两键例子归 B5。
- 五类接缝：shared 类型/schema 与 HTTP、daemon、展示的字段一致；共同边界的前端说明/事件权威与后端阶段判定分工一致；错误仍走 shared codes 和 inline notice；B2 验证缓存/待写并发，B1 验证存储读侧；真实入口主流程未走到，归 B3。通用缓存登记与失效的指定实现缺失，归 B4。

## 发现与修复

- B1 [S2 功能错] 涉及 M9-T22、M8-T8、M8-T9（跨批）：旧两键行附未知字段仍保留非默认开关 → `{"bughunt":1,"wrapupMode":"manual","unexpected":true}` 读出开启/手动且不 warn → 旧行兼容条件多了宽松 OR，绕过严格四键校验 → `packages/daemon/src/domain/pipeline-settings.ts:164`；修复提交 `2b24359`，回归 `packages/daemon/test/unit/pipeline-settings.test.ts:117`。
- B2 [S2 功能错] 涉及 M9-T22：桌面设置页的两个编辑器能互相覆盖选择 → 顶栏 bughunt=1 PATCH 挂起，再页内 wrapupMode=manual，实际提交两次且第二次带 bughunt=0 → hook 的本地缓存/待写锁每挂载一份 → `packages/web/src/features/run-deck/use-pipeline-settings.ts:124`；修复提交 `a48ccdf`，默认共享源和全挂载待写锁；补强提交 `6454951` 防迟到恢复 GET 在远端事件之后解锁，回归 `packages/web/test/pipeline-toggles.test.tsx:172`、`:307`。
- B3 [S2 验收缺口] 涉及 M9-T22、M1-T11（跨批）：本轮正式页面、开关回流与 GUI 修复 GIF 尚未验证 → `pnpm -w e2e` 套件启动失败、7 条未执行 → 当前 Windows 进程无机器级实例锁所需权限，现有通用套件也没有 M9-T22 专属组合断言 → `e2e/smoke.test.ts:372`、`packages/daemon/src/platform/lock.ts:54`。
- B4 [S2 功能错] 涉及 M9-T22、M9-T21、M9-T19（跨批）：通用缓存/断线重取契约未接入 → resource-cache.ts 不存在，生产代码没有失效表调用者，settings.pipeline_changed 查表得空集合 → 源架构规定与落地的缓存接线不一致；局部共享源不能证明指定通用缓存已完成 → `docs/Agent任务调度器-开发文档/02-设计/07-前端架构.md:267`、`packages/web/src/api/cache-invalidation.ts:17`。
- B5 [S3 体验与边角] 涉及 M9-T22、M8-T8、M8-T9（跨批）：旧两键文档与现行四键契约矛盾 → 10 节 GET 示例只有两键、E-318 仍写 PATCH 两字段，与 shared schema required 四项冲突 → E-356 升级后局部文档未同步 → `docs/Agent任务调度器-开发文档/02-设计/10-接口约定.md:273`、`docs/Agent任务调度器-开发文档/02-设计/13-边界问题与异常处理.md` 的 E-318 条目（图谱对应 `图谱/边界/E-318.md:13`）。

## 遗留

B3 未修：需要有权限的正式 daemon 测试环境与本批专属真浏览器回归，不能以本轮单测代替 GUI；本分支不改系统安全配置。B4 按 Jev 采纳 A 列独立范围，B5 属 doc-issue，按收口提示词不直接改源设计定义。三项均有 task 围栏，保持 open。main 尚未包含第 15 批收口记录；此处只提醒，不伪造上一批记录或关闭第 17 批闸门。

## 返工任务

```task
stable-key: batch16-pipeline-real-browser
title: 用正式 daemon 与真浏览器验证流水线设置并留修复 GIF
module: M1
source-tasks: M9-T22
depends-on: M9-T22, M1-T11
input: B3；本轮 pnpm -w e2e 在非管理员 Windows 上于机器级实例锁 ACL 阶段失败，7 条未执行。共享 pipeline 源修复已通过 2517 例全量检查，但没有本轮正式页面与修复 GIF。
output: e2e/pipeline-settings.test.ts、vitest.e2e.config.ts、.github/workflows/desktop-ci.yml、第 16 批收口记录与真实 GIF 证据
acceptance: 1) 在符合生产锁权限要求且隔离数据的环境中，从本任务 HEAD 真起 daemon 与生产 Web，真浏览器通过配对页面领取设备身份，无 mock 锁、HTTP 拦截或 sessionStorage 测试注入；2) 在同一次运行中验证 token computed style、桌面开关位置与 1px 分隔线、两条常驻说明、settings 路由 lazy 与来源句，320/375/414/768 窄屏无溢出且手机只在设置页可切；3) 操作顶栏和页内编辑器，断言 pending 同步、第二次写入不携旧四键、PATCH 响应不乐观翻转、真实 SSE 确认后两处一致，另一真实设备更新及断线恢复仍正确；4) 保留 null、错误与恢复用例的客观证据，录制状态驱动 GIF 标明服务 HEAD、origin 和覆盖范围，并接入正式 e2e CI；5) pnpm -w check、pnpm -w e2e 通过，真实页面、运行流程与修复 GIF 都复核后再写新收口记录。
edges: E-157, E-26, E-306, E-312, E-318, E-356, E-159, E-175, E-265
paths: e2e/pipeline-settings.test.ts, vitest.e2e.config.ts, .github/workflows/desktop-ci.yml, docs/Agent任务调度器-开发文档/_run/batches/batch-16-20260926.md
estimate: 1d
severity: S2
jev: model=jev-1.13.0 line=adjudicate: pick=A(low 0.39) spec_change=no(0.24) outside_stack=no(0.14) needs_owner=no(0.09) reversible=yes(0.77) adopt=take(A)
```

```task
stable-key: batch16-pipeline-resource-cache
title: 将流水线设置接入共享资源缓存与事件失效
module: M9
source-tasks: M9-T22
depends-on: M9-T22, M9-T21, M9-T19
input: B4；07 节 267 行要求 read/invalidate/refetchAll，但 resource-cache.ts 缺失且失效表无生产调用者；已有共享 PipelineSettingsSource 修复双编辑器覆盖，须保留事件权威、四键与 pending 回归。
output: packages/web/src/api/resource-cache.ts、pipeline 源与事件/恢复接线、缓存及流水线回归测试
acceptance: 1) 按现有架构实现 read(key,fetcher)、invalidate(prefix)、refetchAll，共享同 key 在途 Promise 并登记最新 fetcher，无 set/put/patch、轮询或时间型 stale；2) 流水线 GET 用 settingsPipeline key，settings.pipeline_changed 在指定表映射到 settings 前缀，生产入口实际消费失效并重取，事件值不能被过时 GET 覆盖；3) SSE 重放失效与连接恢复从真实注册入口调用已登记的 fetcher，多个编辑器共用 cache/pending，注销后无事件与恢复注册残留；4) 增加真实消费入口测试、并发 read 去重、最新 fetcher、事件失效、恢复、双编辑器及四键保留回归，修前失败修后通过；5) pnpm -w check 与本批真浏览器设置回归通过。
edges: E-157, E-318, E-333, E-356
paths: packages/web/src/api/resource-cache.ts, packages/web/src/api/cache-invalidation.ts, packages/web/src/features/run-deck/use-pipeline-settings.ts, packages/web/test/resource-cache.test.ts, packages/web/test/cache-invalidation.test.ts, packages/web/test/pipeline-toggles.test.tsx
estimate: 1d
severity: S2
jev: model=jev-1.13.0 line=adjudicate: pick=A(high 0.90) spec_change=no(0.35) outside_stack=no(0.12) needs_owner=no(0.24) reversible=yes(0.72) adopt=take(A)
```

```task
stable-key: batch16-pipeline-four-key-docs
title: 同步旧两键流水线文档与现行四键契约
module: M8
source-tasks: M9-T22
depends-on: M9-T22, M8-T9
input: B5 doc-issue；现有 E-356、shared schema 和生产端都是四键，10 节 GET 示例与 E-318 PATCH 描述仍为两键。必须保留只读兼容合法旧两键行及非法形状整体回默认，不降低验收。
output: 接口与边界源文档的局部维护补丁、生成契约和图谱、变化条款复核证据
acceptance: 1) 走 maintain_docs begin/sync/verify 的局部维护补丁，将 GET/PATCH、默认值、事件和字段说明统一为现行四键；2) 明确两键只适用于合法旧数据库行的读侧兼容，PATCH 缺任一四键必为 E_VALIDATION，坏形状 warn 且不改库；3) 对照 shared schema、service、旧行回归及真实 GET/PATCH 逐条登记变化证据，不改 E-356 硬验收；4) build_vault/build_docs 产物一致，所有受影响已落地任务待复验按规定处理，生成器单测与 pnpm -w check 通过。
edges: E-318, E-356
paths: docs/Agent任务调度器-开发文档/02-设计/10-接口约定.md, docs/Agent任务调度器-开发文档/02-设计/13-边界问题与异常处理.md, docs/Agent任务调度器-开发文档/_run/task-contracts.json, docs/Agent任务调度器-开发文档/图谱/边界/E-318.md
estimate: 0.5d
severity: S3
jev: model=jev-1.13.0 line=adjudicate: pick=A(high 0.90) spec_change=no(0.35) outside_stack=no(0.12) needs_owner=no(0.24) reversible=yes(0.72) adopt=take(A)
```

## 自行裁决

1. 冲突：main 已落地本批但无第 15 批记录，本地真实 daemon 锁权限失败。候选 A：继续审查、提交 open 及明确返工；B：仅保留本地修复，等前批与测试环境；C：提交 fixed 并只写限制。Jev `jev-1.13.0`：`adjudicate: pick=A(low 0.39) spec_change=no(0.24) outside_stack=no(0.14) needs_owner=no(0.09) reversible=yes(0.77) adopt=take(A)`。已采纳 A，B3 和 open 记录留实据；低置信保留，不擅写 fixed。
2. 冲突：双编辑器会写旧四键。候选 A：仅修旧行，另派共享状态任务；B：本次实现共享状态与连接恢复、双容器回归，仍等真浏览器；C：设置页隐藏顶栏编辑器并改路由装配。Jev `jev-1.13.0`：`adjudicate: pick=B(low 0.22) spec_change=no(0.27) outside_stack=no(0.23) needs_owner=no(0.10) reversible=yes(0.66) adopt=take(B)`。已采纳 B，提交 `a48ccdf`、`6454951`；低置信风险为仍需正式 GUI 验证。
3. 冲突：指定通用 resource-cache 缺失、失效表没接，旧示例与四键不一致。候选 A：保留局部共享源，另派通用缓存与局部文档补丁两任务；B：本批改全部缓存消费者并同步文档；C：删除通用缓存要求并接受局部实现为最终架构。Jev `jev-1.13.0`：`adjudicate: pick=A(high 0.90) spec_change=no(0.35) outside_stack=no(0.12) needs_owner=no(0.24) reversible=yes(0.72) adopt=take(A)`。已采纳 A，登记 B4/B5，未改源定义。

## 给下一批的提醒

第 17 批保持上锁，先完成 R16 真实浏览器、缓存接线和四键文档返工，再重新收口。M9-T23 新编辑器必须复用共享流水线源，不能另建独立 pending 或用旧四键覆盖；第 15 批收口记录也需由其原会话提交。
