---
batch: 8
tasks: M10-T4, M4-T14, M6-T10, M6-T4, M6-T7, M6-T9, M7-T2, M8-T10, M8-T5, M9-T5
date: 2026-09-22
verdict: open
tests: pass
pr: https://github.com/dawanglaohu/renwudiaoduqi/pull/124
note: E-176 已按 Jev 裁决改成文档补丁闭合；E-133 与 E-36/E-348 由返工任务接线，接线前不要把「临时提升」「派发失败·模型无效」当成已可达的功能；真浏览器冒烟仍归 M1-T11、生产配对入口归 M9-T27。
repair_schema: 1
---

## 交付了什么

本记录是 unattended-run 1.6.0 回灌后对第 8 批的「再收口一次」：不重跑 S1–S6，不改 19 节，只用新格式重收，把 `batch-8-20260920.md` 里仍成立的遗留变成可派发的返工任务；旧记录一个字节不改。第 8 批本身交付的内容不变：两端版本校验与共享 Web 产物（M10-T4）、实时模型清单来源与思考强度取值域（M4-T14）、任务终态会话归档与不复用断言（M6-T10）、停滞检测（M6-T4）、自动模式提问处置（M6-T7）、会话保留与全文查找（M6-T9）、首轮审查派发与 diff 裁剪（M7-T2）、真实子进程派发与事件链（M8-T10）、重跑／换 agent 重派（M8-T5）、自写 SSE 客户端（M9-T5）。2026-09-20 收口已修的 B1／B2（`POST /runs/:id/rerun` 不拉起进程、零产出后常规 `POST /runs` 返回旧 `awaiting_human` 行；提交 `c45f598`、`77d9027`）本次在 main `7333b35` 上复核仍在，不重复计数。

## 测试

- `pass` — WSL（Ubuntu 20.04，node 22，pnpm 9）工作树 `../agent-scheduler-batch-8`，先 `pnpm --filter @agent-scheduler/web build`（退出码 0，产物 `packages/web/dist/`），再 `pnpm -w check`：check-error-codes 49 个错误码与 codes.ts 一致、对比度与 token 对齐、禁令扫描、Biome、`tsc -b --noEmit`、Vitest 全仓 `161` 个测试文件通过、`1890` 个测试通过、`32` 个按平台条件跳过、退出码 0。
- 说明：未先构建 Web 产物直接跑 `pnpm -w check` 时有 14 个文件、52 个用例失败（`static-plugin.test.ts`、`sse.test.ts` 等要求 `packages/web/dist` 存在），构建后同一份代码全绿，属环境前置不是回归；与 2026-09-20 记录的做法一致。
- `pass` — 文档链：`maintain_docs.py status` `stale=[]`、`runtimeDrift=[]`，`review.json` 0 BLOCK；补丁 `e176-font-weights-20260922` 七个受影响契约（M1-T11、M10-T4、M10-T5、M10-T6、M9-T24、M9-T26、M9-T27）逐个 `verify`，四个已落地任务（M10-T4、M10-T5、M9-T24、M9-T26）另按复验流程 `verify` 后 `--landed`，`maintenance.js` 的 `needsReview` 为空。
- `skipped` — 17 节真浏览器端到端冒烟：仓库仍无根 `e2e/`、`vitest.e2e.config.ts`、根 `e2e` 脚本与 `playwright` 依赖（`package.json` 只有 `check` / `test`），归 M1-T11；生产配对入口冒烟归 M9-T27。未用 fixture 或 mock 冒充。

## 发现与修复

- DOC_PATCH，涉及 M10-T4 / E-176：13 节 E-176「常规 + 一个强调档」与 11 节字体表「400 / 500 / 600」矛盾，产物与断言遵循 11 节（`packages/web/src/styles/fonts.css:5-58` 六条 `@font-face`、`packages/web/public/fonts/` 六个 woff2；`font-medium` 47 处、`font-semibold` 88 处都在用）。Jev adjudicate 取 A（只改文档）：`03-质量/13-边界问题与异常处理.md:189` 改为「以 11 节字体表定的字重集合为准，当前两族各 400 / 500 / 600 三档」，补丁 `_run/patches/e176-font-weights-20260922.json` 已 `begin → sync → verify` 至 `verified`，代码与测试不动。本条随本收口 PR 提交。
- 复核旧遗留 E-133（M6-T7）：main 上仍成立——`packages/shared/src/api/runs.ts:138,159` 的 `CreateRunMessageBody` 与 JSON schema 只收 `reply / approve / deny`，`packages/daemon/src/service/message.ts:316` 虽收 `elevate_once` 却不调 `RunService.elevateRunOnce()`（`packages/daemon/src/service/run.ts:1036`），前端无入口；未被后续提交解决，进返工任务。
- 复核旧遗留 E-36／E-348（M8-T5）：main 上仍成立——`packages/daemon/src/service/rerun.ts:570` 的 `handleModelInvalid()` 生产代码零调用，`E_MODEL_INVALID` 只在 `packages/shared/src/api/routes.ts` 的 `POST /api/v1/runs` 错误列表与 10 节错误码表出现、无抛出点；E-348 又禁止从 stderr 推断；未被后续提交解决，进返工任务。
- 复核旧遗留「真浏览器冒烟缺失」：仍缺，已由 19 节原任务 M1-T11 完整覆盖；2026-09-21 主干发现的生产配对入口退化（`#/pair` 只显示 hash 文本）已由 19 节原任务 M9-T27 覆盖。两者都不写围栏。

## 遗留

- M1-T11（未落地）：真 daemon + 真浏览器 + 样式 computed style + 派发主流程冒烟；本批仍无 GIF 与 320px 真实渲染证据，不冒充。
- M9-T27（未落地）：生产装配缺 `pair` 路由的定点修复与独立配对冒烟。
- 返工任务两条见下；E-176 已闭合。

## 返工任务

```task
stable-key: e133-elevate-once-entry
title: 接通 E-133「仅本次运行临时提升」入口：POST /runs/:id/messages 接受 kind=elevate_once 并在运行详情「权限受阻」行提供一次性按钮
module: M6
source-tasks: M6-T7
depends-on: M6-T7, M6-T6, M9-T8
input: 复现：main 7333b35 上 `grep -rn elevate_once packages` 只命中 repo 类型、service/message.ts:316 的 validKinds 与 run.ts:1036 的 elevateRunOnce；shared `CreateRunMessageBody`（packages/shared/src/api/runs.ts:136-160）与真实路由 packages/daemon/src/http/routes/runs.ts 的 schema 只允许 reply／approve／deny 且 text 必填，MessageService 对任何 kind 都不调 elevateRunOnce，packages/web/src 无「临时提升」控件；09 节 run_messages.kind 含 elevate_once 而 10 节 `POST /runs/:id/messages` 未列。Jev adjudicate 取 A：复用 messages 端点，09 节不动、10 节补契约。相关：M6-T7 验收 3、E-133、run.permission_blocked 事件（packages/shared/src/api/events.ts:36）、M9-T8 的日志窗口（packages/web/src/features/run-detail/）。
output: shared 类型与 JSON schema 新增 kind `elevate_once`（仅该 kind 下 text 可空）、daemon 路由与 MessageService 把 elevate_once 分派到 RunService.elevateRunOnce()、10 节接口表补该 kind 的语义与错误码、运行详情里 run.permission_blocked 行高亮并带一次性「仅本次运行临时提升」按钮、对应单元／集成／前端测试；文档改动按 doc-maintenance 的 begin／sync／verify 走补丁。
acceptance: 1) `POST /api/v1/runs/:runId/messages` body `{kind:'elevate_once'}`（text 缺省或空串）在运行处于 awaiting_reply 或 running 时返回 200 且 `RunService.isTemporarilyElevated(runId)` 为 true、运行迁 running 并留事件；text 对 reply／approve／deny 仍必填，schema `additionalProperties:false` 不变（真实 Fastify 集成测试 packages/daemon/test/integration/run-messages-elevate.test.ts 覆盖 200、非法状态 409 E_INVALID_STATE_TRANSITION、已归档 409 E_SESSION_ARCHIVED） 2) 提升只作用于本次运行：不写 agents.json、不改注册表默认档、运行结束后 isTemporarilyElevated 为 false，且 packages/daemon/test/unit/run-service.test.ts 现有 E-133 用例继续通过（E-133） 3) 10 节 `POST /runs/:id/messages` 行列出 `kind` 四值与 elevate_once 的 text 可空规则，09 节不改；`scripts/check-error-codes.mjs` 与 `packages/daemon/test/arch/routes.test.ts` 通过；补丁经 `maintain_docs.py begin/sync/verify` 登记并在同一 PR 提交 4) 运行详情的 run.permission_blocked 行以 needs 色高亮并显示「仅本次运行临时提升」按钮，点击恰发一次上述请求，成功后按钮变为「已临时提升」且不可再点，失败按 error.code 原样展示；packages/web/test/log-window.test.ts 用假 http-client 断言请求体与状态 5) `pnpm -w check` 通过；PR 附从真实 daemon 录的 GIF：权限受阻行出现 → 点按钮 → 运行继续（record-browser-gif）
edges: E-133
paths: packages/shared/src/api/runs.ts, packages/daemon/src/http/routes/runs.ts, packages/daemon/src/service/message.ts, packages/daemon/src/service/run.ts, packages/daemon/src/repo/run-messages-repo.ts, packages/daemon/test/unit/message-service.test.ts, packages/daemon/test/unit/run-service.test.ts, packages/daemon/test/integration/run-messages-elevate.test.ts, packages/web/src/features/run-detail/run-detail-container.tsx, packages/web/src/features/run-detail/use-log-window.ts, packages/web/src/api/http-client.ts, packages/web/test/log-window.test.ts, docs/Agent任务调度器-开发文档/02-设计/10-接口约定.md
estimate: 1d
severity: S2
jev: adjudicate pick=A(medium 0.73) spec_change=yes(0.71) outside_stack=no(0.08) needs_owner=no(0.14) reversible=unsure(0.63) adopt=take(A) model=jev-1.13.0 label=batch-8:wrapup:e133
```

```task
stable-key: e36-model-invalid-structured-signal
title: E-36 精确归因只认 agent 的结构化拒绝信号：新增归一化事件 run.model_rejected 并接通 handleModelInvalid，零产出退出仍走 E-348
module: M8
source-tasks: M8-T5
depends-on: M8-T5, M8-T10, M4-T8, M4-T14
input: 复现：main 7333b35 上 `grep -rn handleModelInvalid packages/daemon/src` 只命中 packages/daemon/src/service/rerun.ts:80,570,611 的定义与导出，无调用；`E_MODEL_INVALID` 只出现在 packages/shared/src/api/routes.ts 的 `POST /api/v1/runs` 错误列表、codes.ts 与 10 节错误码表，无抛出点。模型名无效时 agent 通常零产出退出，走 E-348 停靠 awaiting_human 并带固定提示句；E-348 明令判定只看事件 kind、不匹配 stderr／stdout 文本，因此不能靠字符串猜「模型无效」。Jev adjudicate 取 A（置信度 low，记风险）：只在 agent 通过自身协议给出结构化拒绝时精确归因，否则一律 E-348。相关：M8-T5 验收 6、E-36、E-348、codex map-events.ts 的 turn/failed 结构化 error 对象（packages/daemon/src/adapters/codex/map-events.ts:803-815）、06 节事件命名规则（本产品组 「〈scope〉.〈过去式〉」）。
output: shared EVENT_KINDS 新增 `run.model_rejected`（payload 固定 `{code:'model_invalid', modelName, vendorMessage}`）；各家 map-events.ts 只从厂商协议的结构化错误字段（如 codex JSON-RPC error 对象的 code／type）映射该事件，绝不从 stderr 或自由文本推断；RunService 收到该事件调用 RerunService.handleModelInvalid() 迁 failed 并释放泳道；13 节 E-36 与 19 节 M8-T5 验收 6 按裁决改写「仅在 agent 结构化报错时精确归因，否则按 E-348 转人」，10 节事件表补该 kind；`E_MODEL_INVALID` 要么有真实抛出点要么从 routes.ts 与 10 节错误码表同步移除；对应单元／集成／arch 测试。
acceptance: 1) `packages/shared/src/api/events.ts` 的 EVENT_KINDS 含 `run.model_rejected`（scope run、milestone true），10 节事件表同步，`packages/daemon/test/unit/events.test.ts` 的穷尽性断言通过 2) codex 适配器对录制的 `turn/failed` 结构化错误行（error 对象带模型拒绝的 code／type）产出恰一条 run.model_rejected，对同一内容只出现在自由文本 message 或 stderr 的行不产出；其他家只有厂商协议存在类型化错误字段时才映射，并在该家 capabilities.ts 声明 `reportsModelRejection`（packages/daemon/test/unit/codex-adapter.test.ts、claude 对应用例） 3) RunService 收到 run.model_rejected 后调用 handleModelInvalid：运行迁 failed、queued_reason 为「派发失败·模型无效」、tasks.lane_no 置 NULL 并发 lane.released、不计 rework_count、审批卡不再出现；同一运行随后退出不再走 E-348 的 exited→reviewing→awaiting_human（packages/daemon/test/integration/dispatch-spawn.test.ts 用假子进程覆盖「结构化拒绝→failed」与「纯零产出→awaiting_human 且无归因」两条路径）（E-36、E-348） 4) arch 测试断言 packages/daemon/src/service 与 jobs 下不存在对 stderrTail／stdout 文本做 model／invalid／not found 等正则或 includes 匹配的分支（E-348） 5) 13 节 E-36 行与 19 节 M8-T5 验收 6 改为「仅在 agent 结构化报错时精确标注，否则按 E-348 转人」，补丁经 `maintain_docs.py begin/sync/verify` 登记并同 PR 提交；`E_MODEL_INVALID` 有真实 throw 或从 routes.ts／10 节错误码表同步删除，`scripts/check-error-codes.mjs` 通过 6) `pnpm -w check` 通过；PR 回报里写明 Jev 置信度 low 的风险与实际验证过的 agent 家数
edges: E-36, E-348
paths: packages/shared/src/api/events.ts, packages/shared/src/api/routes.ts, packages/daemon/src/adapters/codex/map-events.ts, packages/daemon/src/adapters/codex/capabilities.ts, packages/daemon/src/adapters/claude/map-events.ts, packages/daemon/src/adapters/claude/capabilities.ts, packages/daemon/src/service/run.ts, packages/daemon/src/service/rerun.ts, packages/daemon/src/boot/container.ts, packages/daemon/test/unit/events.test.ts, packages/daemon/test/unit/codex-adapter.test.ts, packages/daemon/test/unit/rerun.test.ts, packages/daemon/test/unit/run-service.test.ts, packages/daemon/test/integration/dispatch-spawn.test.ts, packages/daemon/test/arch/dispatch-proc.test.ts, docs/Agent任务调度器-开发文档/02-设计/10-接口约定.md, docs/Agent任务调度器-开发文档/03-质量/13-边界问题与异常处理.md, docs/Agent任务调度器-开发文档/04-执行/19-模块任务拆分.md
estimate: 1.5d
severity: S2
jev: adjudicate pick=A(low 0.33) spec_change=yes(0.77) outside_stack=no(0.14) needs_owner=no(0.21) reversible=yes(0.74) adopt=take(A) model=jev-1.13.0 label=batch-8:wrapup:e36 confidence=low
```

## 给下一批的提醒

E-176 的字重口径现在以 11 节字体表为准，新增字重先改 11 节再改打包断言；返工任务落地前，`elevate_once` 与 `handleModelInvalid` 都还是不可达代码，别在 UI 或流水线里当成已有能力引用。真浏览器主流程证据只会来自 M1-T11，配对入口来自 M9-T27，第 9 批再收口时同样不要伪造。
