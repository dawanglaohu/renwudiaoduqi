---
batch: 12
tasks: M2-T7, M2-T8, M7-T8, M7-T9, M8-T7, M9-T11, M9-T12, M9-T16
date: 2026-09-23
verdict: open
tests: fail
pr: https://github.com/dawanglaohu/renwudiaoduqi/pull/135
note: 查 bug 阶段在正式容器里不可达；R12 返工闭环前不得将开启查 bug 的审查 pass 当作已完成。
repair_schema: 1
---

## 交付了什么

M2-T7 将文档刷新、任务列表和批次列表接入鉴权路由，M2-T8 把运行、任务、批次状态与快照数组收紧到共享契约。M7-T8 实现查 bug 提示词、五段解析与结果裁定，M7-T9 装配审查、闸门和五个后台 job。M8-T7 实现收口修复派发、跨批口径与撤回。M9-T11、M9-T12、M9-T16 提供停止与离线呈现、手机布局、四步空态及落地清单。正式入口的查 bug 生命周期仍未贯通，故本批为 `open`。

## 测试

- `pnpm install --frozen-lockfile` 成功；Web 正式构建 `pnpm --filter @agent-scheduler/web build` 成功，CSS 和主 JS 进入 `packages/web/dist`。
- `pnpm -w check` 的错误码、对比度、架构禁令、Biome、TypeScript 阶段通过；第二轮全仓 Vitest 为 157/171 文件通过、1917 通过、22 失败、32 条平台跳过，另有 6 次 worker RPC 超时。Web 构建与测试并发造成测试启动阶段 `dist` 缺失，且 NTFS 读取延迟触发多处 5 秒超时；这轮不能记 pass。首轮本地 `better-sqlite3` 预构建要求更高 glibc；在当前工作树原生重编后 SQLite 读写通过，第二轮才进入有效测试。不能将首轮本机 ABI 故障算作产品回归。
- 隔离复跑：`packages/web/test/routes.test.ts` 28/28、`packages/daemon/test/unit/source-contracts.test.ts` 6/6 通过；本批直接相关的文档路由、容器、派发契约复测分别 6/6、9/9、24/24 通过。快照用例在默认 5 秒超时下失败，单独提高诊断超时到 60 秒后 2/2 通过、状态派生断言没有失败。Web 产物已建、单进程复测后，路由的失败无法复现。
- B3 新增 `daemon-runtime.test.ts` 延迟监听回归，Biome 和 TypeScript 检查通过；本机 Vitest 两次在执行测试前报告 worker `fetch` 超时，修复后再次加载超过八分钟而中止，故该新增用例未记 pass。真 daemon 与真浏览器的修复前两次失败、修复后一次成功和码龄 60→0 秒构成当前可复核的运行证据。
- 真入口：隔离 Linux 挂载命名空间中的 `node packages/daemon/bootstrap.mjs` 在 47651 端口监听；三条 M2-T7 文档路由无令牌均返回 401。修复前两次真 Chrome 配对均因初始码过期失败，第二次测得服务刚可连时码龄 60 秒；修复后码龄 0 秒、配对成功并进入四步引导，正式 CSS 的 `--page` 计算值为 `#ecefee`，360px 视口无横向溢出。配对前后真实画面编码为 6.5 秒 GIF；没有用 mock、fixture 或直接写数据库伪造进度。
- 仓库仍无 17 节的正式 `e2e/`、`vitest.e2e.config.ts` 和根 e2e 脚本；原任务 M1-T11 负责正式冒烟，M9-T27 负责生产配对入口。这里运行了等价的真服务、真浏览器局部冒烟，不冒充尚未建成的完整自动化流水线。

## 逐任务与接缝复核

- M2-T7：`packages/daemon/src/http/routes/documents.ts:246,261,280` 是三条显式路由，`packages/daemon/src/service/docs.ts:919,1010` 管刷新与分页，`packages/daemon/src/repo/tasks.ts:312,757` 在 SQL 派生状态；真入口 401 覆盖 E-08，既有 `documents-routes.test.ts` 覆盖刷新、筛选及错误信封。E-17、E-19、E-82 的数据保留与事件边界落在 `service/docs.ts:919`。
- M2-T8：`packages/shared/src/api/runs.ts:3` 定义 13 状态，`packages/shared/src/api/tasks.ts:3` 和 `packages/shared/src/api/batches.ts:4` 给出其余状态联合；`packages/shared/src/api/snapshot.ts:8` 具名化数组，`packages/shared/src/lib/is-record.ts:1` 为唯一守卫。E-234 的形状对应关系在 `packages/web/src/lib/spine-shape.ts:23,63` 与 `packages/web/test/spine-shape.test.ts:217` 校验。
- M7-T8：`packages/daemon/src/domain/bughunt-prompt.ts:35` 和 `bughunt-report.ts:196,324` 覆盖四段提示、五段解析、FIXED/NOT_FIXED 裁定；`packages/daemon/src/service/bughunt.ts:88,305` 实现派发和收尾。E-306、E-307、E-308、E-320、E-321、E-323、E-328、E-329、E-331、E-341 的真实生命周期在正式入口缺口处中断，见 B1。
- M7-T9：`packages/daemon/src/boot/container.ts:849,949` 装配审查、闸门和五个 job，`packages/daemon/src/main.ts:116` 在监听后启动 job；`packages/daemon/src/service/run.ts:757` 接审查退出。与 M7-T8 的组合根接缝缺口见 B1。
- M8-T7：`packages/daemon/src/domain/task-state.ts:41,97,184` 处理跨批及 HEAD 三态，`packages/daemon/src/domain/path-clash.ts:500` 串行化同轮修复；`packages/shared/src/api/routes.ts:516` 注册撤回路由。收口派发与人工撤回的原任务测试纳入全仓测试。
- M9-T11：`packages/web/src/store/connection-store.ts:50,74` 负责重连补拉和派发可用性，`packages/web/src/features/run-deck/use-stop-intent.ts:52` 实现一帧内停止及失败回滚。正式甲板目前没有注入停止回调；与尚未落地的 M8-T8/M9-T21 泳道接线一并复核，列入 SUSPECT。
- M9-T12：`packages/web/src/components/thumb-bar.tsx:48`、`packages/web/src/features/run-deck/mobile-pane-switcher.tsx:30`、`stop-confirm-dialog.tsx:32`、`mobile-bottom-sheet.tsx:31` 分别承载拇指区、未处理徽标、二次确认与步骤抽屉；`packages/web/src/features/run-detail/use-log-window.ts:83` 将手机首屏设为轻量尾读。真 Chrome 360px 的配对页无横向溢出，不能代替全部运行视图的移动端验证。
- M9-T16：`packages/web/src/components/empty-onboarding.tsx:140,260,418,568` 提供四步、已选文档的批次过滤及 daemon 下发的瓶颈字段；`batch-summary-bar.tsx:84,140,203` 是四数一条而非饼图；`packages/web/src/features/landing/use-landing.ts:32` 从正式路由取清单。空态派发主流程仍待 M1-T11 的真浏览器自动化。

## 发现与修复

- B1 [S2 功能错] 涉及 M7-T8、M7-T9（接缝：判定结果到组合根及运行终态）：`packages/daemon/src/service/review.ts:2531` 仅在可选的 `deps.bughuntService` 存在时派发查 bug；`packages/daemon/src/boot/container.ts:849` 建审查服务却未注入它，生产源码也无 `createBughuntService` 调用。开启 `pipeline.bughunt` 后审查 pass 落到 `review.ts:2550` 的原落地闸门路径，查 bug 被跳过。`run.ts:753-758` 未收尾查 bug，`rerun.ts:275-340` 与 `gates.ts:265-370` 也未承接其失败重派和人工决策。五段解析的单测无法证明生产可达。未作局部假修复；见返工任务。
- B2 [S3 质量门禁] 涉及 M7-T9：`packages/daemon/src/service/run.ts:493,505,529,554` 的四条回调内异步写入已用 `.catch` 处理异常，却没有显式 `void`；`source-contracts.test.ts:65` 在全仓门禁中逐条报出。现补齐意图标记，保持原有等待与错误处理语义；修复前该测试失败，修复后 6/6 通过。
- B3 [S2 功能错] 涉及 M2-T7（跨批 M2-T1、M1-T10）：`packages/daemon/src/boot/container.ts:367` 在正式启动时于 `packages/daemon/src/main.ts:114` 的 HTTP 监听之前签发 60 秒初始码；真实 WSL/NTFS 启动两次均让浏览器看到过期码，而且未配对设备不能调用需令牌的换码端点。正式入口现在延迟到监听成功后签发，直建容器的测试行为保持不变；`daemon-runtime.test.ts` 新增延迟监听后的真实配对领取回归。

## 已修

- B2：四条 `trackWrite(...).catch(...)` 前加 `void`，代码位置 `packages/daemon/src/service/run.ts:493,505,529,554`；提交 `b131f24`。
- B3：`startDaemon` 令正式容器暂缓初始码，`server.listen` 成功后调用 `bootstrapIfNeeded()`；修复后真服务配对成功、初始码在可连接时才开始 60 秒有效期。提交 `db064e4`。

## 遗留

- B1：正式入口查 bug 阶段不可达，且补一处注入仍不能闭合子运行退出、重派、人过闸门与再次审查。Jev 最终选登记独立返工，禁止把 B1 记为已修。
- 正式 e2e 自动化仍由 M1-T11 承接；本批真浏览器证据只覆盖配对与空态进入，没有假称完成导入、派发、SSE 与审查全链。

## 返工任务

```task
stable-key: bughunt-production-lifecycle
title: 贯通正式容器的查 bug 派发、退出、重派与人工闸门
module: M7
source-tasks: M7-T8, M7-T9
depends-on: M7-T8, M7-T9
input: 在 main 上开启 pipeline.bughunt，审查 pass 时 createReviewService 未收到 bughuntService，直接进入落地闸门；查 bug 子运行的退出、重派与人过闸门也未接入。对照 M7-T8 全部验收及 E-306/E-307/E-308/E-320/E-321/E-323/E-328/E-329/E-331/E-341。
output: packages/daemon/src/boot/container.ts、packages/daemon/src/service/bughunt.ts、packages/daemon/src/service/review.ts、packages/daemon/src/service/run.ts、packages/daemon/src/service/dispatch.ts、packages/daemon/src/service/rerun.ts、packages/daemon/src/service/gates.ts 与生产入口集成回归测试。
acceptance: 1) 真 createContainer、正式 run 退出和 tick 路径下，开关在审查 pass 当刻取值，开启派一条有真实 batch 与指派快照的 bughunt 子运行并实际启动，关闭直达既有闸门；2) 子运行 clean、FIXED 有 diff、NOT_FIXED 含 S1/S2、五段不可解析、启动失败或退出失败分别进入 M7-T8 指定状态、原文和事件，审查不重复跑机械检查；3) bughunt_failed 人工重派只重派原子运行，不新开实施运行、不偷增返工次数；人 pass/reject 和 FIXED 再审按 E-307/E-320/E-331 走完，未完不得落地；4) 用真实容器、HTTP 与状态迁移断言这些路径，补修复前失败的回归，跑 pnpm -w check。
edges: E-306, E-307, E-308, E-320, E-321, E-323, E-328, E-329, E-331, E-341
paths: packages/daemon/src/boot/container.ts, packages/daemon/src/service/bughunt.ts, packages/daemon/src/service/review.ts, packages/daemon/src/service/run.ts, packages/daemon/src/service/dispatch.ts, packages/daemon/src/service/rerun.ts, packages/daemon/src/service/gates.ts, packages/daemon/src/workspace/in-head.ts, packages/daemon/src/workspace/diff.ts, packages/daemon/src/workspace/worktree.ts, packages/daemon/test/integration/bughunt-lifecycle.test.ts, packages/daemon/test/integration/pipeline-lanes.test.ts, packages/daemon/test/unit/bughunt-report.test.ts, packages/daemon/test/unit/in-head.test.ts, packages/daemon/test/unit/diff.test.ts, packages/daemon/test/unit/m8-t9-rework.test.ts
estimate: 2d
severity: S2
jev: model=jev-1.13.0 line=adjudicate:pick=B(medium 0.50),spec_change=no(0.13),outside_stack=no(0.11),needs_owner=no(0.10),reversible=unsure(0.62),adopt=take(B)
```

## 给下一批的提醒

`settings.pipeline.bughunt` 为 1 时，当前正式入口仍不能保证查 bug；R12 的真实生命周期回归通过前，不要把服务单测的五段解析视作已可上线。正式启动先监听后签发初始配对码，直建容器仍即时签发；M8-T8 与 M9-T21 尚未落地，泳道资料到甲板的路径需在其落地时复查停止回调和乐观态。
