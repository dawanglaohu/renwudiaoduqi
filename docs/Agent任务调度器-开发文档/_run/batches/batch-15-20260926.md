---
batch: 15
tasks: M1-T11, M9-T17, M9-T21
date: 2026-09-26
verdict: fixed
tests: pass
pr: https://github.com/dawanglaohu/renwudiaoduqi/pull/159
note: 历史泳道只保留最近归档流水线；第 14 批既有 open 返工与下一批闸门仍需单独处理。
repair_schema: 1
---

## 交付了什么

M1-T11 把构建、真实 daemon 自举、浏览器配对、文档导入、假 agent 通过正式 spawn 派发和 SSE 到 DOM 串成独立的七步冒烟。M9-T17 在运行流头部常驻呈现模型、思考强度、权限和 daemon 下发的指派来源，以及中性的并发会话序号。M9-T21 用 daemon 的 `lanes[]` 呈现桌面多泳道、手机单泳道、阶段链和只读历史。本次把任务与收口共用的历史槽收敛为最近一条归档流水线。

## 测试

- 新工作树首次 `pnpm -w check` 在 Web 产物尚未构建时，Fastify 集成测试报 `packages/web/dist` 不存在；先运行 `pnpm --filter @agent-scheduler/web build` 后重跑。与 e2e 同时跑的第二次全量检查有 2497 通过、1 条 `agents-models-login.test.ts` 超过 60 秒，并有 Vitest worker RPC 超时；串行重跑 `pnpm -w check` 最终 203/203 文件、2514 条通过、4 条平台条件跳过。错误码对照、对比度、禁令、Biome 和 TypeScript 均通过。
- `pnpm exec vitest run packages/daemon/test/unit/lanes.test.ts -t 'E-325:'` 在修复前两例均红：任务晚于收口、收口晚于任务时各返回两个历史字段。修复后 `pnpm exec vitest run packages/daemon/test/unit/lanes.test.ts packages/web/test/pipeline-lane.test.tsx packages/web/test/lanes-container.test.tsx` 为 50/50；改动两文件 `pnpm exec biome ci`、`pnpm exec tsc -b --noEmit`、`git diff --check` 均通过。
- 本机 `pnpm -w e2e` 构建成功后在 Windows daemon 启动自检处受机器级锁 ACL 阻断，七个浏览器步骤未开始：`packages/daemon/src/platform/lock.ts:20,52` 规定锁位于 `C:\ProgramData\agent-scheduler` 且仅 Administrators/SYSTEM 可写；临时 `AGSCHED_DATA_DIR` 不改变该锁。主干 `3af6f1f3` 的 GitHub Actions `36215706769` 中 `E2E Smoke (Ubuntu)`、Windows/macOS/Ubuntu Desktop CI 与 Release Gate 均成功。本 PR 的 Ubuntu e2e 与发布门禁仍须核对，以其真实结果为准。
- `finesse-ui audit` 对流头部、阶段链、历史行及流列作只读探测，P0 为零；未把该通用 detector 的组件 stamp 提示当作项目 token 契约。浏览器 GIF 因本机 daemon 锁启动失败没有生成，不将 Ubuntu CI 的 e2e 冒称为本机录屏。

## 逐任务与接缝复核

- M1-T11 AC1、E-139、E-226：`package.json:10` 先 build，`e2e/smoke.test.ts:135,483` 配临时数据目录与端口，经 `packages/daemon/bootstrap.mjs:1` 起真进程并从配对文件 claim；版本垫片和配对权限分别见 `packages/daemon/bootstrap.mjs:1`、`packages/daemon/src/platform/lock.ts:52`、`packages/daemon/test/unit/devices.test.ts:355`。AC2、E-159、E-175：`e2e/smoke.test.ts:520` 走手输地址与配对码并断言 Public Sans、非透明背景、`.flex`，`packages/web/src/styles/tokens.css:1` 集中 token。AC3、E-108、E-10、E-31：`e2e/smoke.test.ts:592,660,769` 导入双任务 fixture、派假 agent、核在线与 SSE 文本并作断流负控；`packages/daemon/test/integration/dispatch-spawn.test.ts:1` 核收流和隔离。AC4、E-135：`e2e/smoke.test.ts:848` 扫无测试后门与无替换 spawn，审查只读档另由 `packages/daemon/src/service/review-agent.ts:659` 接线。AC5、E-265：`e2e/smoke.test.ts:904` 留诊断，`biome.json:46` 排除 e2e，`.github/workflows/desktop-ci.yml:335,412` 上传失败 artifact 并纳入发布门禁。
- M9-T17 AC1、E-347、E-357：`packages/web/src/components/stream-head-meta.tsx:311,368` 对 `RunDto.assignmentSource`/`followedTaskId` 作四值及缺失显示；`packages/shared/src/api/runs.ts:272` 与 `packages/daemon/src/service/dispatch.ts:2480` 是上游字段契约，`packages/web/src/components/stream-column.tsx:406` 常驻挂载。AC2、E-254：`stream-head-meta.tsx:163` 缺失强度显示 —；AC3、E-37、E-256：同文件 `:163,242` 并列所选和自报且高亮；AC4、E-136：同文件 `:270` 持续标最高权限；AC5、E-31：同文件 `:53` 中性序号。`packages/web/src/components/pipeline-lane.tsx:206` 把当前阶段运行 DTO 传给参照条；`packages/web/test/stream-head-meta.test.tsx:58`、`packages/web/test/pipeline-lane.test.tsx:351` 覆盖未知来源、窄档与逐阶段字段。
- M9-T21 AC1、E-317：`packages/web/src/api/lanes.ts:57` 只取快照 `lanes`，`packages/web/src/features/run-deck/lanes-container.tsx:265` 按 `laneNo` 排序；`packages/web/scripts/check-forbidden.ts:427` 禁止呈现层阶段字面量比较。AC2、E-166、E-315：`packages/web/src/components/stage-chain.tsx:39` 的 30/44px 阶段网格和 `packages/web/src/features/run-deck/lane-steps-container.tsx:44` 的紧凑步骤；AC3、E-307：`packages/web/src/lib/spine-shape.ts:718` 回环片段；AC4、E-306：`packages/web/src/lib/stage-rows.ts:260` 在实际查 bug 运行后加节点且落地最后；AC5、E-313：`packages/web/src/components/stage-chain.tsx:39` 节点打开对应 run；AC6、E-314、E-325：`packages/web/src/components/lane-history-row.tsx:51` 只读展开，`packages/daemon/src/domain/lanes.ts:328-355` 经 B1 修正归档来源；AC7、E-309、E-319、E-326、E-332、E-333：`packages/daemon/src/domain/lanes.ts:83,237,339` 与 `packages/web/src/lib/stage-rows.ts:115,260` 分别负责占道、空闲及阶段兜底，`packages/web/src/api/lanes.ts:57` 负责二次失效；AC8、E-106、E-236：`packages/web/src/components/stream-column.tsx:121` 常驻停止/审批槽，两张 5×4 矩阵在 `packages/web/test/pipeline-lane.test.tsx:74` 与 `packages/web/test/stream-column.test.ts:1`；AC9、E-324：`packages/web/src/components/lane-run-strip.tsx:39` 与 `packages/web/src/store/selection-store.ts:26`；AC10：`packages/web/test/stage-rows.test.ts:1`、`packages/web/test/spine-shape.test.ts:1` 随全量测试通过，`packages/web/src/styles/base.css:1` 统一 keyframes。
- 接缝判据：`02-设计/10-接口约定.md:163,393` 的 DTO 与 SSE，`02-设计/06-系统架构与模块划分.md:130,205` 的环境和共享类型，`02-设计/12-UX.md:8,251` 的派发与泳道主路径。检查了 `RunDto`→参照条、daemon `LaneView`→Web 历史/阶段链、`lane.assigned`/`lane.released`→缓存失效以及收口/任务复用历史槽。前两项的字段与渲染测试通过；B1 是跨 M8-T8、M9-T21 的同槽覆盖漏判。`packages/web/src/api/cache-invalidation.ts:27` 同时失效 lanes/runs，`packages/web/src/features/run-deck/run-deck-container.tsx:265` 合并在途重取。人工闸门释放及状态机分支见 `packages/daemon/test/integration/pipeline-lanes.test.ts:1`，不能以静态检查代替 Windows 本机 e2e。

## 发现与修复

- B1 [S2 功能错] 涉及 M9-T21（跨批 M8-T8）：daemon 分别从归档运行里寻找最近任务与最近收口，两个字段可能同时有值；Web 在 `packages/web/src/components/pipeline-lane.tsx:140-151` 优先呈现收口，导致同泳道后来归档的任务仍显示旧收口。`packages/daemon/src/domain/lanes.ts:338-347` 改成按统一时间顺序只保留最近归档运行所属的一种历史；`packages/daemon/test/unit/lanes.test.ts:147-200` 两个方向的测试修前失败、修后通过。修复提交 `869941f`。

## 遗留

none。本机非管理员 Windows 无法写机器级锁的 e2e 启动条件已记录；第 14 批的 `open` 记录及其独立组合返工任务未因本批收口自动清除。

## 返工任务

none。

## 自行裁决

- Windows 本机 e2e 的安全锁与临时数据目录冲突。候选 A：保持机器级锁与 ACL，PR 上以 Ubuntu e2e、三平台 CI 核正式入口并如实报告本机限制；B：在开发模式或指定数据目录时改生产锁路径/权限并重新证明单实例；C：本批保持 open，另立后续本机 Windows 测试环境返工。Jev `jev-1.13.0`：`adjudicate: pick=A(high 0.93) spec_change=no(0.18) outside_stack=no(0.13) needs_owner=no(0.11) reversible=yes(0.76) adopt=take(A)`；执行 A，未修改安全锁。
- 第 14 批仍 open 与本批任务已落地主干并存。候选 A：本批独立按证据裁定，保留第 14 批 open 与下一批闸门；B：等第 14 批返工完成后才给本批裁定；C：在本 PR 用通用 e2e 自动把第 14 批改 fixed。Jev `jev-1.13.0`：`adjudicate: pick=A(high 0.80) spec_change=no(0.28) outside_stack=no(0.09) needs_owner=no(0.13) reversible=yes(0.69) adopt=take(A)`；执行 A，未改第 14 批记录。

## 给下一批的提醒

同一泳道的任务和收口历史是单个槽，不能按两种实体分别求最近。第 14 批的组合返工仍独立 open；派第 16 批前检查其返工与闸门，不用第 15 批的通用冒烟替代组合证据。
