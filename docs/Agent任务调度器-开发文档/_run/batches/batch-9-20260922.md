---
batch: 9
tasks: M10-T5, M7-T3, M8-T11, M9-T24, M9-T6
date: 2026-09-22
verdict: fixed
tests: pass
pr: none
note: SSE 信封现在经 bootstrap.ts 的 attachSseClient 进 event-bus，订阅 eventBus 的功能层不再只看到首屏 HTTP 拉的日志；M7-T3 的判定解析器仍等 M7-T9 接进容器、指派面板等 M9-T18，接线前别把它们当成从入口可达的能力；真浏览器主流程归 M1-T11、生产配对入口归 M9-T27。
repair_schema: 1
---

## 交付了什么

本记录是 unattended-run 1.6.0 回灌后对第 9 批的「再收口一次」：不重跑 S1–S6，不改 19 节，只用新格式重收；旧记录 `batch-9-20260921.md` 一个字节不改。第 9 批本身交付的内容不变：三态审查判定与失败兜底（M7-T3）、批次逐任务指派草稿与并发预览接口（M8-T11）、PostCSS／Tailwind 样式管线进 Vite 构建（M9-T24）、SSE 事件环形缓冲与按帧刷新（M9-T6）、桌面三平台 CI 与支持清单（M10-T5）。2026-09-21 收口已修的 B1／B2／B3（禁用 agent 的零并发语义 `3dc6377`、真实 Tauri bundle 解包 smoke 与 macOS 签名门禁 `74a9b72`、归档会话释放环形缓冲 `d220e34`）本次在 main `92ab4e4` 上复核仍在：`packages/daemon/src/domain/concurrency.ts:318,410,600` 对上限只做 `Math.max(0, …)`，`.github/workflows/desktop-ci.yml:190-236` 以 `tauri build --bundles` 产物为 smoke 输入，`packages/web/src/api/event-bus.ts:475-479` 按 `task.sessions_archived` 的 `runIds` 清缓冲；不重复计数。本次新修一处跨批接缝（见下），修完后 SSE 的同一条流既驱动 connection-store 也喂 M9-T6 的环形缓冲。

## 测试

- `pass` — WSL（Ubuntu 20.04，node 22，pnpm 9）工作树 `../agent-scheduler-batch-9`，main `92ab4e4`：先 `pnpm --filter @agent-scheduler/web build`（退出码 0），再 `pnpm -w check`：check-error-codes、对比度、禁令扫描、Biome、`tsc -b --noEmit`、Vitest 全仓 `161` 个测试文件通过、`1890` 个测试通过、`32` 个按平台条件跳过，退出码 0。这是修复前的基线。
- `pass` — 修复 `81f9a22` 后的最小充分集（dsh-pre-push-checks，改动只在 `packages/web`）：`pnpm exec biome check` 两个改动文件无问题、`pnpm exec tsc -b --noEmit` 退出码 0、`packages/web` 全部 Vitest `388` 个测试通过、退出码 0；新增断言在修复前失败（`eventBus.getBuffer('run-bootstrap-1')` 为 `undefined`）、修复后通过。
- `pass` — 文档链：五个任务 `maintain_docs.py status --task` 均 `stale=[]`、`runtimeDrift=[]`、`ready:true`；`review.json` 0 BLOCK。
- `skipped` — 17 节真浏览器端到端冒烟：仓库仍无根 `e2e/`、`vitest.e2e.config.ts`、根 `e2e` 脚本与 `playwright` 依赖，归 M1-T11；生产配对入口冒烟归 M9-T27。未用 fixture 或 mock 冒充。
- `skipped` — Windows／macOS／Linux Tauri installer 矩阵：本机 2026-09-22 `gh workflow run "Desktop Shell CI & Release Verification" --ref main` 仍返回 `HTTP 422: Actions has been disabled for this user`，`actions/runs` 为空；这是账号级设置，属用户红线类事项，不冒充 runner 证据。workflow YAML、Tauri CLI 参数、资产命名与 Linux deb 解包逻辑仍由 `packages/shell-desktop/test/` 本地覆盖。

## 发现与修复

- B1 [S2 功能错] 涉及 M9-T6（跨批 M9-T26，接缝类型「模块之间的调用」）：`packages/web/src/api/event-bus.ts:628` 导出的 `attachSseClient()` 在生产代码零调用；`packages/web/src/app/bootstrap.ts` 只 `bindConnectionState(sseClient)`（状态与游标进 connection-store）、teardown 时 `eventBus.clearAll()`，没有任何地方把信封 push 进 eventBus。`features/run-detail/use-log-window.ts:177` 订阅的是 eventBus，所以运行详情只会看到首屏 `GET /runs/:id/log` 拉的一段，实时事件永远不到。07 节第 340 行「全应用只开一条连接，按信封的 taskId／runId 分派到 event-bus」是设计口径，M9-T26 的验收却只断言 connection-store；开放中的 PR #118（M9-T25）对 `bootstrap.ts` 只改 hostHint，不补这一步。复现：`bootstrap.test.ts` 第三条用例在假 SSE 服务端写入 `run.state_changed` 后，`eventBus.getBuffer('run-bootstrap-1')` 为 `undefined`。修复见 FIXED。
- 复核 M7-T3：`packages/daemon/src/domain/review-verdict.ts` 的 `parseReviewVerdict()`／`evaluateReviewVerdict()` 在 daemon 源码里没有调用方，`service/review-agent.ts` 也没有第二套解析（只写 `review_verdict: null`），不存在关键词判 rework 的路径（E-278）。接进容器是 19 节原任务 M7-T9 验收 2 的范围（假审查进程吐 `VERDICT: pass` 后出现 `waiting` 闸门行），只点名，不写围栏。
- 复核 M8-T11：`packages/web/src` 无 `/assignments` 调用方，`empty-onboarding.tsx` 只有 `bottleneckSource` 展示位；`GET/POST /batches/:id/assignments` 的前端消费归 19 节原任务 M9-T18（PR #107 开放中），只点名。
- 复核旧遗留「真浏览器 computed-style 与主流程冒烟缺 `e2e/`」：仍缺，由 19 节原任务 M1-T11 完整覆盖，不写围栏。
- 复核旧遗留「三平台 installer 矩阵」：Actions 仍 422（见测试段），用户账号设置，不写围栏。

## 遗留

- M1-T11（未落地）：真 daemon + 真浏览器 + 样式 computed style + 派发主流程冒烟；本批仍无 GIF 与 320px 真实渲染证据，不冒充。
- M9-T27（未落地）：生产配对入口。2026-09-21 在主干 `e5084d7` 实测（真实 daemon 托管的生产 Web 构建，headed Chrome 从 `/` 进入后归一到 `#/pair`，正文只有「离线，最后同步于 — / #/pair」，无配对码输入或确认按钮；主 JS／CSS／本地字体 200、`data-style-loaded=true`、computed `--page` 为 `#ecefee`、`/api/v1/health` 与 `/api/v1/version` 200）；首因是 `app.tsx` 只注册 landing、`routes.tsx` 对缺失页面渲染 hash 占位，M9-T25 的路由实现尚在 PR #118 未合入。分工：M9-T27 证明可见配对表单与一次性码消费，M1-T11 接导入、派发、SSE 到 DOM。原始本机证据在主检出 `.playwright-mcp/real-smoke-20260921/`（`report.json`、两张截图、daemon 日志），只引用，不作为仓库 fixture。
- 三平台 installer 矩阵：GitHub Actions 对该账号返回 422，需用户在 GitHub 账号设置里开启 Actions 后补跑 `desktop-ci.yml`；本次不冒充。
- 返工任务：none（本次新发现的 B1 已当场修复；其余遗留均有原任务或属用户红线）。

## 返工任务

none

## 给下一批的提醒

SSE 信封从 `bootstrap.ts` 的 `attachSseClient(sseClient, eventBus)` 进环形缓冲，features 层只订阅 eventBus，不要再各自 `sseClient.subscribe`；改 `bootstrap.ts` 的任务（M9-T25 的 PR #118 也动它）合并 main 时留意这一段。M7-T3 的判定解析器与 M8-T11 的指派接口在 M7-T9、M9-T18 落地前都不是从入口可达的能力。真浏览器主流程证据只会来自 M1-T11，配对入口来自 M9-T27，第 10 层收口时同样不要伪造。
