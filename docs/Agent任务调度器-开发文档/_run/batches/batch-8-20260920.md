---
batch: 8
tasks: M10-T4, M4-T14, M6-T10, M6-T4, M6-T7, M6-T9, M7-T2, M8-T10, M8-T5, M9-T5
date: 2026-09-20
verdict: open
tests: skipped
pr: https://github.com/dawanglaohu/renwudiaoduqi/pull/120
note: 重跑与换 agent 重派接缝已修；先裁定 E-133、E-36/E-348、E-176 三处契约矛盾，并由 M1-T11 补齐真浏览器冒烟。
---

## 交付了什么

第 8 批把两端版本与共享 Web 产物、实时模型清单与思考强度形状、运行停滞与自动提问处置、会话归档/保留/搜索、首轮审查派发、真实子进程派发、重跑/换 agent 重派和自写 SSE 客户端连成一组。现有实现可构建同一份 Web 产物，由 daemon、Tauri 与 Capacitor 共同消费；daemon 能拉起 agent、收流落盘并在零产出时转人，SSE 客户端能鉴权、重连和处理重放窗口过期。本次收口补上了重跑入口与真实派发链之间的两处断点。

## 测试

- `pnpm --filter @agent-scheduler/web build`：通过，产出相对路径 CSS/JS 与自托管字体。
- 本批 12 个聚焦测试文件：157/157 通过；修复后 M8-T5/M8-T10/M8-T3 三文件聚焦：50/50 通过。
- `pnpm -w check`：通过；161 个测试文件，1884 通过、32 个平台条件跳过，0 失败；错误码、对比度、禁令、Biome 与 TypeScript 同时通过。
- `node …/finesse-ui/scripts/detect.mjs --json packages/web/dist/index.html`：P0=0；只读 UI 审计的 token 对比度、焦点、减弱动态效果与触控尺寸静态证据通过。
- `pnpm -w e2e`：skipped，仓库尚无根 `e2e/`、`vitest.e2e.config.ts`、`e2e` 脚本与 `playwright` 依赖；因此未录制真实浏览器 GIF。

## 发现与修复

- B1，S2，涉及 M8-T5 / M8-T10：`POST /runs/:id/rerun` 只插入 `starting` 行却不调用统一 `launchRun()`，且零产出后的 `awaiting_human` 行被活动查询原样返回；修复为只放行 `exited_before_output` 例外、创建新运行后走统一进程链，并以真实 HTTP 回归覆盖普通失败与零产出两条路径。修复提交 `c45f598`。
- B2，S2，涉及 M8-T5 / M8-T10：零产出卡片跳到指派面板后，常规 `POST /runs` 仍返回旧 `awaiting_human` 行，换 agent 不会新建或启动运行；修复为仅对 `exited_before_output` 停靠态开放显式重派，普通人工闸门继续幂等停靠，替代运行在同一事务中作废旧审批卡，重复请求返回同一新运行。修复提交 `77d9027`。

## 遗留

- doc-issue，M6-T7 / E-133：09 节 `run_messages.kind` 与 `MessageService` 接受 `elevate_once`，但 10 节 `CreateRunMessageBody`、shared schema 与真实路由只允许 `reply / approve / deny`；`RunService.elevateRunOnce()` 因此没有可达入口。需先裁定它是否复用 messages 端点及 `text` 是否可空，再补契约和接线。
- doc-issue，M8-T5 / E-36 / E-348：任务要求模型名失效后精确标 `E_MODEL_INVALID`，但 `handleModelInvalid()` 在生产代码中零调用；E-348 又要求零产出不得从 stderr 推断“未登录/模型无效/参数拒绝”。需先定义跨 agent 的结构化模型拒绝信号，或删除精确归因要求，不能靠字符串猜测。
- doc-issue，M10-T4 / E-176：E-176 写“常规 + 一个强调档”，而 11 节已定 UI 字体表要求 Public Sans 与 Commit Mono 使用 400/500/600，现有打包断言与产物遵循后三档。需统一字体预算契约后再判断是否删除 500 或改 E-176。
- doc-issue，M1-T11：17 节要求的真 daemon + 真浏览器 + 样式 + 派发主流程冒烟尚未落地；M1-T11 已登记负责这些文件，但当前收口提示词要求缺冒烟即记开放项，所以本批无法提供 GIF/320px 真实渲染证据。

## 给下一批的提醒

下一批可以继续消费已修复的 `POST /runs` 与 rerun 入口，但不要把 E-133、E-36/E-348 或 E-176 的现状当成已裁定契约；M1-T11 落地前，任何“真实浏览器主流程已验证”的说法都没有证据。
