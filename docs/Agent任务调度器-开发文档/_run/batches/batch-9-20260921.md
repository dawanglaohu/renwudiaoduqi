---
batch: 9
tasks: M10-T5, M7-T3, M8-T11, M9-T24, M9-T6
date: 2026-09-21
verdict: open
tests: pass
pr: https://github.com/dawanglaohu/renwudiaoduqi/pull/121
note: M1-T11 必须补上真起 daemon、真浏览器 computed-style 与主流程冒烟；本批 CI installer smoke 已改为真实 Tauri bundle 解包后启动。
---

## 交付了什么

本批五个任务已在主干落地：三态审查解析与失败兜底、批次逐任务指派草稿及并发预览、PostCSS/Tailwind 样式管线、SSE 事件环形缓冲与按帧刷新、以及桌面三平台 CI 与支持清单。收口修复补齐了禁用 agent 的零并发语义、真实 Tauri installer 的构建/命名/解包 smoke 与 macOS 签名公证验证、归档会话缓冲释放。组合后，daemon 侧的并发预览与实际 tick 口径一致，桌面发布门禁验证的是用户拿到的安装包布局而不是裸 cargo 产物，Web 客户端不会为已归档 run 无限保留环形事件缓冲。

## 测试

- `pass` — `pnpm -w check`：错误码、对比度、禁令扫描、Biome、TypeScript、全仓 Vitest；最终结果 `161` 个测试文件通过，`1890` 个测试通过，`32` 个按平台条件跳过，退出码 0。
- `pass` — M8-T11 聚焦：`pnpm exec vitest run packages/daemon/test/integration/batch-assignments.test.ts packages/daemon/test/unit/assignments.test.ts packages/daemon/test/unit/concurrency.test.ts`，49 个测试通过。
- `pass` — M9-T6 聚焦：`pnpm exec vitest run packages/web/test/event-bus.test.ts`，25 个测试通过。
- `pass` — M10-T5 聚焦：`pnpm exec vitest run packages/shell-desktop/test/staging-smoke.test.ts packages/shell-desktop/test/platform-support-matrix.test.ts packages/shell-desktop/test/release-gate.test.ts packages/shell-desktop/test/linux-launcher.test.ts`，27 个测试通过；含真实 `.deb` 构造/解包与随包 Node 健康检查。
- `pass` — `pnpm exec tsc -b --noEmit`、`pnpm exec biome check`、`python3` YAML 解析、Tauri CLI `2.11.5 --version` 与 `verify-baselines.ts`。
- `skipped` — 17 节真浏览器端到端冒烟与 `record-browser-gif`：仓库当前没有 `e2e/`、`vitest.e2e.config.ts` 或根 `e2e` 脚本；M1-T11 是该能力的任务归属，未用 fixture、mock 或测试钩子冒充。
- `skipped` — Windows/macOS/Linux Tauri installer 矩阵：当前环境没有 Rust/Tauri 原生三平台 runner；显式对分支触发 GitHub workflow 时 API 返回 `HTTP 422: Actions has been disabled for this user`，未取得远端 runner 证据。workflow 已纳入 Windows NSIS/MSI、macOS app/DMG、Linux deb/AppImage 构建、真实解包 smoke 与 macOS `codesign`/`spctl`/stapler 门禁，待 Actions 恢复后必须补跑。

## 发现与修复

- B1 [S2 功能错] 涉及 M8-T11（跨批 M4-T1/E-91）：`maxConcurrency=0` 在预览、slot allocator 与 tick 上限路径被 `Math.max(1, ...)` 偷改成 1，导致 UI 显示可派而实际 agent 已禁用；修复为保留 0 并补预览/分配负例，提交 `3dc6377`。
- B2 [S2 功能错] 涉及 M10-T5：CI 只构建裸 `cargo build` 产物，却声明并上传 `.exe/.msi/.dmg/.deb/.AppImage` 安装资产，未验证真实安装布局；新增 Tauri CLI bundle 构建、固定资产名、MSI/DMG/deb 解包与同一 `file/args/cwd` daemon smoke，正式 macOS 增加真实签名/Gatekeeper/stapler 验证，提交 `74a9b72`。
- B3 [S2 功能错] 涉及 M9-T6（E-142/E-143）：事件总线按 run 限容但生产没有在 `task.sessions_archived` 后删除历史 run buffer，长期运行会按完成 run 数增长；归档事件按 daemon 给出的 `runIds` 清除对应 buffer，并保留其他任务流，提交 `d220e34`。

## 遗留

- 真浏览器 computed-style 与用户主流程冒烟仍缺少 M1-T11 的 `e2e/` 实现，记录为 `doc-issue`，本批不伪造通过。
- 三平台 installer 构建只能在 GitHub runner 验证；GitHub Actions 当前因用户级禁用返回 422，无法启动矩阵。本地已验证 workflow YAML、Tauri CLI 参数、CI 资产命名与 Linux deb 解包逻辑，但没有把本机环境误报成 Windows/macOS runner 证据。

## 给下一批的提醒

M1-T11 开工前要把真实 daemon、真实 Web 产物和 Playwright/浏览器主流程接起来，并断言 `data-style-loaded` 与 token computed style；M10-T5 的 CI smoke 现在以真实 bundle 为输入，不应再退回裸 `cargo build` 目录。M8 的 agent 上限 0 是禁用语义，任何新的并发预览或分配入口都必须复用该口径。
