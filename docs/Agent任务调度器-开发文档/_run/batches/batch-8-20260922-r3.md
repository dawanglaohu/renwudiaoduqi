---
batch: 8
tasks: M10-T4, M4-T14, M6-T10, M6-T4, M6-T7, M6-T9, M7-T2, M8-T10, M8-T5, M9-T5
date: 2026-09-22
verdict: open
tests: pass
pr: https://github.com/dawanglaohu/renwudiaoduqi/pull/127
note: 第三轮修复主干 Biome 门禁与默认 SSE 双重订阅；E-133、E-36/E-348 两条 R8 返工仍未落地，完整真浏览器主流程仍由 M9-T27、M1-T11 承接，不得用占位配对页冒充。
repair_schema: 1
---

## 交付了什么

第 8 批原有十项能力保持不变：共享 Web 构建与版本校验、实时模型清单与思考强度、运行监管与会话归档、审查派发、真实子进程派发、重跑／换 agent、SSE 重连。本次第三轮从当前 main 重新跑完整门禁、逐项核任务笔记与接缝，并修复两处主干问题：第 9 批合入后遗留的 Biome 导入排序红灯，以及 M9-T5 默认 SSE 单例在模块加载与 `bootstrap()` 两处同时订阅 event-bus、导致每个信封重复入缓冲。真实 daemon 与真实浏览器证明生产构建、字体、token 背景、Tailwind utility 和 320px 无横向溢出；也同时证明 `#/pair` 仍只有 hash 占位文本，因而没有录制虚假的主流程 GIF。

## 测试

- `pass` — `pnpm --filter @agent-scheduler/web build`：65 modules，生产 CSS／JS 与六个自托管字体都在同一份 dist；真实浏览器请求的 CSS、JS、Public Sans 400 与 Commit Mono 400 均由 daemon 返回 200，其余字重由打包断言核对。
- `pass` — WSL Ubuntu 20.04、Node 22.17.0、pnpm 10.18.3 下运行根脚本 `pnpm -w check --maxWorkers=1 --minWorkers=1`，退出码 0；参数只限制 Vitest worker，不改变测试集合、超时或断言。错误码、对比度、禁令、Biome、`tsc -b --noEmit` 全通过；Vitest 161/161 文件、1891 通过、32 个平台条件跳过、0 失败。未限 worker 的同一根脚本出现过 `docs-import`／`retention`／`static-plugin` 的 5 秒资源争用超时，三条均在这次完整单 worker 门禁中通过。
- `pass` — SSE 修复聚焦：`bootstrap.test.ts`、`event-bus.test.ts`、`sse-client.test.ts` 共 44/44；新回归走真实本地 HTTP SSE server 与生产默认 singleton，修复前收到一次 id=5252 却得到 `[5252,5252]`，修复后严格为 `[5252]`。
- `pass` — `python -B -m unittest discover -s docs/Agent任务调度器-开发文档/_run/tests -p 'test_*.py' -q`：172/172；`build_docs.py --batches` 识别原有稳定 key 并仍只生成 R8-T54786768、R8-T97041355，两条状态保持 todo，没有重写前两轮记录。
- `pass` — 全仓并发运行期间出现的三条 5 秒假超时均被隔离复验：`static-plugin.test.ts` 单 worker 2/2，通过真实 `/index.html`／assets／fonts 字节匹配；`docs-import.test.ts` 的真实仓库用例 1/1；`retention.test.ts` 的搜索 HTTP 用例在上述完整单 worker 门禁中通过。Ubuntu 20.04 的 glibc 低于 `better-sqlite3@13.0.3` 预编译件要求，按错误提示在工作树 `node_modules` 本机编译兼容件后实际 `select 1` 通过；仓库与锁文件未因此变化。
- `pass` — 真 daemon + 真浏览器只读冒烟：1280×720 下 `Public Sans` 生效、根背景 `rgb(15,18,19)` 非透明、样式表含 `.flex`、`data-style-loaded=true`；320×640 下 `scrollWidth===innerWidth`，无横向溢出。
- `skipped` — 完整配对→导入文档→派发→SSE 到 DOM 与 GIF：根目录仍无 `e2e/`、`vitest.e2e.config.ts`、`pnpm -w e2e` 和 Playwright 依赖；生产 `#/pair` 仍只显示 hash 文本。该缺口已由原任务 M9-T27、M1-T11 精确定义，不用 fixture、静态页或 GIF 资产冒充。
- `pass` — `finesse-ui` 只读 detector 对真实 dist 报 P0=0；P2 为语义前景纯白 token、`transition-all` 和无 finesse 构建戳，作为 UI 打磨疑点保留，本轮 audit 按技能约束不改界面。

## 发现与修复

- B1，S2，涉及 M9-T26（跨批）：PR #125 向 `packages/web/test/bootstrap.test.ts` 加入 `eventBus` 后未按 Biome 排序，当前 main 的 `pnpm -w check` 在进入 TypeScript／Vitest 前必败；Jev adjudicate 取 A，只做 Biome 给出的两行机械重排，不改第 9 批语义。修复提交 `b6ad38e`。
- B2，S2，涉及 M9-T5／M9-T26：`packages/web/src/api/event-bus.ts` 模块加载时自动 `attachSseClient(default, eventBus)`，而生产 `packages/web/src/app/bootstrap.ts` 又对同一默认 client attach／teardown；真实本地 SSE 回归收到一次信封却在环形缓冲得到两个相同 id。删除模块加载副作用，让 `bootstrap()` 成为唯一生命周期所有者；修复提交 `70966a4`。
- 复核旧遗留 E-133（M6-T7）：shared `CreateRunMessageBody` 与真实路由仍只允许 `reply / approve / deny`，`MessageService` 虽接受 `elevate_once` 却不调用 `RunService.elevateRunOnce()`；R8-T54786768 仍为 todo。
- 复核旧遗留 E-36／E-348（M8-T5）：`handleModelInvalid()` 在生产代码仍零调用，`E_MODEL_INVALID` 仍无抛出点；R8-T97041355 仍为 todo，禁止从 stderr 自由文本猜测模型无效。
- 复核浏览器链：真实构建和静态宿主可用，但 `app.tsx` 仍只注册 landing，`#/pair` 降级为占位文本；M9-T27、M1-T11 均为 todo，因此完整主流程与 GIF 仍不可提供。

## 遗留

- R8-T54786768（stable-key `e133-elevate-once-entry`）仍未落地：临时提升入口不可达。
- R8-T97041355（stable-key `e36-model-invalid-structured-signal`）仍未落地：模型无效缺结构化生产信号。
- M9-T27（既有原任务，非新 R8）：生产配对入口与独立配对 smoke 未落地。
- M1-T11（既有原任务，非新 R8）：配对后导入、真实 spawn、SSE 到 DOM 的完整 e2e 与 GIF 未落地。

## 返工任务

```task
stable-key: e133-elevate-once-entry
title: 接通 E-133「仅本次运行临时提升」入口：POST /runs/:id/messages 接受 kind=elevate_once 并在运行详情「权限受阻」行提供一次性按钮
module: M6
source-tasks: M6-T7
depends-on: M6-T7, M6-T6, M9-T8
input: shared CreateRunMessageBody 与真实路由仍只收 reply／approve／deny 且 text 必填；MessageService 对 elevate_once 不调 RunService.elevateRunOnce()，前端无按钮。沿用 batch-8-20260922 的同一 stable-key 与 Jev A 裁决。
output: shared 类型与 JSON schema、daemon 路由与 MessageService 分派、10 节接口表、运行详情一次性按钮、单元／集成／前端测试与真实 daemon GIF；文档改动走 begin／sync／verify。
acceptance: 1) POST /api/v1/runs/:runId/messages body `{kind:'elevate_once'}` 在合法状态返回 200、临时提升为 true 并留事件，其他 kind 的 text 仍必填 2) 提升不写 agents.json／注册表且运行结束失效 3) 10 节、路由表、JSON schema 与 shared 类型一致并经文档补丁复核 4) 权限受阻行按钮只发一次请求，成功后不可再点，错误按 code 展示 5) pnpm -w check 通过并附真实 daemon 流程 GIF
edges: E-133
paths: packages/shared/src/api/runs.ts, packages/daemon/src/http/routes/runs.ts, packages/daemon/src/service/message.ts, packages/daemon/src/service/run.ts, packages/daemon/src/repo/run-messages-repo.ts, packages/daemon/test/unit/message-service.test.ts, packages/daemon/test/unit/run-service.test.ts, packages/daemon/test/integration/run-messages-elevate.test.ts, packages/web/src/features/run-detail/run-detail-container.tsx, packages/web/src/features/run-detail/use-log-window.ts, packages/web/src/api/http-client.ts, packages/web/test/log-window.test.ts, docs/Agent任务调度器-开发文档/02-设计/10-接口约定.md
estimate: 1d
severity: S2
jev: adjudicate pick=A(medium 0.73) spec_change=yes(0.71) outside_stack=no(0.08) needs_owner=no(0.14) reversible=unsure(0.63) adopt=take(A) model=jev-1.13.0 label=batch-8:wrapup:e133
```

```task
stable-key: e36-model-invalid-structured-signal
title: E-36 精确归因只认 agent 的结构化拒绝信号：新增归一化事件 run.model_rejected 并接通 handleModelInvalid，零产出退出仍走 E-348
module: M8
source-tasks: M8-T5
depends-on: M8-T5, M8-T10, M4-T8, M4-T14
input: handleModelInvalid() 在生产代码仍零调用，E_MODEL_INVALID 无抛出点；E-348 禁止匹配 stderr／stdout 自由文本。沿用 batch-8-20260922 的同一 stable-key 与 Jev A 低置信裁决。
output: shared `run.model_rejected` 事件；只有厂商协议的结构化错误字段可映射；RunService 接通 handleModelInvalid；10/13/19 节补丁与单元／集成／arch 测试。
acceptance: 1) EVENT_KINDS 与 10 节事件表含 run.model_rejected 2) 录制的结构化模型拒绝产出该事件，自由文本与 stderr 不产出 3) 收到该事件后运行 failed、释放泳道、不计返工且不再走 E-348；纯零产出仍 awaiting_human 4) arch 禁止 service/jobs 从 stderr/stdout 猜模型错误 5) E-36、M8-T5 与错误码表经 begin／sync／verify 一致 6) pnpm -w check 通过并报告实际验证的 agent 家数与 Jev low 风险
edges: E-36, E-348
paths: packages/shared/src/api/events.ts, packages/shared/src/api/routes.ts, packages/daemon/src/adapters/codex/map-events.ts, packages/daemon/src/adapters/codex/capabilities.ts, packages/daemon/src/adapters/claude/map-events.ts, packages/daemon/src/adapters/claude/capabilities.ts, packages/daemon/src/service/run.ts, packages/daemon/src/service/rerun.ts, packages/daemon/src/boot/container.ts, packages/daemon/test/unit/events.test.ts, packages/daemon/test/unit/codex-adapter.test.ts, packages/daemon/test/unit/rerun.test.ts, packages/daemon/test/unit/run-service.test.ts, packages/daemon/test/integration/dispatch-spawn.test.ts, packages/daemon/test/arch/dispatch-proc.test.ts, docs/Agent任务调度器-开发文档/02-设计/10-接口约定.md, docs/Agent任务调度器-开发文档/03-质量/13-边界问题与异常处理.md, docs/Agent任务调度器-开发文档/04-执行/19-模块任务拆分.md
estimate: 1.5d
severity: S2
jev: adjudicate pick=A(low 0.33) spec_change=yes(0.77) outside_stack=no(0.14) needs_owner=no(0.21) reversible=yes(0.74) adopt=take(A) model=jev-1.13.0 label=batch-8:wrapup:e36 confidence=low
```

## 给下一批的提醒

默认 SSE 单例现在只在 `bootstrap()` 显式接入 event-bus，禁止恢复模块加载副作用，否则每个运行事件会重复占用环形缓冲并重复渲染。E-133 与 E-36/E-348 的 R8 任务落地前仍不可达；完整浏览器证据只能由 M9-T27、M1-T11 落地后产生，当前占位配对页不能作为 GIF 或主流程通过证据。
