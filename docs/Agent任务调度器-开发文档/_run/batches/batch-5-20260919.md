---
batch: 5
tasks: M1-T8, M2-T2, M2-T6, M3-T3, M3-T4, M4-T3, M5-T1, M8-T1, M9-T1
date: 2026-09-19
verdict: fixed
tests: pass
pr: none
note: 已修文档接管未导入任务/批次与 reuse worktree 丢目录两处跨任务断点。
---

## 交付了什么

第 5 批交付配对信任、路由契约、派发快照、文档接管、worktree、并发计算和 Web 骨架。本次扩大收口发现并修复两处真实接缝，完整分析见 `batch-11-20260919.md`。

## 测试

`pnpm -w check` 全量通过；新增 POST `/documents` 后 tasks/batches 真库可见测试，以及旧 Git 缺目录登记的真实 worktree 重建回归测试。

## 发现与修复

- S1，M3-T4 ↔ M3-T2：POST `/documents` 原先只登记文档而未调用任务导入；现由 DocsService 在同一装配入口写入 tasks/batches，并由 snapshot 集成测试证明。
- S1，M5-T1 / E-277：reuse 模式遇登记仍在但目录丢失时原先返回坏路径或另开分支；现精确移除该登记并在原分支、原路径重建，禁止 prune、`-b` 和换 base。

## 遗留

none。

## 给下一批的提醒

文档接管必须同时更新文档元数据和调度真相表；返工复用只能重建原 worktree，不能悄悄制造干净分支。
