| # | 问题 | 评审 | 选定 | 理由 | 边界编号 | 隐含假设 | 置信 |
|---|------|------|------|------|----------|----------|------|
| 1 | 调度器核心逻辑跑在哪 | sound | A 独立常驻服务 daemon + 桌面 UI 只做壳 | 原话要「完成状态提醒」+「手机 app 端」，提醒送达与下一批触发都要在人离开电脑、桌面窗口已关之后照常发生，核心只能放在能独立存活的进程里 | E-01~E-05 | daemon 与桌面壳同一安装包分发，由桌面壳负责首次安装与开机自启注册。**修订（决策 40）：壳负责触发，机制由 M1 提供**——不得据原措辞把平台分支代码搬进 M10 | high |
| 2 | 手机端怎么连到电脑 | flawed | C 局域网优先 + 外网留给用户自接隧道 | 问题被驳回：A 与 C 不互斥，C 实质是 A 加「允许手填 host:port」，成本近零；真正的二选一是「自建中继」与「不自建」。原话通篇没提账号云端外网，而要传输的是源码与对话内容，自建中继会把数据责任揽到产品身上 | E-06~E-10 | 用户与其 agent 长期在同一台机器/同一局域网，出门远程查看是低频需求 | high |
| 3 | 两个端用什么技术实现 | flawed | C 桌面原生壳 + 手机原生壳，两端共用一套 Web UI | 问题被驳回：选项 B（PWA）省下的工期是假的——局域网 http 下手机浏览器对 LAN IP 拿不到 secure context，service worker 注册不了，PWA 安装/离线/推送整条链路不可用，不只是「iOS 受限」。原话写的是「手机 app 端」不是网页 | E-11~E-15 | 两个壳只负责窗口、系统通知、令牌存储三件事，壳内零业务逻辑 | high |
| 4 | 任务数据怎么读进来 | incomplete | A 直接读 docs-data.js，收敛在唯一适配器模块 | 问题不完整：漏了「改 build_docs.py 导出带 schemaVersion 的 tasks.json」这条路，但那要改技能侧，现在不必等。原话就是「基于这个任务产出的开发文档」，B 等于把解析逻辑抄第二遍，制造第二个会漂移的真相源 | E-16~E-21 | 对 docs-data.js 的读取收敛在唯一一个适配器模块，将来换 tasks.json 只改这一个文件 | high |
| 5 | 「进度」怎么定义 | sound | C 粗状态机 + 通用信号（最后消息/改动文件数/耗时/token） | 原话第 2 条已要求「查看会话内容」，原始流反正要落盘，C 只是在已有流上再取几个通用字段，相对 A 边际成本极小却直接兑现「看到进度」 | E-22~E-27 | 「改了多少文件」用 git 工作区 diff 得出，而非解析各家 tool 事件，四个 agent 共用一套逻辑 | high |
| 6 | 第一版覆盖几个 agent | sound | B 先做已实测的 4 个 + 插件式适配层，DeepSeek Harness 不进第一版 | 原话第 4 条自己把「需要确认能否从外部发派」列为难点，说明用户接受「没确认的先不赌」。**理由已按决策 35 改写**：不是「形态未确认」（调研已查清它有 headless / ACP / 官方 Python SDK），而是**官方 README 明说 THERE WILL BE COMPATIBILITY-BREAKING CHANGES、会话格式版本为 0、且本机未安装无法验证** | E-28~E-32 | 插件接口先用形态差异最大的两个（codex 的 daemon/thread 模型 与 claude 的 stdin/stdout 双向 stream-json）定型，再接 pi/grok | high |
| 7 | 模型选择的粒度 | sound | B 每 agent 默认 + 单任务可临时覆盖 | 补充需求写的是「指定**默认**模型」，「默认」二字本身就是「可被单次覆盖的基线」，B 是对原话最字面的实现 | E-33~E-37 | 单任务覆盖只作用于这一次派发及其重试，绝不回写任何 agent 的配置文件；用户手改配置文件始终优先于缓存值 | high |
| 8 | 模型清单从哪来 | incomplete | C 配置文件 + 列模型命令 + 允许手填 | 问题不完整：选项 B 说「降级」却没定义降到哪里，其唯一落点就是 C 的手填框，B 不是 C 的并列项而是 C 缺了兜底；且「配置里只有当前一个」只对 codex/claude 成立，pi 的 models.json 与 grok 的 [models] 段本身就是全量清单 | E-38~E-45 | 每个 agent 取源方式可各不相同，适配层允许某家只读文件、某家只跑命令；手填值持久化在 daemon 自己的存储里，不写回 agent 配置 | high |
| 9 | 按批次派活怎么推进 | flawed | B 按交接台已算好的并行窗口数限流 | 问题被驳回：伪选择——选项 A「整批一次性全发」与已生效的决定 6「每 agent 有并发上限」直接冲突，选项 C 要求用户手算交接台已经算好的数。原话「按批次派活」指向的就是交接台已排好的批次与并行窗口 | E-46~E-52 | 交接台算出的并行窗口数已考虑依赖正确性，本产品只在它之上再叠机器资源与冲突约束，不重新推导依赖图 | high |
| 10 | 自动/半自动的分界线 | incomplete | C 三个可独立开关的闸门（派发前/审查后/落地前），自动与半自动是预设组合 | 问题不完整：第三个闸门「落地前」是否存在取决于决策 12——既然调度器不执行提交/PR，落地天然由人做，该闸门恒为人工。原话「先实施，审查**可**自动」里的「可」字本身就说明审查是可开可关的闸门 | E-53~E-59 | 用户对「自动」的期待是「不打断我」而非「不告诉我」——全自动下每个闸门通过仍写事件流与通知，只是不阻塞 | medium |
| 11 | 审查由谁执行、审不过怎么办 | flawed | C 两级：先机械检查，过了再派审查 agent | 问题被驳回：成本描述失实——机械检查是本地命令不是 agent，C 只在机械检查通过后才派审查 agent，agent 调用次数恒 ≤ A，比 A 更省而非「成本最高」。「审查可自动」要落在交接台已生成的那份审查提示词上 | E-60~E-68 | 默认审查者 = 实施该任务的同一 agent 的一个独立新 session（只喂审查提示词 + diff，不带实施上下文），零配置可用；设置里可改指定其他 agent/模型 | medium |
| 12 | 调度器管不管 git | flawed | B 只管工作区隔离与读取（worktree + diff），提交/push/PR/合并留给人 | 问题被驳回：伪选择——选项 C「完全不碰 git」与已生效的决定 6「每任务独立工作区」冲突，选项 A 的自动 push/PR/合并在用户只字未提 git 的前提下属不可逆越权。需求未覆盖 git，按可逆优先 | E-69~E-76 | 用户的落地流程仍是技能原本的 `gh stack` 人工流程，调度器只负责把「可以落地的东西」准备好并摆在明面上 | high |
| 13 | 文档重出后在途/历史派发记录怎么办 | flawed | D-counter 做最小实现：快照逐字符比对产出三个布尔标记 + 角标 + 横幅，不做版本子系统 | 问题被驳回：选项 B 与已锁定的决定 4 冲突（派发时已快照三段，不存在「导入覆盖在途提示词」这回事）；而 A 的 3-5 人天里指纹与快照已在决定 4 付过账，真正没做的只剩「拿快照和当前文档比一次并打标」，增量仅 0.5-1 人天。明确不做：diff 可视化、三方合并、自动作废、自动重派、多版本历史 | E-77~E-82 | 假设 docs-data.js 重新生成后同一任务 ID 稳定；若 ID 会随机重排，「任务已消失」将大面积误报，届时需退化为按「标题+批次」模糊匹配并把标记降级为提示 | high |
| 14 | 任务状态要不要回写交接台 | flawed | C 不回写，调度器为唯一权威，并提供「在调度器中打开这份文档」入口 | 问题被驳回：B 与 C 不互斥，C 就是 B 加一个入口按钮；而 A 并非技术上不可能，但等于把稳定性押在一个会被脚本反复重新生成的 DOM 结构上。原话第 2 点是「可以**进入任务调度中**查看」，用户自己把观看位置定在调度器 | E-83~E-87 | 假设用户能接受「阅读器里那套手工三态从此作废」这一次心智切换，且没有第三方流程依赖那份 localStorage | high |
| 15 | agent 接入配置要不要注册表化 | flawed | D-counter 两层注册表：数据层可配置（路径/参数模板/并发/默认模型），代码层不可配置（参数转义、解析器按具名枚举选择） | 问题被驳回：B 的代价被高估——适配器只活在 daemon 侧，厂商改版并不需要重新打包两个壳；而 A 把「模型配置文件格式」也写进配置，正是 4-6 人天和「用户改错更难排查」的主要来源。降到 2-3 人天 | E-88~E-95 | 假设所有 agent 适配逻辑只跑在 daemon 侧，桌面壳与手机壳不含任何厂商相关代码，因此厂商改版只需更新 daemon 一处 | high |
| 16 | 会话内容存多久、怎么加载 | flawed | C 分段加载 + 保留上限告警，但厂商会话文件只读引用不拷贝 | 问题被驳回：题面前提「会话内容存多久」默认这些内容由本产品存储，但已查实四家会话记录是厂商自己的磁盘产物，v1 大多只需引用而非拷贝；真正需要保留策略的只有自己捕获的进程输出 | E-96~E-105 | 假设 daemon 与各 agent 同机同用户运行，能以只读方式访问厂商会话目录；若将来出现跨机或沙箱隔离的 agent，需退化为「只展示我们捕获的输出」 | high |
| 17 | 界面的设计语域（register） | flawed | B workbench + 代理层（工作台 + agent 控制台） | 问题被驳回：伪选择——题面自带的判定口径与本产品场景逐字吻合，A 被该口径直接排除，C 与一个内部调度工具毫无关系。原话「看到进度与完成状态提醒」+「查看发派 agents 出去的会话内容」描述的正是「人在旁边看 agent 干活并决定放不放行」 | E-106~E-111 | 假设桌面端是主要作业面、手机端以监看与审批为主而不承担编排；「成本像收据」退化为「耗时/轮次/可得时的 token」，因四家花费数据未必都能读到 | high |
| 18 | agent 中途提问时人能否回话 | incomplete | A 做双向回话（两端都能回一句、点同意） | 问题不完整：三选项不互斥——C 的「批准/拒绝」在 codex `queue --message` 与 claude stream-json 下就是往同一条注入通道发预设短语，是 A 的 UI 皮肤而非更省的实现；且「卡死」说法不准，headless 下更常见的是 agent 带着提问结束回合。补充需求要「自动派发」，而中档权限必然在装依赖处停下，没有回话通路则第一个提问就整条链停摆 | E-112~E-117 | pi/DeepSeek 等后续 agent 未必有消息注入能力；适配层需把「可回话」做成能力位，UI 按能力位灰掉按钮而不是报错 | high |
| 19 | 中止 / 重派 / 超时 | sound | A 全做（手动中止、换 agent 重派、超时只标记不自动杀） | 原话「可以看到进度与完成状态提醒」的实用意义是发现异常后能处置；只看不能动等于决定 5 的静默检测检出问题却没有出口。可逆优先——超时只标记可撤销，自动杀不可逆且会砍掉长思考中的进度 | E-118~E-124 | 超时阈值按 agent 可配、有出厂默认值，不做全局单一阈值 | high |
| 20 | 单人单机还是多人协作 | flawed | A 单人单机，只做设备配对 | 问题被驳回：伪选择——选项 B 与已定决定 2（不自建服务器）以及原话通篇第一人称「我需要」直接冲突，没有任何多人痕迹。真正该定的是设备配对与凭据吊销粒度，而非要不要账号体系 | E-125~E-128 | 「设备配对」用一次性配对码 + 长期令牌即可，不需要证书体系 | high |
| 21 | 桌面端支持哪些操作系统 | sound | ~~B 第一版只做 Windows~~ → **已由决策 53 取代** | 原需求未覆盖且当时只有 Windows 实测，故先收口平台适配；2026-09-03 用户明确追加 Windows、macOS、Linux 支持后，现行范围改看决策 53～57 | E-129~E-132、E-257~E-271 | 手机端是否做 iOS 仍不由本决定作答 | high |
| 22 | 派发时给 agent 什么权限档位 | sound | A 默认中档（codex `workspace-write` / claude·grok `acceptEdits`），每 agent 可单独调 | 补充需求要「自动/半自动（先实施…）」，实施必须能改文件，最低档与之直接冲突；最高档又推翻决定 6 的隔离前提。中档是唯一同时满足「能自动跑」与「破坏面限于本任务 worktree」的档位 | E-133~E-138 | 决定 6 的每任务独立 worktree 已落在一个可被沙箱声明为可写根的目录下，中档权限才有边界可言 | high |
| 23 | daemon 用什么语言与运行时 | sound | A Node.js + TypeScript | agent 阵容已把 Node 变成既定依赖；与 Web UI 同语言可让任务、事件与运行流类型只写一套 | E-139~E-142 | 决策 53 扩展到三平台后，单文件分发的优势不再是零，但仍不足以抵消既定 Node 生态与共享类型收益；Node 22 和原生模块必须进入三平台 CI | high |
| 24 | Web UI 用什么框架与组件库 | incomplete | A React 18 + TS + Vite + Tailwind + shadcn(radix)，并一并钉死 TanStack Virtual / zustand / 自写 SSE 客户端 | 问题不完整：只定了框架与组件库，却把虚拟滚动、状态管理、SSE 客户端留空——而这三样正是「六个任务长出六种写法」的高发区。代码实际由模型产出，React + shadcn 训练数据最厚，且 shadcn 是复制进仓库的源码而非黑盒依赖，改写成本最低 | E-143~E-145 | Web UI 由 daemon 静态托管同一份产物，桌面壳与手机壳加载的是同一个 URL | high |
| 25 | 两个壳分别用什么 | sound | A 桌面 Tauri v2 + 手机 Capacitor | 选项 B 是无效项（问题自己已指出它与决定 3 冲突）。决定 3 已把壳限定为「窗口 + 系统通知 + 令牌存储、零业务逻辑」，Tauri v2 与 Capacitor 恰好都是纯 WebView 容器定位，且 Tauri v2 的 single-instance / autostart / notification 官方插件正好一一对上这三件事；壳零逻辑也意味着极易回退——真被 Rust 工具链卡住，换 Electron 只影响几十行壳代码 | E-146~E-148 | 手机端第一版只做 Android；iOS 需 macOS 构建，不在范围内 | high |
| 26 | 本地持久化用什么 | sound | A SQLite（better-sqlite3 + WAL）存结构化数据 + 文件系统存日志正文 | 「GB 级日志 + 文档指纹比对 + 列表筛选分页」决定了结构化数据与大文本分离；日志按运行落独立 NDJSON 文件并在库里记录字节偏移索引 | E-149~E-152 | 数据目录默认使用当前平台的用户应用数据目录，可由配置覆盖 | high |
| 27 | daemon 与客户端之间用什么协议 | incomplete | A HTTP(REST) + SSE，但**必须自写 SSE 客户端**（fetch + ReadableStream） | 问题不完整：漏了一个会直接撞车的约束——浏览器原生 `EventSource` 不能带 Authorization 头，而决定 2 规定鉴权默认开启、E-08 明确禁止「局域网免鉴权」分支，所以选项 A 必须附带「不用原生 EventSource」才成立。E-10 的「按 event id 入队补推去重」正是 SSE `Last-Event-ID` 的原生语义 | E-153~E-158 | 局域网与自接隧道场景下 HTTP/1.1 足够，单条 SSE 连接不会触及浏览器每源 6 连接上限，不引入 HTTP/2 | high |
| 28 | 视觉方向要不要让人看图再定 | sound | C 采用这一版，但登记「未经目视确认」风险 + 把「第一个前端任务先出一屏真页给用户看」写成验收前置 | 需求未覆盖观感（原话通篇只有功能动词），按可逆优先 +「宁可朴素能交付」——C 与 A 交付物完全相同，只多两段文字，却不把「用户没看过图」伪装成「用户已同意」；而 B 要求用户回来看图，与本次一次性跑完的契约直接冲突 | E-159~E-162 | 24 节文档里存在风险登记与验收前置两个可挂载点，且第一个前端任务本就要在用户面前跑起来，因此这次目视确认不新增一次往返 | high |
| 29 | 桌面端 5 条以上运行流怎么排 | sound | C 完整档 + 紧凑档开关，紧凑档用换行网格而非横向滚动 | 原话第 2 条写的是「可以**进入**任务调度中查看…会话内容」——用户自己的心智模型就是「外面看状态、进去读内容」，C 是这句话的字面实现；而 B 两头不讨好（扫状态用不上那 5 行日志，读日志又不够） | E-163~E-168 | 紧凑档卡片是完整档卡片的严格子集（同一组件 + 一个截断标志位），是一套设计的两个密度而非两套设计 | high |
| 30 | 浅色模式做到多完整 | sound | B 只保证「能读」，但把下限写成可机检的验收（token 对比度脚本校验 + 语义靠形状文字） | 需求未覆盖浅色，按「宁可朴素能交付」+ 单人自用取 B；但必须现在就把「能读」写成可机检下限——**token 对比度用脚本校验属于验收，逐组件截图打磨不属于**——否则「能读」不是验收标准而是一句安慰话 | E-169~E-173 | token 对比度校验是几十行脚本（遍历前景/背景配对算 WCAG 比值），可进 CI，成本远低于逐组件截图验收 | high |
| 31 | 等宽字体自托管还是系统栈 | sound | A 自托管（Commit Mono，OFL） | 原话第 2 条要「查看会话内容」且要**两个端**都有——日志正文就是本产品的主要内容，而 Windows WebView2 与 Android WebView 的等宽字形完全不重合，走系统栈等于同一份日志两端列宽与数字对齐不一致；产品装在原生壳里走局域网，80-140KB 不是公网首屏预算 | E-174~E-176 | 字体随 daemon 静态资源分发并由 HTTP 缓存，局域网首次加载增量在毫秒级 | high |
| 32 | 手机端能不能派发 | flawed | D-counter 手机端加**「原样重跑」且仅此一种**（复用原派发载荷、无任何选择器、不进拇指条、需一次确认）；换 agent/换模型重派保持桌面端独占 | 问题被驳回：题面两处不成立——其一，选项 A 说手机「只能看+停+批」，但决策 10 的派发前闸门审批键按 E-107 就在手机拇指区，**手机今天已经在触发派发**；其二，选项 B 把「原样重派」（无需选择、复用载荷）和「换模型再跑」（要三级选择器）捆成一项，两者 UI 成本与误触代价差一个数量级。理由：一条挂掉的任务会卡住整批后续派发，若唯一处置面在桌面端，人在外一天就是流水线空转一天 | E-177~E-182 | 派发载荷已按派发时刻快照落库，重跑只是取出同一份载荷再调一次已有派发接口，新增实现量在半天以内 | high |
| 33 | agent 身份用什么视觉形式 | sound | A 字母组 monogram + 中性 ink 片 | 决策 6 与 15 已把 agent 定成**数据层可配置的插件式注册表**（用户可自行接入第五、第六个），字母组只是注册表里多一个两字符字段、零资源，而厂商 logo 需要一条素材管线且用户新接的 agent 根本没有图——视觉形式必须能承受这个已定架构 | E-183~E-185 | 用户在原话里就是用产品名称指代 agent 的，文字化身份与其自身用语一致 | high |
| 34 | 适配层以什么为主 | flawed | D-counter **原生为主干 + ACP 只做词汇表与扩展槽**；不把 acpx 作为运行时依赖 | 问题被驳回：选项不互斥且成本描述失实——D（acpx）不是并列项而是 B 的一种实现；C 记为「复杂度更高」是错的，因为 grok 的 `streaming-json` 本身就是 ACP session updates，把 ACP 当**事件词汇表**零成本，贵的是把它当**传输主干**。更关键的是四个选项都默认「ACP 入口与原生通道同样可靠」，但本机实测过的只有原生四家。原话「此类 agents 产品」预设了第 5、6 个（支持保留 ACP 扩展槽），而第 4 点「需要确认…是否可以从外部发派」表明用户不接受把主干押在未验证通道上 | E-186~E-190 | 四家原生事件都能映射到 ACP 词汇而不丢本产品需要的信息；某家有无法映射的私有字段时，允许适配器携带并只在该 agent 详情视图展示 | high |
| 35 | DeepSeek Harness 第一版接不接 | incomplete | B 仍不接，但改写理由并把接入做成填空题 | 问题不完整：漏了最要紧的事实——**`dsh` 本机根本没装**，另外四家都装了且跑过 `--help`。对单人自用工具，无法在本机验证、用户自己都没装的东西不该进第一版；且决策 34 给了通用 ACP 适配器后，「接不接」退化成填一行注册表数据 | E-191~E-194 | 用户点名 DeepSeek Harness 是「希望将来能用」而非「第一版必须有」——依据是他在同一段话里把「能否从外部发派」列为待确认的难点，而不是当作既成事实 | high |
| 36 | grok 二进制名冲突怎么处理 | flawed | C 探测 + 手填兜底，且**做成注册表里每个 agent 都有的通用字段** | 问题被驳回：伪选择——选项 B（手填绝对路径）在决策 15 里**已经存在**，所以 C 相对 A 的增量是零，真正要定的只有「加不加探测」。另外题面把它当 grok 特例，但 claude/pi/codex 同样可能被同名 shim 占位，应做成通用字段而非给 grok 打补丁 | E-195~E-199 | 官方 grok 与社区 grok-cli 的版本输出可区分（本机实测官方为 `grok 1.0.3 (1a29d5bc12)`、装在 `~/.grok/bin/`，社区包走 npm 全局目录）；若将来撞车，退化为按安装位置 + 文件哈希区分 | high |
| 37 | 模块切成 8 个还是 9 个 | incomplete | D-counter **放宽到 10 个并硬性封顶**：拆原 M1（运行底座 / 接入与设备信任）与原 M8（客户端界面 / 桌面壳与手机壳） | 问题不完整：题面把「只有 M1 一处被迫合并」当前提，但实际**第二处更严重**——原 M8 有 12 个任务且是「React 界面 + 两种壳工艺」的异质拼装，架构师明说并入的唯一理由就是模块数上限。只修小的留下大的，规则不自洽。已核查「模块数 ≤ 8」不是机检（`review.py` 唯一硬门是每模块 ≥ 3 个任务），且**拆分不新增任何任务、只改分组** | E-200 | 模块数上限 8 是流程软规则而非用户约束；`build_docs.py` 用 `\bM\d{1,2}\b` 解析，M1–M10 渲染正常 | high |
| 38 | 「pi cli」的身份确认 | flawed | A 按 `@earendil-works/pi-coding-agent` 写进文档 | 问题被驳回：前提已过时——不是推断，是**本机直接证据**。已在 `node_global/node_modules/@earendil-works/pi-coding-agent/package.json` 读到 `"version": "0.84.4"`、`bin.pi`，且该包**导出 `./client` 与 `./rpc-entry`（自带官方 TypeScript 客户端）**——这使原生 pi 适配器明显优于第三方 `pi-acp@0.0.33`，也是决策 34 选原生主干的一条硬支撑 | E-201~E-203 | none | high |
| 39 | 日志正文保留/归档/清理归哪个模块 | sound | B 归 M6（跟运行语义走） | 原话要「进入任务调度中查看会话内容」——「什么能删、什么必须留」直接决定这句话还成不成立，是运行语义判断不是磁盘事实；且 06 节 M6 职责原文已含「收流与落盘编排」与「厂商会话文件只读引用」，放 M6 是零改写的自洽延续，而 A 会让 M1「不带任何业务概念」的判据当场失效 | E-204~E-207 | M1 只对外提供「按路径删除 / 截断 / 报占用量」三个不带语义的磁盘原语，删什么由 M6 传入路径列表 | high |
| 40 | 开机自启注册归 M1 还是 M10 | incomplete | C **M1 提供机制 + M10 只负责触发**（决策 1 的 amendment） | 自启机制属于运行底座，壳只负责在用户操作时触发；这个责任边界不因平台增加而变化。三套原生实现由决策 55 补全 | E-208~E-212、E-261 | daemon 运行在装有 agent 的 Windows、macOS 或 Linux 宿主，手机端为纯客户端，故自启在手机侧无对物 | high |
| 41 | lint / format 工具链 | incomplete | B Biome 单一工具 | 问题不完整：B 的代价被写轻了——Biome **完全没有类型感知规则**，`no-floating-promises` 这类在 async 密集的 Fastify daemon 里真能抓 bug 的规则一条都拿不到，这个缺口必须显式兜底。仍选 B：分层禁令由 arch 测试零依赖扛下，ESLint 的 `no-restricted-imports` 属重复建设；本地唯一强制点仍是 `pnpm -w check`，决策 57 后三平台 CI 也执行同一命令；Biome→ESLint 迁移只换配置不动源码 | E-213~E-214 | 原「无 CI」前提已由决策 57 取代；Biome 以 devDependency 锁版本安装，保证本地与 CI 跨机一致 | high |
| 42 | 请求体校验的类型来源 | incomplete | A 手写 TS 接口 + 手写 JSON Schema，**但必须补上字段级机器检查** | 问题不完整：A 的「靠 routes.ts 常量表对齐」只能断言「端点有 schema」，断不出**字段级漂移**——加字段只改 TS 接口不改 schema 会编译通过、运行时静默漏校验。仍选 A：「shared 的 dependencies 必须为空」能被机器断言，价值高于省下的双写；且 A→zod 随时可做（只动 daemon 侧），反向要拆掉已进 web bundle 的运行时依赖 | E-215~E-217 | 校验执行器用 Fastify 内置 Ajv；`shared` 只导出 JSON Schema 常量对象与 TS 类型，二者零运行时依赖 | high |
| 43 | 会话正文要不要建全文索引 | sound | A 不建，搜索退化为按偏移分段扫文件 | 原话是「进入任务调度中**查看**会话内容」，全篇没有出现「搜索」二字；而 B 会推翻 09 节已成文的「库里任何列都不得存事件 payload 正文」，属不可逆的方向性改动。A 完全可逆——将来真需要即时搜索，FTS5 可扫已有 NDJSON **离线重建**索引，写入路径一行不改 | E-218~E-221 | 单个会话正文规模在几十 MB 量级封顶（决策 16 的保留上限告警已在这个量级兜底）；若实际出现单会话上百 MB，需回头重裁 | high |
| 44 | 路由怎么实现 | sound | B 自写 hash 路由（约 80 行） | 需求未覆盖路由实现，按可逆优先——原话第 2 条描述的导航深度只有一层，7 条平铺路径用不上嵌套/loader；而 react-router v7 的 data 模式会诱导模型写出与 SSE store 并列的第二条取数路径，**直接撞上 E-157「UI 不拿 POST 响应体直接改状态」** | E-222~E-225 | 运行详情页用虚拟列表 + 「翻页即停止跟随」，浏览器原生滚动恢复对它本就无效甚至有害，因此「没有滚动恢复」不是成本 | high |
| 45 | 无壳时设备令牌存哪 | incomplete | A sessionStorage + 顶栏常驻提示 | 问题不完整：题面把「无壳」当一种场景，实际是威胁模型差一档的两种；**更要紧的是漏了自举问题——`POST /pair/code` 需 device 令牌，全新装机零已配对设备时根本发不出第一个配对码**，选项 B 在两壳都延期的窗口里走不通。原话第 1 条要求界面随时能打开，而 R-01/R-02 都是已发生 | E-226~E-229 | 桌面浏览器直连的对象是本机 daemon（127.0.0.1）；若将来出现跨机直连，会话级明文的评估要重做 | high |
| 46 | 状态字形用什么承载 | incomplete | B 内联 SVG path，文本字符降级为 aria 文案来源 | 问题不完整：漏了「用 lucide」这个现成选项——但它没有「半填方块 = 部分完成」这类本产品自造语义，选它等于大部分形状仍要自画，塌回 B。原话第 1、2 条要求同一份状态在手机上同样可读，而字形是 E-110 下颜色之外的唯一替代信息，把它押在 Android WebView 的字体覆盖上等于把需求押在不可控项上 | E-230~E-234 | 12 个形状全部单色描边、可在 16×16 viewBox 内表达，总量 2KB 量级，无需字体子集化管线 | high |
| 47 | 窄屏各档是不是同一套组件 | incomplete | A 同一批组件 + 单点档位枚举 | 问题不完整：实际有**三档**窄表现（紧凑档 260px / <1100px 单列 / <400px 单栏），题面只问了后两档；且架构师引 E-165 作理由**引错了**——E-165 管的是紧凑档，narrow 与 phone-xs 的真正依据是 E-106（每流常驻停止控件）与 E-107（拇指区常驻）。结论不变但依据已更正。决策 29 已记录「紧凑档卡片是完整档的严格子集」这一模式，narrow 沿用是加法 | E-235~E-240 | <1100px 单列下「其余流留在同屏」物理上不可能，故该档停止控件下沉到每一行内部，仍算同一个常驻槽位实现 | high |
| 48 | 批次与并行窗口数从哪来（**纠正决策 9 的错误前提**） | incomplete | D-counter **批次自己按 `deps` 拓扑分层且规则与阅读器逐字一致；窗口数改为本产品自有设置（默认 2，可调 1-6），明确不移植 `bestLanes()`** | 问题不完整：选项 B 的收益不成立——分层是 `deps` 的纯函数复刻零成本，但 `bestLanes()` 经 `planLanes()` 调 `stOf()`，**读的是阅读器进度 localStorage**，逐字照抄也复现不出同一个默认值。**上次给的前提是错的**：payload 只有 7 个键，没有 batch 也没有 laneCount，交接台的「第 N 批」是浏览器 JS 现算的。补充需求「按批次派活」把交接台定为参照物，所以批次号必须对得上；而窗口数用户一个字没提，做成自有设置最省事也最容易改回来 | E-241~E-246 | 用户认为交接台里的「第 3 批」与调度器里的「第 3 批」是同一批任务；窗口数则不在用户心智内 | high |
| 49 | 要不要沿用阅读器里手调过的窗口数 | incomplete | A 不沿用，用自己的设置项 | 问题不完整：题面把那条键说成「用户真实意图的唯一记录」，但**只有用户点过窗口数按钮该键才存在**，没点过就无记录可继承，而两个选项都没定义读不到时怎么办。用户原话从未出现「并行窗口数」，为一个他没提过的数去凿开 E-83 属收益与代价倒挂 | E-247~E-249 | 用户能接受在调度器里再设一次；外部进程解析浏览器 LevelDB 去取一个 1-6 的整数，单人自用工具维护不起 | high |
| 50 | 要不要给「桌面版能否外部发派」一句正面结论 | sound | A 在 04 节实测表**上方**补结论段，逐条挂证据与**证据强度** | 原始需求第 4 点逐字是「难度：**需要确认** agents 桌面版，是否可以从外部发派任务」——「需要确认」是用户对文档下的明确交付项，把结论埋进 24 节引文让读者自行推断，等于这一条没交付 | E-250~E-252 | 04 节是「开发条件/本机实测」节，用户问的是能力事实而非风险，结论放这里比放 21 节更贴合 | high |
| 51 | dsh 提进第一版后走哪条通道 | sound | **走 `--profile headless`，接为第五家一等 agent；改造 M4-T12 承接，不新增任务** | 用户 2026-09-01 直接指示「把 dsh 提进第一版」。通道只有一个可选：**`--profile acp` 经实测确认不存在**（`dsh-acp` 各版本均无 `dsh.bundle`，是库不是 profile 层；`latest` 的 `0.0.1-rc.1` 依赖 404 装不上；实际启动零输出直接退出）。M4-T12 原设计「通用 ACP 适配器 + dsh 走 ACP 预置项」的前提就此被证伪，**本来就要返工**，改造它承接 dsh 原生适配最省——任务数仍 73、人天仍 115.5、批次账目一行不动 | E-28、E-191~E-194、E-253 | **代价是明写的**：headless 无流式，dsh 的运行轨只有三段、逐步耗时退化为整段耗时。已在 02 节非目标六写死「不伪造流式」，并新增能力位 `hasStreamingEvents`。用户在知晓该代价后仍选择接入 | high |
| 52 | 思考强度（reasoning effort）怎么表达 | sound | **三档抽象（低/中/高）+ 厂商参数映射，与决策 15 的权限档同一手法**；取值链与模型同构（每 agent 默认 + 单任务覆盖）；不支持的 agent 留空不传、显示「—」，不禁用 | 用户 2026-09-01 指出「派发后缺少模型与思考强度的显示」。文档此前只在 04 节顺带记过 codex 的 `model_reasoning_effort`，**既无数据字段也无取值链，是真缺口**。选三档而不是直填厂商原值，理由与 M4-T7 完全一致：五家的参数名与取值域各不相同（codex `model_reasoning_effort`、claude 思考预算、dsh 根本没有），让用户直填等于把厂商差异漏进 UI；而三档是用户真正会做的决定。不支持的 agent **显示「—」而不是补一个默认档冒充** | E-254~E-256 | 三档到各家参数的映射表是代码层，新增一家或某家改参数名要改代码；这与权限档承担的是同一笔代价 | high |
| 53 | 三平台支持覆盖到哪一层 | sound | B daemon、路径与子进程、Tauri 桌面壳均支持 Windows、macOS、Linux；任一层缺失都不得宣称该平台完整受支持 | 用户明确要求「MAC、win、Linux」均获得「支持与运行」，应按完整桌面产品可运行理解，不能让 macOS/Linux 退化为命令行加浏览器 | E-257~E-259 | 「支持与运行」指现有桌面产品整体，而非仅后台 daemon | high |
| 54 | 最低操作系统版本如何约束 | incomplete | B 绑定 Node.js 22 与 Tauri v2 官方支持范围；Windows 10/11、macOS 11+ 与主流 x64/arm64 Linux 的发布承诺以 CI 矩阵和发布资产为准 | 问题缺少用户给出的精确版本与 Linux 发行版；用户使用宽泛的「Linux」，需求未覆盖的版本细节按可逆优先，不把 Linux 偷换成 Ubuntu。macOS 取 Node.js 22 与 Tauri v2 支持范围的交集 11+ | E-139、E-258~E-260 | 只对 CI 矩阵系统与架构作持续验证承诺，其他主流 Linux 环境属于尽力兼容 | medium |
| 55 | 三平台开机自启如何实现 | sound | A M1 统一 `register/status/unregister` 契约，分别封装 Windows Task Scheduler、macOS LaunchAgent、Linux `systemd --user` | 用户要求三平台均能运行，且 daemon 既有架构允许脱离 Tauri 壳独立启动，自启不能强制依赖桌面壳 | E-208~E-212、E-261 | daemon 继续支持脱离 Tauri 壳独立启动 | high |
| 56 | 跨平台可执行文件与参数采用什么规则 | sound | A 全平台保持 `shell:false` + argv；Windows 包装脚本只经 ComSpec，macOS/Linux 解析绝对路径与符号链接并校验普通文件和可执行位后直接 spawn；路径 API 返回本机绝对路径，不跨平台改写格式 | 用户特别点出「文件路径的支持与运行」，只有按当前平台解析路径并直接启动，才能避免三套 shell 转义规则破坏带空格、Unicode 或特殊字符的路径；数据目录同样不得依赖 shell 展开 | E-42、E-129、E-130、E-262~E-264、E-270~E-271 | agent CLI 均有可由进程参数调用的入口，平台包装脚本是显式例外 | high |
| 57 | 没有三台本地实体机时如何验收三平台运行 | sound | A 增加 Windows、macOS、Ubuntu 三平台 CI，运行 workspace check、平台集成测试与桌面壳构建；本机仍保留 Windows 真机验收 | 用户说的是「支持与运行」而不是“代码看起来兼容”，必须由真实平台 runner 持续提供构建与系统调用证据 | E-265~E-269 | 允许使用 GitHub Actions 托管 runner；代码签名、公证和正式发布凭据不属于基础兼容验收 | high |
| 58 | 补充需求前两段（按窗口派发、自动新开会话派审查）要不要新增任务 | sound | 不新增：按窗口派发 = M8-T1 + M8-T3，自动新开会话派审查 = M7-T2；只在 01 节把原话对应到既有能力 | 用户 2026-09-10 直接裁定（D1）。两段描述的能力在既有任务表里逐条可指，再立任务就是重复 | — | 补充需求的「窗口」即本产品的并行窗口设置（决策 48） | high |
| 59 | 返工指令怎么从审查窗口回到实施窗口 | sound | 不建 PR 评论通道。产品自己派了审查运行、审查输出在产品手里：M7-T3 解析器扩展为同时提取 VERDICT 行、REWORK 段 `- R〈n〉` 条目与末尾 `rework` 围栏块，落库为审查运行的 rework_text；解析不出返工块按「审查未完成」转人、保留原文，人可一键把原文投进实施会话（复用 M6-T6 回话通路）；PR 评论只在人工流程留档 | 用户直接裁定（D2）。原话把「返工提示词或分支 PR 评论」并列为难点，但产品既然自己派审查，就没有必要再经 GitHub 绕一圈；技能 1.3.0 审查提示词的 `rework` 围栏就是为下游解析定的固定格式 | E-278 | 审查提示词的三段格式（VERDICT / REWORK / rework 围栏）由技能保证稳定 | high |
| 60 | 返工怎么派回实施会话 | sound | 按 agent 能力位三选一，全部计入 rework_count（上限 2，E-55）：原会话活着 → 回灌（M7-T4）；已结束但支持 resume → 恢复后回灌（E-112，明示新运行）；不支持续接（dsh headless，E-253）→ 新开实施运行：同一任务 attempt_no+1，在原 worktree 与分支上继续，提示词自包含 = 返工块原文 + 派发快照实施提示词的「收到返工指令时」规则 + 工作区指针 + 只改列出条目 | 用户直接裁定（D3）。分支由能力位决定而不是试了再降级，与 E-117「按能力位灰掉」同一手法 | E-277、E-279 | 派发快照里的 adapterKind 决定能力位，不读注册表当前值（E-93） | high |
| 61 | 「小批次总测试」是什么 | sound | 收口 = 一次运行做完「测试 → 逐任务复核 → 查接缝 → 修复 → 记录」，输出固定八段 BATCH_SUMMARY / TESTS / BUGS / FIXED / NOT_FIXED / SUSPECT / RECORD / NEXT，BUGS 与 NOT_FIXED 每条 `- B〈n〉 [S1|S2|S3] 涉及 〈任务ID〉（跨批）：现象 → 复现 → 根因 → 文件:行`；提示词来自 docs-data.js 的 dispatchBatches[层号].wrapup，按 tasks 集合匹配，无匹配用内置通用提示词并标「文档未提供」；wrapup 当引用材料，外层包装说明覆盖其中的提交、推送、PR、合并步骤：修在当前工作区、不 push、产品出落地清单 | 用户直接裁定（D4）。技能 1.3.0 已把收口提示词随派发包导出，产品照用即可 | E-274、E-286、E-296 | 收口 agent 会照八段格式回报；解析不出就转人不猜 | high |
| 62 | 收口什么时候触发 | sound | 本批全部任务 landed 且每个任务分支已进入当前 HEAD（沿用 E-70 的判定；merge-base 祖先判定失败时回退「分支相对 HEAD 无差异」）；判定放在 scheduler-tick 内，不新增 job；产品仍不 commit/push/merge（决策 12）、不回写交接台（决策 14），git 落地照技能流程由人执行 | 用户直接裁定（D5）。收口要在一个已经合并的 HEAD 上跑才有意义，而合并是人做的事 | E-272、E-273 | 人按技能流程用 merge commit 落地；squash 只靠无差异回退兜底 | high |
| 63 | 收口发现的问题怎么变成派发 | sound | 解析 TESTS 失败项、BUGS 未在 FIXED 里的项、NOT_FIXED 项，按「涉及 〈任务ID〉」分组；每个任务派一次修复运行（kind=implement，origin=wrapup-fix，spawned_by 指向收口运行，base=收口分支，权限档 workspaceWrite），提示词 = 修复条目原文（编号 R1…）+ 返工规则；修复运行走正常审查（M7），人按落地清单合并，全部进 HEAD 后收口第 2 轮；上限 2 轮，超限批次转 needs_attention 等人；跨批问题同样处理，批次树标「跨批修复」 | 用户直接裁定（D6）。「获取测试 agents 返回的测试问题总结并派发新的 agents 进行修复」原话就是这条链 | E-275、E-276、E-280、E-290 | 修复运行复用收口 worktree（收口修改未提交，只在该工作区） | high |
| 64 | 「最后自动标记任务」怎么落 | sound | 落地闸门改为可设自动，默认仍「等我确认」；自动时审查 pass 直接 landed（现有边 reviewing → landed）；landed 语义写明为「已验收」，任务行另显示「进 HEAD」派生标记；批次 done 由收口 clean/fixed 自动标；不新增 accepted 状态；E-49 扩展为下一批启动条件 = 本批全部 landed 且收口 clean/fixed；E-53 改写为闸门可自动 | 用户直接裁定（D7）。加 accepted 状态会动已落地的 M6-T1 状态机白名单，还要给 11 节形状表加形状；用既有的 landed + 派生标记最省 | E-281、E-298 | 09 节运行状态机的边集合不变 | high |
| 65 | 批次树怎么折叠 | sound | 左栏批次树与任务列表页都按批次折叠；batch.advanced 到达时自动展开该批，只增不减，不折叠用户手动展开的批；手动开合只存内存（ui-prefs 持久化白名单不变）；批次标题显示「已落地 x/n · 在跑 y · 等你 z」与收口状态；折叠默认值与「能否收口」都是 daemon 给的字段，前端不算 | 用户直接裁定（D8）。原话「按批次折叠，派发时自动展开正在派发的任务批次」 | E-284 | batch.advanced 在每次批次状态迁移时发 | high |
| 66 | 呼吸灯怎么做 | sound | 批次树里正在执行任务的行放 8px 圆点 + 1.6s 呼吸环，与运行轨当前步共用 --pulse 与同一 keyframe，算同一种动画；只对 starting/running/reviewing/reworking 呼吸，awaiting_* 静态暖色，终态无点；prefers-reduced-motion 下关闭；11 节「全页唯一的连续动画」改写为「呼吸环是全页唯一的连续动画种类，只出现在运行轨当前步与批次树执行行」 | 用户直接裁定（D9）。原话「在正在执行任务处添加呼吸灯」；不另开一种动画是守住 SPECTACLE 2 | E-282 | 两处 import 同一个展示组件，不靠 CSS class 复用 | high |
| 67 | 收口运行的名额与权限 | sound | 收口运行占并发额度，权限档 workspaceWrite，不用 unrestricted；审查运行仍 readOnly | 用户直接裁定（D10）。收口要修 bug 必须能写；审查不能写是 E-135 | E-283 | — | high |
| 68 | 落地清单要不要带记录落地的命令 | sound | M5-T4 的落地清单附一行可复制的 `python 〈docs〉/_run/build_docs.py 〈docs〉 --landed 〈ID〉`，复制而不执行 | 用户直接裁定（D11）。人合并后要在文档侧记录落地，产品把命令摆出来但不代跑（决策 14 不回写） | E-74 | — | high |
| 69 | 收口运行能否直接改代码修 bug | flawed | A 能修：收口运行 workspaceWrite，在自己的 wrapup 工作区里直接修根因并配测试，FIXED 段记下；只有它没修完的才派修复运行 | 问题被驳回为伪选择：D4 把「修复」写进收口五步、D6 以「BUGS 未在 FIXED 里的项」为派修复的输入、D10 直接给 workspaceWrite，三处已预设收口会改代码；选 B 与 D10 正面冲突 | E-294、E-295 | 收口运行的 wrapup 工作区就是修复运行的 base，不另开「报告分支」 | high |
| 70 | 修复运行要不要走正常审查 | flawed | A 走：修复运行 exited → 机械检查 → 审查 agent → pass 后按闸门 landed，人合入后第 2 轮收口再整体验一次 | 问题被驳回为伪选择：D6 原文已写「修复运行走正常审查（M7）」；补充需求「实施任务完成后进入审查」也没有例外。不为 origin=wrapup-fix 加任何审查例外分支 | E-300 | 修复运行的审查者沿用决策 11（同 agent 独立新 session、只读档） | high |
| 71 | 批次树默认展开哪些批 | sound | A 只展开有活在跑或等人的批：state ∈ running / wrapping / awaiting_landing / needs_attention 默认展开，其余折叠 | 补充需求逐字是「自动展开**正在派发的**任务批次」，只指有活在跑的批；「下一批」用户没提，需求未覆盖按可逆优先——将来要加只是 daemon 多给一个字段，前端不算 | E-284 | 展开默认值是 batches.state 的纯函数，daemon 不需要新状态 | high |
| 72 | 收口自动轮次上限定几 | flawed | A 2：做成代码常量 WRAPUP_AUTO_MAX_ROUNDS=2，不做设置项 | 问题被驳回为伪选择：D6 原文「上限 2 轮」由用户本人定过，与 E-55 同一个数；用户没提过要调它 | E-276 | 每批一个轮次计数，取 batch_wrapups 的最大 round | high |
| 73 | 产品要不要读取并显示文档侧 batchRecords | sound | A 不读不显示：产品只认自己的 batch_wrapups；文档侧记录人去阅读器看 | 决策 14「不回写交接台，调度器为唯一权威」已把收口记录的看台定在调度器；补充需求说的是产品自己标，没要求搬人工流程的记录；需求未覆盖展示，按可逆优先——将来要显示只是 M3 多读一个只读键 | E-296 | 用户切到本产品派活后不会同一批在两边各收口一次并期望一致 | high |
| 74 | 要不要做 C-101「收口运行与修复运行由哪个 agent／模型执行的取值来源」 | flawed | A 做，+0.5 人天，收进 M8-T6 | 问题被驳回为伪选择：选项 B 自述「无法闭合、只能硬编码」，与 D5（tick 自动触发）、D10（收口是带权限档的真实运行）直接冲突。原话「自动进入小批次总测试」时无人在场选 agent，取值来源必须确定 | E-287 | 收口触发时本批至少存在一条 landed 实施运行（D5 要求全部 landed） | high |
| 75 | 要不要做 C-102「批次 needs_attention 后人的出口动作与收口运行失败的归宿」 | flawed | A 做，+1 人天，收进 M8-T6：批次级收口闸门 + 手动再收口 | 问题被驳回为伪选择加粒度问题：D6「转 needs_attention **等人**」预设人有动作可做；D7 的下一批启动条件让没有出口的 needs_attention 会把整条自动流停死。两个独立决定结论一致故合并裁 | E-288、E-295 | 批次级闸门复用 gates 表（task_id 允许为空），不引入新表；08 节 job 仍五个 | high |
| 76 | 要不要做 C-103「收口运行与收口结果在界面上的落位」 | flawed | A 做，+1 人天，新增 M9-T20 | 问题被驳回为粒度与伪选择：三件事里两件不是选择——批次级落地清单是 D4「产品出落地清单」已定的交付物，收口运行有停止控件是 E-107 硬约束；真正的「做不做」只剩八段可读面板。原话「获取测试 agents 返回的测试问题总结」「方便开发者跟踪进度」要求人能看到这份总结 | E-297 | 面板只在 daemon 已解析结果上做只读展示，不新增存储 | high |
| 77 | 要不要做 C-104「跨批修复运行对前置批次的状态语义」 | flawed | A 做，+0.5 人天，收进 M8-T7：已 done 的批次不因跨批修复重触发收口；计数按最近一次非修复运行；修复运行归触发批次 | 问题被驳回为伪选择：「不做」并不省人天，M8-T6 与 M9-T19 无论如何要各写一条派生判定，区别只是一致还是各猜；B 接受的「收口循环」与 D6 上限、D10 名额语义冲突 | E-275 | origin=wrapup-fix 在 runs 表可查询，派生状态只有一个函数 | high |
| 78 | 要不要做 C-105「落地闸门自动时人对已 landed 任务的撤回动作」 | sound | A 做，+0.5 人天，收进 M8-T7：POST /tasks/:taskId/recall 附一句话起一次修复运行 | 原始功能点 1 在决策 19 已被解读为「发现异常后能处置」；D7 是用户主动要的自动档，自动档不能比手动档（E-59 打回可用）少一个处置出口，且只是复用 D6 的修复运行路径 | E-293、E-300 | 撤回起的修复运行 spawned_by 为空、origin 沿用 wrapup-fix，不引入新运行种类 | medium |
| 79 | 闸门设置存在哪 | sound | A 新迁移加 `settings(key TEXT PK, value_json TEXT NOT NULL, updated_at TEXT NOT NULL)`，闸门存 key='gates' | 10 节 PATCH /settings/gates 不带 docId、E-56 也是全局措辞，闸门本就是全局的；需求未覆盖存储位置按可逆优先——挂 documents 会造出「换文档闸门变」这个用户没要过的语义 | E-292 | settings 表只放本产品全局键，lane_count 仍留在 documents（决策 48） | high |
| 80 | 自动收口用哪个 agent／模型 | incomplete | A 取本批 ended_at 最晚的 landed 实施运行快照里的 agent/model/effort（并列取 run id 字典序最大）；第 2 轮起沿用上一轮收口运行的取值；手动触发可在 body 覆盖；推导出的 agent 不可用则不派、批次 needs_attention，禁止静默换家 | 问题不完整：选项 A 没说 agent 不可用与第 2 轮取谁，裁决已补。补充需求只要求「自动进入」，没有「选收口用的 agent」这件事；决策 11 已定审查者「从实施运行推导、零配置可用」，收口沿用同一手法；B 的设置面是用户没要的 UI | E-287 | 收口运行权限档恒 workspaceWrite（D10），只有 agent/model/effort 三项需要来源 | high |
| 81 | needs_attention 后人怎么让批次 done | incomplete | D-counter 保留 A 的骨架（手动 POST /wrapup 不受自动上限、受硬顶 6；不加 close 路由、不加人工直跳边），但把「批次级收口闸门」统一为 needs_attention 的唯一人工出口：每次进入 needs_attention 都在最近一条收口运行上开一张 task_id 为空的收口闸门；pass 必须带 comment → 写一条 is_human_verdict=1、verdict=clean 的收口记录 → 批次 done；reject 只关闭闸门；同一 round 允许「机器裁定 + 人工裁定」两行 | 问题不完整：A 的人工闸门只开在「解析失败」一种原因上，而 needs_attention 主因是「2 轮仍 open」，A 下唯一出口是再烧 3～6 轮赌它变 clean；B 与 D7「done 由收口自动标」冲突。人工裁定走「收口记录 + 闸门 decide」仍在收口体系内 | E-288 | 人工裁定 clean 表示「我接受本批遗留」，产品不再对该批自动派任何东西 | medium |
| 82 | landed 运行的本地分支已不存在时怎么判进 HEAD | incomplete | D-counter tick 在分支存在时把分支尖 SHA 记进 runs.branch_tip_sha；分支不存在时：有 SHA → 用 SHA 做同一套祖先/无差异判定，都否则 method=branch_gone_unmerged 并给 `git branch 〈原分支〉 〈sha〉` 可复制命令；无 SHA 且 worktree 已清理 → 视为进 HEAD（method=branch_gone）并暴露 method；worktree 仍在 → 用其 HEAD SHA 判定 | 问题不完整：两个选项都把「分支没了就无从判定」当前提，但祖先判定的对象是 commit 不是分支名，SHA 零成本可记；A 只作最后兜底。技能标准流程 `gh pr merge --merge` 后 `git branch -d` 只删已合并分支，B 会让每个按流程落地的任务卡死 | E-289、E-301 | 用户按技能流程用 merge commit 落地；SHA 写在 tick 的批量 UPDATE 事务里，不在事务内跑 git | medium |
| 83 | TESTS 失败项无「涉及 〈任务ID〉」时怎么办 | sound | A 进 unassigned（有效裁定 open）；有其他可派条目照派、靠第 2 轮复跑兜底；全部开放项无主才 needs_attention | 补充需求「有问题需要重新派发新窗口 agents 进行修复」与「最后自动标记任务」都指向不因一行无主失败停摆；D6 已把无主项计入 unassigned 且裁定 open，绝不会被标 clean；B 让「自动」名存实亡 | E-290 | 收口 agent 第 2 轮会重跑同一套 testCommands；产品不猜无主失败属于谁 | high |
| 84 | 迁移要整表重建时怎么处理外键与「其余 PRAGMA 一律不设」 | sound | A 08 节加唯一例外：首行标 `-- requires: foreign_keys=off` 的迁移在事务外关外键、事务内重建、执行后 foreign_key_check 必须零行否则回滚并以非 0 退出、finally 开回；09 节兼容策略加第 6 条「扩枚举 CHECK 与放宽 NOT NULL 走整表重建，重建不算改已应用文件」 | 需求未覆盖迁移手法，按可逆优先——A 是 SQLite 官方 12 步法原样落地，例外范围被首行标记限死；B 的「CHECK 不进 schema、哨兵任务行」会污染任务列表与派生状态，是不可逆的方向性妥协；08 节原意是连接级调优不散落，不是禁止迁移按官方步骤操作 | E-291 | better-sqlite3 允许在事务外 `db.pragma('foreign_keys = OFF')` 再进入事务；例外只影响 db/migrate.ts 一个分支与 arch 测试一条白名单 | high |
| 85 | 收口裁定为 fixed 时批次徽标用什么形状 | flawed | A 与 clean 同用既有 `succeeded ✓`，靠文案「已收口 · 已修」/「已收口 · 干净」区分；open 用 `partial ◧` 暖色「有遗留 · N 条」；needs_attention 用 `◆` 暖色「收口等你」；收口进行中只用中性文案「收口中 · 第 n/2 轮」不占形状；任一轮 FIXED 非空即显示「已修」，轮次进 title | 问题被驳回为伪选择：B 被 11 节「暖色 = 要你」直接排除；D7 把 fixed 与 clean 同定为无需人介入的 done 终态。文案与交接台既有的「已收口 〈日期〉 · 干净／已修／有遗留」一致 | E-297 | 收口裁定枚举只有 clean / fixed / open 三种，needs_attention 是批次状态不是第四种裁定 | high |
| 86 | 窄屏 narrow 档（<1100px 单列）批次树放哪 | incomplete | D-counter 不单独处理：整个左栏沿用 1100–1180 档的同一抽屉（同一组件、同一展开集），不新增「批次」按钮；单列首行保留运行甲板已有的批次汇总条，显示最近一次 batch.advanced 的批「第 N 批 · 已落地 x/n · 在跑 y · 等你 z · 收口状态」，点击打开抽屉并滚到该批标题；抽屉关着时 batch.advanced 只并入展开集、不自动弹开 | 问题不完整：漏了第三条路——11 节 1100–1180 档已把整个左栏做成抽屉，A 会在 1150px 与 1050px 之间造成档位跳变；且 D8 定义的是展开集不是可见性。决策 47 已把 narrow 首屏让给带常驻停止控件的流行（E-106） | E-284 | 批次汇总条在桌面各档都存在于运行甲板；narrow 档补渲染它只是同一组件的一行档位分支 | medium |
