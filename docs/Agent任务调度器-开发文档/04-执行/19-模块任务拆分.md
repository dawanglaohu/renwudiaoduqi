# 模块任务拆分

**粒度**：一个任务 = 一个人 0.5–2 天。标题是「动词 + 具体对象 + 可验证产出」。
**每条验收标准都必须可客观判定**，且引用它覆盖的边界编号（`E-XX`，见 13 节）。

**所有任务的验收标准都隐含一条**：`pnpm -w check` 必须通过
（`biome ci .` + `tsc -b --noEmit` + `vitest run`）——这是每个任务的本地强制点；
三平台发布还必须通过 17 节的 CI 矩阵。
下表中只在需要强调时才重复写出。

| ID | 标题 | 模块 | 依赖 | 输入 | 产出 | 验收标准 | 预估 |
|----|------|------|------|------|------|----------|------|
| M1-T1 | 仓库骨架、进程入口与 Node 版本垫片 | M1 | 无 | 空仓库；05 节的技术栈取值 | pnpm 单仓五包骨架（各包含 `package.json` + `tsconfig.json`）、`biome.json`、`.gitattributes`（无 BOM）、`.gitignore`（含 `node_modules/` 与 `**/*.tsbuildinfo`，无 BOM）、根 `tsconfig.json`/`vitest.config.ts`/`pnpm-lock.yaml`、垫片 `packages/daemon/bootstrap.mjs`（CJS 语法、不依赖 Node 22 专属能力）、`src/main.ts`（TS）、`src/boot/`（node-check、guards、容器接线点）、`packages/daemon/test/unit/` | 1) Node 20 下 `node packages/daemon/bootstrap.mjs` 先打印 `requires Node.js >= 22` 并以非 0 退出；Node 22 下进程持锁常驻（E-139） 2) `packages/daemon/bootstrap.mjs` 是唯一允许以 `node xxx.mjs` 启动的入口；`main.ts` 只靠垫片加载 3) `process.on('unhandledRejection')` 落运行日志，fire-and-forget 一律显式 `void fn()`；`uncaughtException` 打印后由注入 fatal-exit 回调进 `main.ts` 的 `process.exit(1)`（E-213） 4) `biome.json` 设 `formatter.lineEnding:"lf"` 并配 `.gitattributes`（E-214） 5) `assertNever` 外不允许 `throw new Error('...')`，垫片内异常不出现 | 2d |
| M1-T2 | SQLite 连接工厂、四条 PRAGMA 与迁移执行器 | M1 | M1-T1 | M1-T1 的 boot 序列 | `src/db/openDatabase.ts`、`migrate.ts`、`unitOfWork.ts`、`migrations/0001_init.sql` | 1) 打开后四条 PRAGMA（`journal_mode=WAL`、`busy_timeout=5000`、`foreign_keys=ON`、`synchronous=NORMAL`）可被查询验证，且启动失败即退出绝不双写（E-150） 2) 迁移按文件名升序在独立事务执行并写 `schema_migrations` 3) `unitOfWork.run()` 在 `depth>0` 时抛 `E_TX_NESTED`，不用 savepoint 隐式吞掉 4) 原生模块 ABI 不匹配时捕获加载失败并提示 `pnpm rebuild`，不抛裸栈（E-152） | 1d |
| M1-T3 | 三平台路径、数据目录与可执行文件解析 | M1 | M1-T1 | M1-T1 | `src/platform/contract.ts`、`host.ts`、三平台 `path` 实现、`resolveExecutable`、Windows `quoteForCmd` / `comSpec`、`shared/src/shell/daemon-launch-spec.ts` | 1) `appDataDir(hostInputs)` 返回规范化绝对路径：Windows 采用绝对 `APPDATA`，否则回落 `homedir/AppData/Roaming`；macOS 取 `homedir/Library/Application Support`；Linux 采用绝对 `XDG_DATA_HOME`，否则回落 `homedir/.local/share`，最后追加应用目录；覆盖变量缺失、相对值、空格与 Unicode 主目录，结果不得含 `~`、`${...}`、`%...%`，主目录不绝对则报 `E_DATA_DIR_UNRESOLVABLE`（E-271） 2) Windows 盘符、UNC、长路径、空格与 Unicode 可实际读写；不因传统 260 字符限制或字符本身拒绝（E-129） 3) macOS/Linux 解析符号链接，要求普通文件与 `X_OK`；无执行位返回 `E_AGENT_EXEC_NOT_EXECUTABLE`，坏链接/成环/非文件返回 `E_AGENT_EXEC_INVALID_TARGET`，不自动 chmod、不回退 PATH（E-262、E-263） 4) 另一平台路径格式保留原值但标无效，绝不猜测转换（E-264） 5) Windows `.cmd`/`.bat` 只经 `ComSpec`，适配器接收完整原始参数数组后统一包装；`quoteForCmd()` 与包装接口须以真实子进程验证最终 argv，覆盖普通参数、空参数、空格、Unicode、引号、分号（U+003B）、`&`、`^`、`%`、`!`，含空格与元字符的脚本路径也可启动；ComSpec 分支按 08 节设置 `windowsVerbatimArguments:true`，NUL/CR/LF 参数启动前报 `E_VALIDATION`，不得改变参数或执行额外命令 6) 最小环境下优先显式绝对路径，缺失时列出固定候选；代码不读取或执行 shell 启动文件（E-270） 7) `DaemonLaunchSpec` 只含绝对 `file`、`args[]`、绝对 `cwd`，共享 schema 拒绝相对路径与 shell 字符串（E-209） | 1.5d |
| M1-T4 | 日志正文追加写、字节偏移索引与切片 | M1 | M1-T2 | M1-T2 的库连接 | `src/logstore/`：`AppendQueue` / 文件读写与恢复扫描 / 200MB 切片；`repo/events-index-repo.ts`、`repo/log-segments-repo.ts` 保存索引；`service/logstore.ts` 编排先文件后索引；`jobs/log-index-repair.ts` 的启动入口调用该 service，扫描与写库不放在 job 内 | 1) 恒先 append 文件成功再写索引行，代码中不存在反序路径 2) 单次运行超 200MB 时按大小切片并在 `log_segments` 记边界（E-149） 3) 崩溃重启后从最后一条索引的 `byte_offset+byte_len` 扫到 EOF 补齐；「索引比文件多」记 `error` 而非静默修正（E-24） 4) 日志文件被手工删除时返回 `E_LOG_FILE_MISSING` 类型化结果而非抛异常（E-151） | 2d |
| M1-T5 | 磁盘占用统计、写满兜底与受限删除原语 | M1 | M1-T4 | M1-T4 的 logstore | `disk-watch` job、`deleteByPath` / `truncate` / `usage` 三个不带业务语义的原语、`http/routes/system.ts` | 1) 占用超阈值发 `system.disk_warning` 并置「停止新派发」标志（E-103） 2) 存储盘写满时立即停止新派发并明确报错；恢复后能继续追加且已有记录不损坏（E-104） 3) M1 只上报写满，**绝不自行删除任何正文**（E-205） 4) 收到白名单目录之外的删除请求一律拒绝并记违规日志（E-206） 5) 删除目标正被读取（EBUSY）时返回可重试错误而非抛异常（E-204） | 1.5d |
| M1-T6 | 开机自启注册机制与优雅退出对账 | M1 | M1-T1, M1-T9 | M1-T9 的 platform 自启契约与 `DaemonLaunchSpec` | `boot/autostart.ts`、`shutdown()`、`reconcile-runs` job | 1) `register(spec)` 以绝对 `file`、`args[]`、绝对 `cwd` 写入原生自启项且幂等，重复调用不产生第二项；逐字段变化才重写，绝不把三字段拼成 shell 命令（E-209）；注册失败为非致命，daemon 仍可被当场拉起（E-210） 2) Windows Task Scheduler、macOS LaunchAgent、Linux `systemd --user` 均通过同一契约；Linux 没有用户实例时返回 unsupported、给手动启动命令且不写系统级服务（E-261） 3) 不装壳、直接命令行起 daemon 时全部功能照常，自启从未注册（E-212） 4) 优雅退出先停 jobs 再关 HTTP，且**不杀 agent 子进程**（E-01） 5) 重启后按 `runs.pid` 对账：进程不存在标「异常终止」、存在但接不回标「失联」，**绝不推定成功**（E-02、E-123） | 1.5d |
| M1-T7 | 三平台子进程 spawn 基座与行读取器 | M1 | M1-T3, M1-T9 | M1-T3 的路径解析；M1-T9 的进程终止契约 | `src/proc/`：`spawnManaged` / `line-reader` / `env` / `timers` / `registry` | 1) 调用方传原始参数数组且恒 `shell:false`，业务层不得拼命令串（E-42） 2) Windows `.cmd`/`.bat` 将完整参数交 M1-T3 适配器生成冻结的 ComSpec 启动对象，`proc` 原样消费其 args 与 Windows spawn 选项，不追加或二次转义；macOS/Linux 对已校验目标直接 spawn，并为托管运行建立独立进程组（E-119、E-130） 3) 行读取器在 Buffer 上只按 `0x0A` 切、剥尾部 `\r`、再 UTF-8 解码；**含 `U+2028` 的输出必须不被断行**，配回归用例（E-131、E-203） 4) 单行超 1 MiB 截断并附 `{truncated,rawByteLen}` 后继续找下一个 `\n`（E-141） 5) 非 JSON 行归入 `raw.log` 并继续，绝不中断流（E-140） 6) 子进程环境禁用 git 凭据五项且强制 UTF-8；最小环境不读取 shell 启动文件（E-138、E-270） | 2d |
| M1-T8 | 三档超时计时器与背压 | M1 | M1-T7 | M1-T7 的 spawn 基座 | `proc/timers.ts`、`AppendQueue` 背压接线 | 1) 启动超时与思考超时**分开计时**，native 默认 60s、generic-acp 默认 180s（E-190） 2) `idleTimeoutMs` 只喂给停滞检测打弱标记，**proc 层绝不因它杀进程**（E-120） 3) 待写字节超 8 MiB 时 `pause()`、回落 4 MiB 再 `resume()`；代码中不存在把 stdout 读进内存字符串的路径 4) 内存只保留最近 **5000** 条事件的环形缓冲（与 M2-T4 的重放窗口是同一个缓冲，**不另开第二个**），单条 payload 超 32 KiB 入缓冲前替换为引用（E-142） | 1.5d |
| M2-T1 | REST 骨架、插件链顺序与错误信封 | M2 | M1-T2, M1-T10 | M1-T10 的 `createContainer(config)`、`createHttpServer()` 与最小健康检查实例；10 节端点表 | `src/http/plugins/`（00→90 八个）、`errors/AppError.ts`、`httpStatus.ts`、沿用 `http/routes/health.ts` 补齐 10 节健康诊断字段 | 1) 插件按数字前缀注册且顺序为 request-id → logging → security-headers → auth → ratelimit → routes → static → error-handler 2) `90-error-handler` 是全仓唯一出现 HTTP 状态码数字的文件；未登记 code 与非 AppError 一律 500 + `E_INTERNAL` 3) logger `redact` 覆盖 `authorization`、`*.token`、`*.pairingCode`、`*.deviceToken`，日志中不出现明文令牌 4) 不注册 `@fastify/cors`；代码中出现即判失败 5) 错误信封的 `details` 字段能承载结构化信息（如实测版本串与期望模式），供前端就地渲染而不必解析 message（E-195） | 1.5d |
| M1-T9 | 三平台进程树与原生自启适配器 | M1 | M1-T3 | M1-T3 的 platform 契约、宿主路径输入与 `DaemonLaunchSpec` | `src/platform/windows.ts`、`darwin.ts`、`linux.ts` 与平台集成测试 | 1) Windows `killTree` 先 `taskkill /PID 〈pid〉 /T`，3 秒后仍存活再加 `/F`；macOS/Linux 对独立进程组先 `SIGTERM`，3 秒后仍存活再 `SIGKILL`，每次尝试都写事件（E-119） 2) 三平台实现同一 `autostart.register(spec)/status(spec)/unregister()` 契约，分别使用 Task Scheduler、LaunchAgent、`systemd --user`；完整保留 `file/args/cwd`，业务代码不直接判断平台（E-132、E-209） 3) 自启集成测试使用唯一隔离名称并在 `finally` 清理；残留即测试失败且输出人工删除命令（E-269） 4) 三平台之外返回 `E_PLATFORM_UNSUPPORTED`，不得静默套用相近实现 | 2d |
| M1-T10 | 三平台单实例锁、配置装载与最小常驻 daemon | M1 | M1-T1, M1-T2, M1-T3 | M1-T1 的垫片与 boot；M1-T3 的 platform 路径契约；M1-T2 已落地的数据库与迁移工厂 | `src/platform/` 锁原语（锁路径/权限在 platform 收口）、`src/boot/lock.ts`、`src/config/env.ts`、`src/boot/container.ts`（composition root，M2-T1 在此挂插件）、由 `src/main.ts` 调用的 `createHttpServer()` 工厂（本任务建实例并 listen，提供最小 `GET /api/v1/health` 响应 `{ok:true}`；M2-T1 在同一路由补齐 10 节诊断字段并挂插件，不重复注册）、daemon `test/{unit,integration,arch}/`（按 17 节覆盖真实入口、双用户/不同端口互斥、原生权限、陈旧锁并发回收、探活结果不确定、端口占用与启动顺序） | 1) 三平台锁路径明确：Windows `%PROGRAMDATA%\agent-scheduler\daemon.lock`、macOS `/Library/Application Support/agent-scheduler/daemon.lock`、Linux `/var/lib/agent-scheduler/daemon.lock`；锁文件存 `{pid, uid, startedAt, port, bind}` ；同机多用户共用同一锁。为实现 Windows 整机锁，本任务在 `config/env.ts` 额外快照宿主 `PROGRAMDATA` 与 `SystemRoot`，仅分别交给 platform 锁目录和绝对 `icacls.exe` 解析，不纳入五项 `AGSCHED_*` 产品配置或用户数据目录契约 2) 锁文件权限：Windows ACL 仅 Administrators/SYSTEM；macOS root:admin 0600；Linux root:root 0600；用户权限不足时打印所需权限并以非 0 退出，不静默降为每用户锁 3) 第二实例读出持有者 pid 与端口并以 `GET /api/v1/health` 探活；二者都活→打印英文诊断 `Existing instance pid=... port=...`（含义为「已有实例 pid= port=」）后以非 0 退出；两不活→删陈旧锁后重新原子竞争；仅一项存活或任一探测结果不确定时保留锁并拒绝启动，防止持锁者尚未 listen 时被误删 4) listen 被占（EADDRINUSE）时释放锁并以非 0 退出（E-03） 5) `AGSCHED_PORT=abc` 在 config/env.ts 报错；**config/env.ts 是全仓唯一允许读 `process.env` 的文件** 6) 启动顺序：配置装载 → 锁 → 数据目录 → （库/迁移 M1-T2） → createContainer → createHttpServer → listen → listen 成功后报告 ready（本任务无 jobs 段） | 2d |
| M2-T2 | 设备配对、令牌哈希与吊销 | M2 | M2-T1 | M2-T1 的插件链 | `service/pairing.ts`、`repo/devices.ts`、`/pair/*` 与 `/devices/*` 路由 | 1) 配对码一次性、TTL ≤60 秒、只在内存；用掉或过期即失效（E-07） 2) 令牌只存 `scrypt` 哈希 + 随机盐，比对用 `timingSafeEqual`，库中无明文 3) 吊销后该设备的活动连接立即断开并返回 401（E-127） 4) `/pair/*` 每 IP 每分钟 10 次，超限 429 **并立即作废当前配对码**（E-07） 5) 零已配对设备时把一次性配对码打印到控制台并写入数据目录 `pairing-code.txt`；POSIX 断言 `0600`，Windows 断言当前用户专用 DACL，用后即删；**不新增第四个免鉴权端点**（E-226） | 2d |
| M2-T3 | 鉴权中间件与免鉴权白名单 | M2 | M2-T2 | M2-T2 的令牌体系 | `http/plugins/30-auth.ts`、白名单常量数组 | 1) 鉴权 hook 注册在 `/api/v1` 子 scope 内部而非根实例，静态资源不被拦、后注册路由不漏 2) 免鉴权白名单只有 `/health`、`/pair/claim`、`/version` 三条且写死在常量数组里 3) **代码中不存在任何基于 remoteAddress／网段／`isPrivateIp` 的绕过分支**（E-08） 4) 401/403 复用同一响应信封并给出 `E_UNAUTHORIZED` / `E_DEVICE_REVOKED` 明确 code 5) `AGSCHED_DEV=1` 不改变任何鉴权行为，仅影响错误响应带不带 stack 6) 每条写操作记录 `actorDeviceId`（谁派的、谁中止的、谁回的话），系统发起记 `null`；将来加人不必迁移历史数据（E-128） | 1.5d |
| M2-T4 | 事件信封、单调 id 分配器与重放缓冲 | M2 | M1-T2, M1-T10 | M1-T2 的库；M1-T10 的 composition root；08 节信封形状 | `src/events/`：`envelope` / `id-allocator` / `ring-buffer` / `bus`；`repo/event-seq-repo.ts`；在 `boot/container.ts` 构造全进程唯一事件缓冲与总线 | 1) 事件 id 用 SQLite 水位预分配，每满 1000 写一次库；重启后从水位继续，**允许跳号但绝不回退**（E-10） 2) 环形缓冲全局一个、容量恒 5000（E-153 的重放窗口） 3) `bus.publish` 出现在事务回调内由架构测试判失败 4) 信封字段与 `packages/shared/src/api/events.ts` 的判别联合一致，新增 kind 时 switch 有穷尽性检查 | 1.5d |
| M2-T5 | SSE 服务端：分帧、心跳、重放与断流 | M2 | M2-T3, M2-T4 | M2-T4 的事件总线 | `http/sse.ts`、`GET /events` | 1) 响应头含 `text/event-stream`、`no-cache, no-transform`、`keep-alive`、**`X-Accel-Buffering: no`**，每 15 秒写一行 `:\n\n` 心跳（E-154） 2) 带 `Last-Event-ID` 重连时从重放缓冲补齐；小于缓冲最小 id 则返回 409 `E_REPLAY_WINDOW_EXPIRED` 并主动关流，**不静默丢事件**（E-153） 3) 两端同时在线时每条连接各自维护游标与去重，服务端不假设只有一个客户端（E-155） 4) 令牌过期或被吊销时主动关闭该流并返回带信封的 401（E-156） 5) 路由内只操作 `reply.raw`，不调用 `reply.send` | 2d |
| M2-T6 | 请求体校验、路由表一致性与契约断言 | M2 | M2-T1 | 10 节端点表；`shared/src/api/routes.ts` | 各路由的 JSON Schema、`test/arch/routes.test.ts`、键清单常量、`scripts/check-error-codes.mjs` | 1) 架构测试断言「Fastify 实际注册的 method+path 集合 === `routes.ts` 常量表」，多一条少一条都失败 2) 请求体加字段只改 TS 接口未改 Schema 时 `tsc` 报错（键清单 `as const satisfies` 穷尽断言）（E-215） 3) 只改 Schema 未改 TS 接口时契约测试失败（E-216） 4) 所有请求体 Schema 统一 `additionalProperties:false`，多余字段返回 400 并列出字段名，不静默丢弃（E-217） 5) `check-error-codes.mjs` 断言 `codes.ts` 的键集合与 10 节错误码表逐行一致，**方向是「文档为源、代码为投影」**，不一致即 `pnpm -w check` 失败 | 1.5d |
| M3-T1 | docs-data.js 解析与内容指纹 | M3 | M1-T2 | 一份 unattended-run 1.1.0 实际导出的 `docs-data.js`；09 节输入协议 | `service/docs.ts` 的唯一解析适配器、`domain/layer-of.ts`（批次分层纯函数）、`repo/documents.ts` | 1) 强制按 UTF-8 读取，去头尾 `JSON.parse`，不启用 JS 引擎；中文 Windows 默认编码下不乱码（E-16） 2) 校验 schemaVersion=1 与任务包的三处哈希一致；按任务 ID 排序的 id/contractHash 列表算指纹，任务行不变而范围、条款或提示词编译器变化时仍判变更，**不得使用 generated 或 mtime**（E-17） 3) 指纹相同且仅时间戳或 ready/reasons 变化时不设变更标记、不改运行状态，但仍刷新文档复核元数据（E-79） 4) 解析失败、必需字段缺失、协议或哈希不一致、文件被删时保留全部既有记录与快照，置 `is_source_readable=0` 并冻结新派发，**不清空任何数据**（E-82） 5) **批次由本产品按 `deps` 拓扑分层算出**（层号 = 最长前驱链长度，无前驱为 0，显示号 = 层号 + 1），规则与阅读器逐字一致且收敛在这一个模块内；上游日后真导出 `batchNo` 时只改这一个函数（E-246） 6) 阅读器那条窗口数 localStorage 键**不存在也不读**，本产品一律用自己的 `documents.lane_count` 设置（E-247） | 1.5d |
| M3-T2 | 任务、批次与依赖导入 | M3 | M3-T1 | M3-T1 的解析结果 | `repo/tasks.ts`、`repo/batches.ts`、导入校验 | 1) 任务、依赖、批次、自有并行窗口、验收标准与边界正确落库；两段提示词取 dispatch[id].implementation/review，有效范围取 handoff.effectivePaths[id]，保存 contract_hash、is_contract_ready 与 contract_reasons_json；ready=false 的合法任务可查看但禁止新派发，不能当成代码验收失败（E-17、E-82） 2) `deps` 指向不存在的 id 或成环时导入即校验，禁止进入自动批次派发；仅在人工确认依赖异常且当前上游 ready=true 时允许手动单点，确认不写回文档复核证据，ready=false 仍返回 E_DOC_CONTRACT_PENDING，**不因一条脏边卡死整个交接台**（E-20） 3) 多份文档按文档路径 + 指纹隔离，任务 id 不跨项目冲突，列表按文档分组不混排（E-21、E-87） 4) 依赖成环时与上游同规则**就地截断为层 0**（落进第 1 批），并在导入报告与 UI 列出环上任务 id，该批派发前**强制人工确认**依赖异常且上游 ready=true；人工确认不修改或绕过 ready（E-241） 5) `deps` 指向不存在的任务 id 时只计入存在的前驱，**被忽略的幽灵 id 必须在导入报告里列出**（E-242） 6) 批次号**不作持久标识**，每次导入按当前分层重算；历史派发记录只认任务 id 与快照（E-243） 7) 全部任务无依赖时只有「第 1 批」，界面照常按批次呈现，**不退化成无批次列表**（E-244） | 1.5d |
| M3-T3 | 派发快照与三个变更标记 | M3 | M3-T2 | M3-T2 的任务表 | `repo/dispatch-snapshots.ts`、比对逻辑、横幅数据 | 1) 派发那一刻把 input/output/accept、两段提示词、contract_hash 与有效路径作为同一版本逐字快照进运行记录（E-19） 2) 文档重出后逐字符比对产出三个布尔标记：任务已消失／验收标准已变／提示词已变；契约哈希不同也纳入提示词变更标记，不能因任务行相同漏掉范围或条款变化，并给出受影响任务的过滤列表（E-77、E-78） 3) 任务 id 消失但仍有在途运行时不终止不删记录，标「已从文档移除」并禁止再次派发（E-77、E-18） 4) 同一任务重派多轮时比对只针对**最近一次**派发快照，历史轮次只读保留（E-80） 5) 文档在派发瞬间被重新生成时以派发那一刻读到的内容写快照，事后补比对，**绝不回滚已发出的运行**（E-81） | 2d |
| M3-T4 | 文档接管横幅与阅读器入口 | M3 | M3-T2 | M3-T2 | `system.docs_changed` 事件、接管横幅数据、`http/routes/documents.ts`（含 `open-reader` 与 `settings` 两个端点） | 1) 首次接管某文档时给一次性横幅「本文档任务状态已由调度器接管，阅读器里的手工三态不再有效」，可关闭并按文档记住（E-84） 2) 调度器**永不读也永不写**阅读器的 localStorage（E-83） 3) 「打开原文档阅读器」以系统默认浏览器打开原 `index.html`，不注入脚本、不改磁盘任何文件（E-85） 4) 文档目录被移动或重命名时提示「文档路径不可用，请重新定位」，任务记录、快照、会话指针全部保留（E-86） 5) 调度器**不读不写任何浏览器存储**；阅读器里手点三态与手调窗口数都不影响调度器（E-249） | 1d |
| M3-T5 | 审查提示词与验收标准的取用接口 | M3 | M3-T3 | M3-T3 的快照 | 供 M7 取用的 `getReviewContext(taskId)` | 1) 返回该任务的审查提示词、契约哈希与**派发时快照的那一份**验收标准，而非当前文档的；源提示词是引用审查材料，供 M7 组装只读裁定包，文档维护／提交／合并指令不成为运行时权限 2) 快照缺失时明确报错而不是回落到当前文档 3) 批次执行途中任务 ID/contractHash 指纹变化时，已派任务继续用快照跑完，后续派发自动暂停并要人确认，**绝不热改在途任务**（E-50） | 1d |
| M3-T6 | 收口提示词取用与批次匹配 | M3 | M3-T5 | M3-T5 的快照取用接口；04 节的 `dispatchBatches` 输入协议 | `service/docs.ts` 的 `getWrapupContext(batchId)`、`domain/wrapup-builtin-prompt.ts`（内置通用收口提示词常量） | 1) 读派发快照那一刻文档的 `dispatchBatches`，按「`tasks` 集合与本批当前任务集合相等」匹配（顺序无关，多一个少一个都不匹配），命中返回该批 `wrapup` 原文与 `promptSource='docs'`；不按层号匹配（E-296） 2) 匹配不到或文档未导出该键时返回内置通用收口提示词并 `promptSource='builtin'`，不抛错、不让收口停摆（E-296） 3) 返回的材料逐字不改写、不截断；文档侧 `batchRecords` 键不读取（决策 73） 4) 文档指纹在收口途中变化时仍取快照那一份，不热改（E-50） | 0.5d |
| M3-T7 | 查 bug 提示词取用与快照 | M3 | M3-T5 | M3-T5 的快照取用接口；04 节的 `dispatch[id].bug` 输入协议（unattended-run 1.3.1，未落地措辞） | `service/bughunt-context.ts` 的 `getBughuntContext(taskId)`、`domain/bughunt-builtin-prompt.ts`（内置通用查 bug 提示词常量）、`service/docs.ts` 导入 `dispatch[id].bug` 落 `tasks.bug_prompt`、派发快照落 `dispatch_snapshots.bug_prompt`、新增一个迁移（两列 `bug_prompt TEXT NULL`） | 1) 导入文档时把 `dispatch[id].bug` 逐字存 `tasks.bug_prompt`，缺失或不是字符串存 NULL，**不冻结派发、不报 `E_DOC_SOURCE_UNREADABLE`**；派发时随三段快照一起复制进 `dispatch_snapshots.bug_prompt`（E-19、E-316） 2) `getBughuntContext(taskId)` 返回**派发快照那一份** `bugPrompt` 与 `promptSource='docs'`；快照为 NULL 时返回内置通用查 bug 提示词并 `promptSource='builtin'`，不抛错、不回落到当前文档、不让查 bug 阶段停摆（E-316） 3) 返回的材料逐字不改写、不截断；文档指纹在途中变化时仍取快照那一份，不热改（E-50） 4) `bug_prompt` 不进任何事件 payload、日志只打长度与前 200 字节（16 节）；迁移为普通 `ALTER TABLE … ADD COLUMN`，旧库直开不回填 | 0.5d |
| M4-T1 | agent 注册表两层结构与热加载 | M4 | M1-T1 | 04 节的四家配置文件实测结果 | `config/registry.ts`、`config/defaults.ts`、`agents.json` schema | 1) 数据层可配（路径／参数模板／并发上限／默认模型／短码／`adapterKind`／超时），代码层解析器按具名枚举选，用户不能自写解析规则 2) 热加载用 `fs.watch` + 300ms 防抖 + 内容指纹去重，编辑器一次保存不触发多次重载（E-93） 3) 校验失败保留上一版并发警告事件，**绝不因此退出 daemon**；未知字段忽略并 warn，缺字段回落内置默认（E-94） 4) 内置默认与用户覆盖**分开存两份**，升级后能算出差异并给一键采纳（E-92） 5) 并发上限为 0 或负数等同禁用该 agent，不是无限并发（E-91） | 2d |
| M4-T2 | 参数模板校验与模板变量白名单 | M4 | M4-T1 | M4-T1 的注册表 | 模板解析器与保存时校验 | 1) 只允许 `{model}` `{prompt_file}` `{cwd}` `{session_dir}` 四个模板变量；含未知模板变量或括号未闭合时**保存即拒绝**并高亮出错位置（E-89） 2) 注册表只发布模板校验通过的快照，运行时渲染入口仍须拒绝未经校验的模板 3) 两个 agent 配成同一可执行路径时允许；会话目录相同则产生 `reason` 精确等于 `session-dir-overlap` 的英文告警但不拒绝保存，中文文案由设置页映射为「会话记录可能互相覆盖」（E-95） | 1d |
| M4-T3 | 版本指纹探测与可执行路径解析 | M4 | M4-T1, M1-T7 | M4-T1 的注册表；M1-T7 的 spawn | `probeAgent()`、指纹缓存 | 1) 探测做成注册表**每个 agent 都有的通用字段**（探测命令 + 期望模式），不是 grok 专用分支（E-36 的对偶面） 2) 输出不匹配任何已知指纹时标「未识别」且不可派发，界面同时显示实测版本串与期望模式并给手填入口（E-195） 3) 平台候选发现命中多个同名可执行时记录首个但**列出全部候选并要求确认**，绝不静默取第一个（E-196）；GUI/自启最小环境找不到 shell PATH 项时列出已检查位置，不执行 shell 启动文件（E-270） 4) 指纹按「解析后的真实路径 + mtime + 大小」缓存，批次派发时不逐任务探测（E-197） 5) 版本串格式微变但仍是官方时用宽松模式 + 区间匹配通过，不因一次例行升级让整个 agent 掉线（E-198） 6) 用户手填绝对路径时仍经当前平台解析并探测，失败降级为警告横幅而非禁用（E-199、E-264） | 2d |
| M4-T4 | 可用性探测与不可用态 | M4 | M4-T3 | M4-T3 | 启动期探测、`agent.availability_changed` 事件、`http/routes/agents.ts`（列表 / PATCH / probe / models 四个端点） | 1) agent 未安装或不在平台可见候选目录时启动期即标「未检测到」并在 UI 置灰、写明缺什么，**不等到派发那一刻才失败**（E-29、E-40） 2) 可执行路径不存在、POSIX 无执行位、链接目标无效时显示对应类型化原因并从派发目标移除，不阻塞其他 agent，点击可跳到该项配置（E-88、E-262、E-263） 3) `pi` 指向了别的可执行文件时由同一套指纹探测发现不匹配并提示手填，绝不按错误参数静默派发（E-201） | 1d |
| M4-T5 | 四家模型清单读取与降级 | M4 | M4-T1 | 04 节实测的四种配置格式 | `readModels()` 四个具名解析器 | 1) codex TOML／claude JSON／grok TOML／pi models.json 四种格式各自读出清单 2) 列模型命令超 5 秒或退出码非 0 时降级为「配置文件当前值 + 历史用过 + 手填」并标「清单可能不全」，**绝不阻塞派发**（E-38） 3) 输出格式变化解析不了时同样降级，且原始 stdout 保留在可展开日志里不静默吞掉（E-39、E-90） 4) 配置文件缺失或语法损坏时视为「无配置值」不崩溃，提示解析失败并给出文件绝对路径（E-43） 5) 每次打开设置页按文件 mtime 重读，不长期缓存（E-44） | 2d |
| M4-T6 | 模型与思考强度的粒度与透传 | M4 | M4-T5 | M4-T5 的清单 | 默认模型／思考强度与单任务覆盖的取值链 | 1) 每 agent 一个默认 + 单任务可临时覆盖；覆盖只作用于这一次派发及其重试，**绝不回写任何 agent 的配置文件** 2) 清单为空时允许留空表示「跟随 agent 配置」，派发不传 `--model`，不禁用该 agent（E-35、E-41） 3) 模型名含空格、引号或 shell 元字符时以数组形式传给子进程（E-42 的对偶面） 4) 别名与完整 ID 并存时都原样透传，**不做别名映射、不做去重合并、不做跨家等价映射**（E-45、E-30） 5) 设过覆盖的任务重试时沿用上次实际使用的模型，只有显式清除才回落默认（E-33） 6) 切换执行 agent 时清空模型覆盖并回落新 agent 默认，下拉只列当前 agent 的清单（E-34） 7) **思考强度走与模型完全同构的取值链**（每 agent 默认 + 单任务覆盖 + 重试沿用 + 切 agent 清空），落 `runs.effort_tier`（决策 52） 8) 不支持思考强度的 agent 置 NULL、派发不传任何相关参数、UI 显示「—」，**不补默认档冒充**，也不因此禁用该 agent（E-254） 9) 档位超出该模型取值域时派发前校验并就地说清原因与改法，**不静默降档也不静默丢弃**；校验不了时按「不支持」处理（E-255） | 2d |
| M4-T7 | 三档权限与三档思考强度的抽象与厂商参数映射 | M4 | M4-T1 | 04 节实测的各家权限参数；各家思考强度参数 | `domain/permission-tier.ts`、`domain/effort-tier.ts` 两张映射表 | 1) 注册表只存本产品三档抽象（只读／工作区可写／无限制），映射到 `--sandbox` / `--permission-mode` 的表在代码层，**用户不直填厂商参数**（E-137） 2) 默认中档；某 agent 被手动调到最高档时该卡片与每次派发确认页**持续**显示醒目标记（非一次性 toast），且档位不随切到「自动模式」而隐式变化（E-136） 3) 审查阶段派出去的 agent 固定只读档（E-135） 4) **思考强度同样只存三档抽象（低／中／高）**，到各家参数的映射（codex `model_reasoning_effort`、claude 思考预算、其余按实测填）在代码层，用户不直填厂商原值（决策 52） 5) 五家中任一不支持该能力时映射表返回「不支持」而非某个默认值，由 M4-T6 走 E-254 的空值路径 6) 自报值与所选不一致时如实上报两者，**不静默采信任何一边**（E-256） | 1.5d |
| M4-T8 | codex 原生适配器 | M4 | M4-T3, M1-T8 | `codex exec --json` 与 app-server 的实测事件形状 | `adapters/codex/`：`buildLaunchSpec` / `readModels` / `map-events` / `capabilities` | 1) 主用 app-server，保留 `codex exec --json` 为退路；两者产出同一套归一化事件 2) 厂商事件字符串只出现在本目录，`service`/`jobs`/`http` 中出现即架构测试失败 3) 原生通道破坏性变更时把 `adapterKind` 切成 `generic-acp` **只改一行数据不改代码**，能力位随之收窄并由 UI 灰掉按钮（E-186） 4) 能力位声明可回话为真（`codex queue --thread --message`） | 2d |
| M4-T9 | claude 原生适配器 | M4 | M4-T3, M1-T8 | `claude --bg` + `agents --json` 的实测字段 | `adapters/claude/` 四件套 | 1) `--bg` 与 `-p` **不同时使用**；后台会话经 `claude agents --json` 回读 `state`／`waitingFor`／`sessionId` 2) 子进程环境清掉 `ANTHROPIC_MODEL` 与 `ANTHROPIC_DEFAULT_*_MODEL`，并从首帧读回自报的实际模型，与所选不一致时标记（E-37） 3) 未知厂商事件按 E-202 丢弃并对 `runs.unmapped_event_count` 计数，绝不崩溃也绝不臆造 kind 4) 能力位声明可回话为真（`--input-format stream-json`） | 2d |
| M4-T10 | pi 原生适配器（RPC 模式） | M4 | M4-T3, M1-T8 | pi 自带的 `./client` 与 `./rpc-entry` 导出 | `adapters/pi/` 四件套 | 1) 走 `--mode rpc`，用 pi 自带的官方客户端导出而非第三方 ACP 桥接 2) **禁用 Node `readline`**，用 M1-T7 的行读取器；配一条含 `U+2028` 输出的回归用例（E-203） 3) 只依赖已验证的最小命令子集（`prompt`／`steer`／`abort`／`get_state`）与 `agent_settled` 事件；大版本升级改了命令集时未知事件忽略并计数，不崩溃（E-202） 4) `agent_settled` 映射为运行结束信号 | 2d |
| M4-T11 | grok 原生适配器 | M4 | M4-T3, M1-T8 | `--output-format streaming-json`（ACP 原生）与 `grok sessions`／`export` | `adapters/grok/` 四件套 | 1) 直接消费其 ACP session updates，作为归一化词汇的参照实现 2) 会话回读只走 `grok sessions list/search` 与 `grok export 〈id〉`，**禁止直连其 SQLite**（schema 未公开） 3) headless **不读管道 stdin**，提示词经 `--prompt-file` 或参数传入 4) 与社区同名 CLI 的区分由 M4-T3 的指纹探测负责，本任务不再判定 5) 该家拿不到 token 用量时字段置 null 由上层显示「—」，**不得填 0**（E-26） | 1.5d |
| M4-T12 | dsh headless 适配器与通用 ACP 扩展槽 | M4 | M4-T8 | dsh 实测契约（`--profile headless`）；ACP v1 规格 | `adapters/dsh/`：`buildLaunchSpec` / `readModels` / `map-events` / `capabilities`；`adapters/generic-acp/`（为第 6 家保留） | 1) dsh 走 `--profile headless "任务"` 接入为**第一版第五家**：末条 assistant 文本进 stdout、`turn/end` 完成则退出码 0 否则 1、成功时 stderr 为空、不监听端口；CLI 未全局安装，入口默认指向应用内 `bin.js` 且可手填绝对路径（E-193） 2) **能力位 `hasStreamingEvents` 置假**，运行轨只显示三段、逐步耗时退化为整段耗时，**禁止伪造中间事件**；UI 按能力位灰掉「逐步展开」而非留空列表（E-253） 3) 启用前必须过探测 + 冒烟任务，契约任一项不符即判未通过并显示原因（E-191） 4) 启动失败或非 0 退出即标不可用并移出选择器、在途运行标「失联」，**绝不影响其余四家与整批推进**（E-192、E-28） 5) 版本超出注册表已知区间时允许启用但常驻提示「版本未验证」（E-194） 6) 会话回读能力位标「仅本次运行」——dsh 侧无列举接口，历史一律由本产品自己的运行记录提供（E-188） 7) 通用 ACP 适配器仍产出，作为**第 6 家及以后**的兜底入口：只填启动命令即可接入；既不在注册表也无 ACP 入口的明确标「暂不支持」，不提供半可用状态（E-187） 8) 同一 agent 的原生与 ACP 适配器任一时刻只允许一种生效，切换必须在无在途运行时进行（E-189） | 2d |
| M4-T13 | 登录态探测与刷新 | M4 | M4-T3, M4-T4 | M4-T3 的 `probeAgent()`、`executeProbeProcess()` 与 `buildProbeLaunch()`；M4-T4 的 `service/agents.ts`、`http/routes/agents.ts` 四端点与 `agent.availability_changed`；04 节「登录态探测」实测表 | `adapters/login-probe.ts`（`probeLogin()` / `parseLoginOutput()`）、`adapters/grok/login-patterns.ts`、`adapters/probe.ts` 的 `redactSecrets()` 与 `executeProbeProcess()` 导出、`config/defaults.ts` 的 `loginProbe` 字段与五家出厂值、`config/registry.ts` 的字段校验、`service/agents.ts` 的 `refreshLogin()` / `getLogin()` / `loginCache` / `inflightLogin`、`http/routes/agents.ts` 的 probe 响应 `login`、shared `api/agents.ts` 的 `LoginState` / `LOGIN_UNKNOWN_REASONS`、`api/events.ts` 的 `AgentAvailabilityChangedPayload.login`、`errors/codes.ts` 的 `E_AGENT_LOGIN_PROBE_FAILED`、`test/fixtures/login/`、`test/unit/login-probe.test.ts`、`test/arch/registry-forbidden-args.test.ts` | 1) 注册表数据层新增 `loginProbe {args, parser, loggedInPattern, loggedOutPattern, loginCommandHint}`，解析器按具名枚举 `codex_login_status` / `claude_auth_json` / `grok_models_exit` / `pi_auth_check` / `none` 选，注册表只装参数与正则源串；`args` 含 `print-api-key` / `print-bearer-token` / `--credentials` 即 `invalid-config` 拒绝该条；`{provider}` 变量标记只允许在 `pi_auth_check` 的 `args` 里；缺字段回落出厂值，第 6 家回落全 `none`（E-353） 2) 探测子进程只走 `executeProbeProcess()`（`spawnManaged`、5 秒上限、超时 `kill()`），可执行路径由调用方传 M4-T3 已解析的 `resolvedPath`，为空直接 `unknown/exec_missing` 不起进程；四个解析器按 04 节实测形状判定，超时／不匹配／退出码非 0 一律 `unknown` 并给 10 节字段级契约列出的八值 reason 之一（`timeout` / `exec_missing` / `spawn_failed` / `unparsable` / `exit_nonzero` / `not_supported` / `no_provider` / `not_probed`，pi `status:'invalid'` 归 `unparsable`），`warningCode` 只在 `timeout` / `unparsable` / `spawn_failed` / `exit_nonzero` 时置 `E_AGENT_LOGIN_PROBE_FAILED`，该码不作为 HTTP 错误抛出（E-335、E-346） 3) pi 按 provider 逐个并行探测：**provider 集合与 `defaultProvider` 由调用方作参数注入**（`probeLogin({providers, defaultProvider})`，本任务不读 `pi --list-models` 也不读 `settings.json`，接线由 M4-T14 完成），集合为空 → `no_provider` 不起进程，上限 10 个、超出的标 `not_probed`，agent 级状态取 `defaultProvider` 那一项、缺省取第一个 `logged_in`（E-349）；pi 的登录 fixture 放 `test/fixtures/login/`；`parser='none'`（dsh）不起进程、不写缓存、DTO 的 `login` 为 null 4) `logged_out` 与 `unknown` **都不改 `canDispatch`**，`assertCanDispatch()` 不读 `loginCache`（单测断言）；不做「运行成功即改判 `logged_in`」，缓存只由 `refreshLogin()` 写（E-336、E-337） 5) 缓存键 `login:〈agentId〉`，失效点只有四个：`POST /agents/:id/probe`、`GET /agents/:id/models?refresh=1`、可用性翻转、`refreshLogin({trigger:'exited_before_output'})`；同一 agent 在途探测经 `inflightLogin` 去重；`service/agents.ts` 无 `setInterval`（arch 断言）；**每次 `refreshLogin()` 结束**（成功、超时、unparsable、exec_missing）都发 `agent.availability_changed{reason:'login_changed', login}`，`changed` 谓词不把 `login` 算进去，订阅方遇该 reason 不得重探（决策 130，E-335） 6) stdout／stderr 进 logger、`rawStdout`、事件 payload 前必过 `redactSecrets()`，解析器读原文；全仓无 `auth.json` / `credentials` 路径串，除 `adapters/login-probe.ts` 与 `adapters/grok/login-patterns.ts` 外无 `logged in` / `loggedIn` / `not_ready` / `auth check` 字面量（arch 断言）；`LoginState` 只含枚举、时间、`loginCommand` 与 `providers`（E-353、E-354） 7) `loginCommandHint` 只投影到 `LoginState.loginCommand`，`AgentEntryDto` 顶层不含 `loginCommandHint`；hint 为 null 时 `loginCommand` 为 null，pi 出厂即 null；hint 含控制字符或超 200 字加载时置 null 并 warn；非 dsh 的 agent 在首次探测完成前 `login` 为 `{state:'unknown', reason:'not_probed', checkedAt:null, loginCommand}` 而不是 null（决策 133，E-355） 8) 17 节 `login-probe.test.ts` 全部用例通过：四解析器 × 五家 fixture（未实测形态标 `synthetic`）、`redactSecrets` 五样本、并发去重、四失效点、`parser='none'` 不调 `commandRunner` | 1d |
| M4-T14 | 实时清单来源、来源标注与思考强度取值域 | M4 | M4-T5, M4-T7, M4-T8, M4-T10, M4-T11, M4-T13 | M4-T5 的四家 `readModels()` 配置解析器；M4-T8 的 `adapters/codex/`；M4-T10 的 `adapters/pi/`（行读取器、main 上的 `parsePiModelsCommandOutput()`，本任务改名为 `parsePiListModelsTable()` 并改读注册表参数）；M4-T11 的 `adapters/grok/`（main 上的 `parseGrokModelsCommandOutput()`，本任务改名为 `parseGrokModelsOutput()`）；M4-T13 的 `getLogin()` / `refreshLogin()` 与 `redactSecrets()`；M4-T7 的 `domain/effort-tier.ts`；04 节「实时模型清单」实测表与 codex `model/list` 协议记录 | `adapters/models-live.ts`（`readModelsLive()`）、`adapters/codex/model-list-rpc.ts`（`requestModelList()`）、四家 `read-models.ts` 增 `currentConfigEffort` / `normalizeHistoryModelName()`（pi 另增 `currentConfigProvider` / `providers`）、`domain/model-catalog.ts`（`mergeModelSources()`）、`domain/effort-value.ts`（`toEffortColumns()` / `fromEffortColumns()` / `assertVendorEffortInDomain()`）、`domain/effort-tier.ts` 的 `vendorMap` 参数与 `remapEffortAcrossAgents()`、`config/defaults.ts` 的 `modelsLive` / `builtinModels` / `defaultEffortTier` / `effortVendorMap` 字段与出厂值、`config/registry.ts` 的四字段校验与 `AGENT_CONFIG_FIELD_PATHS`、`service/agents.ts` 的 `listAgentModels({refresh})` 与 `liveCache` / `inflightModels`、`toAgentEntryDto()` 组装 `AgentEntryDto.layers`、`updateAgent()` 的 `clearOverrides` 校验、pi provider 集合与 `defaultProvider` 注入 `probeLogin()`、`repo/runs.ts` 的 `listSucceededModelNames()`、`domain/run-state-machine.ts` 的 `SUCCEEDED_RUN_STATES`、`http/routes/agents.ts` 的 models 与 PATCH 端点、一个普通迁移（`runs.effort_vendor`）、shared `api/agents.ts` 的 `EffortValue` / `EffortVendorMap` / `MODEL_SOURCES`（`as const` 并配顺序断言）/ `ListAgentModelsResponse` / `AgentEntryDto` 新字段（`login`、`layers`、`defaultEffortTier`、`effortVendorMap`、`builtinModels`）/ `UpdateAgentBody.defaultEffortTier` 与 `clearOverrides`、`api/runs.ts` 的 `CreateRunBody.effort` 与 `RunDto.effort` / `effortVendor` 类型、`test/fixtures/models/`、`test/unit/models-live.test.ts`、`test/unit/effort-tier.test.ts` 增量 | 1) `GET /agents/:id/models` 返回 `{models: [{name, source, provider?, effortOptions?, isCurrentConfig, isDefault?, note?}], isComplete, refreshedAt, liveFailure, currentConfig, isRefreshing}`（**不含登录态**，登录态只在 `AgentEntryDto.login`，决策 130），`source` 五值按 `live → config → builtin → history` 合并、`name` 精确比较同名只保留更靠前者、daemon 不产出 `manual`；`isComplete === live.ok`；config 当前值不在 `live` 时仍以 `source:'config'` 列出并 `isCurrentConfig:true`（E-338、E-350） 2) `live` 按注册表 `modelsLive.kind` 分派：`command` 走 `executeProbeProcess()` 后按 `parser` 调 grok／pi 文本解析器（pi 的参数从注册表 `['--list-models']` 读，代码中不再出现 `--models`）；`codex_app_server` 走 JSON-RPC：stdin 逐行写 `initialize` → `initialized` → `model/list`，忽略无 id 通知与非本请求 id 的响应，`hidden:true` 过滤，`nextCursor` 非 null 时**不追页**（翻页参数名未实测，04 节）、清单照返回并 warn，收齐即 `kill()`；`none` 返回 `not_supported`；超时按注册表 `modelsLive.timeoutMs`（出厂 5000、codex 10000，缺失回落 5000、上限 60000）；`live` 失败只写 `liveFailure`、HTTP 200、清单回落 config + builtin + history、不阻塞派发（E-38、E-345） 3) `?refresh=1` 删 `liveCache` 与登录缓存后并行起一次 `live` 与一次 `refreshLogin()`（pi 的 `providers` 取本次 `live` 表第一列 DISTINCT、`defaultProvider` 取 `read-models.ts` 新增的 `currentConfigProvider`，`live` 失败只传 `[defaultProvider]`），等两者 settle 再返回；缺省命中缓存直接用，未命中且有在途则**不等待**、用旧或空 `live` 立即返回并 `isRefreshing:true`；同一 agent 同时只一个 `live` 子进程（E-339） 4) history 由 `repo/runs.ts` 一条 SQL 派生：`state IN (SUCCEEDED_RUN_STATES)`、`model_name` 非空、按 `agent_id` 全局、`GROUP BY model_name ORDER BY MAX(ended_at) DESC` 取 40 再由适配器 `normalizeHistoryModelName()`（pi 剥 `:〈thinking〉` 后缀）去重截 20；不建表、不缓存、`repo` 里无厂商串（E-340） 5) `builtinModels` 是通用字段（claude 出厂 `opus` / `sonnet` / `haiku` / `opus[1m]` 各带 note），条目 `name` 含空白或控制字符拒绝该条 agent 并 warn；代码中不存在按 agentId 特判 claude 的分支（E-350） 6) 思考强度值形状 `EffortValue = {tier} ｜ {vendor} ｜ null`，落 `runs.effort_tier`（三档 CHECK 不改）与新列 `runs.effort_vendor` 恒不同时非空，`fromEffortColumns()` 遇双非空抛 `E_INTERNAL`；config 当前值从 codex `model_reasoning_effort` 与 pi `defaultThinkingLevel` 读（claude／grok 无字段不读，dsh 不读），映射不回三档时作 `{vendor}` 原值并 `effortRecognized` 标注；`effortOptions` 只有 `live` 来源给（codex 每模型 `supportedReasoningEfforts`、pi 代码常量七档）（E-351） 7) `PATCH /agents/:id` 收 `defaultEffortTier` 与 `clearOverrides`（`POST /runs` 收 `effort` 的接线归 M8-T9，本任务只交付 `assertVendorEffortInDomain()` 与 shared `CreateRunBody.effort` 类型）：`{vendor}` 经 `assertVendorEffortInDomain()` 校验只允许来自 config 当前值或 `live` 取值域，否则 `E_VALIDATION` 带 `allowed[]`；`clearOverrides: ('defaultModel' ｜ 'defaultEffortTier')[]` 清除该字段覆盖，与同名字段同时出现、重复项或未知字段名 → `E_VALIDATION`，空数组视为未提供，对 dsh 传 `clearOverrides:['defaultEffortTier']` 幂等成功；`defaultEffortTier: null` 不在 `clearOverrides` 里表示「覆盖为跟随」（`hasOverride:true, override:null`）；`AgentEntryDto.layers = {defaultModel: {builtin, config, override, hasOverride}, defaultEffortTier: {…同形}}` 由 `toAgentEntryDto()` 一处按「覆盖 ?? 配置 ?? 内置」算出顶层生效值，`config` 缺失为 null（决策 128、132，E-358）；`effortVendorMap` 为 null 的 agent 传非 null → `E_VALIDATION`（reason `effort_unsupported`）；`{vendor}` 派发原样传不映射，代码中不存在「就近归到三档」的静默映射；所选模型 `effortOptions` 不含该值只返回 warning 不禁用（E-351、E-255） 8) `domain/effort-tier.ts` 改为接收注册表 `effortVendorMap` 作参数（五家出厂值：codex／grok／pi `{low:'low', medium:'medium', high:'high'}`，claude `{low:'2048', medium:'8192', high:'32768'}`，dsh null），参数名仍在代码层 `KNOWN_EFFORT_TRANSPORTS`；`remapEffortAcrossAgents()` 六例通过：同家恒等、`{tier}` 跨家保留、`{vendor}` 反查命中／未命中、目标 null map、`from===to`（E-342 的纯函数侧） 9) 17 节 `models-live.test.ts`（NDJSON 会话回放、`rpc_error`、超时 `kill`、`mergeModelSources` 八例、history 后缀与截断、`isRefreshing`）与 `effort-tier.test.ts` 增量全部通过；`service/agents.ts` 无 `jsonrpc` / `model/list` / `--list-models` 字面量（arch 断言） | 1d |
| M5-T1 | worktree 准备、分支命名与回收 | M5 | M1-T7 | M1-T7 的 spawn（git 一律经它） | `workspace/worktree.ts` | 1) 每任务独立 worktree，分支名冲突时自动加序号后缀，**不复用不覆盖已有分支**（E-71） 2) 目标目录不是 git 仓库时**不静默降级到共享目录并行**，改为强制串行并明确告知原因（E-69） 3) 主工作区有未提交改动时建 worktree 不受影响也**不去 stash 用户的东西**（E-72） 4) 磁盘不足或权限问题时标「派发失败·工作区不可用」并释放窗口名额，不让整批卡死（E-76） | 1.5d |
| M5-T2 | base 选择与上游产出校验 | M5 | M5-T1 | M5-T1；任务依赖图 | base 解析逻辑 | 1) 上游任务改动尚未落地进当前 HEAD 就要派下游时**拒绝派发**并说明「下游 base 缺上游产出」（E-70） 2) 同时提供「以上游任务分支为 base 建 worktree」的显式选项，**绝不默默从旧 HEAD 起**（E-70） 3) 每任务独立 session 与独立工作区，每 agent 有并发上限（E-31） | 1d |
| M5-T3 | diff 读取与改动文件数 | M5 | M5-T1 | M5-T1 的 worktree | `getDiffStat()` / `getDiffText()` | 1) 改动文件数用 git 工作区 diff 得出，**不解析各家 tool 事件**，四个 agent 共用一套逻辑 2) diff 基线取 worktree 自身 HEAD，**不与主工作区混算**（E-72） 3) agent 在 worktree 里自行 `git commit` 或 `git push` 时不拦截，但 push 到远端的事件要在任务详情高亮标注（E-75） | 1d |
| M5-T4 | 落地清单与 worktree 处置 | M5 | M5-T3 | M5-T3 的 diff | `workspace/landing.ts`、`http/routes/tasks.ts`（含 `landing` 与 `worktree/cleanup` 两个端点） | 1) 输出 worktree 绝对路径、分支名、diff 摘要、**可一键复制的 `gh stack` 命令文本**与一行可复制的 `python 〈docsPath〉/_run/build_docs.py 〈docsPath〉 --landed 〈任务ID〉`（`docsPath` 取文档记录的目录），**复制而不执行**，产品不调用任何文档维护脚本（E-74、决策 68） 2) 任务结束后 worktree 默认保留，只在用户显式点「清理」时 `worktree remove`；已清理过的任务重试需重建（E-73） 3) 清单为只读，产品不执行任何提交／push／PR／合并 | 1d |
| M5-T5 | 分支进 HEAD 判定与分支尖 SHA 记录 | M5 | M5-T2 | M5-T2 的 base 解析；M1-T7 的 spawn（git 一律经它） | `workspace/in-head.ts` 的 `isBranchInHead({repoPath, branchName, worktreePath, tipSha})`；收口 worktree 的分支命名规则 | 1) `git merge-base --is-ancestor 〈分支〉 HEAD` 退出 0 判进（method=`ancestor`）；退出 1 回退 `git diff --quiet HEAD 〈分支〉`，无差异判进（`no_diff`，E-273）；`worktreePath` 存在且 `git status --porcelain` 非空一律判未进（`dirty_worktree`，E-301） 2) 分支本地不存在时用调用方传入的 `tipSha` 做同一套判定，都否返回未进且 method=`branch_gone_unmerged` 并附 `git branch 〈原分支〉 〈sha〉` 命令文本；无 SHA 且 worktree 已清理返回进 HEAD 且 method=`branch_gone`（E-289） 3) git 报错返回 method=`error` 与 stderr 尾 200 字节，不推断为已合入（E-301） 4) 全部 git 调用经 proc/spawn、顺序执行不并发；返回 `{inHead, method, tipSha, commandText?, stderrTail?}`，本函数不写库 5) 收口工作区走 M5-T1 建分支 `wrapup/〈批次号〉-〈轮次〉`，分支冲突按 E-71 加序号，base 恒为当前 HEAD | 0.5d |
| M6-T1 | 运行状态机与非法迁移拦截 | M6 | M1-T2 | 09 节的状态机图与非法迁移表 | `domain/run-state-machine.ts` | 1) 状态机迁移表以纯函数实现，`domain/` 顶层零副作用，可被 Vitest 直接 import 2) `exited → landed` 的直接迁移在实现中不存在；落地只能由审查判定或人确认进入（E-23） 3) `interrupted` 为终态，任何出边都不存在（E-123） 4) 时钟与 id 从 DI 注入，状态机测试确定性可复现 | 1.5d |
| M6-T2 | 收流编排与落盘接线 | M6 | M6-T1, M1-T4, M1-T8 | M1-T4 的 logstore；M4 的归一化事件 | `service/run.ts` 的收流路径 | 1) 每条归一化事件写一行 `events.ndjson`（**全量原文永不截断**），每条原始输出行写一行 `raw.log` 2) `events` 索引表只收里程碑事件，高频 `*_chunk` 不进表 3) 事务回调内不出现 `await` 与任何异步副作用；事件发布在事务返回后（E-142 的对偶面） | 1.5d |
| M6-T3 | 进度信号与通用字段提取 | M6 | M6-T2, M5-T3 | M6-T2 的事件流；M5-T3 的 diff | 进度字段：最后一条消息／改动文件数／耗时／token | 1) 五家共用一套逻辑，不做深度语义解析；dsh 无流式时只在完成后补最终字段（E-253） 2) token 用量字段名或单位不一致或缺失时显示「—」，**不得拿 0 冒充**（E-26） 3) 从首帧读回 agent 自报的实际模型并在与所选不一致时高亮；无首帧能力的 agent 保持未知，不猜测（E-37 的呈现侧） | 1d |
| M6-T4 | 停滞检测与静默计时 | M6 | M6-T3 | M6-T3 的 `last_event_at` | `stall-detector` job（30s tick） | 1) 完全无流事件超阈值才升级为醒目告警；**仍在输出时只标弱提示，不改运行状态、不杀进程**（E-22、E-120） 2) 阈值按 agent 可配、有出厂默认，不做全局单一阈值 3) 处于「等待人回话」时归类为「等人」而非「疑似停滞」，不重复告警；超阈值后提示「中止以释放并发窗口」（E-122） 4) tick 不重入，上一次未完成则跳过本次并计数 | 1d |
| M6-T5 | 中止、进程树终止与状态落定 | M6 | M6-T1, M1-T9 | M1-T9 的三平台 `killTree` | `abortRun()`、`POST /runs/:id/abort` | 1) 中止一律走平台 `killTree`，业务层禁止直接杀父进程；Windows 与 POSIX 两阶段终止均通过集成测试（E-119） 2) 中止时 worktree 已有改动则保留 worktree 与 diff，记「已中止（有未验收改动）」，不自动清理不回滚（E-118） 3) 幂等：已终态时直接返回 accepted 4) 失联的运行提供「杀掉」入口，杀后转 `aborted`（E-02 的处置侧） | 1.5d |
| M6-T6 | 回话投递与能力位约束 | M6 | M6-T1, M4-T8 | 各适配器的注入通道与能力位 | `POST /runs/:id/messages`、投递路径 | 1) 「可回话」做成能力位，**UI 按能力位灰掉按钮而不是点了才报错**；不具备时返回 `E_CAPABILITY_UNSUPPORTED`（E-117） 2) 进程已死或管道已断时消息落地为「未送达」并保留原文可复制，**绝不静默丢弃或假装已发出**（E-113） 3) 会话已结束但支持 resume 的走「恢复会话后投递」并明示这是一次新运行；不支持恢复的禁用发送并提示改用重派（E-112） 4) 两端同时回话时按到达先后串行投递，每条记录来源设备与时间戳，**不做去重合并**（E-114） 5) 空消息拒绝提交；超长在发送前提示并让人确认截断点，**不静默截断**（E-116） 6) 写 stdin 时检查 `write()` 返回值并在 `drain` 后续写 | 2d |
| M6-T7 | 自动模式下的提问处置 | M6 | M6-T6 | M6-T6 的回话通路 | `awaiting_reply` 状态接线 | 1) 自动模式下 agent 提问时调度器**一律不代答**，任务转「等待人回话」并计入停滞检测，继续占用该 agent 并发额度（E-115） 2) agent 需要联网装依赖时默认拦下并转回话通路由人决策，**不提供「自动放行网络」开关**（E-134） 3) 沙箱拦下 worktree 之外的写入时记「权限受阻」在时间线高亮，**不判失败**；提供「仅本次运行临时提升」的一次性开关，绝不改写默认档位（E-133） | 1d |
| M6-T8 | 会话分段回读与厂商会话引用 | M6 | M6-T2, M1-T4 | M1-T4 的偏移索引 | `GET /runs/:id/log`、`vendorSessionRef` | 1) 厂商会话文件**只读打开、只存路径与偏移**，任何清理策略永不删除或改写它们（E-96） 2) 厂商会话文件已不在磁盘时显示「原始会话已不在磁盘（附路径）」，仍正常展示自己捕获的输出与三段快照，不报错不崩（E-97） 3) 单会话超 20MB 或 50 万字时默认只加载尾部并给「向上加载更多」，并提供「用系统默认程序打开原始文件」（E-98） 4) 双端游标分页（`fromSeq` + `direction`），向上翻历史与向下跟随都可用 5) 跨设备推到手机前对会话内容做基本脱敏，或在首次开启远程查看时明确告知（E-25） 6) 不依赖「厂商 GUI 里也能看」这一假设——**本产品自己的会话查看界面即唯一入口，必须做完整**（E-32） | 2d |
| M6-T9 | 会话保留策略与全文查找 | M6 | M6-T8, M1-T5 | M1-T5 的删除原语 | 保留策略、`searchInRun()`、`http/routes/runs.ts` 的 `search` 与 `DELETE logs` 两个端点 | 1) 清理**只清自己捕获的日志正文**；任务记录、三段快照、派发元信息一律保留，历史仍可回溯（E-105） 2) 全文查找按分段流式读并周期让出事件循环，硬超时 10s、结果上限 500 条；超时返回「已找到的前 N 条 + 未搜完」并允许取消（E-219） 3) 搜索按发起时刻的偏移索引末尾做快照式扫描，不追新写入，结果标注截止位置（E-220） 4) 目标正文已被清理或缺失时返回「该会话正文已清理」的明确结果，**不返回 500、不用空列表冒充无匹配**（E-221、E-207） 5) 同一任务重试或续跑时进度只展示最近一次运行，历史运行可展开，两次运行的事件不混成一条流（E-27） | 2d |
| M6-T10 | 任务终态会话归档与不复用断言 | M6 | M6-T1, M6-T6, M1-T7 | M6-T1 的状态机与终态判定；M6-T6 的回话投递结果与 `ProcessRegistry`；M1-T7 的 `platform.killTree` 两阶段；09 节的 `session_archived_at` / `lane_no` 列定义 | `service/session-archive.ts`（`archiveTaskInTx()` / `terminateArchived()`）、`service/session-guard.ts`（`assertSessionRefFree()` / `assertNotArchived()`）、`service/run.ts` 终态迁移的后置接线、`repo/runs.ts` 的 `findByVendorSessionRef` / `listByTask` / `markSessionsArchived`、`test/arch/session-isolation.test.ts`、`test/integration/session-archive.test.ts`、新增一个普通迁移（`runs.session_archived_at`、`runs.lane_no`、`tasks.lane_no`、`ux_tasks_lane` 部分唯一索引、`(lane_no)` 索引）、shared 的 `task.sessions_archived` 事件与 `E_SESSION_ARCHIVED` 错误码 | 1) 归档只有一个触发点：`kind='implement'` 行迁到 landed／failed／aborted／interrupted 的同一事务里，对该任务 `session_archived_at IS NULL` 的全部运行行批量写 `session_archived_at`，同事务把 `tasks.lane_no` 置 NULL（`changes===1` 才收集 `lane.released`）；事务内不 await、不 killTree、不 publish（E-302） 2) 事务后对 `pid` 非空且注册表仍持有的运行逐个 `platform.killTree` 两阶段，存活的收进 `residualPids`，然后才发 `task.sessions_archived{taskId, runIds, killedPids, residualPids}`；残留不阻塞槽位释放、不等进程死亡（E-322） 3) `awaiting_human` 只置 `tasks.lane_no` NULL、不归档，会话保持可回灌与可续接（E-54、E-326） 4) `POST /runs/:id/messages`、返工回灌与审查续接命中 `session_archived_at` 非空的行返回 409 `E_SESSION_ARCHIVED`；`vendor_session_ref` 原样保留只读，不删不改不拷贝（E-96、E-302） 5) `assertSessionRefFree({taskId, vendorSessionRef})` 在其他任务任一运行的引用与之相等时抛 `E_SESSION_ARCHIVED` 并带 `conflictTaskKey`；同一任务多轮共享引用放行；代码中不存在 `UNIQUE(vendor_session_ref)`（E-303） 6) `test/arch/session-isolation.test.ts` 静态扫 `src/service/**` 每个 `runsRepo.insert(` 调用点，断言同一函数体内出现 `assertSessionRefFree(`；集成剧本「归档后投递 409」「跨任务复用引用 409」「同任务第 2 轮共享引用 200」「续接目标已归档 409」四条通过 7) 新列全部可空、旧库直开不回填；`killTree` 与残留计数按 16 节埋点记日志 | 1d |
| M7-T1 | 机械检查两层与执行目录 | M7 | M5-T3, M6-T1 | M5-T3 的 diff；文档声明的 build/test 命令 | `service/review.ts` 的机械层 | 1) 零配置层（diff 非空／退出码／会话无致命错误／验收关键词命中）恒执行；项目命令层留空则跳过并在 UI 明示「机械检查覆盖有限」（E-60） 2) 退出码为 0 但 diff 为空时机械层直接判失败，**不派审查 agent**，标「无改动」转人（E-61、E-23） 3) 执行目录恒为该任务的 worktree，**绝不在用户主工作区跑**（E-67） 4) build/test 命令 10 分钟硬超时后杀掉，标「机械检查超时」转人，**不重试**（E-66） | 1.5d |
| M7-T2 | 审查 agent 派发与 diff 裁剪 | M7 | M7-T1, M3-T5, M4-T7 | M3-T5 的审查提示词；M4-T7 的只读档 | 审查运行的启动路径 | 1) 默认审查者的 agent、模型与思考强度**逐字取被审实施运行行的 `agent_id` / `model_name` / `effort_tier`（即任务指派的副本，决策 107），不是该 agent 的注册表默认模型**，且为**独立新 session**，只喂 M3-T5 的快照审查材料 + diff，不带实施上下文；本任务代码中不出现任何「设置里指定其他 agent／模型」分支——阶段覆盖的唯一入口是 pipeline 设置的 `reviewOverride`，其取值链与接线由 M8-T9 交付，本任务把三值的读取收成单一函数入口以便替换，返回形状预留 `{agentId, modelName, effortTier, effortVendor?}`，三值以参数形式由 `service/review.ts` 传入、`review-agent.ts` 不自己查库（E-347） 2) 审查运行固定只读档，实际任务指令只允许核对证据并输出裁定；源提示词以引用材料提供，其中维护文档、提交、推送、合并等步骤不得执行，**审查 agent 不得改代码或文档，也不接手两轮后的写入修复**，超限仍转人（E-135） 3) diff 超出上下文时给 diff stat 全量 + 验收标准命中文件全文、其余折叠，并在结论标注「基于部分 diff」（E-65） 4) 本任务只负责第 1 轮审查会话的新建（`review_round=1`、`continued_from_run_id` 为空）；第 N+1 轮续接同一审查会话与新开审查会话的头部上下文交 M7-T7，本任务代码中不出现按轮次分支（决策 89） | 1.5d |
| M7-T3 | 三态判定与失败兜底 | M7 | M7-T2 | M7-T2 的审查输出 | `ReviewVerdict` 解析器 | 1) 输出不符合 pass/rework/doc-issue 三态时按「审查未完成」处理并保留原文，**不猜测语义**（E-63） 2) 审查 agent 崩溃或超时时既不算 pass 也不算 fail，标「审查未完成」转人工，**任何情况下不默认放行**（E-62） 3) 判定 doc-issue 时停止对该任务的自动 rework，汇总意见给用户，不让 agent 反复改代码（E-64） 4) 解析器同时提取**最后一个** `VERDICT:` 行、`REWORK` 段的 `- R〈n〉` 条目与**最后一个** `rework` 围栏块，返工文本围栏优先、其次条目全文，落 `runs.rework_text` 不截断；VERDICT 为 rework 但两处都没有 R 条目 → `review_verdict='incomplete'` 标「未结构化」、原文全文存 `rework_text` 转人；pass 却带 R 条目 → `inconsistencies` 按 incomplete 处理；doc-issue 忽略围栏；代码中不存在按关键词（「不通过」「有问题」）判 rework 的路径（E-278） | 1.5d |
| M7-T4 | rework 回灌与重试上限 | M7 | M7-T3, M6-T6 | M7-T3 的判定；M6-T6 的注入通路 | rework 循环 | 1) rework 意见经回话通路回灌原会话，重试计数 +1；**自动重试上限默认 2 次**，超限转「待人确认」，不无限自动改（E-55） 2) 回灌后 diff 反而缩小或回退时仍走同一套检查，计数照加，达上限转人（E-68） 3) 人工打回时必须能附一句话意见，该意见复用同一条 rework 通路（E-59） 4) 本任务只负责「原会话活着且 `canReply`」的回灌分支；进程已结束或能力位为假时把 `{reviewRunId, targetRunId, reworkText, source}` 交 M7-T5 按能力位选恢复或新开，代码中不存在「先试投递失败再降级」的路径（E-279 的入口侧） | 1d |
| M7-T5 | 返工承接与新会话派发 | M7 | M7-T4, M8-T5, M5-T2 | M7-T4 交来的 `{reviewRunId, targetRunId, reworkText, source}`；M8-T5 的新建运行路径；M5-T2 的 base 选择；派发快照里的 `adapterKind` | `service/rework.ts` 的 `dispatchRework()` 三分支、`domain/rework-prompt.ts` 的 `assembleReworkPrompt()`、`run.rework_dispatched` 事件 | 1) 按被审运行快照 `launch_spec_json.adapterKind` 读代码层能力位（不读注册表当前值，E-93）：进程活着且 `canReply` → 回灌；已结束且 `canResume` → 恢复后回灌并明示新运行（E-112）；都不能 → 新开实施运行 `origin='rework'`、同任务 `attempt_no+1`、`spawned_by_run_id`=审查运行、同 worktree 与分支（E-279）；分支由能力位决定，代码中不存在「先试投递失败再降级」路径 2) 新会话提示词自包含：返工块原文 + 快照实施提示词「收到返工指令时」段（取不到用内置四句）+ 工作区指针 + 「只改列出条目、不 commit/push」（E-279） 3) worktree 目录已不存在时走 M5-T1 在原分支上 `reuse` 重建；分支不存在转 `awaiting_human` 并在闸门 comment 写 `branch_missing`；不开干净 worktree（E-277） 4) 三分支都 `rework_count+1` 且继承旧计数，旧计数已达 2 时直接 `reviewing→awaiting_human` 不进任何分支（E-55）；每分支在事务后发 `run.rework_dispatched{mode, source}` 5) 人工打回（E-59）与审查产出走同一入口，只以 `source` 区分 | 1d |
| M7-T6 | 收口输出解析与有效裁定 | M7 | M7-T3 | M7-T3 的解析器结构；决策 61 的八段格式 | `domain/wrapup-report.ts`：`parseWrapupReport()` / `deriveWrapupVerdict()` / `planWrapupFixes()` 与全套单测 | 1) 段头须独立成行，取最后一个 `BATCH_SUMMARY` 之后能按模板顺序找齐八段的那一组；找不齐返回 `ok:false` 与精确缺段清单，不做部分解析、不猜（E-274） 2) BUGS/NOT_FIXED 条目按 `B〈n〉 [S1／S2／S3] 涉及 〈任务ID〉（跨批）：现象 → 复现 → 根因 → 文件:行` 解析，半角括号、`->` 与全角冒号同样接受；少于四段保留条目但 `isWellFormed:false`；匹配不到 B 号或任务 ID 的条目进 `unassigned` 保留原文（E-290） 3) 有效裁定由内容算出：TESTS 为 fail、NOT_FIXED 非空、BUGS 有未出现在 FIXED 的、unassigned 非空——任一成立即 `open`；否则有 BUGS 即 `fixed`；否则 `clean`；RECORD 自报值只存 `declaredVerdict`，代码中不存在用自报值当裁定的路径（E-286） 4) `planWrapupFixes` 把开放项（TESTS 失败 ∪ BUGS∖FIXED ∪ NOT_FIXED）按任务分组、重编号 `R1…` 而原文逐字保留，未知任务归 unassigned 5) 纯函数零依赖、顶层零副作用，17 节列出的单测用例全部通过 | 1d |
| M7-T7 | 审查续接与轮次上下文 | M7 | M7-T2, M7-T4, M6-T6 | M7-T2 的第 1 轮审查派发；M7-T4 的返工回灌与 `{reviewRunId, targetRunId, reworkText}`；M6-T6 的类型化投递结果；派发快照里的 `adapterKind` 与能力位 | `service/review.ts` 的 `startReviewRound({taskId, implRunId, round, reworkItems})`、`proc/registry.ts` 的 `reassign(prevRunId, newRunId)`、`domain/rework-prompt.ts` 的续审头部常量、`repo/runs.ts` 的 `findLatestReview`、新增一个普通迁移（`runs.review_round`、`runs.continued_from_run_id REFERENCES runs(id)`、`ux_runs_continued` 唯一部分索引、`(task_id, kind, review_round)` 索引）、`test/unit/review-round.test.ts` | 1) 第 N+1 轮 = 新 `kind='review'` 行（`review_round=N+1`、`continued_from_run_id` 指向第 N 轮审查行、`parent_run_id` 仍指被审实施运行、`permission_tier='readOnly'`）；代码中不存在复用第 N 轮行当第 N+1 轮的路径；`continued_from_run_id` 跨任务或指向非 review 行由 service 拒绝（决策 89） 2) 通路按能力位选、不试了再降级：注册表仍持有第 N 轮进程且 `canReply` → `reply`（`proc.registry.reassign` 后经 M6-T6 向 stdin 注入「第 N+1 轮，只核上一轮的 R 条目：…」）；已结束且 `canResume` 且 `vendor_session_ref` 非空 → `resume`（新 spawn 带 `resumeSessionRef`，`vendor_session_ref` 与第 N 轮相等）；都不 → `new_session`，`vendor_session_ref` 为空、提示词头部为自包含块 `{round, previousReviewRunId, reworkItems[]}`，上一轮 REWORK 条目原文逐字（E-304） 3) 撑爆按事件流结构判定：投递后、该轮首条内容事件前出现未送达／启动超时／退出／错误事件 → 同一函数降级为 `new_session` 恰一次（恢复分支失败行标 `failed`、reason `continuation_exhausted`）；内容事件之后才失败按 E-62 转人不新开；新开后再次内容前失败 → 转人并 comment `review_continuation_failed`；判定函数只读事件种类，厂商错误串只进日志；返工反喂实施会话时用同一判定函数，触发即按决策 60 第三分支新开 `origin='rework'` 运行且 `rework_count` 不重复加（E-330、E-304、E-305、E-113） 4) 续接目标 `session_archived_at` 非空先抛 `E_SESSION_ARCHIVED`（E-302） 5) 第 N+1 轮机械检查照常跑、verdict 写在新行；轮次与返工计数写死为 `review_round = rework_count + 1`；查 bug 触发的再审（`reworkItems` 来自 FIXED 条目）走同一入口（E-307） 6) 旧审查行 `review_round` 为 NULL 时读侧按 1 处理、不回填；事务内不投递、不 spawn、不 reassign（08 节） | 1d |
| M7-T8 | 查 bug 阶段运行与结果解析 | M7 | M7-T3, M3-T7, M8-T6, M7-T7 | M7-T3 的解析器与 `RunTransitionReason`；M3-T7 的 `getBughuntContext()`；M8-T6 的 `runs.kind` 四值重建与 `wrapup-report.ts` 的段头切分／条目解析器；M7-T7 的 `startReviewRound()`；M8-T4 的 `service/settings.ts` 读 `settings.pipeline.bughunt`（`pipeline` 键缺行按 E-356 默认；后续的 `getPipeline()` 落地后改调它）；M5-T5 的 `workspace/in-head.ts` 里读 HEAD SHA 与树对象的 git 调用（E-329 起点树复用它，不在 service 拼 git 命令） | `domain/bughunt-prompt.ts`（`assembleBughuntPrompt()`）、`domain/bughunt-report.ts`（`parseBughuntReport()` / `decideBughuntOutcome()`）、`service/bughunt.ts`（`dispatchBughunt()` / `finalizeBughuntRun()`）、`service/review.ts` 判 pass 后的开关判定接线、`domain/run-state-machine.ts` 新增七个 `RunTransitionReason`、`repo/runs.ts` 的 `(parent_run_id, kind)` 查询、新增一个普通迁移（`runs.prompt_source`、`(parent_run_id, kind)` 索引）、`test/unit/bughunt-report.test.ts`、`test/fixtures/bughunt/` | 1) `finalizeReviewRun()` 判 pass 后读 `settings.pipeline.bughunt`（每次读库、不缓存、不进快照）：为 1 且该实施运行尚无 bughunt 行 → 事务内 INSERT `kind='bughunt'` 行（`queued`、`parent_run_id`=实施运行、`origin='dispatch'`、`permission_tier='workspaceWrite'`、`worktree_path` / `branch_name` 逐字同实施、`prompt_source`），实施行保持 `reviewing`；为 0 → 走既有落地闸门；路由表里没有查 bug 端点，`dispatchBughunt()` 的唯一调用方是判 pass 后的分流；查 bug 运行失败后允许对该行调既有 `POST /runs/:id/rerun`，仅当任务停在 `bughunt_failed` 的 `awaiting_human`（E-306、E-331） 2) 提示词四段顺序写死（包装说明 → 引用材料逐字 → 工作区指针 → 结束语），材料取派发快照 `bug_prompt`，NULL 用内置版并 `prompt_source='builtin'`（E-316）；agent／model／effort **逐字复制被审实施运行行的 `agent_id` / `model_name` / `effort_tier`（任务指派的副本，决策 107），不从 `launch_spec_json.defaultModel` 推导、不读注册表当前默认**，agent 不可用则不派、不换家并转人写 `bughunt_agent_unavailable`（E-328、E-341）；M8-T9 落地后改为经 `readTaskAssignment()` 读快照 `assignment_json`，本任务不留第二条取值路径；三值入口返回形状预留 `effortVendor?`；包装说明与收口共用同一禁令常量、禁 commit，派发时记起点树作 FIXED 与再审 diff 的基线（E-329） 3) bughunt 行走既有边 `queued → starting → running → exited → reviewing → landed／awaiting_human`，进 `reviewing` 后只做五段解析、不跑机械检查、不派审查 agent、豁免 E-61 空 diff；tick 的进 HEAD 扫描跳过它（E-321） 4) `decideBughuntOutcome` 四值：FIXED 非空且工作区 diff 非空 → `rereview`（`rework_count<2` 时 +1 并调 `startReviewRound({round: N+1, reworkItems: FIXED 条目})`；已达 2 → 实施行 `awaiting_human`、闸门 comment `bughunt_fixed_over_limit`）；NOT_FIXED 含 S1/S2 → 实施行与 bughunt 行均 `awaiting_human`（reason `bughunt_open_findings`）并释放槽位；其余 → 走落地闸门；FIXED 非空但 diff 为空按 NOT_FIXED 处理并标「自报已修但无改动」（E-307、E-308、E-321） 5) 不合五段 → 两行均 `awaiting_human`（reason `bughunt_unparsed`），原文保留、不部分解析，人过闸门后实施 `landed`、bughunt `failed`（E-320）；bughunt 行 failed／aborted／interrupted／启动超时 → 实施行 `awaiting_human`（reason `bughunt_failed`），`rework_count` 不动（E-323） 6) 事务前解析、事务内迁移、事务后投递（08 节）；bughunt 行不写 `review_verdict` / `rework_text`，代码中不存在 bughunt 本身计入 `rework_count` 的路径；17 节列出的五段解析单测全部通过 | 1d |
| M8-T1 | 并发窗口计算与瓶颈归因 | M8 | M3-T2, M4-T1 | M3-T2 的批次与窗口数；M4-T1 的并发上限 | `domain/concurrency.ts` | 1) 实际并发取 `min(窗口数, 每 agent 上限, 用户设定)`，并显示这三者中**哪一个成了瓶颈**（E-52） 2) 单 agent 并发已满但窗口还有空位时，空位让给其他 agent 的任务，不空转等待（E-47） 3) 窗口数为 1 或依赖纯串行时退化为逐个派，**不报错、不提示「无法并行」**（E-48） 4) 纯函数实现，可被直接单测 5) 用户设的窗口数只是**上限**，实际并发取 min(设置, 每 agent 上限, 机器资源, 路径冲突)，**设置值不被运行时静默改写**（E-245） | 1d |
| M8-T2 | 路径冲突检测与排队 | M8 | M8-T1 | M8-T1；`taskPaths` | 冲突判定与队列 | 1) 同批内两任务 `taskPaths` 有交集时后者排队，等前者到达**「已落地」（不是「已退出」）**才起（E-46） 2) worktree 隔离**不作为放行理由**，代码中不存在「有 worktree 就放行」的分支 3) 路径比较按段进行，`model/order` 不匹配 `model/orderitem` | 1d |
| M8-T3 | 批次推进与幂等派发 | M8 | M8-T2, M5-T2, M6-T1 | M8-T2 的可放行集合 | `scheduler-tick` job、`http/routes/runs.ts`、`http/routes/batches.ts`、`http/routes/snapshot.ts` | 1) `scheduler-tick` 全局一把内存互斥量，**禁止并发 tick、禁止重入**，同一任务不会被派两次 2) 幂等键唯一，两端同时点派发时第二次返回既有 run 而非起第二个进程（E-126） 3) 批内某任务失败或审查未过时不阻塞同批其他无依赖任务；下一批启动条件是本批 `done`——全部「已落地」且批次状态为 `done`，前一批未 done 时 `POST /batches/:id/start` 返回既有前置检查错误并在 `details.previousBatchState` 写明（E-49、E-281） 4) daemon 重启或断电恢复后按持久化状态续跑，会话还活着就重挂、已死的标「中断」等人决定，**任何情况下不自动重派**（E-51） 5) 批次执行途中任务 ID/contractHash 指纹变化时已派任务用快照跑完，后续派发自动暂停待确认；自动、手动和重派入口在创建新运行前都检查 is_contract_ready，未通过返回 E_DOC_CONTRACT_PENDING 并列出原因，人工确认不绕过文档契约阻断（E-50、E-82） | 2d |
| M8-T4 | 三个闸门与预设组合 | M8 | M8-T3, M7-T3 | M8-T3 的推进逻辑；M7-T3 的判定 | `Gate` 模型、`domain/gates.ts` 的 `resolveAfterReview()`、`POST /gates/:id/decide`、`PATCH /settings/gates`、`settings` 表迁移与 `repo/settings.ts`、`settings.gates_changed` 事件 | 1) 三个闸门（派发前／审查后／落地前）各自可设「自动」或「等我确认」，落地闸门默认 `manual`、可设 `auto`；`auto` 只表示审查 pass 且审查后闸门也为自动时直接进 `landed`（已验收），代码中不存在任何 git 写操作；`gateOverrides` 允许含 `landing` 且只影响本批尚未到达该闸门的任务（E-53、E-56） 2) 预设：半自动 = 派发自动 + 审查后等确认；自动 = 审查 pass 自动进下一个，审查非 pass 一律停；闸门设置存 `settings(key='gates')`，缺行返回内置默认不插行，`value_json` 非法回落默认并 warn 不退出，PATCH 三字段整体写入缺一即 `E_VALIDATION`，写成功发 `settings.gates_changed`（E-292） 2b) `task.landed` payload 带 `by` 取 auto 或 human 与 `gateId` 3) 闸门等确认而人长时间不来时该任务**释放并发额度、不占窗口**，其他任务继续，仅下游依赖被动等待（E-54） 4) 执行中途改开关只影响尚未到达该闸门的任务，不需重启批次（E-56） 5) 两端同时点确认时幂等，第二次提示「已由另一端确认」，不产生两次派发（E-57） 6) 人工操作优先级高于自动推进，批次可中途暂停／接管，自动派发不得覆盖人的判断（E-05） | 2d |
| M8-T5 | 重派、换 agent 与原样重跑 | M8 | M8-T3 | M8-T3 的派发路径 | `POST /runs/:id/rerun`、换 agent 重派 | 1) 换 agent 重派时新建运行记录、旧运行转只读留存；默认开干净 worktree，除非显式选「在原改动上继续」（E-121） 2) 「原样重跑」严格复用原派发载荷（任务／agent／模型／批次／权限档），**不出现任何选择器**，需一次确认（E-177 的服务端侧） 3) 重跑时原 agent 不在线则立即失败并明示，**禁止静默回退到别的 agent 或模型**（E-178） 4) 文档快照已变更时禁止重跑并提示去桌面端处理（E-180） 5) 所属批次已被超越时允许重跑但提示批次错位（E-179） 6) 模型名在派发时已失效时不做前置白名单校验、直接透传，失败后标「派发失败·模型无效」且该任务不占用窗口名额（E-36） | 2d |
| M8-T6 | 批次收口触发、轮次与批次级闸门 | M8 | M8-T3, M8-T4, M3-T6, M7-T6, M5-T5 | M8-T3 的 tick 与批次路由；M3-T6 的收口材料；M7-T6 的解析器；M5-T5 的进 HEAD 判定；M8-T4 的闸门模型 | `domain/batch-state-machine.ts`、`domain/wrapup-prompt.ts`、`domain/wrapup-policy.ts`、`service/wrapup.ts`（`triggerWrapup` / `recordWrapupResult` / `listWrapups`）、`service/batch.ts` 的 `transitionBatch()`、`http/routes/batches.ts` 新增 `wrapup` 与 `wrapups` 两端点、`repo/batch-wrapups.ts`、四表重建迁移（batches 七值、runs 新列与 `kind` 四值含 `bughunt`、dispatch_snapshots / gates 的 task_id 可空、`batch_wrapups` 表）、`db/migrate.ts` 的 `foreign_keys=off` 例外、shared 的 routes / codes / events / batches DTO 增量 | 1) 批次状态七值白名单只在 `transitionBatch()` 里改，每次迁移发 `batch.advanced{from,to,reason}`；tick 第 1 步按节流刷新 `is_in_head` 与 `branch_tip_sha`，第 2 步全部 landed 但有未进 HEAD → `awaiting_landing`（E-272），全部进 HEAD 且无在途收口且轮次 < 2 → 派收口运行且本 tick 不再派发；读接口不跑 git（E-283、E-301） 2) 收口运行 `kind='wrapup'`、`task_id` 为空、`workspaceWrite`、占 1 名额、同批同时只一条（唯一部分索引）；agent/model/effort **默认跟随本批 `ended_at` 最晚的 landed 实施运行的任务指派**（即决策 116 的 `follow` 模式，并列取 run id 字典序最大），第 2 轮沿用上一轮，agent 不可用转 `needs_attention` 不静默换家，手动触发可在 body 覆盖（E-287、E-344）；三值的取值收成 `triggerWrapup()` 内单一函数入口，pipeline 设置 `wrapupAssignment` 的 `fixed` 模式由 M8-T9 接进该入口，本任务不读 `settings.pipeline` 3) 提示词按 08 节四段顺序组装，材料逐字，材料缺失走 builtin（E-296）；工作区指针段只给本批 diff stat 与任务清单、不内联 diff 正文，收口 agent 自己在工作区读文件；报告里「基于部分 diff」的标注由解析器原样保留进 `summary_text`，不因此改裁定（E-285） 4) 收口运行退出后走八段解析不派审查 agent，空 diff 不判失败（E-294）；解析成功 INSERT `batch_wrapups` 并按有效裁定迁移：clean/fixed → `done`，open → 交 M8-T7 派修复；解析失败或运行失败/中止 → 收口运行 `awaiting_human` + `task_id` 为空的收口闸门 + 批次 `needs_attention`，不回滚工作区（E-274、E-295） 5) 批次级闸门 pass 必带 comment → 写 `is_human_verdict=1` 的人工裁定行 → `done`；reject 只关闸门；手动 `POST /batches/:id/wrapup` 从 `needs_attention` 不受自动上限、受硬顶 6 轮，超限 `E_WRAPUP_ROUND_LIMIT`；条件不满足 `E_BATCH_NOT_WRAPPABLE` 带 `details.reason`；幂等键重复 409 返回既有 run（E-288） 6) 迁移首行标记走 `foreign_keys=off` 例外，`foreign_key_check` 零行，含 0001 数据的库 fixture 逐行无丢失、新旧库 `sqlite_master` 一致（E-291） 7) 17 节的 `batch-wrapup` 集成剧本跑到 `done`；事务内不 await/spawn/publish（08 节） | 2d |
| M8-T7 | 收口修复派发、跨批语义与人工撤回 | M8 | M8-T6, M7-T5 | M8-T6 的有效裁定与开放项；M7-T5 的返工提示词组装；M8-T2 的冲突判定 | `service/wrapup.ts` 的修复派发段、`domain/task-state.ts` 的 `origin` 分支与 `deriveTaskInHead()`、`http/routes/tasks.ts` 的 `recall` 端点、`domain/path-clash.ts` 的同收口串行规则、shared 的 `TaskDto.inHead` / `crossBatchFix` | 1) 有效裁定 open 时按 `planWrapupFixes` 分组，每个任务派一次修复运行：`origin='wrapup-fix'`、`spawned_by_run_id`=收口运行、`batch_id`=触发批次、`attempt_no+1`、`rework_count=0`、`workspaceWrite`、复用收口 worktree（`reuse`），agent/model/effort 取该任务最近实施运行快照，agent 不可用则该组不派并计入 unassigned；提示词 = R 条目原文 + 返工规则段（E-275） 2) 同一收口派出的修复运行互相串行（`queued_reason` 以 `wrapup-fix-serial:` 开头），与在途任务路径冲突照 E-46 排队；修复运行走正常审查，代码中不存在按 origin 跳过审查的分支（E-280、决策 70） 3) 本轮修复运行全部 landed 且收口分支 `is_in_head=1` 且轮次 < 2 → tick 触发第 2 轮；第 2 轮仍 open、或开放项全部无主 → `needs_attention` 不派第 3 轮（E-276、E-290） 4) 跨批：修复运行归触发批次、任务行归原批次；已 `done` 的批次不因跨批修复重触发收口、`done` 不回退；批次计数按最近一次非修复运行，任务行标跨批修复（E-275） 5) `POST /tasks/:taskId/recall` comment 必填，起 `origin='wrapup-fix'`、`spawned_by_run_id` 为空的修复运行走正常审查；该任务已有在途修复/返工运行或本批收口运行在跑 → `E_FIX_RUN_IN_FLIGHT`；不计入 E-55 计数；每任务同时只允许一条在途修复运行（E-293、E-300） 6) `TaskDto.inHead` 三态由 `deriveTaskInHead()` 算出（E-298 的数据侧） | 1.5d |
| M8-T8 | 流水线槽位、补位与阶段设置 | M8 | M8-T3, M8-T6, M7-T8, M6-T10 | M8-T3 的 tick 与派发路径；M8-T6 的 `triggerWrapup` 与 `wrapup-policy.ts`；M7-T8 的 bughunt 行与开关读取；M6-T10 的 `tasks.lane_no` / `runs.lane_no` 与 `assertSessionRefFree()`；M8-T4 的 `settings` 表与 `repo/settings.ts`；M8-T2 的可放行集合 | `domain/lanes.ts`（`deriveLanes()`）、`domain/lane-slots.ts`（`countLaneSlots()` / `freeLaneNumbers()`）、`domain/pipeline-settings.ts`、`domain/wrapup-policy.ts` 的 `canWrapup()`、`service/lanes.ts`、`service/settings.ts` 的 `getPipeline()` / `updatePipeline()`、`service/dispatch.ts` 的补位段、`service/wrapup.ts` 的模式校验与收口持槽、`jobs/scheduler-tick.ts` 第 ②③ 步改写、`http/routes/settings.ts` 两端点、`http/routes/documents.ts` 的 `laneCount` 校验与事件、`http/routes/snapshot.ts` 的 `lanes`、shared 的 `api/lanes.ts`（`LANE_STAGES` / `PIPELINE_STAGE_ORDER` / `LaneView`）、`api/settings.ts` 的 `PipelineSettings` 与 schema、`api/events.ts` 的 `lane.assigned` / `lane.released` / `document.settings_changed` / `settings.pipeline_changed`、`api/routes.ts` 两条、`errors/codes.ts` 的 `E_PIPELINE_STAGE_DISABLED`、`test/unit/lanes.test.ts`、`test/unit/lane-slots.test.ts`、`test/unit/pipeline-settings.test.ts`、`test/integration/pipeline-lanes.test.ts` | 1) `deriveLanes()` 纯函数、不读 db／clock／settings：`laneNo` 从 1 到 `max(laneCount, 已占最大 lane_no)` 连续、`overLimit = laneNo > laneCount`，`stage` 只取 `LANE_STAGES` 七值（含 `rework`）且 arch 测试断言产出集合与 shared 逐项相等（E-332），空闲槽 `nextTaskId` 取可放行集合按 `(layerNo, taskKey)` 排序的第 k 个、耗尽时给未派任务并填 `nextBlockedBy`，`archivedTaskIds` 只含该泳道最近归档的一个任务、`archivedWrapupRunId` 同法；所有集合先按 id 排序，同一库重启后输出逐字节相同（E-317、E-319） 2) tick 第 ③ 步按槽位补位：`countLaneSlots` 按 `tasks.lane_no` 非空与持槽收口运行计 `used`，`free = freeLaneNumbers(laneCount, byLane)`，打回后等泳道的返工排在全部新任务之前（按打回时间先后），其余可放行集合排序取前 `free.length` 个再过 M8-T1 的 agent 名额（E-327）；事务内 `UPDATE tasks SET lane_no=? WHERE id=? AND lane_no IS NULL`（`changes===0` 跳过、唯一索引冲突回滚整个 tick 事务）、INSERT 实施运行（`runs.lane_no` 同值、快照含 `bug_prompt`）、经 `assertSessionRefFree()`，事务后 spawn 与 `lane.assigned`；一个 tick 最多补 `laneCount − used` 条，同一任务不分两个槽（E-303、E-311） 3) `calculateConcurrencyLimit` 与 `countsTowardAgentConcurrency` 签名与语义不变，审查／查 bug／收口／修复运行各占 agent 1 名额但不占槽；槽位只看 `lane_no`，代码中不存在用 `runs.state` 数在跑运行来算槽位的路径（E-311） 4) 释放侧：实施行迁 `awaiting_human`／`orphaned` 或终态、收口运行迁终态的事务里置 `tasks.lane_no` NULL 并收集 `lane.released{docId, laneNo, taskId, runId, reason}`（scope `lane`，与 `lane.assigned` 同形；信封 scope 扩为八值且 kind 前缀即 scope 由类型断言机检，E-334），事务后 nudge，下一 tick 因升序拿回同一 laneNo；人打回让任务重回在跑时在同一事务里重新分配最小空闲槽、无空闲则 `max+1` 超限槽（E-326）；`PATCH /documents/:id/settings` 的 `laneCount` 限 1..6、事务后发 `document.settings_changed` 并 nudge：调大立即补位、调小不杀任何运行只停止补位（E-309、E-310） 5) `GET/PATCH /settings/pipeline`：缺行默认 `{bughunt:0, wrapupMode:'auto'}` 不插行、非法回落并 warn、PATCH 两字段整体写入（AJV `additionalProperties:false`）、写成功发 `settings.pipeline_changed` 并 nudge；两开关不进 `gates` 行、不进 `documents`、不进派发快照（E-318） 6) tick 第 ② 步在触发条件成立后读 `wrapupMode`：`manual` 不调 `triggerWrapup`、不迁批次状态、`canWrapup` 由 `wrapup-policy.canWrapup()` 算为 true；`triggerWrapup({trigger:'auto'})` 在 `manual` 下抛 `E_PIPELINE_STAGE_DISABLED{stage:'wrapup'}`，`trigger:'manual'` 两种模式都放行；收口运行有空槽即持槽、无空槽 `queued_reason='lane_full'` 由 tick 先于新任务分槽（E-312、E-283） 7) `GET /snapshot` 的 `lanes` 每次现算、无缓存、不开事务、不跑 git；17 节的 `pipeline-lanes` 集成剧本十二条断言全部通过（E-302、E-303、E-307、E-309、E-310、E-312） | 1.5d |
| M8-T9 | 任务指派贯穿流水线与阶段覆盖取值链 | M8 | M3-T3, M6-T2, M7-T2, M7-T5, M7-T8, M8-T7, M8-T8, M4-T14 | M3-T3 的 `repo/dispatch-snapshots.ts` 与快照写入点；M7-T2 的 `startReviewRound()`；M7-T5 的 `dispatchRework()` 新开分支；M7-T8 的 `dispatchBughunt()` 与 `finalizeReviewRun()` 判 pass 分流；M8-T8 的 `service/settings.ts` `getPipeline()` / `updatePipeline()`、`domain/pipeline-settings.ts`、`refillLanes()` 与 `triggerWrapup()`；M8-T7 的修复派发段；M4-T14 的 `EffortValue`、`remapEffortAcrossAgents()`、`toEffortColumns()` / `fromEffortColumns()`、`assertVendorEffortInDomain()` 与 `CreateRunBody.effort` 类型；M6-T2 的收流路径；M8-T5 的 `rerun` 与 M8-T3 的 `POST /runs` | `domain/assignment.ts`（`resolveAssignment()`）、`domain/content-events.ts`、`service/assignment-reader.ts`（`readTaskAssignment()`）、`service/dispatch.ts` / `service/review.ts` / `service/review-agent.ts` / `service/rework.ts` / `service/bughunt.ts` / `service/wrapup.ts` / `service/run.ts` 的接线、`service/settings.ts` 四键校验、`service/gates.ts` 的 `context` 组装、`repo/dispatch-snapshots.ts` 的 `parent_snapshot_id IS NULL` 过滤与子快照 INSERT、`proc/` 的 stderr 尾环形缓冲与 `run.exited.stderrTail`、`domain/run-state-machine.ts` 的 `EXITED_BEFORE_OUTPUT`、一个普通迁移（`dispatch_snapshots.assignment_json` / `parent_snapshot_id`、`runs.assignment_source`）、shared `api/runs.ts` 的 `AssignmentSnapshot` / `AssignmentResolutionSource` / `RunDto.assignmentSource` / `RunDto.followedTaskId`、`http/routes/runs.ts` 的 `POST /runs` 收 `effort` 与 `rerun` 例外、`api/settings.ts` 的 `PipelineSettings` 四键与 schema、`api/gates.ts` 的 `GateDto.context`、`api/events.ts` 的 `CONTENT_EVENT_KINDS`、`test/unit/assignment.test.ts`、`test/unit/review-zero-output.test.ts`、`test/unit/pipeline-settings.test.ts` 增量、`test/integration/agents-models-login.test.ts` | 1) `resolveAssignment()` 是零依赖纯函数（只 import `./effort-tier.ts` 与 shared），六 `stage` 穷举：`implement` body → agent 默认；`review` `reviewOverride` → 任务指派，跨家时模型置 null 记 `model_dropped_cross_family`、思考强度 `remapEffortAcrossAgents()` 映射不了置 null 记 `effort_unmappable`；`rework` / `bughunt` / `wrapup-fix` **逐字任务指派、零回落**，传入 `stageOverride` / `manualOverride` 被忽略；`wrapup` 手动 body → `fixed` → `follow`，`follow` 取不到返回 `follow_source_missing`；不读 settings／db／clock；缺 agent 由 service 调用后 `assertCanDispatch()` 判（E-341、E-342、E-344） 2) `POST /runs` 与 `refillLanes()` 在 INSERT 快照的同一事务里写 `assignment_json {agentId, modelName, effortTier, effortVendor, source:'task', followedTaskId:null, capturedAt}`；`rerun` **复用原 `snapshot_id`**，不新建快照、不改 `assignment_json`，`assignment_source` 抄原运行（决策 129）；`triggerWrapup()` 写 `source:'wrapup_settings'` 与 `followedTaskId`；`POST /runs` body 收 `effort: EffortValue`，`{vendor}` 经 M4-T14 的 `assertVendorEffortInDomain()` 校验不在域内 → `E_VALIDATION`（`details.field:'effort'`）；查 bug 运行失败后对该 `kind='bughunt'` 行 `rerun` 放行并作废 E-323 开的闸门（E-331）；`assignment_json` 只写不改、旧行 NULL 由 `readTaskAssignment()` 用该快照首条 `runs` 行合成不回填；`assignment_json` 字面量只出现在 `repo/dispatch-snapshots.ts` 与 `service/assignment-reader.ts`（arch 断言）（E-341、E-343） 3) 四个阶段接线各经 `readTaskAssignment(implRunId)` → `resolveAssignment()`：`startReviewRound()`（任一轮）、`dispatchRework()` 新开分支、`dispatchBughunt()`、M8-T7 修复派发；在途阶段不读注册表当前 `defaultModel` / `defaultEffortTier`（单测：改 `agentDefaults` 不影响 review／rework／bughunt／wrapup-fix 输出）；每条运行的 `runs.assignment_source` 写 `resolveAssignment().source` 并随 `RunDto.assignmentSource` 下发；`RunDto.followedTaskId` 由 service 按 `snapshot_id` 集合一次联结取自 `assignment_json.followedTaskId`（旧快照无该键 → null，不新增 `runs` 列）；`RunDto.effort` 经 `fromEffortColumns()` 读 `effort_tier` / `effort_vendor` 两列，`effortTier` 字段保留等于 `effort.tier ?? null`（决策 129，E-93、E-347、E-357） 4) 跨家 `reviewOverride` 时同一事务 INSERT 子快照（`parent_snapshot_id` 指向实施快照，文本列、`contract_hash`、`task_paths_json`、`bug_prompt`、`assignment_json` 逐字复制，`launch_spec_json` 换成覆盖 agent 当时的注册表副本），审查行 `snapshot_id` 指向子快照；同家不建；「最近快照」查询一律 `parent_snapshot_id IS NULL`；覆盖 agent 不可用 → 不建、不派、不换家，实施行 `awaiting_human` 并 comment `review_agent_unavailable`；续接同会话时结果与第 N 轮不等则走 `new_session`（E-352、E-304） 5) `settings.pipeline` 扩为四键 `{bughunt, wrapupMode, reviewOverride, wrapupAssignment}`：AJV `additionalProperties:false` + `required` 四项，缺任一 `E_VALIDATION`；`agentId` 只查注册表快照不起探测子进程；`effortTier` 只收三档，`effortVendorMap` 为 null 的 agent 传非 null → `effort_unsupported`；缺行默认 `{…, reviewOverride:null, wrapupAssignment:{mode:'follow'}}` 不插行，旧两键行读侧补默认不改库；不需迁移；写成功发完整四键的 `settings.pipeline_changed` 并 nudge（E-356） 6) 收口指派：`follow` 取本批 `ended_at` 最晚的 `landed` 实施运行的任务指派（并列取 run id 字典序最大），第 2 轮沿用上一轮收口快照；`fixed` 只填 agentId 时回落 follow 值（跨家重映射）；`follow_source_missing` 或 agent 不可用 → 不派、批次 `needs_attention`，禁止静默换家；手动 body 优先级最高只作用该轮；修复运行逐字读被修任务指派（E-344） 7) 零产出退出：`service/run.ts` 收流路径维护每 run 的 `hasContentEvent`（`CONTENT_EVENT_KINDS` 三种任一即 true，dsh 末条文本映射的 `agent_message_chunk` 同法）；`finalizeReviewRun()` 第一步在机械检查之前判 `hasContentEvent===false`：实施行同一事务 `reviewing → awaiting_human`（reason `exited_before_output`）、INSERT 闸门 comment `exited_before_output`、`lane_no` 置 NULL 并收集 `lane.released`，不跑机械检查、不派审查、不写 `review_verdict`、`rework_count` 不变；审查行走 E-62、查 bug 行走 E-323、收口行走 E-295；启动超时与 spawn 失败仍走 `starting → failed`；事务后 `refreshLogin({trigger:'exited_before_output'})`（dsh 不调）；判定只读事件 kind，代码中不存在匹配 stderr 文本的分支（E-348） 8) `proc/` 每 run 维护最后 20 行 stderr 环形缓冲（每行 ≤1 KiB、总量 ≤8 KiB），**产品级 `run.exited` 由 `service/run.ts` 在进程 `close` 时发**并带 `stderrTail`（脱敏后、成功运行也写；厂商映射产出的同名事件不带该字段，读侧取该 run 最后一条带 `stderrTail` 的）；该闸门 `decide{decision:'reject'}` 走 `awaiting_human → failed`（人判定失败）而不是 E-59 返工路径，`POST /runs/:id/rerun` 对停在 reason `exited_before_output` 的 `awaiting_human` 运行放行（M8-T5「仅终态可重跑」的唯一例外）并作废该闸门；`GET /gates?pending` 对 `comment==='exited_before_output'` 的行组装 `context {exitCode, exitSignal, stderrTail, login}`，`stderrTail` 取该 run 最后一条 `run.exited`，旧事件缺字段返回 `[]`；`context` 不落库（E-348、E-354） 9) 17 节 `assignment.test.ts` 十二例、`review-zero-output.test.ts`、`pipeline-settings.test.ts` 增量与 `agents-models-login.test.ts` 八条集成断言全部通过；事务内不 await／spawn／publish（08 节） | 1d |
| M9-T1 | Web 骨架、token 层与主题 | M9 | M2-T1 | 11 节的 tokens 与组件规范 | `packages/web/` 骨架、`styles/tokens.css`、`theme-provider`、两个检查脚本 | 1) `styles/tokens.css` 的 `:root` 与 `[data-theme]` 两个块是**全仓唯一允许出现颜色字面量**的地方，由 `check-forbidden.ts` 机检（E-170） 2) 深浅两套 token 成对存在，`check-contrast.ts` 遍历前景/背景配对算 WCAG 比值并在不达标时失败（E-169） 3) 主题走 `html` 元素上的 `data-theme` 属性，跟随系统主题与字号，**不锁根字号、不用 `text-size-adjust:none`、不用 zoom**（E-15） 4) 「无色 = 不用管」在浅色下有显式取值，运行轨仍可见且不被误读为禁用（E-171） 5) 视觉方向**只以 token 层表达**——颜色、字号、圆角、形状枚举全部收敛为 CSS 变量与一张枚举表，使方向被否决时代价 ≤ 改一个 token 文件（E-159、E-161） 6) 深色新增任一状态或 chip 时，**同一 PR 内必须补齐浅色 token**，禁止「先深色后补」（E-173） 7) 文档或阅读器中出现的任何配色截图必须标注「示意，未经目视确认」（E-162） 8) 第一个前端任务完成后必须出一屏真页给用户目视确认；用户不看或直接认可即视为该风险关闭（E-160） | 2d |
| M9-T2 | 形状枚举与状态徽标 | M9 | M9-T1 | 11 节的形状表与状态表 | `lib/spine-shape.ts`（内联 SVG path）、`components/status-badge.tsx` | 1) 12 个形状为单色描边内联 SVG，一律 `currentColor`，**path 中无颜色字面量**（E-231） 2) 「失联」「审查未完成」「未识别／降级」各有**专属形状**，绝不复用 `✕`，不允许只靠颜色区分（E-230） 3) 统一 viewBox + `vector-effect:non-scaling-stroke`；在 Windows 125%/150% 缩放与一台 Android 真机上各看一次，形状不糊（E-232） 4) 每个形状带中文 `aria-label`，灰度打印下仍可区分（E-233、E-110） 4b) 形状在浅色下靠**形状本身**区分，**不依赖深色下的发光或光晕**；形状枚举两模式逐一对应（E-172） 5) 形状表与状态枚举一一对应且可机检，不允许「先上线后补形状」（E-234） 6) 徽标为矩形非药丸，恒为「字形 + 文字」，颜色是第三层信息 | 1.5d |
| M9-T3 | 自写 hash 路由与守卫 | M9 | M9-T1 | 07 节的七条路径 | `app/routes.tsx`、`navigateTo()`、`route-guard.tsx` | 1) 七条平铺路径 + 两个单段参数，hash 模式；三种加载源（daemon `/`、`tauri://localhost`、`https://localhost`）下行为一致 2) 命中未登记 hash 时渲染「未知路径」页并给「回到运行甲板」，保留顶栏，**绝不白屏、绝不抛异常**（E-222） 3) 路径参数指向不存在的对象时由**页面自身**渲染「该运行不存在或已被清理」，不由路由层判 404（E-223） 4) 首次加载 hash 为空时启动即归一化为 `#/`，**无中间态白屏**（E-224） 5) 组件内不出现 `location.hash = ...`，导航一律经 `navigateTo()` | 1.5d |
| M9-T4 | HTTP 客户端、错误规格化与重试 | M9 | M9-T1, M2-T6 | `shared/src/api/routes.ts` | `api/http-client.ts`、`base-url.ts` | 1) 全仓除 `http-client.ts` 与 `sse-client.ts` 外不出现 `fetch(`，由 `check-forbidden.ts` 机检 2) baseUrl 三级发现：壳注入 → 用户手填 → `location.origin`；**无构建期常量**（E-06 的兜底侧） 3) 只重试 GET 且只在网络错误与 502/503/504，最多 2 次；**POST 一律不自动重试** 4) 抛 `ApiError{code,message,requestId,details}`，前端一律按 `error.code` 分支，**不按 HTTP 状态码写业务分支** 5) 写请求注入 `X-Idempotency-Key`，重试复用同一个（E-126 的客户端侧） | 1.5d |
| M9-T5 | 自写 SSE 客户端与重连 | M9 | M9-T4, M2-T5 | M2-T5 的 SSE 服务端 | `api/sse-client.ts` | 1) 用 fetch + ReadableStream 自己分帧，**能带 `Authorization` 头**，忽略 `:` 心跳注释（E-08 的客户端侧、E-154） 2) 全应用只开一条连接，按信封的 taskId/runId 分派，**禁止每流一条 SSE** 3) 指数退避 `[1000,2000,4000,8000,15000]` ±20% 抖动封顶 15s，保留 `Last-Event-ID`，**不做无间隔轮询式重连**（E-158） 4) 60s 内未收到任何字节判死连接主动 abort 重连 5) 收到重放窗口过期码时清空本地 buffer → 拉 REST 全量快照 → 从最新 id 续接（E-153 的客户端侧） 6) 收到 401 立即停止退避循环并置 `needsPairing`（E-156 的客户端侧） | 2d |
| M9-T6 | 事件缓冲与按帧渲染 | M9 | M9-T5 | M9-T5 的 SSE 客户端与事件分派接口 | `api/event-bus.ts`（`RunStreamBuffer` 环形数组、`useSyncExternalStore` 订阅） | 1) 事件**不进 zustand 也不进 React state**；SSE 回调只做「push 进环 + 标脏」，不 `setState` 2) flush 由 rAF 合并且最小间隔 80ms，一次 flush 每条脏流只 bump 一个整数 version（E-144） 3) `getSnapshot` **只返回 version 数字**，不返回数组或对象 4) 定长环形数组 `STREAM_BUFFER_MAX=600`，超出丢头部，历史一律回 daemon 拉（E-142 的客户端侧、E-143） | 1.5d |
| M9-T7 | 运行轨与运行流条目 | M9 | M9-T2, M9-T6 | M9-T2 的形状；M9-T6 的缓冲 | `components/spine.tsx`、`stream-row.tsx` | 1) 运行轨**贯穿整栏全高**含未来步骤的虚线段；只画到当前步即判不合格 2) 六种轨段形态（实线／心跳点／虚线／空心方块＋整段转暖／平头截断／半调虚线＋横杠）各自可见且形状先于颜色 3) 条目折叠高度 30px（触摸 44px），网格 `20px minmax(0,1fr) auto 16px`；**每一步都显示耗时** 4) 标签为「工具 + 对象」，成功步默认折叠、失败步默认展开并提供「从这一步重试」 5) 键盘焦点用**内嵌环**，滚动列表不整体抖 6) 轨段形态与状态徽标共用同一张形状枚举，状态区分不只靠色相（E-110）；「失联」「审查未完成」等态各有专属形状而非复用失败形状（E-230） | 2d |
| M9-T8 | 虚拟列表与日志窗口 | M9 | M9-T7 | M9-T7 的条目 | `components/virtual-rows.tsx`、`features/run-detail/log-window.ts` | 1) 十万行日志只挂载可视窗口，**任何时候不把全量放进 DOM 或 store**（E-143） 2) 内存只保留 6 段（每段 ≤2000 行），滚出窗口即丢弃并可重拉（E-98 的客户端侧） 3) 用户滚到中部时新事件到达**不自动跳底**，显示「N 条新事件」；仅贴底才跟随（E-100） 4) ANSI 转义与重复刷新行折叠，控制字符不破坏布局（E-101） 5) 单行超长按宽度截断并可展开，**绝不把整行塞进 DOM**（E-102） 6) 虚拟滚动只经这一个包装使用，**禁止第二处直接 import** | 2d |
| M9-T9 | 多流甲板与密度档 | M9 | M9-T7 | M9-T7；11 节断点表 | `stream-column.tsx`、`useDensityTier()` | 1) 档位是**单点计算**的枚举（`full/compact/narrow/phone/phone-xs`）并以 prop 下传，不散落多处（E-235） 2) 流数 ≥4 默认紧凑档，用 auto-fill **换行网格**（每格 min 260px）**而非横向滚动**（E-164） 3) 紧凑档展开某条时该条占满宽度、其余保持 260px **留在同屏绝不折叠消失**（E-165） 4) 260px 放不下时加宽列宽，**不许砍掉停止控件或把状态降级成纯色点**（E-166） 5) 停止控件与审批槽位渲染在**所有档位分支之外**；配按档位枚举逐档断言停止控件存在的单测（E-236、E-106） 6) narrow 档停止键仍常驻可见，**禁止 hover 显示或收进 `⋯`**（E-237） 7) 拖动窗口切换档位时不重挂虚拟列表、不弹回顶部、不中断跟随（E-238） 8) 档位只依据视口宽度 + 指针类型，**绝不按 UA 猜设备**（E-239） 9) 流数 ≤3 且窗口 ≥1440px 时默认完整档、不出现横向滚动，档位选择记忆到本地（E-163） 10) 完整档横向滚动时若视野外某条流转成「要你」，视口左右边缘出现常驻计数标记（E-167） 11) 桌面窗口 <1100px 退化为单列列表，**不套用手机端规则**，也不允许在 900px 硬塞三栏（E-168） 12) `stream-column.tsx` 是泳道外壳：五段网格 `[head][refBar][body][afterBody][foot]`，停止键与审批槽位在所有 `kind` / 档位分支之外渲染；流数恒等于 `lanes[]` 长度（并行窗口数），一条流就是一个任务槽位，阶段链由 M9-T21 填进 `body`（E-311、E-317 的呈现侧） | 2d |
| M9-T10 | 审批卡与闸门交互 | M9 | M9-T7, M8-T4 | M8-T4 的闸门数据 | `components/gate-card.tsx` | 1) 审批卡**就地插在该流时间轴里**，禁止 import dialog、禁止 `createPortal`，**不弹全局模态**打断其他还在跑的流（E-109） 2) 四段固定顺序：要做什么／影响什么／**凭什么（带可点链接回产出该结论的那一步）**／三个动作，「改一下」不是可选项 3) 底部写明「无人应答不会自动批准，任务保持等待」 4) 全局只用一个未处理计数徽标提示 5) 手机上审批派发前闸门时文案必须写「批准派发」，**不得含糊成「同意」**（E-182） | 1.5d |
| M9-T11 | 停止的乐观呈现与离线态 | M9 | M9-T9, M9-T5 | M9-T5 的连接状态 | `local-intent` Map、`offline-banner.tsx` | 1) 按停止键后**一帧内**本地置态并写明「你在第 N/M 步停止」，不等回包；服务端事件到达即清 intent，失败则清 intent + 横幅回滚（E-157） 2) 断连时顶栏显示「离线，最后同步于 X」并**禁用派发按钮**，恢复后自动补拉（E-12） 3) daemon 未运行时明确提示「电脑上的调度服务未启动」，**而不是连接超时**（E-04） 4) 版本不兼容时提示升级而不是抛底层错误（E-14） | 1.5d |
| M9-T12 | 手机端布局与拇指区 | M9 | M9-T9 | 11 节的手机规格 | `thumb-bar.tsx`、单栏切换 | 1) 手机竖屏 <400px 降级为单栏切换（任务列表／运行流／详情），**不得横向滚动或并排三栏**（E-145） 2) 停止与批准固定在拇指区、分置两端或间距 ≥24px、各 ≥44×44px，**不随日志滚动移出视野**（E-107） 3) 单栏切换到「任务列表」时若某条流转「等你」，必须有可见的未处理计数徽标（E-240） 4) 手机端中止需二次确认防口袋误触（E-124） 5) 首屏加载量显著更小（尾部 32KB），不预取全量；切后台回前台不重拉全量（E-99） 6) 展开的 tool payload 走 bottom sheet 不内联 7) 批次表格在小屏降级为可折叠列表（E-13） 8) app 曾退到后台时重回前台拉未读列表，**等待中的确认项不过期、不自动放行**（E-58） | 2d |
| M9-T13 | 手机原样重跑入口 | M9 | M9-T12, M8-T5 | M8-T5 的重跑接口 | 流详情页末尾的整宽按钮 | 1) 仅对终态失败/已中止的运行可用，严格复用原派发载荷、**不出现任何选择器**，需一次确认（E-177） 2) 入口在流详情页日志末尾的整宽按钮，**不进拇指条**；拇指区仍只有停止与审批 3) 「换 agent／换模型重派」保持桌面端独占，手机上只显示一行「请到桌面端」，不做半截表单 4) 手机端不提供「全部重跑」（E-181） 5) 竖屏 <400px 时该按钮整宽独占一行，与拇指条保持间距防误触 | 1d |
| M9-T14 | 设置页：agent 注册表与模型选择 | M9 | M9-T4, M4-T5, M4-T2 | M4-T5 的清单；M4-T1 的注册表；M4-T2 的 `session-dir-overlap` 告警 | `settings-agents-page` 与其容器 | 1) 每个字段显示「内置默认 / 你的覆盖 / 当前生效」三行并带「恢复默认」（E-92 的呈现侧） 2) 模型下拉与桌面共用同一份清单数据，手机端退化为全屏选择器，**不因屏宽裁剪候选项** 3) 清单不全时明示「清单可能不全」并保留手填入口（E-38 的呈现侧） 4) agent 不可用时置灰并写明缺什么，点击可跳到该项配置（E-88 的呈现侧） 5) 两字符短码可编辑、保存时唯一性校验，撞车要求改一个（E-183） 6) 紧凑档或窄屏下字母组必须配合完整名称出现在流头部，长按/悬停出全名，**字母组不得成为唯一标识**（E-184） 7) 接入第 5/6 个 agent 只需填两字符短码，**不新增任何资源文件**（E-185） 8) 并行窗口数是本产品自己的设置（默认 2，值域 1–6，按文档持久化），设置项旁注明「此值只属于调度器，与阅读器互不影响」（E-248） 9) 收到 `reason` 精确等于 `session-dir-overlap` 的告警时显示「会话记录可能互相覆盖」，不按 daemon 的英文开发者 message 直出中文（E-95 的呈现侧） 10) **呈现侧数据来源与落点（本次审查定点补记，不改已有 1–9 条）**：三层表的「内置默认」「你的覆盖」与顶层生效值只读 daemon 提供的字段——`AgentEntryDto.layers` 与 `PATCH /agents/:id {clearOverrides}` 由 M4-T14 落地，**在此之前 daemon 未提供的层显示「—」**；前端不得自建与 `config/defaults.ts` 平行的注册表副本（含短码／可执行路径等出厂值），也不得按 `agentId` 判「是不是某一家」的分支；「恢复默认」不得把前端算出的默认值当请求体发出（`defaultModel` 之类内置默认非字符串时更不得发）。E-92 的「内置默认已更新（旧值 → 新值）」与一键采纳只呈现 daemon 提供的升级差异，**前端不得伪造 `defaultUpdates` / `warnings` 这类 daemon 不存在的字段**。E-95 只按 `reason` 精确等于 `session-dir-overlap` 分支，该原因经 `agent.availability_changed` 事件的 `vendor.reason` 到达（`packages/daemon/src/boot/container.ts`），**不得按 daemon 的英文 message 匹配或直出**。E-185 指 agent 身份由注册表 `monogram` 文本呈现、接入新 agent 不新增任何资源文件；daemon 没有创建 agent 的路由，故本任务**不提供只改本地 state 的新增入口**。展示组件（`agent-card.tsx` / `model-picker.tsx` 等）落 `packages/web/src/components/`（07 节 components 清单，M9-T23 亦按该落点增量消费），`features/settings-agents/` 只留 `use-〈域〉.ts` 与 `〈域〉-container.tsx`，容器里只许写 grid/flex/gap。 | 2d |
| M9-T15 | 设置页：设备、配对与浏览器模式 | M9 | M9-T4, M2-T2 | M2-T2 的配对接口 | `settings-devices-page`、`pair-page`、`shell-bridge` 降级 | 1) 配对失败报到具体环节（「扫到码但连不上 host:port」）并提供手填地址兜底（E-06） 2) 已配对设备可列出并单独吊销，吊销后该设备连接立即断开（E-127） 3) 保存设备令牌而非 IP；PC 局域网 IP 变化后已授权设备不需重新授权（E-09） 4) 允许配对多台设备，每台独发一枚可单独吊销的令牌（E-125） 5) 无壳时令牌落 `sessionStorage`，顶栏常驻提示同时写明「关闭标签后需重新配对」**与「本模式下没有系统通知」**（E-229） 6) 壳存在时以壳安全存储为唯一真相源，启动清掉会话副本，**绝不双写**（E-227） 7) 同一浏览器第二个标签需重新配对，设备名默认带「浏览器 · 时间」便于辨认与吊销（E-228） | 2d |
| M9-T16 | 空态四步引导、批次汇总与落地清单页 | M9 | M9-T9, M5-T4 | M5-T4 的落地清单数据 | `empty-onboarding.tsx`、`batch-summary-bar.tsx`、`landing-page` | 1) 空态是「选文档 → 选批次 → **逐任务指派** → 派发」四步引导，当前步高亮、已完成步显示所选值可点回改，**不是插画**（E-108） 2) 批次汇总条是四个数字 + 一条比例条，**不得升格成饼图或环形图**挤占运行流主位（E-111） 3) 落地清单页展示 worktree 路径、分支名、diff 摘要、可一键复制的 `gh stack` 命令文本与 `build_docs.py --landed` 命令，每条各带复制按钮（E-74 的呈现侧） 4) 文档变更横幅显示「本文档已更新，N 个任务的依据已变」并可进入受影响任务的过滤列表（E-19 的呈现侧） 5) 「在整个会话中查找」是显式入口，避免用户误以为 Ctrl+F 已搜全文（E-218） 6) 长时间盯屏下深色为默认、日志等宽（E-110 的呈现侧） | 2d |
| M9-T17 | 流头部参照条与会话序号 | M9 | M9-T9, M4-T6, M4-T7, M4-T14, M8-T9 | M4-T6 的取值链；M4-T7 的两张映射表；M4-T14 的 `RunDto.effort`（`EffortValue`）；M8-T9 的 `RunDto.assignmentSource` / `followedTaskId` | `stream-head-meta.tsx` | 1) 每条流头部之下**常驻**一条「模型 · 思考强度 · 权限档 · 来源」参照条（网格 `auto auto auto minmax(0,1fr)`，第四段可截断、前三段不截断），回答四问中的「它凭什么这么干」，**不可折叠**——要点开才看得到等于没答；第四段读 `RunDto.assignmentSource` / `followedTaskId`，文案「来源：任务指派／审查覆盖／收口设置（跟随 〈taskKey〉）」，`agent_default` 显示「来源：任务指派（agent 默认）」（括注可见），四值之外或缺失显示「来源：—」并把原始值放 title，`compact`／`narrow` 档用短文案；`RunDto.effort` 为 `{vendor}` 时第二段显示原串并 title「厂商原值，未映射到三档」；前端不读快照、不读设置推来源（E-347、E-357） 2) 思考强度不支持的 agent 显示「—」并在 title 说明原因，**不补默认档冒充**（E-254） 3) 自报值与所选不一致时**两者都显示**并转 `--needs`，模型与思考强度同规则（E-37、E-256） 4) 权限档为最高档时该段转 `--down` 且**持续显示**，不是一次性提示（E-136 的呈现侧） 5) 同一 agent 的多个并发会话在 monogram 右下角带**中性会话序号**角标，**序号不占色相**——它是身份的一部分不是状态（E-31 的呈现侧、决策 33） | 0.5d |
| M9-T18 | 逐任务指派面板与并发瓶颈说明 | M9 | M9-T16, M4-T6, M8-T2 | M4-T6 的取值链；M8-T2 的窗口计算 | `assign-panel.tsx` | 1) 四步引导第三步是**逐任务指派**而不是「给整批选一个模型」：每行一个任务，各自选 agent／模型／思考强度，已指派行显示所选值可点回改（E-108 的第三步） 2) **同一个 agent 允许被指派多次，每次是一个独立会话**并显示将要使用的会话序号（E-31、决策 7、决策 52） 3) 第四步显示并发三者取 min 的结果，并**明确指出是并行窗口、该 agent 上限还是用户设定成了瓶颈**，不只给一个可放行数字（E-52） 4) 某 agent 已达并发上限时其余任务仍可指派给别家，**不空转等待**（E-47 的呈现侧） | 0.5d |
| M9-T19 | 批次树折叠、自动展开与执行呼吸灯 | M9 | M9-T16, M9-T2, M9-T5 | M9-T16 的左栏与批次汇总条；M9-T2 的形状；M9-T5 的 SSE 与 event-bus；`BatchDto` / `TaskDto` 的新字段 | `components/batch-tree.tsx`、`components/pulse-dot.tsx`、`components/gate-toggles.tsx`、`lib/run-pulse.ts`、`features/run-deck/batch-expansion.ts`、`features/run-deck/use-batch-tree.ts`、`features/run-deck/gate-toggles-container.tsx`、`features/task-list/` 接入、`styles/base.css` 的 `@keyframes agsched-pulse`、`api/cache-invalidation.ts` 与 `cache-keys.ts` 增量、`check-forbidden.ts` 两条新禁令 | 1) 批次树纯 props，折叠的批不渲染子行（不是 `display:none`）；标题计数「已落地 x/n · 在跑 y · 等你 z」、收口徽标、能否收口、默认展开全部读 daemon 字段，组件内不算任何计数，展开集只来自 props（E-284、E-272 的呈现侧） 2) 展开集存 `batch-expansion.ts` 模块级 Set，左栏与任务列表页共用；首次按 `defaultExpanded` seed，`batch.advanced` 经 `subscribeMilestone` 到达且 `to ∈ {running, wrapping, awaiting_landing, needs_attention}` 时并入，任何事件永不删元素，手动折叠后同批事件仍展开；不进 store、不进任何持久化键，`check-forbidden` 白名单不变（E-284） 3) `pulse-dot` 一个组件被 spine 与批次树两处 import；`@keyframes` 只在 `base.css` 且恰一处、`animation:` 不出 `styles/`，两条 grep 禁令进 `pnpm -w check`；`RUN_PULSE` 以 `satisfies` 覆盖 13 个状态，live 恰为 starting/running/reviewing/reworking，awaiting_* 静态暖点，终态与 queued 无点；`prefers-reduced-motion` 下环冻结在中间不透明度（E-282） 4) 任务行「进 HEAD」标记三态同色、固定 `7ch` 列，`false` 的 title 显示判定方法；跨批修复 chip（E-298） 5) 行高 30px，phone/phone-xs 44px 且开合槽 44×44；`role="tree"` 键盘可达；手机档批次树是任务列表 pane 的顶层结构并取代 M9-T12 的折叠列表（E-13），narrow 档沿用左栏抽屉（决策 86） 6) 顶栏三个闸门开关同一形态，落地开关可切、切到自动不弹 dialog 而常驻一句提示，值走 `GET/PATCH /settings/gates` 全量三值，状态等 `settings.gates_changed` 回流不乐观翻转（E-299） | 1.5d |
| M9-T20 | 收口泳道、收口报告面板与批次落地清单 | M9 | M9-T19, M9-T10 | M9-T19 的批次树与 `pulse-dot`；M9-T10 的审批卡；`GET /batches/:id/wrapups` 与 `POST /batches/:id/wrapup` | `components/wrapup-report.tsx`、`features/run-deck/wrapup-panel-container.tsx`、`components/stream-column.tsx` 的收口头部分支、`components/gate-card.tsx` 的条件动作与 `features/run-deck/use-gate-card.ts`、`pages/landing-page.tsx` 的批次级清单 | 1) `kind='wrapup'` 的运行占一条普通泳道，头部「批次收口 · 第 N 轮 · 第 M 批」，停止控件、参照条、运行轨渲染在 kind 分支之外，E-236 的逐档单测加 kind 维度断言停止控件存在（E-297、E-106） 2) 收口报告面板只展示 daemon 已解析字段（tests / findings / unassigned / landing / reportText），解析失败显示「解析失败」并直链原文，代码中不存在前端解析 `reportText` 的路径；findings 整行可点跳该任务，`isWellFormed=false` 行尾 chip「格式不全」，`promptSource='builtin'` 时头部 chip「文档未提供收口提示词」（E-297、E-286 的呈现侧） 3) 批次标题「收口」按钮只在 `canWrapup` 且非手机档渲染，请求在途禁用，接受后不改状态等 `batch.wrapup_started`；`E_BATCH_NOT_WRAPPABLE` / `E_WRAPUP_ROUND_LIMIT` 按 `details.reason` 给具名文案贴在该批标题下（E-157） 4) 审批卡第四个条件动作「投递原文到实施会话」（ghost）只在 `reviewVerdict='incomplete'` 且 `reworkText` 非空时出现，按目标运行 `capabilities.canReply` 灰掉并带 title，走 `POST /runs/:id/messages`，未送达时卡内 inline notice + 原文可复制（E-278、E-117、E-113） 5) 批次级落地清单列每轮收口分支与各修复分支各一行，附可复制命令，`inHead` 为真的行打勾（E-74 的批次侧） | 1d |
| M9-T21 | 任务流水线泳道与历史行 | M9 | M9-T9, M9-T7, M9-T19, M9-T20 | M9-T9 的 `stream-column` 与密度档；M9-T7 的 `spine` 与 `stream-row`；M9-T19 的 `pulse-dot`、`cache-keys` / `cache-invalidation`；M9-T20 的收口泳道头部；10 节快照的 `lanes[]` 与 `lane.assigned` / `lane.released` / `task.sessions_archived` / `document.settings_changed` 事件；shared 的 `PIPELINE_STAGES` | `components/pipeline-lane.tsx`、`stage-chain.tsx`、`lane-history-row.tsx`、`lane-run-strip.tsx`、`lib/stage-rows.ts`、`lib/spine-shape.ts` 的 `LOOP_PIECES`、`spine.tsx` 的 `level` / `loop` 字段、`stream-column.tsx` 改为五段泳道外壳、`features/run-deck/use-lanes.ts` / `lanes-container.tsx` / `lane-steps-container.tsx`、`api/lanes.ts`、`cache-keys.ts` / `cache-invalidation.ts` 增量、`selection-store.mobileLaneNo`、`ui-prefs-store.expandedLaneNo`、`tokens.css` 的 `--spine-node-stage` / `--spine-node-step`、`check-forbidden.ts` 三条新禁令（阶段字面量、泳道号不进 URL、轨不测量 DOM） | 1) 泳道数与顺序只来自 `lanes[]`（按 `laneNo` 升序、不补洞），前端不算槽位、不算下一个任务、不推断阶段；`components/` / `features/` / `pages/` 出现阶段字面量比较即 `check-forbidden` 失败（E-317） 2) 阶段链每阶段一行 30px（触摸 44px），已过阶段折一行带总耗时、当前阶段展开步骤、未来阶段虚线且耗时列留空；紧凑档当前阶段只保留最后一行步骤，五档都不折、不换表示，260px 放不下按 E-166 加宽列宽（E-315） 3) 返工回环由 `LOOP_PIECES` 按行片段画在 20px 轨列内，`reworkCount=0` 不画；「第 N 轮」= `reworkCount+1` 只在 ≥1 时显示；查 bug 后再审的回环指向审查行且轮次沿用同一计数（E-307 的呈现侧）；`spine.tsx` 与 `stage-chain.tsx` 不测量 DOM 4) 「查 bug」行只在存在 `kind='bughunt'` 运行时出现、不画「已跳过」节点（E-306 的呈现侧）；「落地」行恒最后 5) 阶段行 Enter 或点击 → `onOpenRun(runId)` 打开该阶段最近一次运行详情；`readOnly` 或 `runId=null` 的行无 tabIndex（E-313） 6) 历史行只有一条、绑定 `archivedTaskIds[0]`（收口泳道用 `archivedWrapupRunId`），折叠六段固定顺序，展开渲染只读阶段链、零 `pulse-dot`、props 无任何动作 callback；泳道第一个任务不渲染历史行与分隔线（E-314、E-325） 7) 空闲泳道渲染 `idleText`（含 `nextBlockedBy`），不画假链，停止键置灰保持原位；`overLimit` 泳道头部 chip「超出窗口数」；停靠中（`awaiting_human`）的任务不在甲板上（E-309、E-319、E-326） 7b) 阶段顺序只来自 shared 的 `PIPELINE_STAGES`，`rework`／`queued` 落在实施行，未知 `stage` 显示「—」不抛错（E-332）；`lanes` 经 `api/lanes.ts` 从 `GET /snapshot` 取、丢弃其余键，快照缺 `lanes` 显示「泳道数据不可用」（E-333） 8) 停止键与审批槽位只在 `stream-column.tsx` 里、所有 `kind` / `densityTier` 分支之外渲染；`pipeline-lane.test.tsx` 与 `stream-column.test.tsx` 各一张 `5 档 × {running, idle, overLimit, wrapup}` 矩阵断言停止键存在（E-236、E-106） 9) 手机档一次一条泳道 + `lane-run-strip` ◀ ▶（各 ≥44×44，`laneCount<=1` 或到边界 disabled 不隐藏），当前泳道号存 `selection-store` 内存、不进 URL（E-324） 10) `lib/stage-rows.test.ts` 与 `lib/spine-shape.test.ts` 追加用例全部通过；`@keyframes` 仍只在 `base.css` 恰一次 | 2d |
| M9-T22 | 顶栏流水线开关与流水线设置页 | M9 | M9-T21, M8-T8 | M9-T21 的 `lanes-container`；M9-T19 的 `gate-toggles`；M8-T8 的 `GET/PATCH /settings/pipeline` 与 `settings.pipeline_changed` | `ui/segmented-toggle.tsx`、`components/pipeline-toggles.tsx`、`features/run-deck/pipeline-toggles-container.tsx` / `use-pipeline-settings.ts`、`pages/settings-pipeline-page.tsx`、`app/routes.tsx` 的 `#/settings/pipeline`、`i18n/ui-strings.ts` 四条文案、`check-forbidden.ts` 的「开关互不 import」禁令 | 1) 两个开关同一形态（二段、高 `--h-btn-sm`）放三个闸门右侧、1px 竖线隔开；`gate-toggles.tsx` 与 `pipeline-toggles.tsx` 都只 import `ui/segmented-toggle`、互不 import、不复制 markup，`check-forbidden` 机检 2) 受控无内部 state：值来自 `GET /settings/pipeline`，`value=null` 显示「—」并 disabled（E-26）；`onChange` 给出两值，容器用缓存里的 `reviewOverride` / `wrapupAssignment` 补齐后 `PATCH /settings/pipeline` **四键全量**（E-356），`pending` 期间 disabled、DOM 不翻转，状态等 `settings.pipeline_changed` 回流（E-157、E-318） 3) 「查 bug」切开与「收口」切手动各常驻一行说明，不弹 dialog（E-306、E-312） 4) `#/settings/pipeline` 是第八条路由、`lazy` 且与其他 settings 页同一 Suspense，页面只摆同一个容器（`layout='settings'`）并显示「当前值来自 daemon」；手机档顶栏不渲染这五个开关，只在设置页可切 5) `E_PIPELINE_STAGE_DISABLED` 按 `details.stage` 就地 inline notice，不 toast 6) `pipeline-toggles.test.tsx`、`routes.test.ts`、`check-forbidden.test.ts` 新增用例通过 | 0.5d |
| M9-T23 | 指派面板的登录态、清单来源与刷新 | M9 | M9-T18, M9-T14, M9-T22, M4-T13, M4-T14, M8-T9 | M9-T18 的 `assign-panel.tsx` 与派发草稿；M9-T14 的 `settings-agents-page` 与 agent 卡片；M9-T22 的 `use-pipeline-settings.ts` 与流水线设置页；M4-T13 的 `AgentEntryDto.login` 与 `agent.availability_changed{reason:'login_changed'}`；M4-T14 的 `GET /agents/:id/models` 新返回结构、`EffortValue`、`AgentEntryDto.layers` / `effortVendorMap` / `builtinModels`、`PATCH /agents/:id` 的 `defaultEffortTier` / `clearOverrides`；M8-T9 的 `PipelineSettings` 四键；shared `MODEL_SOURCES` | `ui/grouped-select.tsx`、`components/login-badge.tsx` / `login-hint.tsx` / `model-picker.tsx` / `effort-picker.tsx` / `pipeline-assignment.tsx`、`components/assign-panel.tsx` / `stream-head-meta.tsx` / `gate-card.tsx` / `agent-card.tsx` 的增量、`features/settings-agents/use-agent-models.ts`、`features/run-deck/use-assign-panel.ts` / `use-gate-card.ts` / `use-pipeline-settings.ts` 的增量、`pages/settings-agents-page.tsx` / `settings-pipeline-page.tsx` 的增量、`hooks/use-minute-tick.ts`、`lib/model-groups.ts` / `effort-options.ts` / `login-freshness.ts`（各配同名 `.test.ts`）、`api/agents.ts` 的 `listModels(agentId, {refresh})`、`api/cache-keys.ts` 的 `agentModels()`、`api/cache-invalidation.ts` 的 `agent.availability_changed` 映射、`store/selection-store.ts` 的逐任务草稿与 `openReassign()`、`i18n/ui-strings.ts` / `error-messages.ts` 的文案、`styles/tokens.css` 的两个尺寸 token、`scripts/check-forbidden.ts` 六条新禁令、`test/login-badge.test.tsx` / `login-hint.test.tsx` / `model-picker.test.tsx` / `effort-picker.test.tsx` / `use-agent-models.test.tsx` / `pipeline-assignment.test.tsx` 与 `assign-panel` / `stream-head-meta` / `gate-card` / `cache-invalidation` / `check-forbidden` 各测试的增量 | 1) 指派面板每行行首顺序 monogram → agent 名 → `login-badge` → 「刷新清单」图标键；徽标 `login === null` 时不渲染任何 DOM，三态文案与主题按 11 节（`logged_out` 暖色、其余无色），`checkedAt` 超 24 小时退成无色虚线边并加「· 已过期」；`state !== 'logged_in'` 时该行下渲染 `login-hint`：`logged_out` 且 `loginCommand` 非空显示「未登录：在终端运行 〈命令〉 后点刷新」+ 复制键，`logged_out` 且为空显示「未登录：按 〈agent〉 自身文档登录后点刷新」且无复制键，`unknown` 显示「登录态未知：确认后点刷新」（不说「未登录」）；三态徽标文案「已登录／未登录／无法判定」；状态谓词（`isLoggedIn` 等）放 `lib/login-freshness.ts` 导出，组件不写 `'logged_*'` 字面量；**派发按钮与行内 `disabled` 不因任何 `login.state` 改变**，`login.state` 出现在 `disabled` 表达式里即 `check-forbidden` 失败（E-336、E-355 的呈现侧） 2) `model-picker` 基于 `ui/grouped-select`（radix Select 二次封装，`src/**` 出现 `cmdk` 或 `ui/shadcn/command` 即失败）按 shared `MODEL_SOURCES` 顺序出五组、空组不渲染、**不去重不排序不改名**（`new Set(` / `dedupe` / `uniq` 出现即失败），带 `provider` 的条目按首次出现顺序切子组并在标题前放 inline 徽标（`login.providers[provider]` 存在才渲染）；`isCurrentConfig` → chip「当前配置」并由容器作默认选中写进草稿（组件不自己挑）；`source` 不在 `MODEL_SOURCES` 的条目归末尾「其他」组按原串标注，`lib/model-groups.ts` 返回 `unknownSources[]` 由容器 warn（决策 134）；`manual` 组恒一项「手填模型名…」，选中展开等宽输入框，Enter／失焦提交、Escape 回退；`isComplete === false` → footer「清单可能不全，可手填」（`role="note"` 不进方向键序列），`isComplete === true` 无 footer；`catalog === null` 触发器 disabled，`isRefreshing` 沿用旧清单并显示「实时清单获取中」；phone／phone-xs 打开为全屏 sheet、行高 `--row-h-touch`，任何档位不裁剪候选项；代码中不存在按 `agentId` 判「是不是 claude／pi／dsh」的分支（E-338、E-339、E-350） 3) `effort-picker`：`vendorMap === null` → 只读「—」+ title，不渲染选择器不禁用整行；否则「三档」组（跟随／低／中／高）+「厂商原值」组（配置当前值的 `{vendor}` + 所选模型 `effortOptions` 里不等于三档映射值的项，`lib/effort-options.ts` 纯函数），**没有手填框**；`allowVendor=false` 不渲染厂商组；配置当前值命中项加「当前配置」chip，`effortRecognized === false` 再加「无法识别」chip；`effortSupportWarning()` 返回非空时选择器下方显示「该模型不支持 〈值〉」（`aria-live="polite"`）不禁用不换值；`value` 与 `onChange` 的 `'' / 'tier:x' / 'vendor:y'` 编解码在组件内往返（E-254、E-351 的呈现侧） 4) `use-agent-models.ts` 是唯一调 `GET /agents/:id/models` 的地方：首读走普通 fetcher（无 query），`refresh()` 走 `invalidate(exactKey)` → `read(exactKey, refreshFetcher)`（query 精确为 `refresh=1`），同一 agent 在途时后续 `refresh()` 直接返回且两个面板看到同一个 `refreshPending`，settle 后再 `read(key, normalFetcher)` 换回普通 fetcher（`resource-cache.read()` 以最后一次传入的 fetcher 为准，`refetchAll` 不触发子进程）；在途或响应 `isRefreshing:true` 时返回 `useRef` 里最近一次完整清单；`refresh=1` 只在用户点击时发，挂载／重连／事件到达都不许；`cache-keys.agentModels(agentId)` 前缀 `agentModels`，**没有 `agentLogin` key**；`cache-invalidation` 表加 `agent.availability_changed → ['agents','agentModels']`（E-339 的呈现侧） 5) 登录态只从 `AgentEntryDto.login` 读（随 `GET /agents` 的 `agents` key），`ListAgentModelsResponse` 不含登录态；`agent.availability_changed{reason:'login_changed'}` 到达只经 `cache-invalidation` 失效 `agents` key 重拉 `GET /agents`（表按 kind 不按 reason，`resource-cache` 无 patch 接口），不重拉 `/snapshot`、不重探；「N 分钟前探测」由 `hooks/use-minute-tick.ts` 驱动（模块级 `setInterval(60_000)` + `useSyncExternalStore`，无订阅者不起，文件不 import `src/api`），格式函数 `lib/login-freshness.ts` 单测覆盖时钟回拨与 24 小时边界（E-335、E-357） 6) `stream-head-meta.tsx` 第四段与 `{vendor}` 显示按 M9-T17 第 1 条；`gate-card.tsx` 在 `context` 非 null 时四段布局（标题、退出码大数 + 固定提示句、stderr 块三态「无 stderr 输出／无记录（旧运行）／事件缺失」+ 登录态徽标行、三动作 `[重跑][换 agent 重派][标失败]` 复用既有三个 callback 槽位），`context === null` 时快照与现版本逐字相同，仍不 import dialog／portal，卡出现时焦点落 `[重跑]`；`use-gate-card.ts` 把 `onApprove` 映射为 `POST /runs/:runId/rerun`、`onEdit` 由 `use-gate-card.ts` 先调 `features/run-deck/batch-expansion.ts` 展开所在批次、再调 `selection-store.openReassign(taskId)`（清除左栏筛选并轻提示、滚到该行、预选当前快照 agent、焦点落 agent 选择器；`store/` 不 import `features/`）、`onReject` 走既有拒绝二次确认；phone 档不渲染第二个动作、`lines` 折叠末 5 行可展开（E-348、E-359 的呈现侧） 7) 设置页 agent 卡片：三行表加「思考强度」列，「内置默认」「你的覆盖」读 `layers.defaultEffortTier.builtin / .override`（`hasOverride === false` 显示「—」）、「当前生效」读顶层 `defaultEffortTier`，模型列同法读 `layers.defaultModel` 与顶层 `defaultModel`；「你的覆盖」的模型名不在 `catalog.models` 里 → chip「清单未列」；`<400px` 竖排；每个字段各自一个「恢复默认」（无覆盖置灰不隐藏），发 `PATCH /agents/:id {clearOverrides:['defaultModel'｜'defaultEffortTier']}` 只清该字段，`defaultEffortTier: null` 单独发表示「覆盖为跟随」；PATCH 失败「你的覆盖」保持原值不乐观清空；卡片头部徽标 + 引导行，「刷新清单」与「重新探测」两个按钮；dsh 的思考强度块显示「该 agent 不支持」不渲染三行与按钮（E-358） 8) 流水线设置页在两个开关之下加「审查覆盖」与「收口指派」编辑区（`pipeline-assignment.tsx`），跟随态各常驻一行说明不弹 dialog，`effort-picker allowVendor=false`；`use-pipeline-settings.ts` 的 PATCH 永远发四键全量（开关 `onChange` 只给两值时用缓存补齐后两键，反之亦然），`pending` 期间四个控件一起 disabled 等 `settings.pipeline_changed` 回流；`E_VALIDATION` 的 `details.field` 点路径就地渲染到对应控件下并 `aria-invalid`（E-356 的呈现侧） 9) 颜色只在 `tokens.css`，本任务新增尺寸 token 只两个（`--badge-h-inline`、`--picker-note-w`），登录态／provider／来源不分配色相，「已过期」不用透明度；`check-forbidden.ts` 六条新禁令（agent 特判、登录态字面量、登录态当 disabled、来源字面量、前端去重、禁用 command）与「不轮询」规则改写各一正一反例；17 节「E-335～E-339 / E-350 / E-351 / E-355～E-359（前端侧）」一行列出的 `login-badge` / `login-hint` / `model-picker` / `effort-picker` / `use-agent-models` / `pipeline-assignment` / `use-minute-tick` / `agent-card` 用例与 `assign-panel` / `stream-head-meta` / `gate-card` / `cache-invalidation` 增量全部通过，`lib/` 三个新文件各有同名 `.test.ts`；GIF 覆盖真实服务可达的流程（徽标与刷新、分组下拉、思考强度、恢复默认、流水线设置页、错模型名触发的零产出审批卡），`legacy_run` / `event_missing` / `unknown:timeout` 等不可达态以单测为准；界面中文字符串只从 `i18n/ui-strings.ts` 来，固定提示句不写「未登录导致」定论 | 0.5d |
| M10-T1 | 壳桥接契约与能力探测 | M10 | M9-T1 | 07 节的三件能力 | `shared/src/shell/bridge-contract.ts`、`web/src/shell/` 两个文件 | 1) 能力只有三件（`tokenStore`／`notify`／`hostHint`）加两个只读位，契约以类型形式约束，**加第四件需先改文档** 2) 探测同步读两个全局并在模块加载时冻结，**禁止用 userAgent 判定、禁止 try/catch 试探、禁止渲染中重复探测** 3) 全仓除 `src/shell/` 两文件外不出现 `__TAURI` / `Capacitor`，由 `check-forbidden.ts` 机检 4) 无壳时三件能力各有降级实现，桌面浏览器直连**功能不缺**（E-200） | 1.5d |
| M10-T2 | 三平台桌面壳（Tauri v2） | M10 | M10-T1, M1-T6 | M10-T1 的契约；M1-T6 的自启机制与 `DaemonLaunchSpec` | `packages/shell-desktop/` | 1) Windows、macOS、Linux 只做窗口与单实例、系统通知、令牌安全存储三件事；**壳内不出现 task/run/gate/batch/agent 任何业务词**，任一平台缺壳不得标完整支持（E-257） 2) daemon 未运行时显示连接失败页 + 「启动 daemon」按钮；壳每次启动根据实际 `current_exe/resource_dir` 解析并校验绝对 `file`、`args[]`、绝对 `cwd`，按钮直接消费该冻结对象且不经 shell，**壳自身不内嵌任何调度或重试策略**（E-146、E-209） 3) Windows 缺 WebView2 时由启动检测给安装引导；Linux 由安装前置检查或命令行 launcher 在创建 WebView 前诊断 WebKitGTK/GTK，不能要求尚未启动的 GUI 自己报错；macOS 使用系统 WebKit（E-148、E-258） 4) 触发平台原生自启注册：把与按钮相同的 `DaemonLaunchSpec` 交给 M1，逐字段校验注册项 `file/args/cwd`，不一致则重写；失败降级为手动启动并给本平台命令（E-209、E-210） 5) 用户手动关掉自启项时只检测并询问，**不静默重注册**，询问带「不再提醒」（E-208） 6) 通知点击以 `#/run/〈runId〉` 交给 WebView，**不做自定义 IPC 传业务数据** | 2d |
| M10-T3 | 手机壳（Capacitor / Android） | M10 | M10-T1 | M10-T1 的契约 | `packages/shell-mobile/` | 1) 同一份 web 产物，`webDir` 指向同一次构建，**不为壳单独打第二份产物** 2) 令牌存 Capacitor Preferences，**不落 localStorage** 3) 硬件返回键映射到 `history.back()`，hash 栈退到底时才允许退出 app，**不得一按就退出**（E-225） 4) app 被系统杀掉后重开用本地 `Last-Event-ID` 补拉未读，**不引入离线推送**（E-147、E-11） 5) 「未注册自启」在手机侧不产生任何告警或 UI（E-211） 6) 无 Android 构建环境时降级为浏览器打开局域网 URL，界面验收标准不变（E-200） | 2d |
| M10-T4 | 两端版本校验与共享打包管线 | M10 | M10-T2, M10-T3 | 两个壳；10 节 `/api/v1/version` 端点表与 `VersionResponse` | daemon 侧暴露 `GET /api/v1/version`（`{daemon, apiVersion, node}`）、两个壳启动期消费同一次构建前的版本校验接线、共享打包脚本（daemon 静态托管 / Tauri `frontendDist` / Capacitor `webDir` 指向同一份 `packages/web/dist`，字体二进制随该产物自托管，web 具备可构建入口） | 1) 壳启动时校验 `apiVersion`，不兼容时提示升级**而不是抛底层错误**（E-14） 2) daemon 的 `@fastify/static`、三平台 Tauri 的 `frontendDist`、Capacitor 的 `webDir` **全部消费同一次构建**，产物内容一致 3) `base: './'` 相对路径在各加载源下均可加载，无 404 4) 字体随产物托管，**不引入任何 CDN 或外链**（E-175） 5) 字体加载失败时回退 `ui-monospace` 且布局不塌，列宽用 `ch` 计算（E-174） 6) 只自托管实际用到的字重（E-176） | 1.5d |
| M10-T5 | 三平台 CI、桌面产物与支持清单 | M10 | M10-T4 | M10-T4 的共享打包管线；17 节测试矩阵 | GitHub Actions 三平台 workflow、运行时启动清单解析器与前置检查 launcher、平台/架构支持清单、人工 GUI 验收模板 | 1) Windows、macOS、Ubuntu runner 均执行 `pnpm -w check`、平台集成测试、daemon 启动 smoke 与 Tauri 构建，任一失败阻断整个版本（E-265） 2) 每个平台把安装产物展开到含空格与 Unicode 的临时根目录，以该位置模拟 `current_exe/resource_dir` 并生成经共享 schema 校验的绝对 `DaemonLaunchSpec`；smoke 必须用同一个 `file/args/cwd` 真正启动 daemon 并通过健康检查，安装包不得预写构建机绝对路径；daemon、路径/进程适配与桌面壳任一产品层缺失都阻断发布（E-209、E-257） 3) Linux 包元数据或 WebView 前命令行 launcher 检测 WebKitGTK/GTK，缺依赖时进程在创建 GUI 前列出发行版对应安装指引；该检查本身纳入 Ubuntu runner（E-258） 4) CI 不声称自动完成 GUI 交互；每个平台另有人工启动/通知/路径验收记录（E-266） 5) 支持清单逐项列出 OS、版本基线、CPU 架构和资产名；Windows arm64、Intel/Apple Silicon macOS 与 Linux x64/arm64 缺漏时标“未覆盖”，不提供错架构包（E-259） 6) macOS 未签名/未公证产物只标构建验证件，正式发布门禁失败（E-267） 7) Linux 写明 Ubuntu 已验证版本与 WebKitGTK/GTK 依赖，其他发行版只标尽力兼容（E-258、E-268） 8) Node/Tauri 提升平台下限时，CI、环境矩阵、发布说明缺一项更新都失败（E-260） | 2d |
