---
type: task
id: M9-T25
module: M9
depends_on: [M9-T3, M9-T8, M9-T9, M9-T12, M9-T14, M9-T15, M9-T16]
unlocks: [M1-T11]
edges: [E-06, E-222, E-223, E-224]
est_days: 1.5
branch: task/M9-T25
status: todo
---

# M9-T25　七条路由挂进装配件并可导航

- 所属模块：[[M9]]　**客户端界面**——两端共用的那一套界面本身：多流监看、会话查看、审批与闸门、设置与配对、落地清单、按批次折叠的批次树与执行行呼吸灯、按窗口数呈现的任务流水线泳道与「会话已归档」历史行、顶栏两个流水线开关，以及同一套代码的窄屏降级。它只通过 M2 的契约与 daemon 通信，**界面里不含任何业务判定**（折叠默认值、「能否收口」、泳道分配、阶段推进与会话归档都是 daemon 给的字段）；指派面板的分组模型下拉、登录态徽标与刷新按钮、参照条的「来源」段同样只呈现 daemon 下发的字段，不算取值链
- 前置依赖：[[M9-T3]]、[[M9-T8]]、[[M9-T9]]、[[M9-T12]]、[[M9-T14]]、[[M9-T15]]、[[M9-T16]]
- 完成后解锁：[[M1-T11]]

## 输入 / 产出

- **输入**：M9-T3 的 `routes.tsx` / `route-guard.tsx`；M9-T9 的 `RunDeckContainer`；M9-T8 的 `RunDetailContainer`；M9-T12 的 `mobile-batch-list.tsx`；M9-T14 的 `settings-agents-container.tsx`；M9-T15 的 `settings-devices-container.tsx` 与 `pairing-container.tsx`；M9-T16 的 `app.tsx` 与 `landing-page.tsx`
- **产出**：`app/app.tsx` 的 `components` 表七键必填；`pages/deck-page.tsx`（装配 `RunDeckContainer`）、`pages/tasks-page.tsx`（装配 `features/run-deck/mobile-batch-list.tsx` 与 `batch-summary-bar`，数据来自 `GET /api/v1/snapshot` 的 `batches` / `tasks`）、`pages/run-detail-page.tsx`（装配 `RunDetailContainer`）、`pages/settings-devices-page.tsx`、`pages/pair-page.tsx`（装配 `pairing-container.tsx`）、`pages/settings-agents-page.tsx` 接入；`routes.tsx` 的 `components` prop 改为 `Record` 型必填、删除缺键时渲染 `data-testid=route-page-…` 空块的分支；`packages/web/test/route-entries.test.tsx`

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) `app.tsx` 七键齐全：`deck` / `tasks` / `runDetail` / `landing` / `settingsAgents` / `settingsDevices` / `pair` 各指向真实页面装配件
`routes.tsx` 缺键即 `tsc` 报错，源码里不再存在按 `match.id` 渲染空 `div` 的分支（[[E-222]]）
2) `route-entries.test.tsx` 逐条 `navigateTo()` 七条 hash，断言渲染出对应容器的 `data-component` 属性
`#/pair` 在无令牌时渲染配对容器，能输入手填地址与配对码并触发 `POST /pair/claim`（假 http 客户端断言请求）（[[E-06]]、[[E-224]]）
3) `#/tasks` 页在 M9-T19 落地前由 `mobile-batch-list` 按 `GET /snapshot` 数据渲染批次与任务行，缺字段显示「—」不自算
M9-T19 落地后由其把 `batch-tree` 换进同一页面（07 节 features 规则）
4) 懒加载切点只有 `settings/*` 与 `landing`（`React.lazy` + 共享 Suspense），`deck` / `tasks` / `runDetail` 静态 import
构建后主 chunk 含 `run-deck` 与 `run-detail` 代码（对 `dist/assets` 文件名与内容断言）（07 节）
5) 未知 hash 仍渲染「未知路径」页并保留顶栏
`#/run/` 加不存在的 id 由页面自身渲染「该运行不存在」而不是路由层 404（[[E-222]]、[[E-223]]）

## 必须处理的边界

- [[E-06]]　同网段但连不上
    - 触发：手机与 PC 不同网段，或 WiFi 开了 AP 隔离
    - 期望：配对失败报到具体环节（「扫到码但连不上 host:port」），并提供手填地址兜底
- [[E-222]]　命中未登记的 hash
    - 触发：`#/xxx` 或 `#/run/` 缺参数
    - 期望：渲染「未知路径」页并给「回到运行甲板」，保留顶栏；**绝不白屏、绝不抛异常**
- [[E-223]]　路径参数指向不存在的对象
    - 触发：运行已清理、任务已从文档移除
    - 期望：由**页面自身**渲染「该运行不存在或已被清理」，不由路由层判 404，壳与顶栏保持在位
- [[E-224]]　壳内首次加载 hash 为空
    - 触发：`tauri://localhost`、`https://localhost`、daemon 的 `/` 三处加载
    - 期望：启动即归一化为 `#/`，**不得出现无 hash 的中间态白屏**

## 代码位置

<!-- code:begin -->
_（**落地前必须回填**。一行一处，格式：`路径:行号` — 说明）_
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
_（**落地前必须回填**：怎么实现的、为什么这么选、踩了什么坑）_
<!-- notes:end -->

---

栈分支 `task/M9-T25` @ `agent-scheduler`　落地进 `main`
