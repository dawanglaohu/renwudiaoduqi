---
type: task
id: R8-T54786768
status: todo
module: M6
source_batch: 8
stable_key: e133-elevate-once-entry
---

# R8-T54786768　接通 E-133「仅本次运行临时提升」入口：POST /runs/:id/messages 接受 kind=elevate_once 并在运行详情「权限受阻」行提供一次性按钮

> 由第 8 批收口记录 `batch-8-20260922` 的未通过问题生成；stable-key 去重，原任务与原收口记录不改写。

- 所属模块：[[M6]]
- 来源任务：[[M6-T7]]
- 前置依赖：[[M6-T6]]、[[M6-T7]]、[[M9-T8]]
- 严重级：S2
- Jev 裁决：adjudicate pick=A(medium 0.73) spec_change=yes(0.71) outside_stack=no(0.08) needs_owner=no(0.14) reversible=unsure(0.63) adopt=take(A) model=jev-1.13.0 label=batch-8:wrapup:e133

## 输入 / 产出

- **输入**：复现：main 7333b35 上 `grep -rn elevate_once packages` 只命中 repo 类型、service/message.ts:316 的 validKinds 与 run.ts:1036 的 elevateRunOnce；shared `CreateRunMessageBody`（packages/shared/src/api/runs.ts:136-160）与真实路由 packages/daemon/src/http/routes/runs.ts 的 schema 只允许 reply／approve／deny 且 text 必填，MessageService 对任何 kind 都不调 elevateRunOnce，packages/web/src 无「临时提升」控件；09 节 run_messages.kind 含 elevate_once 而 10 节 `POST /runs/:id/messages` 未列。Jev adjudicate 取 A：复用 messages 端点，09 节不动、10 节补契约。相关：M6-T7 验收 3、E-133、run.permission_blocked 事件（packages/shared/src/api/events.ts:36）、M9-T8 的日志窗口（packages/web/src/features/run-detail/）。
- **产出**：shared 类型与 JSON schema 新增 kind `elevate_once`（仅该 kind 下 text 可空）、daemon 路由与 MessageService 把 elevate_once 分派到 RunService.elevateRunOnce()、10 节接口表补该 kind 的语义与错误码、运行详情里 run.permission_blocked 行高亮并带一次性「仅本次运行临时提升」按钮、对应单元／集成／前端测试；文档改动按 doc-maintenance 的 begin／sync／verify 走补丁。
- **有效路径**：`packages/shared/src/api/runs.ts`、`packages/daemon/src/http/routes/runs.ts`、`packages/daemon/src/service/message.ts`、`packages/daemon/src/service/run.ts`、`packages/daemon/src/repo/run-messages-repo.ts`、`packages/daemon/test/unit/message-service.test.ts`、`packages/daemon/test/unit/run-service.test.ts`、`packages/daemon/test/integration/run-messages-elevate.test.ts`、`packages/web/src/features/run-detail/run-detail-container.tsx`、`packages/web/src/features/run-detail/use-log-window.ts`、`packages/web/src/api/http-client.ts`、`packages/web/test/log-window.test.ts`、`docs/Agent任务调度器-开发文档/02-设计/10-接口约定.md`

## 验收标准

1) `POST /api/v1/runs/:runId/messages` body `{kind:'elevate_once'}`（text 缺省或空串）在运行处于 awaiting_reply 或 running 时返回 200 且 `RunService.isTemporarilyElevated(runId)` 为 true、运行迁 running 并留事件；text 对 reply／approve／deny 仍必填，schema `additionalProperties:false` 不变（真实 Fastify 集成测试 packages/daemon/test/integration/run-messages-elevate.test.ts 覆盖 200、非法状态 409 E_INVALID_STATE_TRANSITION、已归档 409 E_SESSION_ARCHIVED） 2) 提升只作用于本次运行：不写 agents.json、不改注册表默认档、运行结束后 isTemporarilyElevated 为 false，且 packages/daemon/test/unit/run-service.test.ts 现有 E-133 用例继续通过（E-133） 3) 10 节 `POST /runs/:id/messages` 行列出 `kind` 四值与 elevate_once 的 text 可空规则，09 节不改；`scripts/check-error-codes.mjs` 与 `packages/daemon/test/arch/routes.test.ts` 通过；补丁经 `maintain_docs.py begin/sync/verify` 登记并在同一 PR 提交 4) 运行详情的 run.permission_blocked 行以 needs 色高亮并显示「仅本次运行临时提升」按钮，点击恰发一次上述请求，成功后按钮变为「已临时提升」且不可再点，失败按 error.code 原样展示；packages/web/test/log-window.test.ts 用假 http-client 断言请求体与状态 5) `pnpm -w check` 通过；PR 附从真实 daemon 录的 GIF：权限受阻行出现 → 点按钮 → 运行继续（record-browser-gif）

## 必须处理的边界

[[E-133]]

## 代码位置

<!-- code:begin -->
`packages/daemon/src/http/plugins/90-error-handler.ts:44` — 统一错误处理限定映射：针对带有 details.from/state/targetState 的 E_INVALID_STATE_TRANSITION 映射为 409（R1，AC 1）
`packages/daemon/src/service/message.ts:307` — validateInput 允许 kind='elevate_once' 下 text 为空或缺省（AC 1）
`packages/daemon/src/service/message.ts:373` — performDelivery 处理 elevate_once 分派：缺 elevateRunOnce 时标未送达并抛错（R2），成功后落库 delivered 记录并流转状态（AC 1）
`packages/daemon/src/service/run.ts:1310` — elevateRunOnce 在 temporarilyElevatedRuns 内存 Set 中登记 runId，并在状态迁移抛错时安全回滚（R2，AC 1，AC 2，E-133）
`packages/daemon/src/service/run.ts:1335` — isTemporarilyElevated 提供运行级单次提升态查询，运行结束自动清理（AC 1，AC 2，E-133）
`packages/daemon/src/http/routes/runs.ts:145` — POST /api/v1/runs/:runId/messages 提取 elevateRunOnce 闭包注入 messageService（AC 1）
`packages/daemon/src/http/routes/runs.ts:200` — createRunMessageBodySchema 校验 elevate_once 且限定 additionalProperties:false（AC 1）
`packages/shared/src/api/runs.ts:136` — CreateRunMessageBody 判别联合与 RunMessageKindSchema 新增 elevate_once（AC 1）
`packages/web/src/features/run-detail/run-detail-container.tsx:69` — PermissionBlockedTimelineRow 时间线事件行纯展示组件，采用 needs 暖色高亮与一次性按钮（R3，AC 4，E-133）
`packages/web/src/features/run-detail/run-detail-container.tsx:135` — 容器层以 useRef 同步锁严格防并发连点，在 runId 切换时重置提升态与错误，提升成功后禁用（R3，AC 4）
`packages/web/src/features/run-detail/use-log-window.ts:109` — useLogWindow 初始化与 SSE 订阅监听 run.permission_blocked 事件，供给时间线行（AC 4，E-133）
`packages/daemon/test/integration/run-messages-elevate.test.ts:1` — 真实 Fastify 集成测试覆盖 200、409 非法状态迁移、409 已归档会话以及失败回滚负例（R1，R2，AC 1）
`packages/daemon/test/unit/message-service.test.ts:1063` — MessageService 单元测试覆盖缺 elevateRunOnce 时的未送达与抛错断言，以及抛错时不入库的负例（R2）
`packages/web/test/log-window.test.ts:835` — 前端测试通过真实 DOM 点击断言请求体、成功后禁用、连点防重、失败回显以及切换 runId 状态隔离（R3，AC 4）
`docs/Agent任务调度器-开发文档/02-设计/10-接口约定.md:394` — 接口约定补齐 POST /runs/:id/messages 四值与 elevate_once text 可空规则（AC 3）
`docs/Agent任务调度器-开发文档/02-设计/08-后端架构.md:174` — 统一错误处理处架构说明补齐 E_INVALID_STATE_TRANSITION 409 限定映射规则（R1，AC 1）
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
1. **统一错误处理限定映射（R1 / Jev B 裁决）**：
   - 审查要求非法状态迁移返回 409 `E_INVALID_STATE_TRANSITION`。根据 Jev B 方案，我们在 `packages/daemon/src/http/plugins/90-error-handler.ts` 集中收口，检测到 `details` 中带有 `from`、`state`、`targetState` 等状态机上下文时，映射 HTTP 状态码为 409，而非直接修改基础 `codes.ts` 的全局映射；
   - 同步定点更新 `02-设计/08-后端架构.md` 第 174 行说明，并通过文档维护补丁 `r8-t54786768-err-handler-support-20260925` 完成契约同步与复核；集成测试全面改为断言 409。
2. **防假投递与内存隔离回滚（R2）**：
   - `MessageService.performDelivery` 在 `elevate_once` 分支下，若未注入 `elevateRunOnce` 函数，立即调用 `handleUndelivered` 标为 `elevate_unavailable` 并抛错，绝不返回 `delivered: true`；
   - 只有在 `await elevateFn()` 成功后才在 `run_messages` 中写入 delivered 记录；
   - `RunService.elevateRunOnce` 增加异常捕获，在状态迁移失败时即时回滚 `temporarilyElevatedRuns.delete(runId)`，杜绝失败请求残留提升状态。
3. **时间线事件行与真实 DOM 测试（R3）**：
   - 移除供测试使用的产品 props，组件展示完全依赖 `useLogWindow` 提供的 `permissionBlocked` 状态，纯展示组件重构为 `PermissionBlockedTimelineRow`；
   - 容器层采用 `isElevatingRef` 与 `isElevatedRef` 同步锁机制彻底杜绝用户快速双击并发重复发请求；
   - `useEffect` 针对 `runId` 切换进行提升状态与错误信息的清理与隔离；
   - 前端测试在真实 DOM 下模拟用户点击，完整验证单次请求、成功禁用、连点防重与错误码原始回显。
4. **真实流程动图资产与样式生效（R4）**：
   - 搭建基于真实 Fastify 后端与生产前端产物的录制流水线，捕获「权限受阻行出现 → 用户点击提升 → 按钮变为已临时提升且禁用 → 运行继续流转」全流程帧序列；
   - 生成 1200x750 规格的优化 GIF（71,652 字节，SHA-256 `E2DB5ED2522F49C414DA1C34651DDD92CFE4F3A08DC0FC261E4B63820801A6B2`），上传并推送至 `agent-scheduler-assets` 与 `assets` 分支，确认 `--needs`、`--needs-soft`、`--on-needs` 等样式 tokens 均完整生效。
<!-- notes:end -->
