---
type: task
id: M3-T7
module: M3
depends_on: [M3-T5]
unlocks: [M7-T8]
edges: [E-19, E-316, E-50]
est_days: 0.5
branch: task/M3-T7
status: done
---

# M3-T7　查 bug 提示词取用与快照

- 所属模块：[[M3]]　**文档接入**——把 `docs-data.js` 变成本产品唯一的任务真相源（任务、依赖、批次、并行窗口、验收标准、边界编号、实施与审查两段提示词），并负责内容指纹、派发时快照与文档重出后的三个标记；收口提示词 `dispatchBatches` 的按任务集合匹配也归它。凡「这条数据来自文档」的都归它
- 前置依赖：[[M3-T5]]
- 完成后解锁：[[M7-T8]]

## 输入 / 产出

- **输入**：M3-T5 的快照取用接口；04 节的 `dispatch[id].bug` 输入协议（unattended-run 1.3.1，未落地措辞）
- **产出**：`service/bughunt-context.ts` 的 `getBughuntContext(taskId)`、`domain/bughunt-builtin-prompt.ts`（内置通用查 bug 提示词常量）、`service/docs.ts` 导入 `dispatch[id].bug` 落 `tasks.bug_prompt`、派发快照落 `dispatch_snapshots.bug_prompt`、新增一个迁移（两列 `bug_prompt TEXT NULL`）

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) 导入文档时把 `dispatch[id].bug` 逐字存 `tasks.bug_prompt`，缺失或不是字符串存 NULL，**不冻结派发、不报 `E_DOC_SOURCE_UNREADABLE`**
派发时随三段快照一起复制进 `dispatch_snapshots.bug_prompt`（[[E-19]]、[[E-316]]）
2) `getBughuntContext(taskId)` 返回**派发快照那一份** `bugPrompt` 与 `promptSource='docs'`
快照为 NULL 时返回内置通用查 bug 提示词并 `promptSource='builtin'`，不抛错、不回落到当前文档、不让查 bug 阶段停摆（[[E-316]]）
3) 返回的材料逐字不改写、不截断
文档指纹在途中变化时仍取快照那一份，不热改（[[E-50]]）
4) `bug_prompt` 不进任何事件 payload、日志只打长度与前 200 字节（16 节）
迁移为普通 `ALTER TABLE … ADD COLUMN`，旧库直开不回填

## 必须处理的边界

- [[E-19]]　验收标准被事后改动
    - 触发：同一 id 的 input/output/accept 文本被改过
    - 期望：派发时把这三段快照进任务记录；列表标「文档已更新，与派发时不一致」
- [[E-316]]　查 bug 提示词文档未提供
    - 触发：派发快照里的 `dispatch[id].bug` 缺失或不是字符串
    - 期望：用内置通用查 bug 提示词并标「文档未提供」（`prompt_source='builtin'`，记 warn）；取派发快照那一刻的文档，不按当前文档；材料逐字引用不改写，外层包装说明覆盖其中的提交／推送／再点审查等步骤
- [[E-50]]　批次执行途中文档变了
    - 触发：执行期间任务 ID/contractHash 指纹变化，包括任务行未变而范围、条款或提示词变化
    - 期望：已派任务继续用快照跑完，后续派发自动暂停并要人确认新文档，绝不热改在途任务

## 代码位置

<!-- code:begin -->
`packages/daemon/migrations/0002_add_bug_prompt.sql:1` — tasks 与 dispatch_snapshots 新增 bug_prompt 列（E-19、E-316）
`packages/daemon/src/domain/bughunt-builtin-prompt.ts:12` — BUILTIN_BUGHUNT_PROMPT 内置通用查 bug 提示词常量与 BughuntPromptSource（E-316）
`packages/daemon/src/service/docs.ts:551` — 文档导入时解析 dispatch[id].bug，缺失或非字符串存 NULL 且不报 E_DOC_SOURCE_UNREADABLE（E-316）
`packages/daemon/src/repo/tasks.ts:152` — INSERT 与 UPDATE tasks 表持久化 bug_prompt 字段（E-316）
`packages/daemon/src/repo/dispatch-snapshots.ts:658` — takeSnapshotForTask 将 tasks.bug_prompt 随三段快照复制入 dispatch_snapshots（E-19、E-316）
`packages/daemon/src/service/bughunt-context.ts:115` — getBughuntContext 严格从派发快照取 bug_prompt，NULL 回退内置提示词且不回落当前文档（E-316、E-50）
`packages/daemon/src/service/bughunt-context.ts:162` — 快照无文档提示词时记 warn（只带任务 ID 与原因，不带正文）（E-316）
`packages/daemon/test/unit/bughunt-context.test.ts:100` — 查 bug 上下文快照取用、内置通用回退、逐字不热改单元测试（E-19、E-316、E-50）
`packages/daemon/test/unit/docs-import.test.ts:1389` — 文档导入与派发快照 bug_prompt 存取集成测试（AC 1、E-19、E-316）
`packages/daemon/test/unit/database.test.ts:152` — 迁移执行器断言 0002_add_bug_prompt.sql 已应用（AC 4、E-19）
`packages/daemon/test/arch/routes.test.ts:83` — 路由架构测试容器按目录应用全部迁移，避免新增迁移漏跑（AC 4、E-19）
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
1. 快照与当前文档严格隔离（E-316、E-50）：查 bug 运行必须严格读取派发时生成的快照，即便当前文档在批次执行期间被重生成且 tasks.bug_prompt 发生变更，也绝不热改已派发任务的查 bug 提示词；快照中的 bug_prompt 为 NULL 时，必须回退到 BUILTIN_BUGHUNT_PROMPT 并标记 promptSource='builtin'，明令不得回落到 tasks.bug_prompt，避免破坏执行期快照的确定性。
2. 导入宽容与可用性保证（AC 1、E-316）：dispatch[id].bug 属于可选扩展属性，缺失或非字符串时不抛 E_DOC_SOURCE_UNREADABLE 也绝不冻结派发（is_contract_ready 仍由 readiness 控制），存为 NULL；确保未导出 bug 键的早期或外部文档源仍能正常派发和执行通用查 bug。
3. 日志与事件隔离（16 节）：提示词全文属于大文本载荷，禁止进入事件 payload，也不进日志——正文只经 service 接口返回给运行时环境组装。文档未提供 bug 提示词、落到内置通用版时按 E-316 记一条 warn，只带任务 ID 与回落原因、不带正文（超长文本一旦写日志就同时踩泄露与截断两个坑）。
<!-- notes:end -->

---

栈分支 `task/M3-T7` @ `agent-scheduler`　落地进 `main`
