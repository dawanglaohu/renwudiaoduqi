---
type: task
id: M4-T7
module: M4
depends_on: [M4-T1]
unlocks: [M7-T2, M9-T17]
edges: [E-135, E-136, E-137, E-254, E-256]
est_days: 1.5
branch: task/M4-T7
status: todo
---

# M4-T7　三档权限与三档思考强度的抽象与厂商参数映射

- 所属模块：[[M4]]　**Agent 适配**——把形态各异的 agent 抽象成同一个可派发对象：注册表（含每个 agent 通用的版本指纹探测字段与具名的「适配器种类」枚举）、可执行探测、模型清单读取与降级、启动参数与三档权限映射、以各家原生通道为主干把输出归一化成 ACP `session/update` 词汇的事件、可回话与**是否提供流式事件**等能力位，并自实现一个通用 ACP 适配器作为兜底与第 6 家及以后的入口。凡「某一家 agent 跟别家怎么不一样」的都归它
- 前置依赖：[[M4-T1]]
- 完成后解锁：[[M7-T2]]、[[M9-T17]]

## 输入 / 产出

- **输入**：04 节实测的各家权限参数；各家思考强度参数
- **产出**：`domain/permission-tier.ts`、`domain/effort-tier.ts` 两张映射表

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) 注册表只存本产品三档抽象（只读／工作区可写／无限制），映射到 `--sandbox` / `--permission-mode` 的表在代码层，**用户不直填厂商参数**（[[E-137]]）
2) 默认中档
某 agent 被手动调到最高档时该卡片与每次派发确认页**持续**显示醒目标记（非一次性 toast），且档位不随切到「自动模式」而隐式变化（[[E-136]]）
3) 审查阶段派出去的 agent 固定只读档（[[E-135]]）
4) **思考强度同样只存三档抽象（低／中／高）**，到各家参数的映射（codex `model_reasoning_effort`、claude 思考预算、其余按实测填）在代码层，用户不直填厂商原值（决策 52）
5) 五家中任一不支持该能力时映射表返回「不支持」而非某个默认值，由 M4-T6 走 [[E-254]] 的空值路径
6) 自报值与所选不一致时如实上报两者，**不静默采信任何一边**（[[E-256]]）

## 必须处理的边界

- [[E-135]]　审查 agent 的权限
    - 触发：审查阶段派出去的 agent
    - 期望：固定给只读档（read-only / plan），审查 agent 不得改代码，否则「审查通过」失去意义
- [[E-136]]　某 agent 被调到最高档
    - 触发：用户手动提权
    - 期望：该 agent 卡片与每次派发确认页持续显示醒目标记（非一次性 toast）；档位不随切换到「自动模式」而隐式变化
- [[E-137]]　各家权限参数语义不一
    - 触发：codex `--sandbox` vs claude/grok `--permission-mode`
    - 期望：注册表只存本产品的三档抽象（只读 / 工作区可写 / 无限制），映射到厂商参数的表放在代码层，不让用户直填原始参数
- [[E-254]]　agent 不支持思考强度
    - 触发：dsh 无对应参数；某些模型或某些 agent 版本也没有
    - 期望：该 agent 的思考强度置 NULL、**派发时不传任何相关参数**，UI 一律显示「—」并在 title 说明原因。**绝不补一个默认档冒充**——补了等于谎称控制生效（与 E-26「数据缺失显示—，不拿 0 冒充」同理）。不因不支持而禁用该 agent（与 E-35 对称）
- [[E-256]]　自报思考强度与所选不一致
    - 触发：agent 实际用的档位与派发时所选不同
    - 期望：与 `reported_model` 对称：**流头部持续高亮标出两者**（`高 → 实际 中`），不静默采信任何一边；该运行的收据里一并记录实际值

## 代码位置

<!-- code:begin -->
`packages/daemon/src/domain/permission-tier.ts:11` — 声明三档权限抽象常量与类型（readOnly / workspaceWrite / unrestricted），供注册表与运行记录共用（E-137）
`packages/daemon/src/domain/permission-tier.ts:37` — 权限档类型守卫与断言函数，严格校验三档抽象并拒绝厂商私有参数串（E-137）
`packages/daemon/src/domain/permission-tier.ts:75` — 权限档安全与 UI 元数据计算，最高档固定要求持续高亮与醒目标记（E-136）
`packages/daemon/src/domain/permission-tier.ts:88` — 自动化模式切换保护函数，确保档位不随自动模式切换而隐式变更（E-136）
`packages/daemon/src/domain/permission-tier.ts:99` — 运行权限档强制约束，审查阶段固定强制只读档锁定（E-135）
`packages/daemon/src/domain/permission-tier.ts:239` — 各 agent 权限参数映射解析，将三档抽象映射至厂商参数且 dsh 返回不支持（E-137）
`packages/daemon/src/domain/effort-tier.ts:11` — 声明三档思考强度抽象常量与类型（low / medium / high，决策 52）
`packages/daemon/src/domain/effort-tier.ts:20` — 思考强度类型守卫与断言函数，拒绝厂商私有参数直填
`packages/daemon/src/domain/effort-tier.ts:157` — 思考强度能力位探测，判定 agent 是否支持思考强度配置（E-254）
`packages/daemon/src/domain/effort-tier.ts:166` — 思考强度厂商参数映射表，不支持时返回 unsupported 拒绝补默认档冒充（E-254）
`packages/daemon/src/domain/effort-tier.ts:204` — 思考强度命令行参数提取，不支持或为 null 时返回空数组不传任何参数（E-254）
`packages/daemon/src/domain/effort-tier.ts:266` — 自报思考强度与所选档位比对判定，如实保留并上报两者不静默采信（E-256）
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
- 权限与思考强度抽象收敛在 `src/domain/` 零依赖叶子层，对外暴露纯三档枚举；厂商参数（如 `--sandbox`、`--permission-mode`、`model_reasoning_effort`、`--effort`、`--thinking`）由映射表集中翻译，隔离厂商改版对数据层与 UI 的侵入（E-137、决策 52）。
- DeepSeek Harness (`dsh`) 与通用 ACP 不支持思考强度时，映射函数统一返回 `{ supported: false }`，命令行参数返回空数组，不回落至任何虚构的默认档位；避免因补默认档而向用户谎称控制生效（E-254）。
- 审查阶段派发的 agent 必须由 `enforceRunPermissionTier` 强制收敛为 `readOnly` 档，无条件忽略传入的提权覆盖，防止审查代理改动工作区代码使审查结论失真（E-135）。
- 自报思考强度比对逻辑保留原始自报字符串与用户选定档位双份事实，比对不一致仅作高亮标记与收据记录，不隐式覆盖或单向采信任何一方（E-256）。
<!-- notes:end -->

---

栈分支 `task/M4-T7` @ `agent-scheduler`　落地进 `main`
