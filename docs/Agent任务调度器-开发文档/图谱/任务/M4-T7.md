---
type: task
id: M4-T7
module: M4
depends_on: [M4-T1]
unlocks: [M7-T2, M9-T17]
edges: [E-135, E-136, E-137, E-254, E-256]
est_days: 1.5
branch: task/M4-T7
status: todo
---

# M4-T7　三档权限与三档思考强度的抽象与厂商参数映射

- 所属模块：[[M4]]　**Agent 适配**——把形态各异的 agent 抽象成同一个可派发对象：注册表（含每个 agent 通用的版本指纹探测字段与具名的「适配器种类」枚举）、可执行探测、模型清单读取与降级、启动参数与三档权限映射、以各家原生通道为主干把输出归一化成 ACP `session/update` 词汇的事件、可回话与**是否提供流式事件**等能力位，并自实现一个通用 ACP 适配器作为兜底与第 6 家及以后的入口。凡「某一家 agent 跟别家怎么不一样」的都归它
- 前置依赖：[[M4-T1]]
- 完成后解锁：[[M7-T2]]、[[M9-T17]]

## 输入 / 产出

- **输入**：04 节实测的各家权限参数；各家思考强度参数
- **产出**：`domain/permission-tier.ts`、`domain/effort-tier.ts` 两张映射表

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) 注册表只存本产品三档抽象（只读／工作区可写／无限制），映射到 `--sandbox` / `--permission-mode` 的表在代码层，**用户不直填厂商参数**（[[E-137]]）
2) 默认中档
某 agent 被手动调到最高档时该卡片与每次派发确认页**持续**显示醒目标记（非一次性 toast），且档位不随切到「自动模式」而隐式变化（[[E-136]]）
3) 审查阶段派出去的 agent 固定只读档（[[E-135]]）
4) **思考强度同样只存三档抽象（低／中／高）**，到各家参数的映射（codex `model_reasoning_effort`、claude 思考预算、其余按实测填）在代码层，用户不直填厂商原值（决策 52）
5) 五家中任一不支持该能力时映射表返回「不支持」而非某个默认值，由 M4-T6 走 [[E-254]] 的空值路径
6) 自报值与所选不一致时如实上报两者，**不静默采信任何一边**（[[E-256]]）

## 必须处理的边界

- [[E-135]]　审查 agent 的权限
    - 触发：审查阶段派出去的 agent
    - 期望：固定给只读档（read-only / plan），审查 agent 不得改代码，否则「审查通过」失去意义
- [[E-136]]　某 agent 被调到最高档
    - 触发：用户手动提权
    - 期望：该 agent 卡片与每次派发确认页持续显示醒目标记（非一次性 toast）；档位不随切换到「自动模式」而隐式变化
- [[E-137]]　各家权限参数语义不一
    - 触发：codex `--sandbox` vs claude/grok `--permission-mode`
    - 期望：注册表只存本产品的三档抽象（只读 / 工作区可写 / 无限制），映射到厂商参数的表放在代码层，不让用户直填原始参数
- [[E-254]]　agent 不支持思考强度
    - 触发：dsh 无对应参数；某些模型或某些 agent 版本也没有
    - 期望：该 agent 的思考强度置 NULL、**派发时不传任何相关参数**，UI 一律显示「—」并在 title 说明原因。**绝不补一个默认档冒充**——补了等于谎称控制生效（与 E-26「数据缺失显示—，不拿 0 冒充」同理）。不因不支持而禁用该 agent（与 E-35 对称）
- [[E-256]]　自报思考强度与所选不一致
    - 触发：agent 实际用的档位与派发时所选不同
    - 期望：与 `reported_model` 对称：**流头部持续高亮标出两者**（`高 → 实际 中`），不静默采信任何一边；该运行的收据里一并记录实际值

## 代码位置

<!-- code:begin -->
`packages/daemon/src/domain/permission-tier.ts:21` — 声明三档权限抽象常量与类型（readOnly / workspaceWrite / unrestricted），供注册表与运行记录共用（E-137）
`packages/daemon/src/domain/permission-tier.ts:46` — 权限档类型断言函数，严格校验三档抽象并以 AppError(E_VALIDATION) 拒绝厂商私有参数串（E-137, R4）
`packages/daemon/src/domain/permission-tier.ts:78` — 权限档安全与告警元数据计算，最高档固定要求持续高亮与醒目标记，移除 CSS token 泄露（E-136, R4）
`packages/daemon/src/domain/permission-tier.ts:91` — 自动化模式切换保护函数，确保档位不随自动模式切换而隐式变更（E-136）
`packages/daemon/src/domain/permission-tier.ts:102` — 运行权限档强制约束，审查阶段固定强制只读档锁定（E-135）
`packages/daemon/src/domain/permission-tier.ts:239` — 各 agent 权限参数映射解析，修复 Pi 中高同权并拒绝 generic-acp 臆造参数与原型键崩溃（E-137, R2, R3）
`packages/daemon/src/domain/permission-tier.ts:283` — 获取权限参数数组，不支持时抛 E_CAPABILITY_UNSUPPORTED 阻断静默启动（R2）
`packages/daemon/src/domain/effort-tier.ts:12` — 声明三档思考强度抽象常量与类型（low / medium / high，决策 52）
`packages/daemon/src/domain/effort-tier.ts:29` — 思考强度类型断言函数，以 AppError(E_VALIDATION) 拒绝厂商私有参数直填（R4）
`packages/daemon/src/domain/effort-tier.ts:196` — 思考强度能力感知，结合 agent 与模型（如 Claude haiku / Codex gpt-4o）判定支持态（E-254, R3）
`packages/daemon/src/domain/effort-tier.ts:217` — 思考强度厂商参数映射表，Claude 采用思考预算 --settings，不支持时明确返回 unsupported（AC 4, E-254, R2）
`packages/daemon/src/domain/effort-tier.ts:266` — 思考强度命令行参数提取，null 返回空数组，有值但不支持时抛 E_CAPABILITY_UNSUPPORTED（E-254, R2）
`packages/daemon/src/domain/effort-tier.ts:283` — 自报思考强度反向映射，归一化 token 数量与档位别名（R3）
`packages/daemon/src/domain/effort-tier.ts:329` — 自报思考强度与所选档位精确比对判定，如实保留原始自报串并精准判 mismatch（E-256, R3）
`packages/daemon/src/config/defaults.ts:33` — AgentConfig 及内置默认 agent 声明完整接入 permissionTier 默认中档 workspaceWrite（R1）
`packages/daemon/src/config/registry.ts:191` — agents.json 注册表 schema、校验解析、合并与字段访问接入 permissionTier 并持久化（E-136, E-137, R1）
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
- 权限与思考强度抽象收敛在 `src/domain/` 零依赖叶子层，对外暴露纯三档枚举，通过 `AppError('E_VALIDATION')` 替代裸 `TypeError`，并完全剥离 daemon 内前端 CSS token（R4）。
- agents.json 注册表全流程接入 `permissionTier`（默认中档 `workspaceWrite`、支持最高档 `unrestricted` 持久化且校验层严拒厂商原始参数如 `--sandbox` 或 `acceptEdits`）（R1）。
- 厂商参数映射严格贴合真实通道能力：Claude 采用思考预算（`thinking_budget` + `--settings {"maxThinkingTokens":...}`）；Pi 在 `workspaceWrite` 下严格仅开放读写编辑文件工具、剔除 `bash`/`powershell` 防止越界写，与 `unrestricted` 严格区分；generic ACP 明确不臆造 CLI 启动参数；对于不支持的权限档或思考强度配置，参数提取函数显式抛出 `E_CAPABILITY_UNSUPPORTED` 阻断启动，绝不静默放行（R2）。
- 思考强度映射与自报反向解析深度感知模型能力（如 Claude haiku 系列与 Codex 非 reasoning 模型不误报支持），所有字典查找均采用 `Object.hasOwn` 防御原型链属性（如 `constructor`）崩溃，并在自报值比对时原样保留原始文本且精准识别 mismatch（R3）。
<!-- notes:end -->

---

栈分支 `task/M4-T7` @ `agent-scheduler`　落地进 `main`
