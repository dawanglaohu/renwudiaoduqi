---
type: task
id: M4-T6
module: M4
depends_on: [M4-T5]
unlocks: [M9-T17, M9-T18]
edges: [E-254, E-255, E-30, E-33, E-34, E-35, E-41, E-42, E-45]
est_days: 2
branch: task/M4-T6
status: todo
---

# M4-T6　模型与思考强度的粒度与透传

- 所属模块：[[M4]]　**Agent 适配**——把形态各异的 agent 抽象成同一个可派发对象：注册表（含每个 agent 通用的版本指纹探测字段与具名的「适配器种类」枚举）、可执行探测、模型清单读取与降级、启动参数与三档权限映射、以各家原生通道为主干把输出归一化成 ACP `session/update` 词汇的事件、可回话与**是否提供流式事件**等能力位，并自实现一个通用 ACP 适配器作为兜底与第 6 家及以后的入口。凡「某一家 agent 跟别家怎么不一样」的都归它
- 前置依赖：[[M4-T5]]
- 完成后解锁：[[M9-T17]]、[[M9-T18]]

## 输入 / 产出

- **输入**：M4-T5 的清单
- **产出**：默认模型／思考强度与单任务覆盖的取值链

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) 每 agent 一个默认 + 单任务可临时覆盖
覆盖只作用于这一次派发及其重试，**绝不回写任何 agent 的配置文件**
2) 清单为空时允许留空表示「跟随 agent 配置」，派发不传 `--model`，不禁用该 agent（[[E-35]]、[[E-41]]）
3) 模型名含空格、引号或 shell 元字符时以数组形式传给子进程（[[E-42]] 的对偶面）
4) 别名与完整 ID 并存时都原样透传，**不做别名映射、不做去重合并、不做跨家等价映射**（[[E-45]]、[[E-30]]）
5) 设过覆盖的任务重试时沿用上次实际使用的模型，只有显式清除才回落默认（[[E-33]]）
6) 切换执行 agent 时清空模型覆盖并回落新 agent 默认，下拉只列当前 agent 的清单（[[E-34]]）
7) **思考强度走与模型完全同构的取值链**（每 agent 默认 + 单任务覆盖 + 重试沿用 + 切 agent 清空），落 `runs.effort_tier`（决策 52）
8) 不支持思考强度的 agent 置 NULL、派发不传任何相关参数、UI 显示「—」，**不补默认档冒充**，也不因此禁用该 agent（[[E-254]]）
9) 档位超出该模型取值域时派发前校验并就地说清原因与改法，**不静默降档也不静默丢弃**
校验不了时按「不支持」处理（[[E-255]]）

## 必须处理的边界

- [[E-254]]　agent 不支持思考强度
    - 触发：dsh 无对应参数；某些模型或某些 agent 版本也没有
    - 期望：该 agent 的思考强度置 NULL、**派发时不传任何相关参数**，UI 一律显示「—」并在 title 说明原因。**绝不补一个默认档冒充**——补了等于谎称控制生效（与 E-26「数据缺失显示—，不拿 0 冒充」同理）。不因不支持而禁用该 agent（与 E-35 对称）
- [[E-255]]　思考强度档位无效或超出该模型取值域
    - 触发：用户选了高档但该模型只接受 low/medium；或厂商改了取值域
    - 期望：派发前校验，不通过则**就地说清原因与改法**（「`gpt-5.6-mini` 不接受思考强度 `高`，可选 低/中」），不静默降档也不静默丢弃；校验不了时按「不支持」处理走 E-254
- [[E-30]]　模型名无权限或已改名
    - 触发：从配置文件读到的模型实际不可用
    - 期望：派发前做一次轻量校验并明确指向「模型配置问题」；绝不做跨家模型等价映射，每个 agent 只用它自己配置里读到的名字
- [[E-33]]　设了覆盖模型的任务重试
    - 触发：任务上设过模型覆盖，随后重派
    - 期望：沿用该任务上次实际使用的模型（记在任务快照里），不回落 agent 默认；只有用户显式清除覆盖才回落
- [[E-34]]　任务切换执行 agent
    - 触发：用户把某任务从 codex 改派给 grok
    - 期望：清空该任务的模型覆盖并回落新 agent 的默认；模型下拉只列当前 agent 的清单，不允许跨家残留
- [[E-35]]　从未设过默认模型
    - 触发：首次使用，agent 默认模型为空
    - 期望：不阻塞派发，派发时不传 `--model`，UI 标注「跟随 agent 配置」，而不是弹窗强制先去设置
- [[E-41]]　模型清单最终为空
    - 触发：配置无值、命令不可用、用户未手填
    - 期望：允许留空表示「跟随 agent 自身配置」，派发时不传 `--model`，而不是禁止使用该 agent
- [[E-42]]　模型名含 shell 元字符
    - 触发：模型名带空格、引号或 `;` `&` 之类
    - 期望：调用方传原始参数数组，业务层不拼命令串；原生程序直接传 argv，Windows 包装脚本仅由适配器按 08 节组装完整 ComSpec 命令，最终参数值与个数必须不变且不得执行额外命令
- [[E-45]]　别名与完整模型 ID 并存
    - 触发：claude 的 `opus`/`sonnet` 别名与完整 ID 同时出现在清单
    - 期望：两者都原样透传，不做别名映射、不做去重合并

## 代码位置

<!-- code:begin -->
`packages/daemon/src/domain/model-selection.ts:169` — `resolveModel`: 默认模型与单任务临时覆盖取值链，支持重试沿用与显式清除回落（E-33、E-35、E-41）
`packages/daemon/src/domain/model-selection.ts:249` — `resolveEffortTier`: 思考强度同构取值链，不支持思考强度的 agent 强制置 NULL 且不补默认档（E-254）
`packages/daemon/src/domain/model-selection.ts:402` — `resolveTaskModelAndEffort`: 组合模型与思考强度派发对象，生成 UI 显示与原始参数（E-35、E-254）
`packages/daemon/src/domain/model-selection.ts:426` — `switchTaskAgent`: 切换执行 agent 时重置所有模型与思考强度覆盖并清空重试快照（E-34）
`packages/daemon/src/domain/model-selection.ts:448` — `filterAvailableModelsForAgent`: 下拉列表仅列当前 agent 模型，别名与完整 ID 原样透传且不去重不映射（E-45）
`packages/daemon/src/domain/model-selection.ts:465` — `validateModelSelection`: 派发前轻量校验模型是否在当前 agent 配置内，禁止跨家等价映射（E-30）
`packages/daemon/src/domain/model-selection.ts:509` — `validateEffortTierForModel`: 校验思考强度档位是否超出该模型取值域，就地说明原因与改法；校验不了时按不支持处理，绝不静默降档或丢弃（E-255）
`packages/daemon/src/domain/model-selection.ts:600` — `validateDispatchModelAndEffort`: 派发前复合校验入口，统一返回类型化校验结果
`packages/daemon/src/domain/model-selection.ts:692` — `buildModelLaunchArgs`: 生成独立 argv 元素，模型名含空格或 shell 元字符时保持原样不经 shell 拼接（E-42）
`packages/daemon/src/domain/model-selection.ts:706` — `applyModelToArgsTemplate`: 模板参数替换；模型为空时安全剥离 `--model` / `{model}` 占位而不阻塞派发（E-35、E-41）
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
1. 取值链同构性与不可回写保证：任务级覆盖仅作为单次派发与其重试的输入，决不回写注册表与磁盘配置文件。重试时沿用上一运行实际生效的模型与思考强度，只有用户显式传入 `clearModelOverride`/`clearEffortOverride` 标记时才回落到 agent 默认。切换 agent 时立即清空所有覆盖并重置重试快照，防止旧 agent 的参数残留污染新 agent。
2. 不支持思考强度时坚决不伪造默认档（E-254）：DeepSeek Harness、通用 ACP 或未声明思考强度的模型统一置 NULL，UI 显示 `—` 并在 tooltip 中说明不支持原因，派发参数与环境变量均不发射相关字段。绝不为了「界面好看」或「字段非空」补填 `low` 或 `medium`，否则会给用户带来调度器在控制思考强度的虚假印象。
3. 超域校验拒绝静默降级（E-255）：当模型具备明确思考强度取值域但所选档位超域时（如 `gpt-5.6-mini` 选了 `high`），直接返回类型化 E_VALIDATION 失败并提供包含可选范围的具体修复说明，严禁在后台静默降档到 `medium` 或丢弃字段；当模型能力探针无法判断或校验不了时，则安全回退到 E-254 按不支持处理。
4. 别名与全称并存透传、杜绝跨家映射（E-45、E-30）：Claude 的 `opus`/`sonnet` 别名与完整 ID 并存在清单中时，不做任何标准化映射或合并去重，用户选什么就原样透传给子进程。不同 agent 间绝不做等价转换，轻量校验只比对该 agent 自己的可用模型列表。
5. 参数数组直传规避 Shell 注入（E-42）：模型参数恒以独立字符串数组元素（`[flag, model]`）提交给子进程，严禁在业务层拼装命令行字符串，确保无论模型名包含空格、引号还是分号、管道符等 shell 元字符，均在 `shell: false` 下以单元素 argv 直达进程。
<!-- notes:end -->

---

栈分支 `task/M4-T6` @ `agent-scheduler`　落地进 `main`
