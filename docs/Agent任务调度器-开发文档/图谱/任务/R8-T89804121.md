---
type: task
id: R8-T89804121
status: todo
module: M6
source_batch: 8
stable_key: e133-live-permission-transport
---

# R8-T89804121　接通 E-133 真实代理授权传输：权限受阻后仅本次运行放行并在同一运行继续

> 由第 8 批收口记录 `batch-8-20260922` 的未通过问题生成；stable-key 去重，原任务与原收口记录不改写。

- 所属模块：[[M6]]
- 来源任务：[[M6-T7]]、[[M8-T10]]
- 前置依赖：[[M6-T7]]、[[M8-T10]]
- 严重级：S2
- Jev 裁决：adjudicate pick=B(high 0.77) spec_change=unsure(0.64) outside_stack=no(0.22) needs_owner=no(0.20) reversible=yes(0.76) adopt=take(B) model=jev-1.13.0 label=R8-T54786768:round2-protocol

## 输入 / 产出

- **输入**：2026-09-25 审查 PR #150 发现：生产 Codex 派发强制 exec 模式，沙箱权限在启动参数中固定且 stdin 关闭；RunService 的临时提升标记没有任何进程或适配器消费者。录制脚本直接向 RunService 注入 run.permission_blocked 与 agent_message_chunk，不是真实代理继续。R8 的 API/UI 入口必须等待真实授权传输，不能用模拟事件或状态迁移代替。
- **产出**：为至少一种正式支持的 agent 建立可持续的双向协议会话，处理真实权限请求、请求 ID 关联、一次性人工授权答复和同一 run 的继续执行；将权限受阻事件从厂商协议归一化，按 agent/运行能力禁用不支持的提升入口。消息落库、运行状态与授权答复的失败顺序必须保证不假报 delivered，也不留下已提升标记；终态清除临时授权，绝不改 agents.json 或注册表默认档。录制最终代码头上的真实 agent、daemon、浏览器 GIF。
- **有效路径**：`packages/daemon/src/adapters/codex/build-launch-spec.ts`、`packages/daemon/src/adapters/codex/map-events.ts`、`packages/daemon/src/service/dispatch.ts`、`packages/daemon/src/service/message.ts`、`packages/daemon/src/service/run.ts`、`packages/daemon/src/boot/container.ts`、`packages/daemon/src/proc/spawn.ts`、`packages/daemon/src/http/routes/runs.ts`、`packages/daemon/test/integration/dispatch-spawn.test.ts`、`packages/daemon/test/integration/run-messages-elevate.test.ts`、`packages/daemon/test/unit/codex-adapter.test.ts`、`packages/daemon/test/unit/message-service.test.ts`、`packages/daemon/test/unit/run-service.test.ts`、`packages/web/src/features/run-detail/run-detail-container.tsx`、`packages/web/test/log-window.test.ts`、`docs/Agent任务调度器-开发文档/02-设计/08-后端架构.md`、`docs/Agent任务调度器-开发文档/02-设计/10-接口约定.md`

## 验收标准

1) 真实受限 agent 尝试写 worktree 外路径时，正式派发链产出 run.permission_blocked 并使运行等待人处理，未注入事件、未直接种库、未伪造日志 2) 用户经正式运行详情按钮和 POST /runs/:runId/messages 选择 elevate_once 后，授权答复送达同一存活 agent 会话，原 runId 继续产出由该 agent 产生的事件；不新建 run，不改变注册表或后续运行的默认权限 3) 无双向授权能力、进程已退出、答复失败或消息持久化失败时返回类型化错误且不留下 delivered 假记录或临时提升态；重启与终态后的授权均不可复用 4) 适配器、进程、组合根、路由和服务层各守 06/08 节边界，录制真实协议帧的单元与集成测试覆盖请求关联、一次性消费、并发拒绝和失败清理 5) PR 附从最终代码启动的真实 daemon 与浏览器 GIF，显示真实代理受阻、点击和同一 run 继续，录制输出不注入事件或直接写 SQLite；pnpm -w check 通过。

## 必须处理的边界

[[E-133]]

## 代码位置

<!-- code:begin -->
_落地前回填：`路径:行号` — 作用。_
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
_落地前回填实现选择、失败教训与维护约束。_
<!-- notes:end -->
