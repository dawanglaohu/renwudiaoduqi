---
type: task
id: M4-T2
module: M4
depends_on: [M4-T1]
unlocks: [M9-T14]
edges: [E-89, E-95]
est_days: 1
branch: task/M4-T2
status: todo
---

# M4-T2　参数模板校验与模板变量白名单

- 所属模块：[[M4]]　**Agent 适配**——把形态各异的 agent 抽象成同一个可派发对象：注册表（含每个 agent 通用的版本指纹探测字段与具名的「适配器种类」枚举）、可执行探测、模型清单读取与降级、启动参数与三档权限映射、以各家原生通道为主干把输出归一化成 ACP `session/update` 词汇的事件、可回话与**是否提供流式事件**等能力位，并自实现一个通用 ACP 适配器作为兜底与第 6 家及以后的入口。凡「某一家 agent 跟别家怎么不一样」的都归它
- 前置依赖：[[M4-T1]]
- 完成后解锁：[[M9-T14]]

## 输入 / 产出

- **输入**：M4-T1 的注册表
- **产出**：模板解析器与保存时校验

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) 只允许 `{model}` `{prompt_file}` `{cwd}` `{session_dir}` 四个模板变量
含未知模板变量或括号未闭合时**保存即拒绝**并高亮出错位置（[[E-89]]）
2) 注册表只发布模板校验通过的快照，运行时渲染入口仍须拒绝未经校验的模板
3) 两个 agent 配成同一可执行路径时允许
会话目录相同则产生 `reason` 精确等于 `session-dir-overlap` 的英文告警但不拒绝保存，中文文案由设置页映射为「会话记录可能互相覆盖」（[[E-95]]）

## 必须处理的边界

- [[E-89]]　参数模板写错
    - 触发：含未知模板变量或括号未闭合
    - 期望：保存时即校验并拒绝，高亮出错位置；运行时永不使用未通过校验的模板
- [[E-95]]　两个 agent 配成同一路径
    - 触发：用户复制配置后没改
    - 期望：允许；会话目录若也相同，daemon 产生 `reason` 精确等于 `session-dir-overlap` 的英文告警但不拒绝保存，设置页按该原因显示「会话记录可能互相覆盖」

## 代码位置

<!-- code:begin -->
`packages/daemon/src/domain/launch-template.ts:4` — 声明只允许 `{model}` `{prompt_file}` `{cwd}` `{session_dir}` 四个模板变量白名单
`packages/daemon/src/domain/launch-template.ts:33` — 格式化出错行与 `^` 指针的高亮字符串，精确定位模板语法错误位置
`packages/daemon/src/domain/launch-template.ts:53` — 纯函数状态机逐字符校验模板参数，返回未知变量、未闭合或多余括号的详细起止位置与高亮
`packages/daemon/src/domain/launch-template.ts:188` — 校验参数模板数组并生成不可变快照，含错立即返回类型化失败结构
`packages/daemon/src/domain/launch-template.ts:217` — 纯函数执行白名单变量安全替换渲染，强制前置校验，未提供变量上下文时阻断执行
`packages/daemon/src/domain/launch-template.ts:276` — 定义 `session-dir-overlap` 英文警告文案常量
`packages/daemon/src/domain/launch-template.ts:302` — 提供区分 POSIX 大小写敏感与 Windows 不区分大小写的客观路径归一化比对
`packages/daemon/src/domain/launch-template.ts:349` — 提取参数模板显式 `--session-dir` 或可执行路径默认空间计算会话身份
`packages/daemon/src/domain/launch-template.ts:377` — 比较两 agent 可执行路径与会话身份，同路径同会话目录生成告警对象
`packages/daemon/src/domain/launch-template.ts:417` — 全量扫描注册表已解析 agent 的会话目录重叠风险
`packages/daemon/src/config/registry.ts:318` — 在注册表重载流中校验 defaults 与 overrides 模板语法，非法时保留上一版并上报详细高亮位置
`packages/daemon/src/config/registry.ts:336` — 校验合并后的生效 agent 模板，非法时 rejected 并发布 invalid-config 警告
`packages/daemon/src/config/registry.ts:354` — 校验生效 agent 的会话目录重叠，仅发布 session-dir-overlap 警告而不拒绝加载
`packages/daemon/src/config/registry.ts:641` — 在单配置解析层对 argsTemplate 执行语法校验并注入参数下标与高亮
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
- `launch-template.ts` 作为 domain 叶子模块，严格保持零副作用纯函数设计，不引入基础设施层及 daemon 自定义错误类，所有校验以结果对象形式向外传递，由调用边界决定转化为 AppError 或注册表告警。
- 模板变量限定为 `{model}`、`{prompt_file}`、`{cwd}`、`{session_dir}` 四项，采用逐字符状态机精确定位错误起止列偏移，规避全局正则替换无法准确定位未闭合括号及行内高亮的问题。渲染逻辑收敛为纯字符串展开，不耦合特定参数的业务特判。
- 跨平台路径比对显式区分平台语义：POSIX 下保持大小写敏感且不转换反斜杠，Windows 下统一正斜杠并按不区分大小写比对，不通过可执行文件后缀猜测宿主环境。
- 注册表加载与热更流程中分别覆盖 defaults、overrides 及最终生效模板的语法合法性，任一层非法均拒绝换代并保留上一版；同路径同会话目录重叠仅触发 `session-dir-overlap` 告警而不阻断加载，告警文案保持英文规范。
<!-- notes:end -->

---

栈分支 `task/M4-T2` @ `agent-scheduler`　落地进 `main`
