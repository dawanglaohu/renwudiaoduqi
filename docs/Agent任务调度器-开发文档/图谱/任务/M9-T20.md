---
type: task
id: M9-T20
module: M9
depends_on: [M9-T19, M9-T10]
unlocks: [M9-T21]
edges: [E-106, E-113, E-117, E-157, E-236, E-278, E-286, E-297, E-74]
est_days: 1
branch: task/M9-T20
status: todo
---

# M9-T20　收口泳道、收口报告面板与批次落地清单

- 所属模块：[[M9]]　**客户端界面**——两端共用的那一套界面本身：多流监看、会话查看、审批与闸门、设置与配对、落地清单、按批次折叠的批次树与执行行呼吸灯、按窗口数呈现的任务流水线泳道与「会话已归档」历史行、顶栏两个流水线开关，以及同一套代码的窄屏降级。它只通过 M2 的契约与 daemon 通信，**界面里不含任何业务判定**（折叠默认值、「能否收口」、泳道分配、阶段推进与会话归档都是 daemon 给的字段）；指派面板的分组模型下拉、登录态徽标与刷新按钮、参照条的「来源」段同样只呈现 daemon 下发的字段，不算取值链
- 前置依赖：[[M9-T19]]、[[M9-T10]]
- 完成后解锁：[[M9-T21]]

## 输入 / 产出

- **输入**：M9-T19 的批次树与 `pulse-dot`；M9-T10 的审批卡；`GET /batches/:id/wrapups` 与 `POST /batches/:id/wrapup`
- **产出**：`components/wrapup-report.tsx`、`features/run-deck/wrapup-panel-container.tsx`、`components/stream-column.tsx` 的收口头部分支、`components/gate-card.tsx` 的条件动作与 `features/run-deck/use-gate-card.ts`、`pages/landing-page.tsx` 的批次级清单

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) `kind='wrapup'` 的运行占一条普通泳道，头部「批次收口 · 第 N 轮 · 第 M 批」，停止控件、参照条、运行轨渲染在 kind 分支之外，[[E-236]] 的逐档单测加 kind 维度断言停止控件存在（[[E-297]]、[[E-106]]）
2) 收口报告面板只展示 daemon 已解析字段（tests / findings / unassigned / landing / reportText），解析失败显示「解析失败」并直链原文，代码中不存在前端解析 `reportText` 的路径
findings 整行可点跳该任务，`isWellFormed=false` 行尾 chip「格式不全」，`promptSource='builtin'` 时头部 chip「文档未提供收口提示词」（[[E-297]]、[[E-286]] 的呈现侧）
3) 批次标题「收口」按钮只在 `canWrapup` 且非手机档渲染，请求在途禁用，接受后不改状态等 `batch.wrapup_started`
`E_BATCH_NOT_WRAPPABLE` / `E_WRAPUP_ROUND_LIMIT` 按 `details.reason` 给具名文案贴在该批标题下（[[E-157]]）
4) 审批卡第四个条件动作「投递原文到实施会话」（ghost）只在 `reviewVerdict='incomplete'` 且 `reworkText` 非空时出现，按目标运行 `capabilities.canReply` 灰掉并带 title，走 `POST /runs/:id/messages`，未送达时卡内 inline notice + 原文可复制（[[E-278]]、[[E-117]]、[[E-113]]）
5) 批次级落地清单列每轮收口分支与各修复分支各一行，附可复制命令，`inHead` 为真的行打勾（[[E-74]] 的批次侧）

## 必须处理的边界

- [[E-106]]　桌面端多流并置
    - 触发：同时 5 个以上运行流
    - 期望：多流并置（分栏或网格），每流各自带常驻停止控件；不做「一次只能看一个」的模态
- [[E-113]]　消息投递失败
    - 触发：进程已死或管道已断
    - 期望：消息落地为「未送达」并保留原文可复制，绝不静默丢弃或假装已发出
- [[E-117]]　agent 不具备注入能力
    - 触发：后续接入的 agent 没有消息注入通道
    - 期望：「可回话」做成能力位，UI 按能力位灰掉按钮，而不是点了才报错
- [[E-157]]　写操作与事件流打架
    - 触发：客户端发起「中止运行中的任务」
    - 期望：走 POST 接口，最终状态一律以回流事件为准，UI 不拿 POST 响应体直接改状态
- [[E-236]]　某一档漏掉停止键
    - 触发：档位分支影响了控件存在性
    - 期望：停止控件与审批槽位**必须渲染在所有档位分支之外**；分支只允许影响布局与截断。**配一条按档位枚举逐档断言停止控件存在的单测，把 E-106 变成机检**
- [[E-278]]　返工块解析不出 R 条目
    - 触发：VERDICT 为 rework 但围栏块与 REWORK 段都没有 `R〈n〉` 条目，或 VERDICT 行缺失
    - 期望：标「未结构化」：`review_verdict=incomplete`，原文全文存 `rework_text`，转 `awaiting_human`；审批卡给「投递原文到实施会话」按钮，按能力位灰掉；不猜语义、不裁剪
- [[E-286]]　收口自报裁定与实际不符
    - 触发：RECORD 段写 clean 但 BUGS 有未修条目、NOT_FIXED 非空或 TESTS fail
    - 期望：有效裁定由内容算出（open），自报值只存 `declared_verdict`；两者不一致记 warn，UI 同时显示两者；绝不按自报放行
- [[E-297]]　收口运行的界面落位
    - 触发：收口运行没有 task_id
    - 期望：流甲板占一条普通泳道（头部写「批次收口 · 第 N 轮 · 第 M 批」），停止控件、参照条、运行轨与实施流一致；批次树在该批标题下挂一行「批次收口 · 第 N 轮」；收口报告面板只显示 daemon 已解析字段，解析失败显示「解析失败」并直链原文；手机端不出「收口」按钮
- [[E-74]]　用户要落地
    - 触发：任务已验收，准备合并
    - 期望：提供只读落地清单（worktree 绝对路径、分支名、diff 摘要、可一键复制的 `gh stack` 命令文本），复制而不执行

## 代码位置

<!-- code:begin -->
`packages/web/src/components/stream-column.tsx:35` — `StreamColumnKind` 三值（task/wrapup/idle），kind 只允许换头部文案，不参与任何控件的存在性判定（E-297、E-236）
`packages/web/src/components/stream-column.tsx:256` — 收口泳道头部「批次收口 · 第 N 轮 · 第 M 批」，N/M 缺失显示「—」，没有 task_id 时不退化出任务标识（E-297）
`packages/web/src/components/stream-column.tsx:324` — 常驻停止控件，位于所有 kind 与档位分支之后（E-106、E-236）
`packages/web/src/components/stream-column.tsx:410` — 常驻运行轨列 `data-slot="spine"`，收口泳道与实施流共用同一条轨（E-297）
`packages/web/src/features/run-deck/run-deck-container.tsx:85` — `buildDeckLanes`：从 daemon 的泳道、运行、任务、批次和闸门映射甲板（E-297、E-317）
`packages/web/src/features/run-deck/run-deck-container.tsx:169` — 首屏取 snapshot + runs + gates，事件到达才重取，不轮询
`packages/web/src/features/run-deck/batch-expansion.ts:28` — 快照中的完成态收口运行仍填入批次树收口行（E-297）
`packages/web/src/features/run-deck/run-deck-container.tsx:177` — 从真实收口记录恢复轮次，刷新后批次树与泳道标题仍有轮次（E-297）
`packages/web/src/features/run-deck/run-deck-container.tsx:233` — 闸门裁定走 POST /gates/:id/decide，界面状态等回流（E-157）
`packages/web/src/features/run-deck/run-deck-container.tsx:248` — 批次树「收口」按钮的默认动作 `startBatchWrapup`（E-157）
`packages/web/src/features/run-deck/run-deck-view.tsx:116` — `LaneGateCard`：生产审批卡挂在该泳道审批槽，投递原文走 use-gate-card（E-278、E-117、E-113）
`packages/web/src/features/run-deck/run-deck-view.tsx:170` — 收口泳道主体装配收口报告面板（E-297）
`packages/web/src/features/run-deck/run-deck-view.tsx:180` — 审批槽只在 daemon 真的下发待处理闸门时画卡，不画假按钮
`packages/web/src/features/run-deck/wrapup-panel-container.tsx:326` — `startBatchWrapup`：只判接受、幂等键跨重试复用、不在响应体上改状态（E-157）
`packages/web/src/features/run-deck/wrapup-panel-container.tsx:373` — 订阅 `batch.wrapup_started`/`_finished` 清在途并重取（E-157）
`packages/web/src/features/run-deck/wrapup-panel-container.tsx:422` — `useBatchWrapupOverview` 返回每批在途集合及具名拒绝原因（E-157）
`packages/web/src/features/run-deck/wrapup-panel-container.tsx:460` — 收口轮次只取 daemon 事件或持久记录（E-297）
`packages/web/src/features/run-deck/wrapup-panel-container.tsx:137` — `buildBatchLandingList` 每轮收口及各修复分支各一行（E-74）
`packages/web/src/features/run-deck/wrapup-panel-container.tsx:126` — 批次清单只提供 `gh stack push`，不拼 shell 路径（E-74）
`packages/web/src/components/batch-tree.tsx:470` — 原有收口按钮按本批在途位禁用（E-157）
`packages/web/src/components/batch-tree.tsx:498` — 具名拒绝原因贴在该批标题行之下（E-157）
`packages/web/src/components/batch-tree.tsx:572` — 展开有收口运行的批次时保留报告面板入口（E-297）
`packages/web/src/components/wrapup-report.tsx:515` — `BatchWrapupFailureLine`：只画拒绝原因，按钮仍归批次树（R2 不重复画按钮）
`packages/web/src/components/wrapup-report.tsx:133` — 收口报告卡片主体：只渲染 daemon 已解析字段，原文进可展开块不做解析（E-297）
`packages/web/src/components/wrapup-report.tsx:579` — `BatchLandingChecklist`：批次级只读清单，inHead 为真的行打勾、逐行可复制命令（E-74）
`packages/web/src/components/wrapup-report.tsx:181` — 解析失败时「直链原文」入口，指向该收口运行的会话详情（E-274、E-297）
`packages/web/src/components/wrapup-report.tsx:273` — `promptSource='builtin'` 头部 chip「文档未提供收口提示词」（E-297）
`packages/web/src/components/wrapup-report.tsx:365` — `isWellFormed=false` 行尾 chip「格式不全」（E-290 的呈现侧）
`packages/web/src/components/wrapup-report.tsx:88` — 有效裁定与自报裁定不一致判定，两者同时显示，绝不按自报放行（E-286）
`packages/web/src/features/landing/landing-container.tsx:45` — `useBatchLanding`：任务→批次读快照 `TaskDto.batchId`，收口记录取 GET /batches/:id/wrapups（R4）
`packages/web/src/features/landing/landing-container.tsx:253` — 批次级清单装配在任务级清单之前（E-74）
`packages/web/src/features/task-list/task-list-container.tsx:62` — 任务列表页共用同一份批次收口状态（R2）
`packages/web/src/features/run-deck/use-gate-card.ts:146` — `useGateCard`：能力位缺失即不可回话（E-117）
`packages/web/src/features/run-deck/use-gate-card.ts:158` — 原文逐字投 `POST /runs/:runId/messages`，只判送达不判状态（E-278、E-157）
`packages/web/src/features/run-deck/use-gate-card.ts:210` — 原文复制，失败也留着原文可复制（E-113）
`packages/web/src/components/gate-card.tsx:764` — 第四个条件动作「投递原文到实施会话」ghost，按能力位灰掉并带 title（E-278、E-117）
`packages/web/src/components/gate-card.tsx:794` — 未送达时卡内 inline notice + 原文可复制的复制键（E-113）
`packages/shared/src/api/runs.ts:186` — `RunCapabilitiesDto`：逐运行的能力位（E-117）
`packages/shared/src/api/runs.ts:250` — `RunDto.reworkText`：未结构化审查原文的客户端出口（E-278）
`packages/daemon/src/http/routes/runs.ts:336` — 给 GET /runs 与 GET /runs/:id 的每条运行挂真实能力位（E-117）
`packages/daemon/migrations/0011_review_rework_text.sql:1` — 审查原文独立持久化列（E-278）
`packages/daemon/src/repo/runs.ts:371` — 将未结构化审查原文写入 RunDto（E-278）
`packages/daemon/src/service/review.ts:2515` — 审查完成时持久化原文并交给人工闸门（E-278）
`packages/web/src/i18n/error-messages.ts:158` — 收口被拒具名文案：`details.reason` 优先（E-157）
`packages/web/src/i18n/error-messages.ts:133` — `E_WRAPUP_ROUND_LIMIT` 无 reason，按 `details.trigger` 归一到 auto/manual 两极（E-157）
`packages/web/src/i18n/error-messages.ts:85` — 投递四态中文文案，未送达与失败都点明原文还在（E-113）
`packages/web/src/api/cache-keys.ts:51` — `wrapups` 缓存键与前缀，按批次粒度（E-157）
`packages/web/test/stream-column.test.ts:673` — 3 kinds × 5 tiers 逐档断言停止键/参照条/轨列/审批槽位存在（E-236、E-106、E-297）
`packages/web/test/stream-column.test.ts:773` — 源码扫描断言停止控件之后不出现任何 kind/tier 条件（E-236）
`packages/web/test/wrapup-report.test.tsx:693` — 收口运行占普通泳道 + 五流并置 + 投递目标能力位（R1、E-106、E-117）
`packages/web/test/wrapup-report.test.tsx:503` — store POST 后不改状态、等 `batch.wrapup_started` 才清在途（E-157）
`packages/web/test/wrapup-report.test.tsx:347` — 源码扫描断言四处接线不写 API URL 字面量（R5）
`packages/web/test/wrapup-report.test.tsx:296` — 源码扫描断言不存在前端解析 `reportText` 的路径（E-297）
`packages/web/test/wrapup-report.test.tsx:652` — 批次级落地清单行数与勾选（E-74）
`packages/web/test/gate-card.test.ts:690` — 投递链路：能力位、原文逐字、未送达提示、复制（E-278、E-117、E-113、E-157）
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
**返工第 1 轮（R1–R5 已实现，R6 受环境阻断）**

**收口控件只有一个**　AC3 约束的「收口」按钮在 `components/batch-tree.tsx` 第 4 槽（M9-T19 的产出）。本任务把按批次的在途集合与具名原因**喂进那颗按钮**（`wrapupPendingBatchIds` / `wrapupFailureByBatch`），把上一轮自建的 `BatchWrapupControl` 删掉，只留 `BatchWrapupFailureLine` 画拒绝原因。接线时若又冒出第二颗「收口」按钮，就是回退。

**批次收口状态是模块级 store**　`wrapup-panel-container.tsx` 里的按批次 store 由运行甲板与任务列表页共用（`useBatchWrapupOverview`），否则同一批在两个页面会各发一次 POST、各存一份在途态。收口轮次另存 `runId→round`，只从 daemon 的事件 payload 或收口记录的 `runId` 取，前端不算 `max+1`。

**泳道列表只认 daemon**　合并 main 后 M8-T8 已落地：`SnapshotResponse.lanes: LaneView[]` 就是泳道分配的唯一来源（`LaneView.stage === 'wrapup'` 标出收口泳道，`laneNo` 由 daemon 给）。`buildDeckLanes()` 只做 LaneView → 甲板泳道的映射，**不排序、不补号、不按运行自己分泳道**；`lanes` 缺失时渲染空甲板而不是自己算一套，否则就是 E-317 禁止的第二套分配算法。单测里有一条专门断言「照 daemon 给的顺序与编号原样输出」。

**完成态报告仍可见**　daemon 释放完成的收口泳道后，报告不能只挂活动泳道。快照中的收口运行仍投影成批次树标题下的行；展开该批显示同一报告容器，轮次从持久化的收口记录恢复，原文直链仍指向收口运行详情。

**投递目标是实施运行**　审查运行判「未结构化」时，原文要投回**被审的实施会话**：目标是 `run.parentRunId`，`capabilities.canReply` 取的也是那条实施运行的能力位（不是审查运行自己的）。取错会让按钮的灰/亮与实际能否投递相反。

**泳道数据要取 `GET /runs` 而不是快照**　`RunCapabilitiesDto` 挂在 `GET /runs` 与 `GET /runs/:id` 上，`canReply` 逐运行由 `MessageService.canReply()` 回答（agent 位 ∩ 管道可写），`canResume` 取 agent 位。快照里的运行**没有**这一位（`SnapshotResponse.runs` 由 dispatch 的 `toRunDtoWithInHeadWarning` 出来，不经路由），所以甲板要同时取快照与 `GET /runs`。

**审查原文从审查运行读取**　审查会话退出后 daemon 泳道会重新指向实施运行；未结构化裁定及原文存储在最新审查运行，甲板按 `parentRunId` 找回那一行，再从目标实施运行取 `capabilities.canReply`。原文经 `runs.rework_text` 持久化并进入 `RunDto.reworkText`，不从闸门短注释推断或截取。

**快照的 gates 恒为空**　`service/dispatch.ts` 的 `getSnapshot()` 返回 `gates: Object.freeze([])`，所以甲板上的审批卡**不能**从快照取闸门；本任务改从 `GET /api/v1/gates` 取，按 `runId` 匹配待处理闸门。缺了这一步就只能画一颗点了没反应的「批准」。

**真实录制尚缺运行链**　非提权 Windows 会话无法取得产品实例锁；用 `startDaemon()` 仅替换锁适配器启动了本分支真实服务，浏览器完成配对，项目文档通过正式 API 导入。隔离数据库把新导入任务置为未落地，`canWrapup` 因而关闭；没有既有已落地实施运行，也无法执行第一轮收口的跟随分配。临时 Git 路径辅助使既有 Git 集成测试超时，已撤销。不能把导入任务直接改成已落地或注入收口记录来补 GIF；本轮没有真实模型收口轮次，尚不满足界面证据门槛。

**收口控件的落点**　沿用 `components/batch-tree.tsx` 的唯一「收口」按钮。审查补丁把按批次在途状态与拒绝原因从共享 store 送到这颗按钮；`wrapup-panel-container.tsx` 不再建第二颗按钮。

**「解析失败」只能来自事件**　daemon 解析不出八段时**不写 `batch_wrapups` 行**（`service/wrapup.ts` 的 `!parsed.ok` 分支只把运行转 `awaiting_human` 并开闸门），所以 `GET /batches/:id/wrapups` 里永远看不到失败轮。面板的失败条目因此只认 `batch.wrapup_finished` 的 `verdict='unparsed'`——那是 daemon 自己下的结论，不是前端读原文得出的。不要改成「wrapups 少一轮就显示解析失败」：那会把界面逻辑绑死到 daemon 的写库时机上。

**原文只当字符串**　`reportText` 仅进可展开块。任何 `split('BATCH_SUMMARY')`、`JSON.parse(reportText)` 一类写法都会被 `wrapup-report.test.tsx` 的源码扫描挡下（E-297 要的是机检，不是自觉）。

**能力位必须由外部给**　`GET /runs` 和 `GET /runs/:id` 为各运行附 `capabilities`，快照里的运行没有这一位。`useGateCard` 只消费目标实施运行的 `canReply`；缺失即 false，按钮灰掉并写明原因。不能把缺失当成可回话。

**幂等键跨重试复用**　容器把 `idempotencyKey` 存在 ref 里，被拒后重试复用同一个；http-client 对 POST 不自动重试，key 一换，daemon 侧就多算一次收口尝试（去重靠 `runsRepo.findByIdempotencyKey`）。

**「已接受」不等于「已开始」**　POST 成功后控件只置在途并保持禁用，批次状态一个字不改，等 `batch.wrapup_started` 回流才清在途并重取。不要从 POST 响应里读 `batch.state` 当界面状态。

**批次命令只复制栈操作**　`BatchWrapupLandingDto` 只有 worktree / 分支 / diffStat，没有任务级 `GET /tasks/:id/landing` 那样的 `commands[]`。清单单独列出 worktree 与分支，命令统一是 `gh stack push`，用户在所列 worktree 中执行；不把 daemon 路径插入 shell 命令，也不回退成普通 `git push`。

**轨列常驻但按插槽渲染**　`spineSlot` 与 kind/档位无关地渲染，但只在传了插槽时占位。M9-T21 的 `pipeline-lane` 把阶段链塞进 body；若这里无条件加 20px 轨槽，会与阶段行自己的 20px 轨列叠成两列。

**逐档 × 逐 kind 的机检**　`stream-column` 里 `kind` 只允许出现在 head 段内换文案。单测除 15 组 kind×tier 组合断言停止键/参照条/轨列/审批槽位都在，还扫源码断言 `data-action="stop-stream"` 之后不再出现 `kind ===` / `tier ===`，把 E-236 从「人肉看一遍」变成改动即失败的约束。

**`initialWrapups` 是一次性种子**　容器拿它当首次取数的替代，之后不再重放：调用方若每次都传新数组，同步回 state 会自激重渲染（React 只在 `Object.is` 相等时才跳过）。要刷新走 `refetch()` 或等 `batch.wrapup_*` 事件——收口轮次本来就是事件驱动新增的。`unparsableEntries` 相反可以合并，因为它带「没有新条目就返回原引用」的短路。

**复制逻辑暂有三份**　`features/landing/landing-container.tsx`、`wrapup-panel-container.tsx`、`use-gate-card.ts` 各有一份剪贴板降级链（clipboard API → execCommand → 失败返回 false）。`src/lib/` 不在本任务有效路径里，第三份先就近放着；下一个碰这几处的任务应把它收进 `lib/`。

**清单里的 `.tsx` 与仓库里的 `.ts`**　`gate-card.test.ts` / `stream-column.test.ts` 是既有文件（任务提示词自己的「代码位置」段也引 `.ts`）。新建 `.tsx` 会让同一组件出现两个测试文件，所以在既有 `.ts` 上扩写；只有真正新增的 `wrapup-report.test.tsx` 按清单新建。
<!-- notes:end -->

---

栈分支 `task/M9-T20` @ `agent-scheduler`　落地进 `main`
