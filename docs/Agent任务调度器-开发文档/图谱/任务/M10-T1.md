---
type: task
id: M10-T1
module: M10
depends_on: [M9-T1]
unlocks: [M10-T2, M10-T3]
edges: [E-200]
est_days: 1.5
branch: task/M10-T1
status: todo
---

# M10-T1　壳桥接契约与能力探测

- 所属模块：[[M10]]　**桌面壳与手机壳**——只做窗口与单实例、系统通知、令牌安全存储三件事的两个原生容器，外加 daemon 未运行时的引导与运行时缺失检测。壳按界面声明的可选桥接接口去实现，界面在没有壳时按能力探测降级，因此依赖方向恒为壳→界面
- 前置依赖：[[M9-T1]]
- 完成后解锁：[[M10-T2]]、[[M10-T3]]

## 输入 / 产出

- **输入**：07 节的三件能力
- **产出**：`shared/src/shell/bridge-contract.ts`、`web/src/shell/` 两个文件

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) 能力只有三件（`tokenStore`／`notify`／`hostHint`）加两个只读位，契约以类型形式约束，**加第四件需先改文档**
2) 探测同步读两个全局并在模块加载时冻结，**禁止用 userAgent 判定、禁止 try/catch 试探、禁止渲染中重复探测**
3) 全仓除 `src/shell/` 两文件外不出现 `__TAURI` / `Capacitor`，由 `check-forbidden.ts` 机检
4) 无壳时三件能力各有降级实现，桌面浏览器直连**功能不缺**（[[E-200]]）

## 必须处理的边界

- [[E-200]]　壳模块受阻而界面已就绪
    - 触发：Android 构建环境不可用导致手机壳延期
    - 期望：「客户端界面」必须能在桌面浏览器直连 daemon 使用，其验收标准**不因壳模块延期而改变**

## 代码位置

<!-- code:begin -->
_（**落地前必须回填**。一行一处，格式：`路径:行号` — 说明）_
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
_（**落地前必须回填**：怎么实现的、为什么这么选、踩了什么坑）_
<!-- notes:end -->

---

栈分支 `task/M10-T1` @ `agent-scheduler`　落地进 `main`
