---
type: task
id: M7-T2
module: M7
depends_on: [M7-T1, M3-T5, M4-T7]
unlocks: [M7-T3, M7-T7, M8-T9]
edges: [E-135, E-347, E-65]
est_days: 1.5
branch: task/M7-T2
status: done
---

# M7-T2　审查 agent 派发与 diff 裁剪

- 所属模块：[[M7]]　**审查流水线**——对已结束的运行做「合不合格」的判定：先跑本地机械检查，通过后以只读档派一个 agent 跑文档给出的审查提示词，产出 pass / rework / doc-issue 并回灌 rework 意见；从审查输出提取返工文本、按能力位选回灌／恢复／新开返工运行，以及解析收口运行的八段报告，也归它；第 N+1 轮审查续接同一审查会话、查 bug 阶段的运行与五段报告解析同样归它。凡「判定一次运行的结果」的都归它；审查、查 bug 阶段的运行用哪个 agent、模型与思考强度，一律读派发快照里的任务指派（审查可被 pipeline 设置的「审查覆盖」替换），不自己取 agent 默认
- 前置依赖：[[M7-T1]]、[[M3-T5]]、[[M4-T7]]
- 完成后解锁：[[M7-T3]]、[[M7-T7]]、[[M8-T9]]

## 输入 / 产出

- **输入**：M3-T5 的审查提示词；M4-T7 的只读档
- **产出**：审查运行的启动路径

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) 默认审查者的 agent、模型与思考强度**逐字取被审实施运行行的 `agent_id` / `model_name` / `effort_tier`（即任务指派的副本，决策 107），不是该 agent 的注册表默认模型**，且为**独立新 session**，只喂 M3-T5 的快照审查材料 + diff，不带实施上下文
本任务代码中不出现任何「设置里指定其他 agent／模型」分支——阶段覆盖的唯一入口是 pipeline 设置的 `reviewOverride`，其取值链与接线由 M8-T9 交付，本任务把三值的读取收成单一函数入口以便替换，返回形状预留 `{agentId, modelName, effortTier, effortVendor?}`，三值以参数形式由 `service/review.ts` 传入、`review-agent.ts` 不自己查库（[[E-347]]）
2) 审查运行固定只读档，实际任务指令只允许核对证据并输出裁定
源提示词以引用材料提供，其中维护文档、提交、推送、合并等步骤不得执行，**审查 agent 不得改代码或文档，也不接手两轮后的写入修复**，超限仍转人（[[E-135]]）
3) diff 超出上下文时给 diff stat 全量 + 验收标准命中文件全文、其余折叠，并在结论标注「基于部分 diff」（[[E-65]]）
4) 本任务只负责第 1 轮审查会话的新建（`review_round=1`、`continued_from_run_id` 为空）
第 N+1 轮续接同一审查会话与新开审查会话的头部上下文交 M7-T7，本任务代码中不出现按轮次分支（决策 89）

## 必须处理的边界

- [[E-135]]　审查 agent 的权限
    - 触发：审查阶段派出去的 agent
    - 期望：固定给只读档（read-only / plan），审查 agent 不得改代码，否则「审查通过」失去意义
- [[E-347]]　同一任务各阶段的模型、思考强度或来源不一致
    - 触发：实施用任务指派、审查用 `reviewOverride`、查 bug 逐字实施；或某阶段 agent 自报值与所选不同
    - 期望：流头部参照条**逐阶段**显示「模型 · 思考强度 · 来源」，来源三值文案「任务指派」「审查覆盖」「收口设置（跟随 〈taskKey〉）」，`agent_default` 显示「来源：任务指派（agent 默认）」（决策 124）；自报不一致按 E-37／E-256 逐阶段高亮，不做跨阶段比对、不把阶段间的合法差异当不一致；前端不算取值链，只显示 `RunDto.assignmentSource` 与 `effort`
- [[E-65]]　diff 超出审查模型上下文
    - 触发：一次改了上百个文件
    - 期望：给 diff stat 全量 + 验收标准命中文件的全文，其余折叠，并在结论标注「基于部分 diff」

## 代码位置

<!-- code:begin -->
`packages/daemon/src/service/review-agent.ts:63` — `readDefaultReviewAssignment` 单一入口提取实施运行的模型与思考强度（AC 1, E-347）
`packages/daemon/src/service/review-agent.ts:145` — `isAcceptanceMatchedFile` 判定改动文件是否直接命中验收标准或有效路径（AC 3, E-65）
`packages/daemon/src/service/review-agent.ts:204` — `formatFullDiffStat` 格式化输出全量 diff stat 表格与汇总（AC 3, E-65）
`packages/daemon/src/service/review-agent.ts:293` — `pruneDiff` 超出上下文阈值时裁剪折叠非关键文件并标记基于部分 diff（AC 3, E-65）
`packages/daemon/src/service/review-agent.ts:477` — `buildReviewAgentPrompt` 组装包含严格只读禁令与快照引用材料的审查提示词（AC 2, E-135, E-65）
`packages/daemon/src/service/review-agent.ts:648` — `buildReviewLaunchSpec` 构造审查进程启动参数并固定 readOnly 权限模式（AC 1, AC 2, E-135）
`packages/daemon/src/service/review-agent.ts:847` — `prepareReviewRun` 构造第 1 轮审查运行记录（review_round=1, continued_from_run_id=null, vendor_session_ref=null）（AC 1, AC 4）
`packages/daemon/src/service/review-agent.ts:957` — `dispatchReviewRun` 持久化审查运行行并广播 run.started 启动审查（AC 1-4）
`packages/daemon/src/service/review-agent.ts:971` — 插入审查行前复用 `assertSessionRefFree`，与其余 service 插入点同守 E-303／M6-T10 AC6
`packages/daemon/src/repo/runs.ts:40` — `RunRow` 新增 `effort_vendor`、`review_round`、`continued_from_run_id`、`assignment_source` 四个可选列定义（AC 1, AC 4, E-347）
`packages/daemon/src/repo/runs.ts:80` — `RunInsertRow` 同四列的写入形状，支持写入审查轮次与指派来源（AC 1, AC 4）
`packages/daemon/src/repo/runs.ts:236` — `toRunDto` 由 `effort_tier`／`effort_vendor` 还原 `RunDto.effort` 并下发 `effortVendor`（AC 1, E-347）
`packages/daemon/src/repo/runs.ts:287` — `createRunsRepo` PRAGMA table_info 列探测，按实际存在的列预编译 insert 语句（AC 1, AC 4）
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
1. 审查指派取值边界与解耦（决策 107、E-347）：
   - 默认审查者的模型与思考强度逐字复制自实施运行记录（`agent_id` / `model_name` / `effort_tier` / `effort_vendor`），绝不回落至 agent 注册表当前默认，确保任务级临时覆盖在审查阶段完整延续。
   - `review-agent.ts` 不自查数据库推断指派，而是收敛至纯函数 `readDefaultReviewAssignment` 并由调用方传参，预留 `{agentId, modelName, effortTier, effortVendor?}`，使后续 M8-T9 接入 `reviewOverride` 取值链时直接注入即可，无须在审查派发核心硬编码分支。
2. 权限沙箱与提示词净化（E-135）：
   - 审查运行强制固定 `permission_tier='readOnly'`，通过 `resolvePermissionMapping` 映射底层执行权限（codex `--sandbox read-only`、claude/grok `--permission-mode plan`、pi `--tools read,grep,find,ls`、dsh `DSH_PERMISSION_MODE: read-only`）。
   - 快照源提示词中的提交、推送、合并与文档生成指令，统一包裹进「引用审查材料」区块，顶层指令强调仅核对证据并输出三态裁定，禁止执行写操作且不得接手代码修复，超限一律转人。
3. 超上下文 diff 裁剪（E-65）：
   - 当改动文件数超过 100 或字符数超过 80,000 时触发裁剪。保留全量 `diff stat` 汇总保证宏观可见性；根据验收标准提及文件名与 `taskPaths` 保留关键文件完整 patch，其余文件生成折叠摘要；同时在提示词与结果中强约束裁定结论必须标注「基于部分 diff」。
4. 轮次解耦与首轮会话独立性（决策 89）：
   - 本任务严格仅负责首轮审查会话新建（`review_round=1`、`continued_from_run_id=null`），`vendor_session_ref` 置空以开启全新会话，不携带实施过程中的长会话上下文。代码中杜绝按轮次分支，续接与会话回话交由 M7-T7 单独承接。
<!-- notes:end -->

---

栈分支 `task/M7-T2` @ `agent-scheduler`　落地进 `main`
