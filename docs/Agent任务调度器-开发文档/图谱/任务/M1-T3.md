---
type: task
id: M1-T3
module: M1
depends_on: [M1-T1]
unlocks: [M1-T6, M1-T7, M6-T5]
edges: [E-119, E-129, E-132]
est_days: 1.5
branch: task/M1-T3
status: todo
---

# M1-T3　platform 模块：进程树终止、路径与 cmd 引用

- 所属模块：[[M1]]　**运行底座**——让 daemon 在这台机器上稳定跑起来并把字节落到磁盘上：进程单实例与端口、启动自检、开机自启注册、数据目录、结构化存储的连接与迁移、日志正文的追加写与字节偏移索引、以及全部平台差异（进程树终止、路径规范化与长路径、配置目录、子进程编码）的唯一收口。凡与操作系统、磁盘、进程存活打交道而不带任何业务概念的都归它
- 前置依赖：[[M1-T1]]
- 完成后解锁：[[M1-T6]]、[[M1-T7]]、[[M6-T5]]

## 输入 / 产出

- **输入**：M1-T1
- **产出**：`src/platform/`：`killTree` / `normalizePath` / `isLongPath` / `quoteForCmd` / `comSpec` / `appDataDir` / `autostart`

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) `killTree` 先 `taskkill /PID 〈pid〉 /T` 等 3 秒、仍存活再加 `/F`，两次都写事件（[[E-119]]）
2) 路径超 260 字符或含空格中文时提前校验并明确报错，不跑到写文件才失败（[[E-129]]）
3) `quoteForCmd()` 有覆盖空格、引号、`
`、`&`、`^` 五种输入的单元测试
4) 非 win32 分支抛 `E_NOT_IMPLEMENTED`，不静默走通用路径（[[E-132]]）

## 必须处理的边界

- [[E-119]]　Windows 进程树残留
    - 触发：agent 派生了 node/git/npm 子进程
    - 期望：中止必须杀整棵进程树（job object 或 `taskkill /T`）；只杀父进程会留下占文件锁的孤儿进程
- [[E-129]]　路径含空格中文或超长
    - 触发：Windows 路径超过 260 字符或含中文
    - 期望：参数按 Windows 引号规则拼接；长路径提前校验并明确报错，不能跑到一半炸在文件写入上
- [[E-132]]　平台分支散落各处
    - 触发：代码里出现 Windows 专有逻辑
    - 期望：一律收口到单一 platform 适配模块（进程树终止、开机自启、配置目录、路径规范化）；非 Windows 分支抛 not-implemented，而不是静默走错路径

## 代码位置

<!-- code:begin -->
_（**落地前必须回填**。一行一处，格式：`路径:行号` — 说明）_
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
_（**落地前必须回填**：怎么实现的、为什么这么选、踩了什么坑）_
<!-- notes:end -->

---

栈分支 `task/M1-T3` @ `agent-scheduler`　落地进 `main`
