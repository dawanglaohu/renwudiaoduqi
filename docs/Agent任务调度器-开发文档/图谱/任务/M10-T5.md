---
type: task
id: M10-T5
module: M10
depends_on: [M10-T4]
unlocks: []
edges: [E-209, E-257, E-258, E-259, E-260, E-265, E-266, E-267, E-268]
est_days: 2
branch: task/M10-T5
status: done
---

# M10-T5　三平台 CI、桌面产物与支持清单

- 所属模块：[[M10]]　**桌面壳与手机壳**——只做窗口与单实例、系统通知、令牌安全存储三件事的两个原生容器，外加 daemon 未运行时的引导、Windows/macOS/Linux 桌面构建与运行依赖检测。壳按界面声明的可选桥接接口去实现，界面在没有壳时按能力探测降级，因此依赖方向恒为壳→界面
- 前置依赖：[[M10-T4]]
- 完成后解锁：无，它不卡别人

## 输入 / 产出

- **输入**：M10-T4 的共享打包管线；17 节测试矩阵
- **产出**：GitHub Actions 三平台 workflow、运行时启动清单解析器与前置检查 launcher、平台/架构支持清单、人工 GUI 验收模板

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) Windows、macOS、Ubuntu runner 均执行 `pnpm -w check`、平台集成测试、daemon 启动 smoke 与 Tauri 构建，任一失败阻断整个版本（[[E-265]]）
2) 每个平台把安装产物展开到含空格与 Unicode 的临时根目录，以该位置模拟 `current_exe/resource_dir` 并生成经共享 schema 校验的绝对 `DaemonLaunchSpec`
smoke 必须用同一个 `file/args/cwd` 真正启动 daemon 并通过健康检查，安装包不得预写构建机绝对路径
daemon、路径/进程适配与桌面壳任一产品层缺失都阻断发布（[[E-209]]、[[E-257]]）
3) Linux 包元数据或 WebView 前命令行 launcher 检测 WebKitGTK/GTK，缺依赖时进程在创建 GUI 前列出发行版对应安装指引
该检查本身纳入 Ubuntu runner（[[E-258]]）
4) CI 不声称自动完成 GUI 交互
每个平台另有人工启动/通知/路径验收记录（[[E-266]]）
5) 支持清单逐项列出 OS、版本基线、CPU 架构和资产名
Windows arm64、Intel/Apple Silicon macOS 与 Linux x64/arm64 缺漏时标“未覆盖”，不提供错架构包（[[E-259]]）
6) macOS 未签名/未公证产物只标构建验证件，正式发布门禁失败（[[E-267]]）
7) Linux 写明 Ubuntu 已验证版本与 WebKitGTK/GTK 依赖，其他发行版只标尽力兼容（[[E-258]]、[[E-268]]）
8) Node/Tauri 提升平台下限时，CI、环境矩阵、发布说明缺一项更新都失败（[[E-260]]）

## 必须处理的边界

- [[E-209]]　升级后自启项指向旧路径
    - 触发：安装位置、macOS `.app` 位置或 Node 脚本参数变化，原生自启项仍引用旧启动方式
    - 期望：壳每次启动根据实际安装位置解析完整 `DaemonLaunchSpec`；`register` 幂等接收该冻结对象，逐字段校验原生注册项与当前绝对 `file`、`args[]`、绝对 `cwd`，不一致则重写；桌面按钮和自启共用同一对象且恒不经 shell，安装包不得预写固定绝对路径；重写失败降级为「需手动启动」并给出该平台可复制命令
- [[E-257]]　平台支持只完成一半
    - 触发：某平台能启动 daemon，但桌面壳无法构建或启动
    - 期望：该平台不得标记为完整支持；三平台发布检查失败，并列出缺失的产品层
- [[E-258]]　Linux 缺少桌面运行依赖
    - 触发：WebKitGTK、GTK 或 Tauri v2 要求的系统库缺失，GUI 甚至无法创建窗口
    - 期望：安装包前置检查或命令行 launcher 在启动 WebView 前列出缺失包与当前发行版可执行的安装指引；不得依赖 GUI 自己报错，该环境不计入已验证平台
- [[E-259]]　CPU 架构没有发布资产
    - 触发：Windows arm64、Apple Silicon、Intel Mac、Linux x64/arm64 中某架构未进入构建矩阵
    - 期望：发布清单明确标「未覆盖」，下载入口不向该架构提供其他架构安装包
- [[E-260]]　上游收紧系统版本下限
    - 触发：Node.js 22 或 Tauri v2 升级后提高最低 OS、libc 或 WebKit 要求
    - 期望：依赖升级必须同步调整 CI 矩阵、环境矩阵与发布说明；未同步则发布检查失败
- [[E-265]]　三平台矩阵任一失败
    - 触发：Windows、macOS、Ubuntu 任一 runner 的 workspace check、平台集成测试或桌面壳构建失败
    - 期望：整体发布检查失败；不得把失败平台降级成可忽略项后继续标记三平台版本
- [[E-266]]　CI 无法执行完整 GUI 测试
    - 触发：托管 runner 缺少交互式桌面或图形会话不稳定
    - 期望：CI 至少验证安装包生成、daemon 启动和平台适配器集成；GUI 操作进入按平台记录的人工验收清单，不得伪报自动化通过
- [[E-267]]　macOS 产物未签名或未公证
    - 触发：CI 没有 Apple 签名/公证凭据
    - 期望：产物只标为构建验证件，不作为可直接分发的正式安装包；正式发布必须由单独签名门禁放行
- [[E-268]]　Linux 只在 Ubuntu 验证
    - 触发：CI 使用 Ubuntu，用户运行其他发行版
    - 期望：发布说明写明 Ubuntu 已验证及依赖基线；其他发行版标为尽力兼容，不表述为同等级支持

## 代码位置

<!-- code:begin -->
`.github/workflows/desktop-ci.yml:34` — 三平台矩阵（windows-latest / macos-latest / ubuntu-latest），每个 runner 依次跑 workspace check、平台集成测试、daemon 分发构建、Tauri 壳构建与真实 smoke（E-265）
`.github/workflows/desktop-ci.yml:119` — 平台集成测试单独成步（`packages/daemon/test/platform/`），让门禁能按步骤归因（E-265）
`.github/workflows/desktop-ci.yml:122` — Ubuntu runner 内跑 Linux launcher 的 WebKitGTK/GTK 前置检查（E-258）
`.github/workflows/desktop-ci.yml:139` — 构建随包 daemon 分发（`pnpm deploy --prod` + 钉版 Node 运行时），供 tauri-build 作为 bundle 资源复制（E-257、E-260）
`.github/workflows/desktop-ci.yml:157` — 展开安装载荷并真正启动随包 daemon；POSIX 上以 `sudo` 执行以满足机器级实例锁的 root 目录要求（E-209、E-257）
`.github/workflows/desktop-ci.yml:169` — macOS 签名/公证判定，缺凭据只标「构建验证件」（E-267）
`.github/workflows/desktop-ci.yml:208` — 独立签名/公证门禁 job，只在 `formal_release=true` 时运行且缺凭据即失败（E-267）
`.github/workflows/desktop-ci.yml:227` — 发布门禁：从 Actions API 读回各平台 job 的步骤结论再逐平台逐步骤断言（E-265）
`packages/shell-desktop/launchers/evaluate-release-gate.ts:57` — `collectHostResults`：按 job 名与步骤名把真实结论映射成 `HostVerificationResult[]`，失败平台连同失败步骤一起点名（E-265、E-267）
`packages/shell-desktop/src/platform-support.ts:17` — 平台与 CPU 架构支持矩阵；Windows arm64、Intel macOS、Linux arm64 标「未覆盖」且资产为 null（E-259）
`packages/shell-desktop/src/platform-support.ts:91` — `resolvePlatformAsset`：未覆盖架构不给任何包，macOS 未签名/未公证只给构建验证件、正式发布抛错（E-259、E-267）
`packages/shell-desktop/src/platform-support.ts:148` — Linux 发行版分级：Ubuntu 已验证基线，其余尽力兼容（E-258、E-268）
`packages/shell-desktop/src/platform-support.ts:228` — `assertFullPlatformSupport`：产品层缺失时列出缺哪一层（E-257）
`packages/shell-desktop/src/platform-support.ts:281` — `assertReleaseVerification`：三平台四步骤任一失败即抛错并列出平台与步骤，正式发布再校验 macOS 签名与公证（E-265、E-267）
`packages/shell-desktop/src/platform-support.ts:354` — `validateUpstreamBaselineSync`：Node 22 / Tauri v2 / 三平台基线在 package.json、Cargo.toml、CI、文档与随包运行时清单之间锁步（E-260）
`packages/shell-desktop/launchers/verify-baselines.ts:18` — 读随包运行时清单 `daemon-runtime.json` 并交给锁步校验，CI 每个 runner 执行（E-260）
`packages/shell-desktop/daemon-runtime.json:1` — 随包 Node 运行时的唯一钉版处（E-260）
`packages/shell-desktop/src/launch-spec.ts:61` — `resolveShippedDaemonLayout`：`<resource_dir>/daemon-runtime/runtime/node[.exe]` + `<resource_dir>/daemon-runtime/bootstrap.mjs` 的安装布局，TS 与 Rust 两侧共同遵守（E-209）
`packages/shell-desktop/src/launch-spec.ts:91` — `resolveLaunchSpec`：每次启动只按实际 `current_exe/resource_dir` 解析并冻结 `DaemonLaunchSpec`，不预写任何绝对路径（E-209）
`packages/shell-desktop/src-tauri/src/lib.rs:129` — 原生壳按同一布局推导 `{file, args:[bootstrap.mjs], cwd}`，`launch_service` 不经 shell 启动（E-209）
`packages/shell-desktop/src-tauri/tauri.conf.json:27` — `bundle.resources` 把 daemon 分发与 `web/dist` 声明为随包资源，`cargo build` 即复制到可执行文件旁（E-209、E-257）
`packages/shell-desktop/scripts/build-daemon-distribution.mjs:149` — `bundleRuntime`：优先复用 pnpm 提升的钉版运行时，缺失平台（darwin-arm64）从 nodejs.org 取并按 SHASUMS256 校验（E-257、E-260）
`packages/shell-desktop/src/artifact-staging.ts:96` — `stageInstalledProduct`：把 daemon 分发、`web/dist` 与壳二进制展开到含空格与 Unicode 的临时根目录（E-209）
`packages/shell-desktop/src/artifact-staging.ts:166` — `inspectProductLayers`：daemon（含运行时与 web/dist）、路径适配器、桌面壳三层全部读自展开后的文件，逐项列缺（E-257）
`packages/shell-desktop/src/staging-smoke.ts:98` — `inspectBuildPathResidue`：展开载荷内的绝对路径与构建机前缀比对，大小写与分隔符不敏感（E-209）
`packages/shell-desktop/src/staging-smoke.ts:176` — `executeDaemonSmoke`：用冻结 spec 真正 spawn（`shell:false`）、探活 `/api/v1/health`；起不来、提前退出、超时都返回失败（E-209、E-265）
`packages/shell-desktop/launchers/smoke-runner.ts:83` — 构建机路径禁用前缀含本检出根目录与其上级（E-209）
`packages/shell-desktop/launchers/smoke-runner.ts:113` — `executeStagingSmokeCheck`：展开 → 产品层 → 路径残留 → 以壳的同一入口解析 spec → 真启动随包 daemon（E-209、E-257、E-265）
`packages/shell-desktop/launchers/linux-launcher.sh:53` — 启动 GUI 前用 ldconfig/固定目录探测 WebKitGTK 与 GTK3，缺失时按发行版打印安装命令并退出 1（E-258、E-268）
`packages/shell-desktop/launchers/linux-launcher.ts:46` — 同一检查的 TypeScript 实现与终端提示格式化（E-258、E-268）
`packages/shell-desktop/src-tauri/tauri.conf.json:38` — Linux deb 元数据声明 WebKitGTK 与 GTK3 依赖（E-258）
`docs/platform-support.md:11` — OS、版本基线、CPU 架构与资产名逐项清单，未覆盖架构明标（E-259）
`docs/platform-support.md:26` — Ubuntu 已验证版本、WebKitGTK/GTK 依赖与其他发行版「尽力兼容」（E-258、E-268）
`docs/platform-support.md:52` — macOS 构建验证件与正式发布门禁（E-267）
`docs/platform-support.md:65` — Node/Tauri 下限锁步规则与随包运行时版本（E-260）
`docs/platform-support.md:87` — 安装布局与 daemon 启动契约，含实例锁的提权要求（E-209）
`docs/platform-support.md:108` — 三平台 CI 各步骤与「CI 不声称 GUI 自动化」（E-265、E-266）
`docs/platform-support.md:123` — 人工 GUI 验收记录模板（E-266）
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
1. **daemon 以「Node 应用 + 随包运行时」的形态进安装包。** daemon 没有编译产物，入口是 `bootstrap.mjs`，还要 `typescript` 做加载器；离开单仓后这些依赖全部消失。安装包因此携带 `pnpm deploy --prod` 的完整树（daemon 自带 `typescript` 依赖）和一份钉版 Node（`daemon-runtime.json`），启动契约固定为 `runtime/node bootstrap.mjs`。npm 的 `node-darwin-arm64` 包停在 18，所以 Apple Silicon 的运行时从 nodejs.org 取并按 SHASUMS256 校验。
2. **布局由解析器定义，打包和 smoke 只能服从。** `resolveShippedDaemonLayout` 是唯一的布局来源，Rust 壳按同样三个常量推导，测试断言两侧字面一致。smoke 不传 `customDaemonPath`，而是调用壳启动时的同一个 `resolveLaunchSpec`；解析出来的文件不在展开目录里，检查直接失败。用构建机的 `process.execPath` 起一个只服健康端点的桩会绕开「包里的 daemon 能不能起来」这个问题，所以 smoke 只认展开目录里的运行时。
3. **产品层要读文件，不能读常量。** 三层完整性全部来自展开后的磁盘内容：daemon 层包括入口、清单、随包运行时和 `web/dist/index.html`（daemon 的静态插件缺它即启动失败），桌面壳层是 `cargo build` 真实产出的二进制。壳没构建成功时，smoke 列出 `desktop-shell` 缺失；staging 不写占位文件，否则这一层永远「有」。
4. **发布门禁不能只看 `needs.result`。** 聚合结果只能阻断，列不出失败平台。门禁 job 用 Actions API 读回本次运行每个平台 job 的步骤结论，映射成 `HostVerificationResult[]` 后交给 `assertReleaseVerification`，失败平台与失败步骤一起进错误信息。平台集成测试单独成步就是为了这层归因。
5. **实例锁要求提权，这是 M1 的设计，smoke 只能顺着它。** 锁目录在 `/var/lib`、`/Library/Application Support`、`%ProgramData%`，要求 root / Administrators 所有。POSIX runner 上 smoke 以 `sudo -E` 执行，Windows runner 本身已提权；`AGSCHED_PORT` / `AGSCHED_DATA_DIR` 两个产品环境变量把 smoke 的 daemon 隔离在临时目录与指定端口，`file/args/cwd` 不变。桌面壳非提权地 spawn daemon 是否能过这把锁，不在本任务范围，记入后续任务。
6. **架构清单只写构建矩阵里有的架构。** CI 只有一个 macOS runner（Apple Silicon），Intel macOS 没有进构建矩阵，按 E-259 标「未覆盖」且不给资产；一个从未构建过的架构写成「支持」就是向 Intel 用户递 arm64 包。补 Intel 腿要等有可用的 Intel runner。
7. **Windows runner 上 `pnpm -w check` 转绿靠的是三处宿主差异修正，都在 daemon 侧。** `better-sqlite3` 走自带 prebuilds 并 `ignoredBuiltDependencies`，避开 node-gyp 在 VS 18 上的版本探测崩溃；测试传 `platform:'linux'` 但文件落在 NTFS 上，配对码文件的权限断言按文件系统能力判定；`hasUnexpandedPathToken` 的 `~` 只判词首，否则 runner 自己的 `RUNNER~1` 短名会让 daemon 拒绝启动，13 节「不得把 ~ 当路径交给文件系统」指的正是词首展开符，回归由 `platform.test.ts` 钉住。这些文件登记在本任务的 supportPaths 里。
<!-- notes:end -->

---

栈分支 `task/M10-T5` @ `agent-scheduler`　落地进 `main`
