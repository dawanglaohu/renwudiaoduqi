---
type: task
id: M8-T7
module: M8
depends_on: [M8-T6, M7-T5]
unlocks: [M8-T9]
edges: [E-275, E-276, E-280, E-290, E-293, E-298, E-300, E-46, E-55]
est_days: 1.5
branch: task/M8-T7
status: todo
---

# M8-T7　收口修复派发、跨批语义与人工撤回

- 所属模块：[[M8]]　**派发调度**——决定什么时候把哪个任务、用哪个 agent 与哪个模型、以什么权限派出去，并按批次、并行窗口、每 agent 并发与路径冲突限流，在三个闸门处停或放行、失败或重派时重新入队；判定本批分支是否都进了 HEAD、触发收口运行、按收口报告派修复运行与管理收口轮次也归它；泳道（窗口槽位）分配、任务终态后的补位、查 bug 与收口模式两个开关的持久化同样归它——泳道分配是它算出的派生数据。凡涉及顺序、名额、放行、重派的都归它；任务指派写进派发快照、阶段取值链（任务指派 → 阶段覆盖 → agent 默认）与收口指派设置也归它——它是唯一决定「这一阶段用谁跑」的地方，M7 只消费结果
- 前置依赖：[[M8-T6]]、[[M7-T5]]
- 完成后解锁：[[M8-T9]]

## 输入 / 产出

- **输入**：M8-T6 的有效裁定与开放项；M7-T5 的返工提示词组装；M8-T2 的冲突判定
- **产出**：`service/wrapup.ts` 的修复派发段、`domain/task-state.ts` 的 `origin` 分支与 `deriveTaskInHead()`、`http/routes/tasks.ts` 的 `recall` 端点、`domain/path-clash.ts` 的同收口串行规则、shared 的 `TaskDto.inHead` / `crossBatchFix`

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) 有效裁定 open 时按 `planWrapupFixes` 分组，每个任务派一次修复运行：`origin='wrapup-fix'`、`spawned_by_run_id`=收口运行、`batch_id`=触发批次、`attempt_no+1`、`rework_count=0`、`workspaceWrite`、复用收口 worktree（`reuse`），agent/model/effort 取该任务最近实施运行快照，agent 不可用则该组不派并计入 unassigned
提示词 = R 条目原文 + 返工规则段（[[E-275]]）
2) 同一收口派出的修复运行互相串行（`queued_reason` 以 `wrapup-fix-serial:` 开头），与在途任务路径冲突照 [[E-46]] 排队
修复运行走正常审查，代码中不存在按 origin 跳过审查的分支（[[E-280]]、决策 70）
3) 本轮修复运行全部 landed 且收口分支 `is_in_head=1` 且轮次 < 2 → tick 触发第 2 轮
第 2 轮仍 open、或开放项全部无主 → `needs_attention` 不派第 3 轮（[[E-276]]、[[E-290]]）
4) 跨批：修复运行归触发批次、任务行归原批次
已 `done` 的批次不因跨批修复重触发收口、`done` 不回退
批次计数按最近一次非修复运行，任务行标跨批修复（[[E-275]]）
5) `POST /tasks/:taskId/recall` comment 必填，起 `origin='wrapup-fix'`、`spawned_by_run_id` 为空的修复运行走正常审查
该任务已有在途修复/返工运行或本批收口运行在跑 → `E_FIX_RUN_IN_FLIGHT`
不计入 [[E-55]] 计数
每任务同时只允许一条在途修复运行（[[E-293]]、[[E-300]]）
6) `TaskDto.inHead` 三态由 `deriveTaskInHead()` 算出（[[E-298]] 的数据侧）

## 必须处理的边界

- [[E-275]]　收口发现跨批问题
    - 触发：BUGS/NOT_FIXED 条目「涉及」前置批次的任务
    - 期望：给该任务派修复运行（`origin=wrapup-fix`、attempt_no+1、归触发批次），任务原 landed 记录不改；已 done 的前置批次不重触发收口、done 不回退；前置批次「已落地 x/n」按最近一次非修复运行计，任务行标「跨批修复」并呼吸
- [[E-276]]　第 2 轮收口仍 open
    - 触发：自动轮次达上限（2）时有效裁定仍为 open
    - 期望：批次 `needs_attention`，不派第 3 轮修复运行、不自动再收口；开批次级收口闸门等人
- [[E-280]]　修复运行与在途任务抢路径
    - 触发：修复运行的有效路径与同批在途任务有交集，或同一收口派出多条修复运行
    - 期望：前者按 M8-T2 排队等对方 landed；同一收口运行派出的修复运行互相冲突（共用收口工作区），`queued_reason=wrapup-fix-serial:〈runId〉` 逐个跑；不为修复运行新建 worktree
- [[E-290]]　TESTS 失败项无「涉及 〈任务ID〉」
    - 触发：收口报告的测试失败行没写任务号，或任务 ID 不在文档里
    - 期望：进 unassigned，有效裁定为 open；有其他可派条目照派、第 2 轮复跑兜底；全部开放项无主才 `needs_attention`（可派修复条目为零）；不猜归属、不去重
- [[E-293]]　人工撤回已自动 landed 的任务
    - 触发：落地闸门自动时人在收口前发现审查误判
    - 期望：`POST /tasks/:taskId/recall` 附言必填，起一次修复运行（`origin=wrapup-fix`、spawned_by 为空、在原 worktree 与分支上 attempt_no+1）走正常审查；该任务已有在途修复/返工运行或本批收口运行在跑时返回 409；不计入 E-55 自动返工上限；手机端不放此入口
- [[E-298]]　任务行的「进 HEAD」标记
    - 触发：任务已验收、未验收或从未派发
    - 期望：三态：true「进 HEAD」、false「未进 HEAD」（title 显示 method）、null「—」；同色不用色相；前端只读 `TaskDto.inHead`，不由分支名或运行状态推断
- [[E-300]]　同一任务被重复判定要修
    - 触发：两轮收口或两个批次的收口同时给同一任务开修复条目，或撤回撞上在途修复
    - 期望：每任务同时只允许一条在途修复运行；后到的合并进落地清单提示而不再派；重复撤回返回 409
- [[E-46]]　同批任务抢同一批文件
    - 触发：同批内两个任务的 `taskPaths` 有交集
    - 期望：后者排队，等前者到达「已落地」（不是「已退出」）才起；worktree 隔离不作为放行理由
- [[E-55]]　全自动下审查反复判 rework
    - 触发：审查连续不通过
    - 期望：重试上限默认 2 次，超限转「待人确认」，不无限自动改；回灌、恢复后回灌、新开返工运行三种投递方式计入同一个计数

## 代码位置

<!-- code:begin -->
`packages/shared/src/api/tasks.ts:23` — TaskDto 扩展 inHead 与 crossBatchFix 契约字段（AC 4, AC 6, E-298, E-275）
`packages/shared/src/api/tasks.ts:44` — RecallTaskBody 与 RecallTaskResponse 契约及穷尽性断言（AC 5, E-293）
`packages/shared/src/api/routes.ts:136` — REQUEST_BODY_SCHEMAS 注册 recallTask 请求体 schema 与 key 穷尽性（AC 5, E-293）
`packages/shared/src/api/routes.ts:516` — ROUTES 注册 POST /api/v1/tasks/:taskId/recall 端点及契约错误码（AC 5, E-293）
`packages/daemon/src/domain/task-state.ts:41` — deriveTaskState() 扩展 origin 分支支持批次级忽略修复运行口径（AC 4, E-275）
`packages/daemon/src/domain/task-state.ts:97` — deriveTaskInHead() 纯函数推导任务三态进 HEAD 标记（AC 6, E-298）
`packages/daemon/src/domain/task-state.ts:152` — deriveTaskCrossBatchFix() 判定任务是否处于在途跨批修复（AC 4, E-275）
`packages/daemon/src/domain/path-clash.ts:421` — buildWrapupFixSerialReason() 构造同收口串行排队原因（AC 2, E-280）
`packages/daemon/src/domain/path-clash.ts:500` — evaluateWrapupFixSerialization() 实现同收口运行修复任务串行规则（AC 2, E-280）
`packages/daemon/src/http/routes/tasks.ts:110` — POST /api/v1/tasks/:taskId/recall 路由处理函数与入参/权限解析（AC 5, E-293）
`packages/daemon/src/http/routes/tasks.ts:144` — 注册 recall 路由及其路径参数与请求体 schema（AC 5, E-293）
`packages/daemon/src/service/wrapup.ts:382` — triggerWrapup() 校验上轮收口分支进入 HEAD（AC 3, E-272）
`packages/daemon/src/service/wrapup.ts:893` — recordWrapupResult() 开放项分组派发修复运行与同收口串行（AC 1, AC 2, AC 3, E-275, E-280, E-290）
`packages/daemon/src/service/wrapup.ts:1240` — recallTask() 实现人工撤回已验收任务并创建修复运行（AC 5, E-293, E-300）
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
1. 修复工作区与同收口串行（E-280）：收口阶段直接在收口 worktree 修 bug 但未提交，若修复运行新开 worktree 会丢失这部分改动。因此修复运行必须复用收口 worktree (`reuse`)，同收口派出的多条修复运行共享该目录，必须以 `wrapup-fix-serial:<runId>` 严格串行执行；首条修复运行与在途其他任务发生路径重叠时，照 E-46 排队等待 landed。
2. 跨批修复与批次状态解耦（E-275）：当收口报告中的开放项涉及前置批次任务时，修复运行归属于触发批次（`runs.batch_id`），而任务行归属不变（`tasks.batch_id`）。前置已 `done` 批次绝不重入收口循环，批次级计数通过 `deriveTaskState(..., { ignoreWrapupFix: true })` 忽略在途修复运行，仅在任务行呈现跨批修复与呼吸状态。
3. 无主开放项与轮次上限保护（E-276、E-290）：收口报告中未指定任务 ID 或指向非文档已知任务的开放项，全量归入 unassigned。当且仅当全部开放项均无主导致可派修复数为 0，或自动轮次达上限 2 且仍为 open 时，批次转 `needs_attention` 并开立 `task_id = null` 闸门，杜绝死循环空转与无主猜测。
4. 撤回运行与审查链路（E-293、E-300）：`POST /tasks/:taskId/recall` 作为落地闸门自动时的人工出口，以 `origin='wrapup-fix'`、`spawned_by_run_id=null` 起新实施运行并走完整审查。当该任务存在在途修复/返工或本批收口在跑时直接返回 409 `E_FIX_RUN_IN_FLIGHT`；撤回运行重置 `rework_count = 0`，不消耗 E-55 的审查打回上限。
5. 任务进 HEAD 三态判定（E-298）：`deriveTaskInHead()` 严格区分「已验收进 HEAD (true)」、「已验收未进 HEAD (false)」与「未验收/未派发 (null)」，仅依据实施运行的 `state` 与 `is_in_head` 派生，前端无权根据分支名猜测。
<!-- notes:end -->

---

栈分支 `task/M8-T7` @ `agent-scheduler`　落地进 `main`
