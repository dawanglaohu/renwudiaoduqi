---
type: task
id: M2-T6
module: M2
depends_on: [M2-T1]
unlocks: [M9-T4]
edges: [E-215, E-216, E-217]
est_days: 1.5
branch: task/M2-T6
status: todo
---

# M2-T6　请求体校验、路由表一致性与契约断言

- 所属模块：[[M2]]　**接入与设备信任**——定义两端如何连上 daemon 并不丢事件地保持同步：一次性配对码与可单独吊销的设备令牌、鉴权与操作归属、REST 骨架与 API 版本、SSE 服务端的事件信封／重放窗口／保活，以及自写的 SSE 客户端。凡「谁能连上、连上之后按什么协议收发」的都归它
- 前置依赖：[[M2-T1]]
- 完成后解锁：[[M9-T4]]

## 输入 / 产出

- **输入**：10 节端点表；`shared/src/api/routes.ts`
- **产出**：各路由的 JSON Schema、`test/arch/routes.test.ts`、键清单常量、`scripts/check-error-codes.mjs`

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) 架构测试断言「Fastify 实际注册的 method+path 集合 === `routes.ts` 常量表」，多一条少一条都失败
2) 请求体加字段只改 TS 接口未改 Schema 时 `tsc` 报错（键清单 `as const satisfies` 穷尽断言）（[[E-215]]）
3) 只改 Schema 未改 TS 接口时契约测试失败（[[E-216]]）
4) 所有请求体 Schema 统一 `additionalProperties:false`，多余字段返回 400 并列出字段名，不静默丢弃（[[E-217]]）
5) `check-error-codes.mjs` 断言 `codes.ts` 的键集合与 10 节错误码表逐行一致，**方向是「文档为源、代码为投影」**，不一致即 `pnpm -w check` 失败

## 必须处理的边界

- [[E-215]]　加字段只改了类型
    - 触发：请求体加字段，改了 TS 接口没改 JSON Schema
    - 期望：**编译期失败**：每个请求体类型旁强制配一份 `as const satisfies readonly (keyof T)[]` 的键清单并做穷尽断言，漏字段则 `tsc` 报错
- [[E-216]]　加字段只改了 schema
    - 触发：改了 JSON Schema 没改 TS 接口
    - 期望：契约测试断言「`schema.properties` 的键集合 === 该类型的键清单常量」，不等即失败
- [[E-217]]　客户端发来多余字段
    - 触发：请求体含 schema 未声明的字段
    - 期望：所有请求体 schema 统一 `additionalProperties: false`，返回 400 **并在错误体里列出多余字段名**，不静默丢弃

## 代码位置

<!-- code:begin -->
_（**落地前必须回填**。一行一处，格式：`路径:行号` — 说明）_
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
_（**落地前必须回填**：怎么实现的、为什么这么选、踩了什么坑）_
<!-- notes:end -->

---

栈分支 `task/M2-T6` @ `agent-scheduler`　落地进 `main`
