---
type: task
id: M9-T18
module: M9
depends_on: [M9-T16, M4-T6, M8-T2, M8-T11]
unlocks: [M9-T23]
edges: [E-108, E-31, E-47, E-52]
est_days: 0.5
branch: task/M9-T18
status: todo
---

# M9-T18　逐任务指派面板与并发瓶颈说明

- 所属模块：[[M9]]　**客户端界面**——两端共用的那一套界面本身：多流监看、会话查看、审批与闸门、设置与配对、落地清单、按批次折叠的批次树与执行行呼吸灯、按窗口数呈现的任务流水线泳道与「会话已归档」历史行、顶栏两个流水线开关，以及同一套代码的窄屏降级。它只通过 M2 的契约与 daemon 通信，**界面里不含任何业务判定**（折叠默认值、「能否收口」、泳道分配、阶段推进与会话归档都是 daemon 给的字段）；指派面板的分组模型下拉、登录态徽标与刷新按钮、参照条的「来源」段同样只呈现 daemon 下发的字段，不算取值链
- 前置依赖：[[M9-T16]]、[[M4-T6]]、[[M8-T2]]、[[M8-T11]]
- 完成后解锁：[[M9-T23]]

## 输入 / 产出

- **输入**：M4-T6 的取值链；M8-T2 的窗口计算；M8-T11 的 `GET/POST /api/v1/batches/:batchId/assignments`（`drafts[].sessionNo` 与 `preview`，10 节）；M9-T16 的 `EmptyOnboarding` `step3Slot` / `step4Slot`
- **产出**：`assign-panel.tsx`、`features/run-deck/use-assign-panel.ts` + 接进 `run-deck-view.tsx` 的 `step3Slot` / `step4Slot`、`api/batches.ts` 的 `readAssignments()` / `putAssignments()`、`store/selection-store.ts` 的逐任务草稿

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) 四步引导第三步是**逐任务指派**而不是「给整批选一个模型」：每行一个任务，各自选 agent／模型／思考强度，已指派行显示所选值可点回改（[[E-108]] 的第三步）
2) **同一个 agent 允许被指派多次，每次是一个独立会话**并显示将要使用的会话序号（[[E-31]]、决策
7、决策 52）
3) 第四步显示并发三者取 min 的结果，并**明确指出是并行窗口、该 agent 上限还是用户设定成了瓶颈**，不只给一个可放行数字（[[E-52]]）
4) 某 agent 已达并发上限时其余任务仍可指派给别家，**不空转等待**（[[E-47]] 的呈现侧）
5) **数据来源与写入路径**（2026-09-19 补丁，决策 136）：会话序号、agent 已用／上限／是否满额、有效并发、瓶颈来源、是否越过窗口一律读 `GET /api/v1/batches/:batchId/assignments` 的 `drafts[].sessionNo` 与 `preview`，用户每次改选后 `POST` 同一端点整批覆写并用返回值刷新，组件不按数组顺序自算序号、不用 `Math.min` 自算并发、字段缺失显示「—」
面板经 M9-T16 的 `step3Slot` / `step4Slot` 接进真实零运行流程，`run-deck-view.tsx` 里不存在内置的文档／批次／任务／agent 演示数组
GIF 从真实 daemon 录（[[E-52]]、[[E-47]]、[[E-108]]）

## 必须处理的边界

- [[E-108]]　零运行空态
    - 触发：首次使用或全部空闲
    - 期望：空态直接是「选文档 → 选批次 → 选模型 → 派发」的四步引导，而不是一张插画
- [[E-31]]　同一 agent 并发多任务
    - 触发：一个 agent 被同时派多个任务
    - 期望：每任务独立 session 与独立工作区（grok 用自带 --worktree，其余用 git worktree 兜底），并设每 agent 并发上限
- [[E-47]]　单 agent 并发满但窗口有空位
    - 触发：某 agent 达到并发上限，整体窗口未满
    - 期望：空位让给其他 agent 的任务，不空转等待
- [[E-52]]　并发瓶颈来源不明
    - 触发：实际并发数低于用户预期
    - 期望：实际并发取 min(窗口数, 每 agent 上限, 用户设定)，UI 显示这三者中哪一个成了瓶颈；用户可向下调，向上超过窗口数需显式解锁并提示后果

## 代码位置

<!-- code:begin -->
`packages/web/src/components/assign-panel.tsx:299` — `TaskAssignRow`: 逐任务独立指派表单与已指派回显行，提供「修改指派」回改入口，删除组件层补算且缺失显示「—」（AC 1、E-108、R1）
`packages/web/src/components/assign-panel.tsx:376` — `TaskAssignRow`: 同一 Agent 多次指派读取 daemon 明确下发的独立会话序号徽标（AC 2、E-31、决策 7、决策 52、R1）
`packages/web/src/components/assign-panel.tsx:479` — `TaskAssignRow`: 切换 Agent 清空模型覆盖，不支持思考强度显示「—」不伪造默认档（M4-T6、E-34、E-254、R1）
`packages/web/src/components/assign-panel.tsx:512` — `TaskAssignRow`: 某 Agent 已达并发上限时提示排队，其余任务仍可指派给别家 Agent 不空转等待（AC 4、E-47）
`packages/web/src/components/assign-panel.tsx:640` — `TaskAssignmentList`: 逐任务指派列表展示组件，顶部呈现各 Agent 活跃会话与并发容量指标（AC 1、AC 4、E-47、E-108）
`packages/web/src/components/assign-panel.tsx:736` — `ConcurrencyBottleneckCard`: 并发限制审计与三者取 min 结果展示，对比窗口数/Agent上限/用户设定并明确指出瓶颈归因，缺失显示「—」（AC 3、E-52、R1）
`packages/web/src/components/assign-panel.tsx:883` — `ConcurrencyBottleneckCard`: 用户设定调节步进器，向上超过窗口数需显式解锁并提示后果，按钮保障 44×44 触控（AC 3、E-52、R3）
`packages/web/src/components/assign-panel.tsx:974` — `AssignPanel`: 指派面板主入口容器组件，支持 step 2/3 及 mode 细粒度步骤与一体化渲染（AC 1、AC 3、E-108、E-52）
`packages/web/src/components/empty-onboarding.tsx:604` — `EmptyOnboarding`: 步骤 3 槽位接入 AssignPanel 逐任务指派正文（R2、E-108）
`packages/web/src/components/empty-onboarding.tsx:664` — `EmptyOnboarding`: 步骤 4 槽位接入 AssignPanel 并发审计与瓶颈归因说明（R2、E-52）
`packages/web/src/features/run-deck/run-deck-view.tsx:198` — `RunDeckView`: 零运行空态挂载就绪任务清单与 Agent 配置进入真实引导流程（R2、E-108）
`packages/web/src/app/app.tsx:25` — `App`: 路由装配挂接运行甲板与配对容器，实现从认证到零运行流程真实贯通（R2）
`packages/web/test/assign-panel.test.ts:32` — 单元测试：逐任务指派与点回改、会话序号独立显示、并发三者取 min 与瓶颈说明、单 Agent 满额顺延别家、R1 缺失显示「—」、R2 槽位接入、R3 focus-visible 与 44px 触控保障（AC 1-4, E-108, E-31, E-47, E-52, R1-R3）
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
1. 逐行独立配置而非整批套用（AC 1、E-108）：否决了「整批一键统一套用模型」的快捷开关或批量指派按钮。四步引导的核心是「逐任务独立治理」，批量套用会破坏对每个任务模型与思考强度的精细权衡，且容易导致不支持思考强度的 Agent 被批量误赋参数。界面坚持每行独立配置，已指派行可随时点击「修改指派」回改。
2. 同 Agent 多次指派的独立会话语义（AC 2、E-31、决策 7、决策 52）：同一个 Agent 被指派给多个任务时，必须作为相互隔离的独立 session 执行。前端在指派阶段即为每个 Agent 动态计算并展示将分配的会话序号（`会话 #1`、`会话 #2`），使用户在派发前便明确认知它们是独立的运行流与工作树，避免误以为同一 Agent 执行会共享上下文或产生写竞争。
3. 单 Agent 满额空位顺延机制（AC 4、E-47）：当某一 Agent 被指派的任务数达到其 `maxConcurrency`（如 2/2）时，界面仅在该 Agent 关联的编辑行和选项中提示满额排队，绝不在全局阻塞其他未指派任务或将其他 Agent 设为不可用。并行窗口的空余槽位必须向其他仍有容量的 Agent（如 Grok、Claude）开放，防止调度器因单一 Agent 满额而空转等待。
4. 并发瓶颈明确归因与越界显式解锁（AC 3、E-52）：有效并发容量取 `min(窗口数, 每 agent 上限, 用户设定)`，但 UI 不能只给出一个单薄的放行数字。展示层必须三因子并列对比并用醒目标签指出具体瓶颈来源（拓扑窗口、Agent 配额还是用户设置）。用户设定允许向下调，但向上超过拓扑窗口数时必须显式勾选解锁框并展示后果提示——因为超过依赖拓扑窗口数并不会带来物理加速，反而会误导用户对并发能力的预期。
5. M4-T6 思考强度与缺省值展示纪律（E-254、E-34、E-41）：不支持思考强度的 Agent（如 Grok）强制置 `null` 并在界面显示 `—`，严禁出于「UI 对齐」或「避免空值」而伪造 `low`/`medium` 默认档，切换 Agent 时立即重置模型覆盖。所有 daemon 下发指标在数据未就绪或为 `null` 时一律显示 `—`，严格杜绝前端私自补 `0` 冒充有效状态。
6. 严格剥离组件层业务补算（返工 R1）：彻底删除组件层对默认 Agent、默认模型、思考强度、会话序号累加、Agent 容量占用遍历统计、用户并发偏好补 2 缺省以及越界算术判定的补算逻辑。展示层严格遵循 07 节约定，纯粹依赖 daemon 与 feature 下发的明确字段；任何字段缺失时统一回退展示 `—`，越界判定与警告完全依据 `isExceedingWindow` 字段，严禁在前端推演业务状态。
7. 无障碍与触控目标合规（返工 R3）：全部交互控件移除无替代的 `outline-none`，统一补齐产品级 `focus-visible:outline-none focus-visible:shadow-[0_0_0_3px_var(--needs-soft)]` 焦点环；在手机触控端，所有操作按钮（修改、重置、确认、取消、设定调节、解锁复选框等）均满足 `min-h-[44px]` 或 `min-w-[44px]` 的 44×44px 物理触控底线。
<!-- notes:end -->

---

栈分支 `task/M9-T18` @ `agent-scheduler`　落地进 `main`
