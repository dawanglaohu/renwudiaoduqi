---
type: task
id: M8-T5
module: M8
depends_on: [M8-T3]
unlocks: [M7-T5, M9-T13]
edges: [E-121, E-177, E-178, E-179, E-180, E-36]
est_days: 2
branch: task/M8-T5
status: todo
---

# M8-T5　重派、换 agent 与原样重跑

- 所属模块：[[M8]]　**派发调度**——决定什么时候把哪个任务、用哪个 agent 与哪个模型、以什么权限派出去，并按批次、并行窗口、每 agent 并发与路径冲突限流，在三个闸门处停或放行、失败或重派时重新入队；判定本批分支是否都进了 HEAD、触发收口运行、按收口报告派修复运行与管理收口轮次也归它。凡涉及顺序、名额、放行、重派的都归它
- 前置依赖：[[M8-T3]]
- 完成后解锁：[[M7-T5]]、[[M9-T13]]

## 输入 / 产出

- **输入**：M8-T3 的派发路径
- **产出**：`POST /runs/:id/rerun`、换 agent 重派

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) 换 agent 重派时新建运行记录、旧运行转只读留存
默认开干净 worktree，除非显式选「在原改动上继续」（[[E-121]]）
2) 「原样重跑」严格复用原派发载荷（任务／agent／模型／批次／权限档），**不出现任何选择器**，需一次确认（[[E-177]] 的服务端侧）
3) 重跑时原 agent 不在线则立即失败并明示，**禁止静默回退到别的 agent 或模型**（[[E-178]]）
4) 文档快照已变更时禁止重跑并提示去桌面端处理（[[E-180]]）
5) 所属批次已被超越时允许重跑但提示批次错位（[[E-179]]）
6) 模型名在派发时已失效时不做前置白名单校验、直接透传，失败后标「派发失败·模型无效」且该任务不占用窗口名额（[[E-36]]）

## 必须处理的边界

- [[E-121]]　换 agent 重派
    - 触发：用户判断当前 agent 不合适
    - 期望：新建运行记录，旧运行转只读留存；默认开一个干净 worktree，除非用户显式选「在原改动上继续」
- [[E-177]]　手机重跑撞上已在跑
    - 触发：该任务已从桌面端重派或已重新在跑
    - 期望：按钮置灰 + 幂等键拦截，绝不产生第二次运行
- [[E-178]]　重跑时原 agent 不在线
    - 触发：原派发载荷指向的 agent 不可达
    - 期望：快速失败并明示「该 agent 不在线」；**禁止静默回退到别的 agent 或别的模型**——那等于替用户做了一次未授权的编排选择
- [[E-179]]　批次错位重跑
    - 触发：该任务所属批次已被后续批次超越
    - 期望：允许重跑但横幅提示批次错位（如「批次 3 已开始，本条属批次 2」）；单人自用下允许 + 告警优于阻断
- [[E-180]]　拿旧快照盲跑
    - 触发：决策 13 的三个标记显示该任务文档快照已变更
    - 期望：手机端禁用重跑并提示去桌面端处理
- [[E-36]]　模型名在派发时已失效
    - 触发：用户在别处删了该模型，或手填了不存在的名字
    - 期望：不做前置白名单校验（清单本就可能不全），直接透传；失败后标「派发失败·模型无效」并原样展示 agent 报错，该任务不占用批次窗口名额

## 代码位置

<!-- code:begin -->
_（**落地前必须回填**。一行一处，格式：`路径:行号` — 说明）_
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
_（**落地前必须回填**：怎么实现的、为什么这么选、踩了什么坑）_
<!-- notes:end -->

---

栈分支 `task/M8-T5` @ `agent-scheduler`　落地进 `main`
