---
type: task
id: M9-T15
module: M9
depends_on: [M9-T4, M2-T2]
unlocks: [M9-T25, M9-T26]
edges: [E-06, E-09, E-125, E-127, E-227, E-228, E-229]
est_days: 2
branch: task/M9-T15
status: done
---

# M9-T15　设置页：设备、配对与浏览器模式

- 所属模块：[[M9]]　**客户端界面**——两端共用的那一套界面本身：多流监看、会话查看、审批与闸门、设置与配对、落地清单、按批次折叠的批次树与执行行呼吸灯、按窗口数呈现的任务流水线泳道与「会话已归档」历史行、顶栏两个流水线开关，以及同一套代码的窄屏降级。它只通过 M2 的契约与 daemon 通信，**界面里不含任何业务判定**（折叠默认值、「能否收口」、泳道分配、阶段推进与会话归档都是 daemon 给的字段）；指派面板的分组模型下拉、登录态徽标与刷新按钮、参照条的「来源」段同样只呈现 daemon 下发的字段，不算取值链
- 前置依赖：[[M9-T4]]、[[M2-T2]]
- 完成后解锁：[[M9-T25]]、[[M9-T26]]

## 输入 / 产出

- **输入**：M2-T2 的配对接口
- **产出**：`settings-devices-page`、`pair-page`、`shell-bridge` 降级

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) 配对失败报到具体环节（「扫到码但连不上 host:port」）并提供手填地址兜底（[[E-06]]）
2) 已配对设备可列出并单独吊销，吊销后该设备连接立即断开（[[E-127]]）
3) 保存设备令牌而非 IP
PC 局域网 IP 变化后已授权设备不需重新授权（[[E-09]]）
4) 允许配对多台设备，每台独发一枚可单独吊销的令牌（[[E-125]]）
5) 无壳时令牌落 `sessionStorage`，顶栏常驻提示同时写明「关闭标签后需重新配对」**与「本模式下没有系统通知」**（[[E-229]]）
6) 壳存在时以壳安全存储为唯一真相源，启动清掉会话副本，**绝不双写**（[[E-227]]）
7) 同一浏览器第二个标签需重新配对，设备名默认带「浏览器 · 时间」便于辨认与吊销（[[E-228]]）

## 必须处理的边界

- [[E-06]]　同网段但连不上
    - 触发：手机与 PC 不同网段，或 WiFi 开了 AP 隔离
    - 期望：配对失败报到具体环节（「扫到码但连不上 host:port」），并提供手填地址兜底
- [[E-09]]　PC 局域网 IP 变化
    - 触发：DHCP 续租或切换网络
    - 期望：手机保存设备令牌而非 IP；重连走重新发现或提示重扫码，已授权设备不需重新授权
- [[E-125]]　配对多台设备
    - 触发：手机 + 平板 + 第二台电脑
    - 期望：允许，每设备独发一枚可单独吊销的令牌，仍不引入用户实体
- [[E-127]]　设备丢失或令牌泄露
    - 触发：手机丢了
    - 期望：桌面端能列出已配对设备并一键吊销；吊销后该设备的活动连接立即断开
- [[E-227]]　先浏览器后装壳
    - 触发：壳存在但 sessionStorage 里有旧令牌
    - 期望：以**壳的安全存储为唯一真相源**，启动时清掉会话副本，**绝不双写**
- [[E-228]]　同一浏览器开第二个标签
    - 触发：sessionStorage 按标签隔离
    - 期望：第二个标签需重新配对；设备名默认带「浏览器 · 时间」以便在设备列表里辨认并批量吊销，避免设备列表被脏条目淹没
- [[E-229]]　浏览器模式的能力缺失
    - 触发：无壳运行
    - 期望：顶栏提示除「关闭标签后需重新配对」外，**必须同时写明「本模式下没有系统通知」**，否则用户会以为提醒功能坏了

## 代码位置

<!-- code:begin -->
`packages/web/src/shell/shell-bridge.ts:16` — 本会话设备身份的会话存储键（非凭证，E-127）
`packages/web/src/shell/shell-bridge.ts:34` — 原生壳适配器接入时再清一次会话副本（令牌 + 设备身份），杜绝双写（E-227）
`packages/web/src/shell/shell-bridge.ts:86` — 模块加载即清会话副本，壳存在时以此为准（E-227）
`packages/web/src/shell/shell-bridge.ts:97` — tokenStore 三方法在壳模式只走 nativeAdapter 并直接返回，无任何会话存储读写（E-227）
`packages/web/src/shell/shell-bridge.ts:211` — 免壳浏览器模式判定能力（E-229）
`packages/web/src/shell/shell-bridge.ts:219` — 记住 claim 返回的 deviceId，供「当前设备」标注与自吊销跳转（E-127）
`packages/web/src/shell/shell-bridge.ts:229` — 读取本会话设备身份，壳模式回退为空（E-227）
`packages/web/src/features/pairing/use-pairing.ts:45` — 浏览器设备名的时间戳格式（E-228）
`packages/web/src/features/pairing/use-pairing.ts:58` — 默认设备名生成规则，浏览器模式自动注入时间戳（E-228）
`packages/web/src/features/pairing/use-pairing.ts:71` — 从生效的 baseUrl 解析 host:port，供失败环节上报（E-06）
`packages/web/src/features/pairing/use-pairing.ts:144` — 经 httpClient 调 POST /api/v1/pair/claim（auth: none）
`packages/web/src/features/pairing/use-pairing.ts:151` — 配对成功后令牌按运行环境落壳存储或会话存储（E-227、E-229）
`packages/web/src/features/pairing/use-pairing.ts:154` — 记住本会话的 deviceId（E-127）
`packages/web/src/features/pairing/use-pairing.ts:176` — 配对网络故障报告具体环节与目标 host:port，并展开手填兜底（E-06）
`packages/web/src/features/pairing/pairing-view.tsx:31` — 字段错才给输入框 aria-invalid / aria-describedby（07 节错误体系）
`packages/web/src/features/pairing/pairing-view.tsx:42` — 浏览器模式常驻告知关闭标签需重配且无系统通知（E-229）
`packages/web/src/features/pairing/pairing-view.tsx:66` — 带具体 host:port 的配对失败就地 notice，不整页替换（E-06）
`packages/web/src/features/pairing/pairing-view.tsx:141` — 手填调度服务地址兜底入口（E-06、E-09）
`packages/web/src/features/pairing/pairing-container.tsx:17` — 配对视图容器层，仅限布局样式（07-前端架构）
`packages/web/src/features/settings-devices/use-settings-devices.ts:87` — 本会话设备身份只读，决定「当前设备」与自吊销路径（E-127）
`packages/web/src/features/settings-devices/use-settings-devices.ts:96` — 加载已授权设备清单（GET /api/v1/devices，E-127）
`packages/web/src/features/settings-devices/use-settings-devices.ts:125` — 生成临时配对码（POST /api/v1/pair/code，E-125）
`packages/web/src/features/settings-devices/use-settings-devices.ts:157` — 配对码倒计时按服务端 expiresAt 走（E-125）
`packages/web/src/features/settings-devices/use-settings-devices.ts:198` — 吊销设备（DELETE /api/v1/devices/:deviceId）与自吊销时清令牌回 #/pair（E-127）
`packages/web/src/features/settings-devices/browser-mode-banner.tsx:13` — 浏览器模式常驻提示条，明确标签重配与无系统通知限制（E-229）
`packages/web/src/features/settings-devices/device-revoke-dialog.tsx:14` — 吊销设备二次确认模态窗（白名单弹窗、E-127）
`packages/web/src/features/settings-devices/device-list-view.tsx:39` — 挂载浏览器模式常驻提示（E-229）
`packages/web/src/features/settings-devices/device-list-view.tsx:143` — 设备列表、状态徽标、浏览器标签识别与逐设备吊销（E-125、E-127、E-228）
`packages/web/src/features/settings-devices/device-list-view.tsx:220` — 局域网 IP 与连接地址维护区，解耦令牌与物理 IP（E-09）
`packages/web/src/features/settings-devices/settings-devices-container.tsx:15` — 设备设置页容器层，仅限布局样式（07-前端架构）
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
- 令牌存储边界：令牌绝对不进入 `localStorage`。`localStorage` 跨同源所有标签页持久共享，一旦写入会导致标签页间会话边界失效，破坏无用户模型的单设备鉴权语义；无壳时严格写入 `sessionStorage` 保证标签页隔离，装壳后以原生密钥库为唯一真相源，启动即清空会话副本避免产生双写。
- 浏览器设备名默认值：免壳模式下用户开启新标签页会产生独立的会话配对诉求，若设备名均默认为空白或固定值，桌面端设备列表将充斥大量无法区分的无效条目；按时间戳生成「浏览器 · MM-DD HH:mm」让用户可在管理列表中一眼定位具体标签并精准吊销。
- 令牌与网络 IP 解耦：鉴权凭证完全收敛于 Authorization 头中的 Bearer 令牌，服务端不将客户端 IP 作为授权主体；当局域网 DHCP 续租或客户端漫游导致 PC 局域网 IP 变动时，仅需更新请求目标 BaseURL，已有设备令牌持续有效，无需重新申请配对码。
- 配对异常诊断定位：移动端或跨网段访问最常见故障为路由器开启 AP 隔离或子网掩码不同，此时配对码本身并未失效；捕获连接错误后直接报出「扫到码但连不上 host:port」并自动展开手填输入框，防止用户误判为配对码过期而在服务端频繁无谓重置。
- 本会话设备身份：`POST /pair/claim` 返回的 `deviceId` 必须留下，否则设备列表认不出「当前设备」，吊销自己这台时也要等下一次请求 401 才断开。它与令牌分开存：令牌进壳密钥库或 sessionStorage 的 `agsched.token`，设备身份另存 `agsched.current_device_id`，壳模式启动时与令牌一起清掉——身份不是凭证，但同样不留在会话副本里。
<!-- notes:end -->

---

栈分支 `task/M9-T15` @ `agent-scheduler`　落地进 `main`
