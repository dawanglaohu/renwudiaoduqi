---
type: task
id: M3-T1
module: M3
depends_on: [M1-T2]
unlocks: [M3-T2]
edges: [E-16, E-17, E-246, E-247, E-79, E-82]
est_days: 1.5
branch: task/M3-T1
status: todo
---

# M3-T1　docs-data.js 解析与内容指纹

- 所属模块：[[M3]]　**文档接入**——把 `docs-data.js` 变成本产品唯一的任务真相源（任务、依赖、批次、并行窗口、验收标准、边界编号、实施与审查两段提示词），并负责内容指纹、派发时快照与文档重出后的三个标记。凡「这条数据来自文档」的都归它
- 前置依赖：[[M1-T2]]
- 完成后解锁：[[M3-T2]]

## 输入 / 产出

- **输入**：一份 unattended-run 1.1.0 实际导出的 `docs-data.js`；09 节输入协议
- **产出**：`service/docs.ts` 的唯一解析适配器、`domain/layer-of.ts`（批次分层纯函数）、`repo/documents.ts`

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) 强制按 UTF-8 读取，去头尾 `JSON.parse`，不启用 JS 引擎
中文 Windows 默认编码下不乱码（[[E-16]]）
2) 校验 schemaVersion=1 与任务包的三处哈希一致
按任务 ID 排序的 id/contractHash 列表算指纹，任务行不变而范围、条款或提示词编译器变化时仍判变更，**不得使用 generated 或 mtime**（[[E-17]]）
3) 指纹相同且仅时间戳或 ready/reasons 变化时不设变更标记、不改运行状态，但仍刷新文档复核元数据（[[E-79]]）
4) 解析失败、必需字段缺失、协议或哈希不一致、文件被删时保留全部既有记录与快照，置 `is_source_readable=0` 并冻结新派发，**不清空任何数据**（[[E-82]]）
5) **批次由本产品按 `deps` 拓扑分层算出**（层号 = 最长前驱链长度，无前驱为 0，显示号 = 层号 +
1），规则与阅读器逐字一致且收敛在这一个模块内
上游日后真导出 `batchNo` 时只改这一个函数（[[E-246]]）
6) 阅读器那条窗口数 localStorage 键**不存在也不读**，本产品一律用自己的 `documents.lane_count` 设置（[[E-247]]）

## 必须处理的边界

- [[E-16]]　docs-data.js 编码读错
    - 触发：在中文 Windows 上按系统默认编码读取
    - 期望：强制按 UTF-8 读取（生成时 ensure_ascii=False）；格式为 `window.DOCS = 〈JSON 字面量〉;` 去头去尾 JSON.parse，不需 JS 引擎
- [[E-17]]　漏判或误判文档变更
    - 触发：重建仅改变时间戳，或任务行不变但范围、条款、提示词编译器已变
    - 期望：校验 schemaVersion=1 与任务包哈希一致性；对按任务 ID 排序的 id/contractHash 列表算指纹，识别任务增删和契约变化，generated 与 mtime 不作变更依据
- [[E-246]]　上游日后导出了 batchNo
    - 触发：`build_docs.py` 改了 payload
    - 期望：只改适配器里那一个分层函数，其余不动（分层规则收敛在唯一适配器模块内）
- [[E-247]]　阅读器里那条键根本不存在
    - 触发：用户从没点过窗口数按钮（最常见）
    - 期望：无可沿用，也无需任何特殊分支——本产品一律用自己的设置项
- [[E-79]]　文件变了但任务契约没变
    - 触发：id/contractHash 指纹相同，仅重建时间戳、mtime 或契约复核 ready/reasons 变化
    - 期望：不产生变更标记、不弹横幅、不改任务运行状态；成功读取时仍刷新文档复核元数据，允许已复核的同版任务解除新派发限制
- [[E-82]]　文档源不可读或任务包不可派发
    - 触发：文件被删、解析失败、缺协议字段或版本不一致；或合法任务包的 ready=false
    - 期望：源不可读时保留全部记录与快照、提示原因并冻结新派发；合法但待契约复核时仅阻止该任务派发并说明 reasons，不清空数据、不视为代码验收失败

## 代码位置

<!-- code:begin -->
- `packages/daemon/src/domain/layer-of.ts:9` — `layerOf()` 按依赖拓扑计算层号，成环就地截断，忽略幽灵依赖（E-241、E-242、E-244、E-246）
- `packages/daemon/src/domain/layer-of.ts:44` — `batchNoOf()` 将层号转换为显示批次号（层号 + 1）（E-246）
- `packages/daemon/src/domain/docs-fingerprint.ts:13` — `canonicalizeDocsFingerprintPayload()` 跨平台 UTF-16 二进制排序规范化载荷，零外部 crypto 依赖（R2）
- `packages/daemon/src/domain/docs-fingerprint.ts:25` — `computeDocsFingerprint()` 接收注入 hasher 计算指纹，彻底解耦加密库（R2、E-17、E-79）
- `packages/daemon/src/repo/documents.ts:161` — `createDocumentsRepo()` 只收发 snake_case DocumentRow，prepare 与执行原生异常均包为 AppError（R3）
- `packages/daemon/src/repo/documents.ts:195` — `insert()` 校验并写入 snake_case DocumentRow，约束 lane_count 1–6（R3、E-247）
- `packages/daemon/src/repo/documents.ts:239` — `markSourceUnreadable()` 在输入损坏时将 is_source_readable 置 0，保留所有既有记录（R1、R3、E-82）
- `packages/daemon/src/service/docs.ts:96` — `mapDocumentRow()` 在 service 层单点执行 snake_case Row 向 camelCase DocumentRecord 的转换（R3）
- `packages/daemon/src/service/docs.ts:113` — `assertValidEffectivePath()` 严格校验 1.1.0 effectivePaths 非空相对路径，拦截绝对路径与 traversal（R1）
- `packages/daemon/src/service/docs.ts:157` — `parseDocsDataContent()` 严格校验 schemaVersion=1、唯一任务 ID、必填字段、三处非空哈希一致与 ready/reasons 形状（R1、E-16、E-17）
- `packages/daemon/src/service/docs.ts:514` — `parseDocsDataFile()` 强制按 UTF-8 读取文件，将底层读取异常包为类型化 E_DOC_SOURCE_UNREADABLE（E-16、E-82）
- `packages/daemon/src/service/docs.ts:545` — `importDocument()` 编排接入与指纹比对，输入损坏时保留记录并置不可读（R1、R3、E-79、E-82、E-247）
- `packages/daemon/test/unit/layer-of.test.ts:7` — 单元测试覆盖拓扑分层、成环截断、幽灵任务过滤、无依赖全批次 1 以及与真实 78 任务的逐字对齐（E-241、E-242、E-244、E-246）
- `packages/daemon/test/unit/docs-fingerprint.test.ts:11` — 单元测试验证 domain 零 crypto 依赖，并用固定已知摘要锁定规范字节串（R2）
- `packages/daemon/test/unit/docs-import.test.ts:181` — 单元测试严格覆盖唯一 ID、三处非空哈希、ready/reasons 形状与 effectivePaths 路径规则（R1）
- `packages/daemon/test/unit/docs-import.test.ts:389` — 负向测试覆盖已有文档后输入损坏时保留全部既有记录并置 is_source_readable=0（R1）
- `packages/daemon/test/unit/docs-import.test.ts:489` — 负向测试验证 repo/documents 只收发 snake_case Row 且异常在边界包成 AppError（R3）
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
`docs-data.js` 的解析采用正则剥离 `window.DOCS = ` 前缀和尾部分号后直通 `JSON.parse`，解析前剥离 UTF-8 BOM（`\uFEFF`）。对文档进行严格校验：任务 ID 必须全局唯一且非空；任务标题、模块、依赖数组与验收标准均非空必填；三处契约哈希（dispatch.contractHash、contracts.hash、readiness.contractHash）必须全为非空字符串且逐字相等；ready 必须为原生布尔值，reasons 必须为字符串数组；1.1.0 的 effectivePaths 必须为非空数组且每个路径严格限制为相对规范路径，彻底拦截遍历符 `..`、根绝对路径与空字符。任一不符合均抛出 `E_DOC_SOURCE_UNREADABLE`。

`domain/docs-fingerprint.ts` 彻底剥离对 `node:crypto` 的依赖，作为零依赖纯函数叶子节点。任务 ID 采用原生 UTF-16 二进制代码单元比较（`<`、`>`）实现跨平台跨语言确定性排序，生成规范 JSON 字节串。哈希计算（SHA-256）收敛在 service 层或由调用方依赖注入，并在单测中通过固定已知输入和摘要锁定规范字节与十六进制输出。

`repo/documents.ts` 遵从分层契约，严格只收发与数据库列名一一对应的 snake_case `DocumentRow`，所有 camelCase 到 `DocumentRecord` 的双向转换移至 `service/docs.ts`。repo 内部的所有 `prepare`、`run`、`get`、`all` 调用全部经 `toDatabaseError` 在边界包装为携带原生错误 cause 的类型化 `AppError`（如 UNIQUE 约束冲突转为包含原生 SQLite 错误 cause 的 `E_INTERNAL`，非法 `lane_count` 校验转为 `E_VALIDATION`）。

对于已有文档发生输入损坏（包括 JSON 语法错误、文件丢失、任务字段缺失、ID 重复、哈希不一致、路径越界等），service 层在抛出 `E_DOC_SOURCE_UNREADABLE` 的同时，通过 repo 立即将对应记录的 `is_source_readable` 置 0 并刷新 `last_seen_at`，完整保留数据库中所有既有文档、任务与快照数据，杜绝级联删除与脏覆盖。
<!-- notes:end -->

---

栈分支 `task/M3-T1` @ `agent-scheduler`　落地进 `main`
