---
type: task
id: M3-T1
module: M3
depends_on: [M1-T2]
unlocks: [M3-T2]
edges: [E-16, E-17, E-246, E-247, E-79, E-82]
est_days: 1.5
branch: task/M3-T1
status: todo
---

# M3-T1　docs-data.js 解析与内容指纹

- 所属模块：[[M3]]　**文档接入**——把 `docs-data.js` 变成本产品唯一的任务真相源（任务、依赖、批次、并行窗口、验收标准、边界编号、实施与审查两段提示词），并负责内容指纹、派发时快照与文档重出后的三个标记。凡「这条数据来自文档」的都归它
- 前置依赖：[[M1-T2]]
- 完成后解锁：[[M3-T2]]

## 输入 / 产出

- **输入**：一份 unattended-run 1.1.0 实际导出的 `docs-data.js`；09 节输入协议
- **产出**：`service/docs.ts` 的唯一解析适配器、`domain/layer-of.ts`（批次分层纯函数）、`repo/documents.ts`

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) 强制按 UTF-8 读取，去头尾 `JSON.parse`，不启用 JS 引擎
中文 Windows 默认编码下不乱码（[[E-16]]）
2) 校验 schemaVersion=1 与任务包的三处哈希一致
按任务 ID 排序的 id/contractHash 列表算指纹，任务行不变而范围、条款或提示词编译器变化时仍判变更，**不得使用 generated 或 mtime**（[[E-17]]）
3) 指纹相同且仅时间戳或 ready/reasons 变化时不设变更标记、不改运行状态，但仍刷新文档复核元数据（[[E-79]]）
4) 解析失败、必需字段缺失、协议或哈希不一致、文件被删时保留全部既有记录与快照，置 `is_source_readable=0` 并冻结新派发，**不清空任何数据**（[[E-82]]）
5) **批次由本产品按 `deps` 拓扑分层算出**（层号 = 最长前驱链长度，无前驱为 0，显示号 = 层号 +
1），规则与阅读器逐字一致且收敛在这一个模块内
上游日后真导出 `batchNo` 时只改这一个函数（[[E-246]]）
6) 阅读器那条窗口数 localStorage 键**不存在也不读**，本产品一律用自己的 `documents.lane_count` 设置（[[E-247]]）

## 必须处理的边界

- [[E-16]]　docs-data.js 编码读错
    - 触发：在中文 Windows 上按系统默认编码读取
    - 期望：强制按 UTF-8 读取（生成时 ensure_ascii=False）；格式为 `window.DOCS = 〈JSON 字面量〉;` 去头去尾 JSON.parse，不需 JS 引擎
- [[E-17]]　漏判或误判文档变更
    - 触发：重建仅改变时间戳，或任务行不变但范围、条款、提示词编译器已变
    - 期望：校验 schemaVersion=1 与任务包哈希一致性；对按任务 ID 排序的 id/contractHash 列表算指纹，识别任务增删和契约变化，generated 与 mtime 不作变更依据
- [[E-246]]　上游日后导出了 batchNo
    - 触发：`build_docs.py` 改了 payload
    - 期望：只改适配器里那一个分层函数，其余不动（分层规则收敛在唯一适配器模块内）
- [[E-247]]　阅读器里那条键根本不存在
    - 触发：用户从没点过窗口数按钮（最常见）
    - 期望：无可沿用，也无需任何特殊分支——本产品一律用自己的设置项
- [[E-79]]　文件变了但任务契约没变
    - 触发：id/contractHash 指纹相同，仅重建时间戳、mtime 或契约复核 ready/reasons 变化
    - 期望：不产生变更标记、不弹横幅、不改任务运行状态；成功读取时仍刷新文档复核元数据，允许已复核的同版任务解除新派发限制
- [[E-82]]　文档源不可读或任务包不可派发
    - 触发：文件被删、解析失败、缺协议字段或版本不一致；或合法任务包的 ready=false
    - 期望：源不可读时保留全部记录与快照、提示原因并冻结新派发；合法但待契约复核时仅阻止该任务派发并说明 reasons，不清空数据、不视为代码验收失败

## 代码位置

<!-- code:begin -->
- `packages/daemon/src/domain/layer-of.ts:9` — `layerOf()` 按依赖拓扑计算层号，成环就地截断，忽略幽灵依赖（E-241、E-242、E-244、E-246）
- `packages/daemon/src/domain/layer-of.ts:44` — `batchNoOf()` 将层号转换为显示批次号（层号 + 1）（E-246）
- `packages/daemon/src/domain/docs-fingerprint.ts:14` — `computeDocsFingerprint()` 按任务 ID 排序并对 `[id, contractHash]` 算 SHA-256 指纹，排除 generated 与 mtime 干扰（E-17、E-79）
- `packages/daemon/src/repo/documents.ts:191` — `createDocumentsRepo()` 预编译 documents 表增删查改语句，限制 lane_count 1–6（E-247）
- `packages/daemon/src/repo/documents.ts:259` — `markSourceUnreadable()` 在文档不可读时置 `is_source_readable = 0`，不清空任何既有数据（E-82）
- `packages/daemon/src/service/docs.ts:79` — `parseDocsDataContent()` 剥离外壳并以 UTF-8 `JSON.parse`，校验 schemaVersion=1、必需字段与三处哈希一致性（E-16、E-17）
- `packages/daemon/src/service/docs.ts:347` — `parseDocsDataFile()` 强制按 UTF-8 读取文件，文件缺失或损坏时包成类型化 `E_DOC_SOURCE_UNREADABLE`（E-16、E-82）
- `packages/daemon/src/service/docs.ts:376` — `importDocument()` 编排文档接入、指纹比对、无变更时仅刷新元数据、异常时保留既有快照并置不可读（E-17、E-79、E-82、E-247）
- `packages/daemon/test/unit/layer-of.test.ts:7` — 单元测试覆盖拓扑分层、成环截断、幽灵任务过滤、无依赖全批次 1 以及与真实 78 任务的逐字对齐（E-241、E-242、E-244、E-246）
- `packages/daemon/test/unit/docs-fingerprint.test.ts:6` — 单元测试覆盖排序无关性、契约哈希敏感性、任务增删探测与 generated/mtime 无关性（E-17、E-79）
- `packages/daemon/test/unit/docs-import.test.ts:181` — 单元测试覆盖中文 UTF-8 编码、BOM 剥离、三处哈希校验、ready=false 契约待复核、lane_count 维持与真实 docs-data.js 接入（E-16、E-17、E-79、E-82、E-247）
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
`docs-data.js` 的外壳剥离直接采用正则剥离 `window.DOCS = ` 前缀与尾部分号后 `JSON.parse`，杜绝调用 JS 引擎或 `eval` 带来的安全风险与环境依赖。在解析前显式剥除 UTF-8 BOM（`\uFEFF`），防止 Windows 编辑器保存的签名破坏 JSON 解析。

文档内容指纹严格基于任务 ID 升序排序的 `[id, contractHash]` 列表哈希，坚决不包含 `generated` 时间戳与文件 `mtime`。文档重建时若仅更新构建时间或由复核工具改变了 `ready/reasons`，指纹保持恒定，`hasChanged` 返回 false，既不产生虚假变更标记，也不打乱在途任务运行状态，同时仍将最新的 `last_seen_at` 与复核元数据持久化。

依赖拓扑分层规则收敛在 `domain/layer-of.ts`，实现与阅读器 `layerOf` 逐字对齐。遇环状依赖时沿递归调用栈检测截断为 0，避免死循环爆栈；遇到引用不存在的任务 ID 时在过滤层直接忽略，不影响有效任务拓扑。日后上游若导出 `batchNo`，只需定点调整 `layerOf` 函数，调度系统其余逻辑完全不受影响。

文档源不可读（文件丢失、格式损坏、schemaVersion 异常、三处哈希不一致）时，调度器必须保留数据库中既有的文档记录、历史快照与任务运行状态，仅置 `is_source_readable = 0` 阻断新任务派发并抛出 `E_DOC_SOURCE_UNREADABLE`。严禁在解析失败时清空既有数据或级联删除关联表。

并行窗口 `lane_count` 是本产品自有的调度配置，默认值为 2（约束在 1–6）。解析文档时坚决不读取阅读器的 localStorage 键，后续重新导入同份文档时也保留现有用户的 `lane_count` 设置，不被导入逻辑重置。
<!-- notes:end -->

---

栈分支 `task/M3-T1` @ `agent-scheduler`　落地进 `main`
