---
type: task
id: M1-T9
module: M1
depends_on: [M1-T3]
unlocks: [M1-T6, M1-T7, M6-T5]
edges: [E-119, E-132, E-209, E-269]
est_days: 2
branch: task/M1-T9
status: done
---

# M1-T9　三平台进程树与原生自启适配器

- 所属模块：[[M1]]　**运行底座**——让 daemon 在这台机器上稳定跑起来并把字节落到磁盘上：进程单实例与端口、启动自检、开机自启注册、数据目录、结构化存储的连接与迁移、日志正文的追加写与字节偏移索引、以及全部平台差异（进程树终止、路径规范化与长路径、配置目录、子进程编码）的唯一收口。凡与操作系统、磁盘、进程存活打交道而不带任何业务概念的都归它
- 前置依赖：[[M1-T3]]
- 完成后解锁：[[M1-T6]]、[[M1-T7]]、[[M6-T5]]

## 输入 / 产出

- **输入**：M1-T3 的 platform 契约、宿主路径输入与 `DaemonLaunchSpec`
- **产出**：`src/platform/windows.ts`、`darwin.ts`、`linux.ts` 与平台集成测试

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) Windows `killTree` 先 `taskkill /PID 〈pid〉 /T`，3 秒后仍存活再加 `/F`
macOS/Linux 对独立进程组先 `SIGTERM`，3 秒后仍存活再 `SIGKILL`，每次尝试都写事件（[[E-119]]）
2) 三平台实现同一 `autostart.register(spec)/status(spec)/unregister()` 契约，分别使用 Task Scheduler、LaunchAgent、`systemd --user`
完整保留 `file/args/cwd`，业务代码不直接判断平台（[[E-132]]、[[E-209]]）
3) 自启集成测试使用唯一隔离名称并在 `finally` 清理
残留即测试失败且输出人工删除命令（[[E-269]]）
4) 三平台之外返回 `E_PLATFORM_UNSUPPORTED`，不得静默套用相近实现

## 必须处理的边界

- [[E-119]]　三平台进程树残留
    - 触发：agent 派生了 node/git/npm 子进程
    - 期望：中止必须杀整棵进程树：Windows 先 `taskkill /T` 后按需 `/F`；macOS/Linux 对独立进程组先发 `SIGTERM`、3 秒后按需 `SIGKILL`。只杀父进程会留下占文件锁的孤儿进程
- [[E-132]]　平台分支散落各处
    - 触发：业务代码直接判断 `process.platform`，或某一宿主静默走另一宿主逻辑
    - 期望：一律收口到单一 `platform` 契约及 Windows/macOS/Linux 三个适配器（进程树终止、自启、配置目录、路径与可执行文件解析）；三平台之外返回 `E_PLATFORM_UNSUPPORTED`
- [[E-209]]　升级后自启项指向旧路径
    - 触发：安装位置、macOS `.app` 位置或 Node 脚本参数变化，原生自启项仍引用旧启动方式
    - 期望：壳每次启动根据实际安装位置解析完整 `DaemonLaunchSpec`；`register` 幂等接收该冻结对象，逐字段校验原生注册项与当前绝对 `file`、`args[]`、绝对 `cwd`，不一致则重写；桌面按钮和自启共用同一对象且恒不经 shell，安装包不得预写固定绝对路径；重写失败降级为「需手动启动」并给出该平台可复制命令
- [[E-269]]　自启集成测试留下真实注册项
    - 触发：测试中断或清理失败
    - 期望：使用带唯一测试标识的隔离自启项并在 `finally` 清理；清理失败必须使测试失败并输出残留项与人工删除命令

## 代码位置

<!-- code:begin -->
- `packages/daemon/src/platform/contract.ts:53` — `PlatformAdapter` 把路径、自启和进程树终止收口为业务层使用的单一平台契约（E-132）
- `packages/daemon/src/platform/host.ts:92` — `createPlatformAdapter()` 是唯一平台选择点，绑定自启名称与注入依赖；三平台之外返回 `E_PLATFORM_UNSUPPORTED`
- `packages/daemon/src/platform/kill-tree-posix.ts:16` — `posixKillTree()` 对独立进程组执行 `SIGTERM`、3 秒探活与按需 `SIGKILL`，每次尝试及最终结果均发出事件（E-119）
- `packages/daemon/src/platform/windows.ts:229` — `windowsKillTree()` 执行 `taskkill /PID <pid> /T`、3 秒探活与按需追加 `/F`，命令、探活、时钟和等待均由调用方注入（E-119）
- `packages/daemon/src/platform/autostart-contract.ts:30` — 三平台共用 `register(spec)/status(spec)/unregister()`，并提供保留 `file/args/cwd` 的手动启动命令和残留项删除命令
- `packages/daemon/src/platform/windows.ts:284` — Task Scheduler 适配器用 UTF-8 XML 保存原生 `Command/Arguments/WorkingDirectory`，逐字段读取比较并在变化时以 `/F` 重建（E-209）
- `packages/daemon/src/platform/darwin.ts:127` — LaunchAgent 适配器交叉核对 `launchctl` 与 plist；重写前先卸载旧任务，卸载失败时保留旧 plist 并返回类型化错误（E-209）
- `packages/daemon/src/platform/linux.ts:137` — `systemd --user` 适配器交叉核对 `is-enabled` 与 unit 正文，`enable --now` 失败时执行 disable、删 unit 和 daemon-reload 回滚（E-209）
- `packages/daemon/test/unit/kill-tree.test.ts:22` — 单元测试锁定 POSIX 与 Windows 的软终止、3 秒等待、探活、强制升级和事件顺序
- `packages/daemon/test/platform/autostart.test.ts:105` — 三平台契约测试覆盖特殊参数逐字段往返、幂等、三字段分别变化后的重写、原生字段篡改、失败回滚与 unsupported 分支
- `packages/daemon/test/platform/kill-tree-native.test.ts:13` — 原生测试启动父子进程树，Windows 验证真实 `/T`→`/F`，POSIX 验证真实进程组 `TERM`→`KILL`，并确认父子 PID 都退出（E-119）
- `packages/daemon/test/platform/autostart-native.test.ts:31` — 原生自启测试使用 PID+UUID 隔离名称，在 `finally` 注销并复查残留；失败时输出可复制的人工删除命令（E-269）
- `packages/shared/src/errors/codes.ts:50` — 登记自启 unsupported、注册失败和注销失败三类共享错误码
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
平台适配器只决定策略和原生格式，不直接拥有子进程、文件系统、时钟或等待。调用方注入 `KillTreeProcessOps` 与 `AutostartDependencies`；M1-T7 的 proc 层负责实现原生进程操作，后续 boot/壳接线只消费同一个 `PlatformAdapter`。这样 `process.platform` 只出现在 `host.ts`，`child_process` 与 `process.kill` 不会从 proc 层泄漏到生产 platform 代码。

自启状态以原生字段为准，嵌入的 base64 `agent-scheduler-spec` 只负责无损携带完整参数：Windows 读取任务 XML 的 `Command/Arguments/WorkingDirectory`，macOS 读取 plist 的 `ProgramArguments/WorkingDirectory`，Linux 要求 unit 正文与从标记恢复的 spec 重新生成结果完全一致。`status()` 还要同时确认 Task Scheduler、`launchctl` 或 `systemctl --user` 的注册状态，不能让标记掩盖原生字段篡改或已被用户禁用的任务。

Windows 的 `taskkill` 退出码 128 不是单义值：PID 不存在和非强制阶段因后代仍运行而失败都可能返回 128，原生操作实现必须结合 stderr 区分，不能一律当成已终止。原生回归使用会派生后代且拒绝软终止的父进程，确保 `/T` 后的探活会进入 `/F`，并核对父子 PID 均退出。

注册失败会携带保留 `file/args/cwd` 的平台可复制手动启动命令；原生测试另持有残留项删除命令。macOS 在覆盖 plist 前必须先成功卸载旧任务；Linux 的 `enable --now` 部分失败会继续尝试 disable、删除 unit 并 reload，任一回滚失败均返回类型化错误而不是静默吞掉。

验证记录（2026-09-08）：Windows 上真实 Task Scheduler 与父子进程树测试通过；WSL 的 Linux Node 下真实 POSIX 进程组测试与无 `systemd --user` 降级测试通过。macOS 原生用例由同一个 `test/platform` 文件在 macOS runner 执行；此记录不包含 macOS 本机结果，三平台发布矩阵仍由 M10-T5 的发布门禁负责。
<!-- notes:end -->

---

栈分支 `task/M1-T9` @ `agent-scheduler`　落地进 `main`
