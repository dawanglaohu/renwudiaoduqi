---
type: task
id: M1-T9
module: M1
depends_on: [M1-T3]
unlocks: [M1-T6, M1-T7, M6-T5]
edges: [E-119, E-132, E-209, E-269]
est_days: 2
branch: task/M1-T9
status: todo
---

# M1-T9　三平台进程树与原生自启适配器

- 所属模块：[[M1]]　**运行底座**——让 daemon 在这台机器上稳定跑起来并把字节落到磁盘上：进程单实例与端口、启动自检、开机自启注册、数据目录、结构化存储的连接与迁移、日志正文的追加写与字节偏移索引、以及全部平台差异（进程树终止、路径规范化与长路径、配置目录、子进程编码）的唯一收口。凡与操作系统、磁盘、进程存活打交道而不带任何业务概念的都归它
- 前置依赖：[[M1-T3]]
- 完成后解锁：[[M1-T6]]、[[M1-T7]]、[[M6-T5]]

## 输入 / 产出

- **输入**：M1-T3 的 platform 契约、宿主路径输入与 `DaemonLaunchSpec`
- **产出**：`src/platform/windows.ts`、`darwin.ts`、`linux.ts` 与平台集成测试

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) Windows `killTree` 先 `taskkill /PID 〈pid〉 /T`，3 秒后仍存活再加 `/F`
macOS/Linux 对独立进程组先 `SIGTERM`，3 秒后仍存活再 `SIGKILL`，每次尝试都写事件（[[E-119]]）
2) 三平台实现同一 `autostart.register(spec)/status(spec)/unregister()` 契约，分别使用 Task Scheduler、LaunchAgent、`systemd --user`
完整保留 `file/args/cwd`，业务代码不直接判断平台（[[E-132]]、[[E-209]]）
3) 自启集成测试使用唯一隔离名称并在 `finally` 清理
残留即测试失败且输出人工删除命令（[[E-269]]）
4) 三平台之外返回 `E_PLATFORM_UNSUPPORTED`，不得静默套用相近实现

## 必须处理的边界

- [[E-119]]　三平台进程树残留
    - 触发：agent 派生了 node/git/npm 子进程
    - 期望：中止必须杀整棵进程树：Windows 先 `taskkill /T` 后按需 `/F`；macOS/Linux 对独立进程组先发 `SIGTERM`、3 秒后按需 `SIGKILL`。只杀父进程会留下占文件锁的孤儿进程
- [[E-132]]　平台分支散落各处
    - 触发：业务代码直接判断 `process.platform`，或某一宿主静默走另一宿主逻辑
    - 期望：一律收口到单一 `platform` 契约及 Windows/macOS/Linux 三个适配器（进程树终止、自启、配置目录、路径与可执行文件解析）；三平台之外返回 `E_PLATFORM_UNSUPPORTED`
- [[E-209]]　升级后自启项指向旧路径
    - 触发：安装位置、macOS `.app` 位置或 Node 脚本参数变化，原生自启项仍引用旧启动方式
    - 期望：壳每次启动根据实际安装位置解析完整 `DaemonLaunchSpec`；`register` 幂等接收该冻结对象，逐字段校验原生注册项与当前绝对 `file`、`args[]`、绝对 `cwd`，不一致则重写；桌面按钮和自启共用同一对象且恒不经 shell，安装包不得预写固定绝对路径；重写失败降级为「需手动启动」并给出该平台可复制命令
- [[E-269]]　自启集成测试留下真实注册项
    - 触发：测试中断或清理失败
    - 期望：使用带唯一测试标识的隔离自启项并在 `finally` 清理；清理失败必须使测试失败并输出残留项与人工删除命令

## 代码位置

<!-- code:begin -->
- `packages/daemon/src/platform/kill-tree-posix.ts:12` — `posixKillTree(pid, opts)`：对独立进程组先发 SIGTERM、3 秒（KILL_TREE_GRACE_MS）后仍存活再 SIGKILL，每次尝试派发 KillTreeEvent（E-119）
- `packages/daemon/src/platform/kill-tree-posix.ts:56` — `signalGroup()`：用 `process.kill(-pid, signal)` 向整个进程组发信号；ESRCH→terminated、EPERM→not-process-owner
- `packages/daemon/src/platform/windows.ts:209` — `windowsKillTree(pid, opts)`：先 `taskkill /PID 〈pid〉 /T`，3 秒后仍存活再追加 `/F`；spawnImpl 可注入以便测试（E-119）
- `packages/daemon/src/platform/windows.ts:240` — `defaultWindowsTaskkill`：`shell:false + windowsHide:true` 直接 spawn taskkill.exe；exit 128 视为已终止，stderr 中的 Access denied 映射为 not-process-owner
- `packages/daemon/src/platform/autostart-support.ts` — `encodeSpec/decodeSpec/specsEqual/quoteForWindowsArgv/xmlEscape` 等三个平台共享的纯函数，spec 编码上岸成 file/cwd/JSON args 三行
- `packages/daemon/src/platform/windows.ts:446` — `buildWindowsTaskXml(spec)`：按 Windows Task Scheduler 1.2 格式生成注册 XML，Exec 动作逐字段保留 file/args/cwd，`XML Description` 内嵌完整 spec 供 E-209 逐字段比对
- `packages/daemon/src/platform/windows.ts:373` — `WINDOWS_AUTOSTART.register(status|unregister)`：`register` 幂等且 status 读取原注册项后逐字段匹配 spec，不一致才重写（E-209）；`unregister` 用 /Delete /F
- `packages/daemon/src/platform/darwin.ts:144` — `darwinAutostartRegister`：写入 plist 进 `~Library/LaunchAgents` 后用 `launchctl load -w` 激活；`unload` 失败不拆错，只是幂等前缀
- `packages/daemon/src/platform/darwin.ts:250` — `parseLaunchAgentPlist/parseEmbeddedSpec`：优先读 base64-embedded spec，回退解析 ProgramArguments/WorkingDirectory，供 status() 逐字段比对（E-209）
- `packages/daemon/src/platform/linux.ts:160` — `linuxAutostartRegister`：写 `~/.config/systemd/user/<name>.service` 到 `systemctl --user daemon-reload + enable --now`；失败时 rm 兜底并返回 E_AUTOSTART_REGISTER_DENIED / E_AUTOSTART_UNSUPPORTED（E-209、E-210、E-261）
- `packages/daemon/src/platform/linux.ts:280` — `parseSystemdUnit`：从 unit 文件尾部的 `# agent-scheduler-spec` 标记行恢复 spec 用于 matchesSpec（E-209），无需原生读 systemd 语言
- `packages/daemon/src/platform/darwin.ts:101`、linux:107 — `darwinKillTree/linuxKillTree = posixKillTree`
- `packages/daemon/test/unit/kill-tree.test.ts` — 单测 SIGTERM→SIGKILL 时序、事件派发、ESRCH/EPERM 両种終局
- `packages/daemon/test/integration/autostart-linux.test.ts` — 测试名字里带 `agent-scheduler-test-<pid>-<ts>` 唯一隔离串并在 afterAll 里尾部清理并打印删除命令（E-269）
- `packages/shared/src/errors/codes.ts` — 新增平台侧错误码：`E_AUTOSTART_UNSUPPORTED`、`E_AUTOSTART_REGISTER_DENIED`、`E_AUTOSTART_UNREGISTER_DENIED`，供 E-210 / E-261 / E-269 落地
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
两个关键设计选择：
1. **IDEMPOTENT：没有本地记录对不上状态**。E-209 要求升级后重写，但原生机制除了 systemd 都不提供“读取相面”的 API，所以三个适配器都在「应在的落盘物」里嵌入一份 `agent-scheduler-spec` 的尖锐标记：
   - Windows：任务 XML 的 `<Description>` 里加 `agent-scheduler-spec\n<encodeSpec>`
   - Darwin：plist 注释行里写 base64 编码后的 spec 字符串
   - Linux：systemd unit 文件尾部加一行 `# agent-scheduler-spec\n# <encodeSpec>`
   三个格式的编码函数都在 `autostart-support.ts` 里一平共享（一行：`file\ncwd\nJSON.stringify(args)`）； status() 读回原生内容时优先取这个标记，取不到才回退去解析原生子段字段。
2. **不拿 PID 去探活着**：E-119 说树死没死，但发起者未必是父进程。
   Windows 用 taskkill 的退出码区分「已被 taskkill 干掉」与「谈 process」； Linux 用 `kill(-pid, 0)` 探 liveness、`ESRCH` vs `EPERM` 区分 terminated vs not-process-owner，进程组独负这一扇门。
3. **macOS/status() 写入 failure 面临“双路回退”**：填写 plist 是江南 fs 写活，`launchctl load` 是江南系统 API； status() 只读取 plist 文件本身，不跟 `launchctl list` 发颖，所以 E-209 指定的不可复制升级重写仅在 plist 层面做， `launchctl` 报错在 register() 里报成 `E_AUTOSTART_REGISTER_DENIED`。
<!-- notes:end -->

---

栈分支 `task/M1-T9` @ `agent-scheduler`　落地进 `main`
