---
type: task
id: R13-T98191508
status: done
module: M2
source_batch: 13
stable_key: batch-dto-authoritative-summary
---

# R13-T98191508　让正式批次 DTO 下发计数和默认展开并在两端批次树呈现

> 由第 13 批收口记录 `batch-13-20260924` 的未通过问题生成；stable-key 去重，原任务与原收口记录不改写。

- 所属模块：[[M2]]
- 来源任务：[[M9-T19]]、[[M9-T25]]
- 前置依赖：[[M2-T8]]、[[M9-T19]]、[[M9-T25]]
- 严重级：S2
- Jev 裁决：model=jev-1.13.0 line=adjudicate: pick=B(low 0.41) spec_change=no(0.20) outside_stack=no(0.20) needs_owner=no(0.18) reversible=unsure(0.63) adopt=take(B)

## 输入 / 产出

- **输入**：正式 BatchDto 与 BatchService 不含 taskCount、landedCount、runningCount、waitingCount、defaultExpanded；批次树只能显示「—」且不能按服务端状态默认展开。对照 11-UI.md:381-386、M9-T19 AC1/2、E-272/E-284。
- **产出**：packages/shared/src/api/batches.ts、packages/daemon/src/service/batch.ts、packages/daemon/src/service/dispatch.ts、packages/web/src/features/run-deck/batch-expansion.ts、packages/web/src/features/run-deck/use-batch-tree.ts、packages/web/src/features/run-deck/run-deck-view.tsx、packages/web/src/features/task-list/task-list-container.tsx、packages/daemon/test/integration/batch-summary.test.ts、packages/web/test/batch-tree.test.tsx
- **有效路径**：`packages/shared/src/api/batches.ts`、`packages/daemon/src/service/batch.ts`、`packages/daemon/src/service/dispatch.ts`、`packages/web/src/features/run-deck/batch-expansion.ts`、`packages/web/src/features/run-deck/use-batch-tree.ts`、`packages/web/src/features/run-deck/run-deck-view.tsx`、`packages/web/src/features/task-list/task-list-container.tsx`、`packages/daemon/test/integration/batch-summary.test.ts`、`packages/web/test/batch-tree.test.tsx`

## 验收标准

1) 正式 GET batches 与 GET snapshot 的同一 BatchDto 带四个从权威任务状态派生的计数和基于 running/wrapping/awaiting_landing/needs_attention 的 defaultExpanded；2) 事务后读取与并发更新保持一致，空批次、人工停靠、跨批修复和未进 HEAD 分支各有断言；3) 真 daemon 导入文档后浏览器批次树显示实际数字和服务端默认展开，左栏与任务页一致，刷新和文档切换遵守 E-284；4) 补修复前失败的生产入口回归，跑 pnpm -w check。

## 必须处理的边界

[[E-272]]、[[E-284]]、[[E-298]]

## 代码位置

<!-- code:begin -->
`packages/shared/src/api/batches.ts:32` — BatchDto 契约扩充 taskCount/landedCount/runningCount/waitingCount 与 defaultExpanded
`packages/daemon/src/service/batch.ts:46` — isBatchDefaultExpanded 实现基于 running/wrapping/awaiting_landing/needs_attention 的默认展开状态判定
`packages/daemon/src/service/batch.ts:64` — computeBatchSummary 纯函数依据权威任务状态派生四项计数并忽略跨批修复在途运行
`packages/daemon/src/service/batch.ts:133` — toBatchDto 组装下发完整权威计数字段与默认展开字段
`packages/daemon/src/service/batch.ts:253` — getBatch 服务端实现装配批次统计和默认展开
`packages/daemon/src/service/batch.ts:282` — listBatches 服务端实现批量装配批次统计和默认展开
`packages/daemon/src/service/dispatch.ts:90` — dispatch 模块重导出与统一使用 batch 服务的 toBatchDto
`packages/daemon/src/service/dispatch.ts:934` — getSnapshot 聚合快照下发与 batches 路由一致的权威计数和默认展开
`packages/web/src/features/run-deck/batch-expansion.ts:141` — batch-expansion 响应 defaultExpanded 初始化展开集并在 batch.advanced 时遵守 E-284 自动展开只增不减
`packages/web/src/features/run-deck/batch-expansion.ts:25` — 根据所选文档筛选正式批次 DTO
`packages/web/src/features/run-deck/use-batch-tree.ts:124` — 快照取数后筛选文档并重置展开种子
`packages/web/src/features/run-deck/run-deck-view.tsx:278` — 运行甲板批次树接入所选文档
`packages/web/src/features/task-list/task-list-container.tsx:60` — 任务页批次树接入同一所选文档
`packages/daemon/test/integration/batch-summary.test.ts:40` — 集成测试断言空批次、人工停靠、跨批修复、未进 HEAD、事务与并发一致性及 HTTP 响应
`packages/daemon/test/integration/batch-summary.test.ts:348` — 第二个 SQLite 连接持有未提交写事务，验证提交前后正式服务读取一致性
`packages/web/test/batch-tree.test.tsx:408` — 批次树组件测试断言数字渲染非破折号、桌面与移动端一致、E-284 展开交互与 B2 入口缺陷回归
`packages/web/test/batch-tree.test.tsx:634` — 两文档切换时筛选批次并重新初始化展开集
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
1. **权威计数口径（AC 1, AC 2）**：批次级统计严格使用 `deriveTaskState({ manualState: task.manual_state, runs: taskRuns }, undefined, { ignoreWrapupFix: true })` 派生单任务权威状态。其中跨批修复运行（`origin='wrapup-fix'`）不影响已验收任务的 `landed` 归属（E-275）。`taskCount` 为批次下任务总数，`landedCount` 为 `state === 'landed'`，`runningCount` 为 `{starting, running, reviewing, reworking}`，`waitingCount` 为 `{awaiting_human, awaiting_reply, orphaned}`。
2. **默认展开判定（AC 1, E-284, 决策 71）**：`defaultExpanded` 在批次处于 `{running, wrapping, awaiting_landing, needs_attention}` 四种活跃或需关注状态时为 `true`，其余状态为 `false`。前端 `batch-expansion.ts` 接收快照中的 `defaultExpanded` 作为种子；用户手动折叠后不被后续快照静默冲掉；当 `batch.advanced` 推动批次进入上述状态时，重新并入展开集合，保证自动展开只增不减。
3. **接口一致性与并发安全（AC 1, AC 2）**：正式 `GET /api/v1/documents/:docId/batches` 与 `GET /api/v1/snapshot` 统一通过 `computeBatchSummary` 与 `toBatchDto` 产出同构 `BatchDto`。另一 SQLite 连接持有未提交写事务时读取已提交旧值，提交后读取新值。
4. **规避破坏既有测试的契约设计与 Jev 裁决**：`taskCount/landedCount/runningCount/waitingCount/defaultExpanded` 作为可选只读属性加入 `BatchDto`，与 `canWrapup?: boolean` 及 `notInHeadCount?: number` 风格完全统一；在服务端正式产出中始终保证下发确切数值，同时避免因强制必填破坏范围外未 mock 这些字段的历史测试。经 Jev 裁决拍板（model=jev-1.13.0 line=adjudicate: pick=B(high 0.99) spec_change=no(0.12) outside_stack=no(0.05) needs_owner=no(0.05) reversible=yes(0.75) adopt=take(B)），选定方案 B：在 6 个有效路径内以可选契约字段实现，保持范围收敛与完全向后兼容。
5. **正式入口验证与文档切换（AC 3, E-284）**：审查时以正式 daemon 和浏览器配对，HTTP 导入项目文档及第二份独立文档，验证运行甲板和任务页只显示所选文档的批次。第一份文档导入 114 个任务、17 个批次；两端都显示正式 DTO 的数字计数。经正式启动接口产生 `batch.advanced` 后任务页自动展开；刷新后从服务端 `defaultExpanded` 重新展开。独立 SQLite 连接在未提交写事务中并发修改任务状态，服务读取在提交前后分别一致。
<!-- notes:end -->
