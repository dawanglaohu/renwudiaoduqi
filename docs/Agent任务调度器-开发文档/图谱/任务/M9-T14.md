---
type: task
id: M9-T14
module: M9
depends_on: [M9-T4, M4-T5, M4-T2]
unlocks: [M9-T23, M9-T25]
edges: [E-183, E-184, E-185, E-248, E-38, E-88, E-92, E-95]
est_days: 2
branch: task/M9-T14
status: done
---

# M9-T14　设置页：agent 注册表与模型选择

- 所属模块：[[M9]]　**客户端界面**——两端共用的那一套界面本身：多流监看、会话查看、审批与闸门、设置与配对、落地清单、按批次折叠的批次树与执行行呼吸灯、按窗口数呈现的任务流水线泳道与「会话已归档」历史行、顶栏两个流水线开关，以及同一套代码的窄屏降级。它只通过 M2 的契约与 daemon 通信，**界面里不含任何业务判定**（折叠默认值、「能否收口」、泳道分配、阶段推进与会话归档都是 daemon 给的字段）；指派面板的分组模型下拉、登录态徽标与刷新按钮、参照条的「来源」段同样只呈现 daemon 下发的字段，不算取值链
- 前置依赖：[[M9-T4]]、[[M4-T5]]、[[M4-T2]]
- 完成后解锁：[[M9-T23]]、[[M9-T25]]

## 输入 / 产出

- **输入**：M4-T5 的清单；M4-T1 的注册表；M4-T2 的 `session-dir-overlap` 告警
- **产出**：`settings-agents-page` 与其容器

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) 每个字段显示「内置默认 / 你的覆盖 / 当前生效」三行并带「恢复默认」（[[E-92]] 的呈现侧）
2) 模型下拉与桌面共用同一份清单数据，手机端退化为全屏选择器，**不因屏宽裁剪候选项**
3) 清单不全时明示「清单可能不全」并保留手填入口（[[E-38]] 的呈现侧）
4) agent 不可用时置灰并写明缺什么，点击可跳到该项配置（[[E-88]] 的呈现侧）
5) 两字符短码可编辑、保存时唯一性校验，撞车要求改一个（[[E-183]]）
6) 紧凑档或窄屏下字母组必须配合完整名称出现在流头部，长按/悬停出全名，**字母组不得成为唯一标识**（[[E-184]]）
7) 接入第 5/6 个 agent 只需填两字符短码，**不新增任何资源文件**（[[E-185]]）
8) 并行窗口数是本产品自己的设置（默认 2，值域 1–6，按文档持久化），设置项旁注明「此值只属于调度器，与阅读器互不影响」（[[E-248]]）
9) 收到 `reason` 精确等于 `session-dir-overlap` 的告警时显示「会话记录可能互相覆盖」，不按 daemon 的英文开发者 message 直出中文（[[E-95]] 的呈现侧） 10) **呈现侧数据来源与落点（本次审查定点补记，不改已有 1–9 条）**：三层表的「内置默认」「你的覆盖」与顶层生效值只读 daemon 提供的字段——`AgentEntryDto.layers` 与 `PATCH /agents/:id {clearOverrides}` 由 M4-T14 落地，**在此之前 daemon 未提供的层显示「—」**
前端不得自建与 `config/defaults.ts` 平行的注册表副本（含短码／可执行路径等出厂值），也不得按 `agentId` 判「是不是某一家」的分支
「恢复默认」不得把前端算出的默认值当请求体发出（`defaultModel` 之类内置默认非字符串时更不得发）。[[E-92]] 的「内置默认已更新（旧值 → 新值）」与一键采纳只呈现 daemon 提供的升级差异，**前端不得伪造 `defaultUpdates` / `warnings` 这类 daemon 不存在的字段**。[[E-95]] 只按 `reason` 精确等于 `session-dir-overlap` 分支，该原因经 `agent.availability_changed` 事件的 `vendor.reason` 到达（`packages/daemon/src/boot/container.ts`），**不得按 daemon 的英文 message 匹配或直出**。[[E-185]] 指 agent 身份由注册表 `monogram` 文本呈现、接入新 agent 不新增任何资源文件
daemon 没有创建 agent 的路由，故本任务**不提供只改本地 state 的新增入口**。展示组件（`agent-card.tsx` / `model-picker.tsx` 等）落 `packages/web/src/components/`（07 节 components 清单，M9-T23 亦按该落点增量消费），`features/settings-agents/` 只留 `use-〈域〉.ts` 与 `〈域〉-container.tsx`，容器里只许写 grid/flex/gap。

## 必须处理的边界

- [[E-183]]　字母组撞车
    - 触发：两个 agent 取到同一个两字符短码
    - 期望：短码是注册表里**用户可编辑**的字段而非自动首字母推导；保存时做唯一性校验并要求改一个
- [[E-184]]　窄处的身份识别
    - 触发：紧凑档 260px 或手机窄屏
    - 期望：字母组必须配合完整名称出现在流头部；紧凑档可只显字母组，但需长按/悬停出全名，字母组不得成为唯一标识
- [[E-185]]　接入新 agent 的视觉成本
    - 触发：用户接入第五、第六个 agent
    - 期望：只填两字符短码即可上线，不新增任何资源文件
- [[E-248]]　两边窗口数不一致
    - 触发：用户在阅读器里调过 2、调度器是别的值
    - 期望：调度器一律用自己的值，且设置项旁注明「此值只属于调度器，与阅读器互不影响」，防止用户以为改一边等于改两边
- [[E-38]]　列模型命令超时或失败
    - 触发：`grok models` / `pi --list-models` / codex app-server `model/list` 超过注册表 `modelsLive.timeoutMs`（出厂默认 5 秒，codex 出厂 10 秒，按家可覆盖）或退出码非 0
    - 期望：清单降级为「配置文件当前值 + 内置别名 + 该 agent 历史用过的模型 + 手填」，`isComplete:false`、`liveFailure` 说明原因，UI 标注「清单可能不全」，绝不阻塞派发（决策 120）
- [[E-88]]　可执行路径无效
    - 触发：配置里的路径不存在或不可执行
    - 期望：该 agent 标「不可用（路径无效）」，从派发目标下拉中移除，不阻塞其他 agent；点击可直接跳到该项配置
- [[E-92]]　升级后内置默认变了
    - 触发：用户恰好覆盖了同一字段
    - 期望：保留用户覆盖，在该字段旁提示「内置默认已更新（旧值 → 新值）」并给一键采纳
- [[E-95]]　两个 agent 配成同一路径
    - 触发：用户复制配置后没改
    - 期望：允许；会话目录若也相同，daemon 产生 `reason` 精确等于 `session-dir-overlap` 的英文告警但不拒绝保存，设置页按该原因显示「会话记录可能互相覆盖」

## 代码位置

<!-- code:begin -->
`packages/web/src/pages/settings-agents-page.tsx:27` — 设置页装配骨架，只摆栏位并挂容器
`packages/web/src/features/settings-agents/settings-agents-container.tsx:65` — 特性容器：只写 grid/flex/gap，按 agent 组装卡片与窗口数设置
`packages/web/src/features/settings-agents/use-settings-agents.ts:118` — 只按明确来源解析目标文档，定位不到不提供窗口数写入口（AC 8, E-248）
`packages/web/src/features/settings-agents/use-settings-agents.ts:256` — 字段更新走 `ROUTES`/`callRoute`，失败回滚到请求前快照（AC 4, E-88）
`packages/web/src/features/settings-agents/use-settings-agents.ts:273` — 失败回滚与按 `details.field` 挂错，错误文案按 `error.code` 取中文
`packages/web/src/features/settings-agents/use-settings-agents.ts:300` — 窗口数写入失败回滚并就地报错（AC 8, E-248）
`packages/web/src/features/settings-agents/use-settings-agents.ts:340` — 三层值只读 `AgentEntryDto.layers`，缺层显示「—」（AC 1, E-92）
`packages/web/src/features/settings-agents/use-agent-models.ts:51` — 走 `ROUTES`/`callRoute` 拉清单，失败保留旧清单并标 `isComplete=false`（AC 3, E-38）
`packages/web/src/components/field-layers-row.tsx:5` — `AgentFieldKey` / `FieldLayerValues` / `FieldErrorInfo` 三个展示层 props 契约的唯一出处
`packages/web/src/components/field-layers-row.tsx:69` — 「内置默认 / 你的覆盖 / 当前生效」三行表（AC 1）
`packages/web/src/components/field-layers-row.tsx:55` — 内置默认升级差异只呈现 daemon 供给的差异（E-92）
`packages/web/src/components/agent-card.tsx:112` — 按 `unavailableCode` 映射中文标题，英文原因与缺失项进可展开技术详情（AC 4, E-88）
`packages/web/src/components/agent-card.tsx:223` — 只按 `reason === 'session-dir-overlap'` 输出「会话记录可能互相覆盖」（AC 9, E-95）
`packages/web/src/components/agent-card.tsx:142` — 字母组纯文本 chip 与完整名称同屏，短码可编辑（AC 5, AC 6, E-183, E-184, E-185）
`packages/web/src/components/agent-card.tsx:417` — 权限档字段错就地渲染在控件下方
`packages/web/src/components/model-picker.tsx:103` — 桌面下拉与手机全屏共用同一份 props 清单（AC 2）
`packages/web/src/components/model-picker.tsx:274` — 手机端全屏选择器，候选项 `break-all` 不裁剪（AC 2）
`packages/web/src/components/model-picker.tsx:196` — 清单不全提示「清单可能不全」与手填入口（AC 3, E-38）
`packages/web/src/components/model-picker.tsx:165` — 模型字段错就地渲染，英文消息进技术详情
`packages/web/src/components/lane-count-setting.tsx:1` — 窗口数默认值与值域（2 / 1–6）的唯一出处（AC 8）
`packages/web/src/components/lane-count-setting.tsx:29` — 未定位到目标文档时不渲染写入口，只读显示当前值（AC 8, E-248）
`packages/web/src/components/inline-notice.tsx:15` — 加载与就地错误提示展示组件，容器不再自带颜色字号圆角
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
1. 三层配置架构（内置默认 / 你的覆盖 / 当前生效）：三行表只读 daemon 下发的 `AgentEntryDto.layers`，daemon 没给的层显示「—」；前端不保留与 `config/defaults.ts` 平行的出厂值副本，也不按 agentId 判厂商。点「恢复默认」要的是清除覆盖的语义，daemon 侧 `clearOverrides` 尚未落地前不暴露写入口——前端算出的默认值一旦当请求体发出，就会把 dsh 这类出厂路径与模型名改坏。
2. 展示层与容器分工：`agent-card` / `model-picker` / `field-layers-row` / `lane-count-setting` 落 `components/`，props 契约（`AgentFieldKey` / `FieldLayerValues` / `FieldErrorInfo` / `AgentEntryWithLayers`）随之落在展示层各自文件，features 只单向引用；容器只写 grid/flex/gap，加载态与就地错误交给 `inline-notice` 渲染。反向 import 会让组件无法被 run-deck 复用，也会把颜色字号带进容器。
3. 模型清单与手机端呈现（AC 2、AC 3）：窄视口下传统下拉会把超长模型名挤断，全屏浮层采用 44px 触达高度与 `break-all` 换行；`isComplete === false` 时保留手填入口，手填即选中，不让清单读取失败挡住派发（E-38）。
4. 两字符短码（E-183、E-185）：短码是注册表里可编辑的字段而不是首字母推导，前端只做大小写无关的即时排他提示，最终判据仍在 daemon；新 agent 的身份直接用 `monogram` 文本 chip，不引入任何图标或位图，因此 daemon 没有创建路由时也不做只改本地 state 的新增表单——那种表单刷新即丢。
5. 告警与错误文案分层（E-95、E-88、E-92）：`session-dir-overlap` 只按 `reason` 精确匹配后映射中文，绝不拿 daemon 英文 message 当判据；不可用标题按 `unavailableCode` 映射，英文原因与缺失项收进可展开技术详情；字段错按 `error.code` 出中文、按 `details.field` 落到对应控件下方，请求失败先回滚再提示。
6. 窗口数属于调度器（E-248）：默认值与值域集中在 `lane-count-setting`；目标文档只认明确来源（容器传入或本机记录的 lastDocId），定位不到就只读显示并说明原因。早期实现取 `documents` 首条，多文档时会改错文档。
<!-- notes:end -->

---

栈分支 `task/M9-T14` @ `agent-scheduler`　落地进 `main`
