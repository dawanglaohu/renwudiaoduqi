---
type: task
id: M2-T8
module: M2
depends_on: [M2-T6, M3-T2, M4-T4, M6-T1, M8-T4, M8-T6, M9-T2]
unlocks: []
edges: [E-234]
est_days: 1.5
branch: task/M2-T8
status: todo
---

# M2-T8　契约枚举收紧：状态联合与快照数组具名

- 所属模块：[[M2]]　**接入与设备信任**——定义两端如何连上 daemon 并不丢事件地保持同步：一次性配对码与可单独吊销的设备令牌、鉴权与操作归属、REST 骨架与 API 版本、SSE 服务端的事件信封／重放窗口／保活，以及自写的 SSE 客户端。凡「谁能连上、连上之后按什么协议收发」的都归它
- 前置依赖：[[M2-T6]]、[[M3-T2]]、[[M4-T4]]、[[M6-T1]]、[[M8-T4]]、[[M8-T6]]、[[M9-T2]]
- 完成后解锁：无，它不卡别人

## 输入 / 产出

- **输入**：M6-T1 的 `RUN_STATES` 与 `RunState`；M8-T6 的 shared `BatchState`；M9-T2 的 `STATUS_STATES` 形状表；M8-T4 的 `GateDto`；M4-T4 的 `AgentEntryDto`；M3-T2 的 `TaskDto` / `DocumentDto`；M2-T6 的架构测试
- **产出**：`shared/src/api/runs.ts` 导出 `RUN_STATES as const` 与 `RunState`（daemon `domain/run-state-machine.ts` 改为 import 并 re-export，不再本地定义数组）、`RunDto.state: RunState`；`shared/src/api/tasks.ts` 的 `TaskState`（`RunState` 加 `never_dispatched`）与 `TaskDto.state: TaskState`；`shared/src/api/snapshot.ts` 六个数组改 `DocumentDto[] / BatchDto[] / TaskDto[] / RunDto[] / GateDto[] / AgentEntryDto[]`；`SearchRunLogResponse.hits: SearchHitDto[]`、`BatchWrapupDto.findings: WrapupFindingDto[]`；`shared/src/lib/is-record.ts`（全仓唯一 `isRecord`，daemon 与 web 五处改 import）；web `lib/spine-shape.ts` 增 `RUN_STATE_TO_STATUS` 映射表并以 `satisfies` 覆盖全部 `RunState`；`test/arch/shared-contracts.test.ts`；`web/test/spine-shape.test.ts` 增量；daemon 既有 `domain/task-state.ts`、`repo/batches.ts` 只从 shared 引用状态值域；`service/dispatch.ts` 的快照任务状态按 09 节派生，并增快照端点集成断言

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) `packages/shared/src` 里 `unknown[]` 为 0 处、`state: string` 为 0 处（arch 测试 grep 固定，含 `readonly` 变体）（[[E-234]]）
2) `RunState` / `BatchState` / `TaskState` 各只在 shared 定义一处：daemon 与 web 的 `src/` 出现由 `queued`、`starting`、`running` 等状态字面量拼成的联合类型即 arch 测试失败（`test/` 目录除外）
`RUN_STATES.length === 13` 且与 09 节状态机图逐字一致（[[E-234]]）
3) `function isRecord` 全仓恰 1 处，位于 `packages/shared/src/lib/is-record.ts`
`packages/shared` 的 `dependencies` 仍为空（arch 断言）
4) web `RUN_STATE_TO_STATUS` 用 `satisfies` 覆盖 13 个状态，删去任一键即 `tsc` 报错
`STATUS_STATES` 由它派生，形状表与状态枚举一一对应可机检（[[E-234]]）
5) `pnpm -w check` 通过
除纠正旧快照把无人工状态任务误报为 `pending`、并按 09 节完整派生任务状态外，本任务不改运行时行为
既有集成测试基线逐字节不变，新增快照端点集成断言锁住该纠偏

## 必须处理的边界

- [[E-234]]　新增状态没配形状
    - 触发：状态枚举加了一项
    - 期望：形状表与状态枚举**必须一一对应且可机检**，不允许「先上线后补形状」

## 代码位置

<!-- code:begin -->
`packages/shared/src/lib/is-record.ts:1` — 全仓唯一 `isRecord` 纯类型守卫函数定义（AC 3）
`packages/shared/src/api/runs.ts:3` — 导出 `RUN_STATES as const` 与 `RunState` 13 项合法运行状态，收紧 `RunDto.state: RunState`（AC 1, AC 2, E-234）
`packages/shared/src/api/runs.ts:223` — 导出 `SearchHitDto` 并收紧 `SearchRunLogResponse.hits`（AC 1）
`packages/shared/src/api/tasks.ts:3` — 导出 `TaskState` 与 `TASK_STATES`，收紧 `TaskDto.state: TaskState`（AC 1, AC 2）
`packages/shared/src/api/snapshot.ts:8` — 收紧 `SnapshotResponse` 六个数组为具体具名 DTO 数组（AC 1）
`packages/shared/src/api/batches.ts:79` — 导出 `WrapupFindingDto` 并收紧 `BatchWrapupDto.findings`（AC 1）
`packages/shared/src/api/events.ts:175` — 导出 `PlanEntryDto` 并收紧 `PlanPayload.entries` 消除 shared 最后一处 `unknown[]`（AC 1）
`packages/daemon/src/domain/run-state-machine.ts:4` — 改为从 shared 导入并 re-export `RUN_STATES` 与 `RunState`，消除 daemon 本地重复联合定义（AC 2）
`packages/web/src/lib/spine-shape.ts:25` — 声明 `RUN_STATE_TO_STATUS` 映射表并 satisfies 覆盖全部 13 个 `RunState`，派生 `StatusState` 与双向穷尽断言（AC 4, E-234）
`packages/daemon/test/arch/shared-contracts.test.ts:77` — 架构测试断言 shared 中 0 处 `unknown[]` / 0 处 `state: string`、状态联合唯一定义、`isRecord` 全仓唯一与依赖为空（AC 1, AC 2, AC 3, AC 4, E-234）
`packages/web/test/spine-shape.test.ts:236` — 增量单元测试断言 `RUN_STATE_TO_STATUS` 覆盖 13 态且与 `STATUS_STATES` / `STATUS_SHAPES` 1:1 映射（AC 4, E-234）
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
- 契约收敛与单一事实源（AC 2, E-234）：全仓运行状态 `RUN_STATES` 与 `RunState` 统一由 `packages/shared/src/api/runs.ts` 作为唯一权威定义源；daemon 的 `run-state-machine.ts` 纯粹 re-export 消费。`TaskState` 严格建模为 `RunState | 'never_dispatched'`，消除任务状态与运行状态的割裂。
- 编译期与运行时双重闭环（AC 4, E-234）：前端通过 `satisfies Record<RunState, string>` 确保编译期穷尽 13 个后端运行状态，任何后端状态增减若前端未配置对应映射，`tsc -b --noEmit` 直接阻断；`StatusState` 显式联合 `(typeof RUN_STATE_TO_STATUS)[RunState]`，并通过双向类型排除断言 `_assertStatusStatesExhaustive` 与单测保持与 `STATUS_SHAPES` 的 1:1 映射，杜绝「先上线后补形状」。
- 消除弱类型渗漏（AC 1, AC 3）：`packages/shared/src` 中彻底清除全部 `unknown[]`（包含 `snapshot.ts` 六大数组、`SearchRunLogResponse.hits`、`BatchWrapupDto.findings` 以及 `PlanPayload.entries`）和 `state: string`，以架构测试 grep 机制硬性锁定。全仓工具函数遵循 08 节禁令，仅在 `packages/shared/src/lib/is-record.ts` 保留全仓唯一的 `isRecord` 纯类型守卫。
<!-- notes:end -->

---

栈分支 `task/M2-T8` @ `agent-scheduler`　落地进 `main`
