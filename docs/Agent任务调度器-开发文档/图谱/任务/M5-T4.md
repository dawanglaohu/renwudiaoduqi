---
type: task
id: M5-T4
module: M5
depends_on: [M5-T3]
unlocks: [M9-T16]
edges: [E-73, E-74]
est_days: 1
branch: task/M5-T4
status: done
---

# M5-T4　落地清单与 worktree 处置

- 所属模块：[[M5]]　**工作区与 Git**——为每个任务准备、隔离与回收独立 worktree，从中读出改动（改动文件数、diff stat、diff 正文）并生成只读落地清单。凡要碰 git 或任务工作目录的都归它
- 前置依赖：[[M5-T3]]
- 完成后解锁：[[M9-T16]]

## 输入 / 产出

- **输入**：M5-T3 的 diff
- **产出**：`workspace/landing.ts`、`http/routes/tasks.ts`（含 `landing` 与 `worktree/cleanup` 两个端点）

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) 输出 worktree 绝对路径、分支名、diff 摘要、**可一键复制的 `gh stack` 命令文本**与一行可复制的 `python 〈docsPath〉/_run/build_docs.py 〈docsPath〉 --landed 〈任务ID〉`（`docsPath` 取文档记录的目录），**复制而不执行**，产品不调用任何文档维护脚本（[[E-74]]、决策 68）
2) 任务结束后 worktree 默认保留，只在用户显式点「清理」时 `worktree remove`
已清理过的任务重试需重建（[[E-73]]）
3) 清单为只读，产品不执行任何提交／push／PR／合并

## 必须处理的边界

- [[E-73]]　任务结束后 worktree 处置
    - 触发：任务跑完，worktree 仍在磁盘
    - 期望：默认保留（人还要用它落地），只在用户显式点「清理」时 `worktree remove`；已清理过的任务重试需重建
- [[E-74]]　用户要落地
    - 触发：任务已验收，准备合并
    - 期望：提供只读落地清单（worktree 绝对路径、分支名、diff 摘要、可一键复制的 `gh stack` 命令文本），复制而不执行

## 代码位置

<!-- code:begin -->
`packages/daemon/src/workspace/landing.ts:108` — generateLandingCommands：生成只读且可一键复制的 gh stack 命令文本与 python build_docs.py --landed 〈任务ID〉 文本，规范化路径斜杠，产品不执行任何脚本（E-74、决策 68）
`packages/daemon/src/workspace/landing.ts:269` — getTaskLanding：解析任务与文档元数据，输出 worktree 绝对路径、分支名、只读 diff 摘要与复制命令数组，缺失或已清理 worktree 报 E_NOT_FOUND（E-73、E-74）
`packages/daemon/src/workspace/landing.ts:382` — cleanupTaskWorktree：仅在用户显式请求时通过 removeWorktree 回收工作区并执行 git worktree prune，保留分支不删，幂等处理已移除状态（E-73）
`packages/daemon/src/workspace/landing.ts:436` — createLandingService：封装工作区落地清单与处置门面，供 service 层装配
`packages/daemon/src/service/landing.ts:20` — LandingService 接口：service 层落地清单接缝，路由与容器只依赖它，不直接依赖 workspace 与 repo（08 节分层）
`packages/daemon/src/service/landing.ts:30` — createLandingService：把 workspace/landing.ts 的门面装配成容器里的 services.landing
`packages/daemon/src/http/routes/tasks.ts:13` — taskRouteParamsSchema：严格校验 taskId 参数，additionalProperties:false
`packages/daemon/src/http/routes/tasks.ts:32` — resolveLandingService：从容器 services.landing 取服务，缺失报 E_INTERNAL，路由不自建 repo 与 git runner
`packages/daemon/src/http/routes/tasks.ts:65` — registerTasksRoutes：注册 GET /api/v1/tasks/:taskId/landing（只读落地清单，E-74）与 POST /api/v1/tasks/:taskId/worktree/cleanup（显式工作区清理，E-73）路由
`packages/daemon/src/http/plugins/50-routes.ts:52` — registerTasksRoutes(target)：把两个端点接进路由插件，并在 customRegisteredPaths 登记，避免落进「未实现」占位
`packages/daemon/src/boot/container.ts:223` — 容器装配 landing 服务：注入 tasksRepo、documentsRepo、platform、hostInputs、ids
`packages/daemon/test/unit/landing.test.ts:142` — generateLandingCommands 纯单元测试：验证命令格式与路径规范化（E-74、决策 68）
`packages/daemon/test/unit/landing.test.ts:182` — getTaskLanding 验收与边界用例：覆盖只读约束、绝对路径、diff 摘要与缺失工作区 E_NOT_FOUND 报错（AC 1、AC 3、E-73、E-74）
`packages/daemon/test/unit/landing.test.ts:382` — cleanupTaskWorktree 用例：覆盖显式回收、默认保留、分支留存与幂等清理（AC 2、E-73）
`packages/daemon/test/unit/landing.test.ts:513` — Fastify HTTP 路由契约用例：覆盖 landing 与 worktree/cleanup 端点及错误信封与参数校验
`packages/daemon/test/unit/landing.test.ts:690` — 路由接线回归用例：经 routesPlugin 以 /api/v1 前缀注册后命中真实 handler，防端点漏接线后回落到「未实现」占位
`packages/daemon/test/unit/landing.test.ts:736` — 真实 Git 仓库全生命周期集成测试：验证准备工作区、改动提取、只读落地清单、显式清理与重试重建完整链路（E-73、E-74）
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
1. 落地清单的只读性与不代行 Git/文档操作（E-74、决策 68）：
落地清单（`GET /api/v1/tasks/:taskId/landing`）只提取 worktree 绝对路径、分支名、diff 统计和一键复制命令，产品严格不调用任何提交、push、PR 或文档脚本。命令文本中的 `<docsPath>` 动态读取自任务关联文档记录的 `docs_path`，确保跨平台与非标准文档路径的一致性。

2. 工作区默认保留与显式清理（E-73）：
任务在执行或审查完成后，工作区默认驻留磁盘，供人工审查和后续落地复制使用；只有用户显式发起 `POST /api/v1/tasks/:taskId/worktree/cleanup` 时才调用 `removeWorktree`。清理默认 `deleteBranch: false`，仅回收磁盘上的 worktree 目录并执行 `git worktree prune`，不删除分支引用；若用户后续重试该任务，底层工作区分配机制（M5-T1）会探测分支并自动生成序号后缀（如 `task/ID-2`）重新建立隔离 worktree，避免分支冲突与历史覆盖。

3. 工作区缺失与清理后的错误语义：
若任务工作区从未创建或已被用户显式清理，`getTaskLanding` 抛出类型化 `E_NOT_FOUND` 而非 `E_WORKSPACE_UNAVAILABLE`，对齐 HTTP 路由错误码白名单（`E_UNAUTHORIZED`、`E_DEVICE_REVOKED`、`E_NOT_FOUND`、`E_INTERNAL`），明确指示该任务不可直接落地、需重建工作区。

4. 分层接缝与路由接线：
git 与工作区操作全部留在 `workspace/landing.ts`，`service/landing.ts` 只做装配，路由层只认 `LandingService`、不再自建 repo 与 git runner，容器独占依赖注入（08 节「http/routes 禁止跨层直调 workspace/repo」）。路由必须显式接进 `50-routes.ts` 的 `customRegisteredPaths`；否则端点会被登记表里的「未实现」占位吞掉，架构测试比较 method+path 集合照样通过，功能却是死的——接线回归用例就是为了盯住这一点。
<!-- notes:end -->

---

栈分支 `task/M5-T4` @ `agent-scheduler`　落地进 `main`
