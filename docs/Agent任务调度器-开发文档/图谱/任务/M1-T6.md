---
type: task
id: M1-T6
module: M1
depends_on: [M1-T1, M1-T3]
unlocks: [M10-T2]
edges: [E-01, E-02, E-123, E-210, E-212]
est_days: 1.5
branch: task/M1-T6
status: todo
---

# M1-T6　开机自启注册机制与优雅退出对账

- 所属模块：[[M1]]　**运行底座**——让 daemon 在这台机器上稳定跑起来并把字节落到磁盘上：进程单实例与端口、启动自检、开机自启注册、数据目录、结构化存储的连接与迁移、日志正文的追加写与字节偏移索引、以及全部平台差异（进程树终止、路径规范化与长路径、配置目录、子进程编码）的唯一收口。凡与操作系统、磁盘、进程存活打交道而不带任何业务概念的都归它
- 前置依赖：[[M1-T1]]、[[M1-T3]]
- 完成后解锁：[[M10-T2]]

## 输入 / 产出

- **输入**：M1-T3 的 platform
- **产出**：`boot/autostart.ts`、`shutdown()`、`reconcile-runs` job

## 验收标准

> 逐条都要满足，这是验收时的唯一依据。

1) `register(execPath)` 幂等，重复调用不产生第二个自启项
注册失败为非致命，daemon 仍可被当场拉起（[[E-210]]）
2) 不装壳、直接命令行起 daemon 时全部功能照常，自启从未注册（[[E-212]]）
3) 优雅退出先停 jobs 再关 HTTP，且**不杀 agent 子进程**（[[E-01]]）
4) 重启后按 `runs.pid` 对账：进程不存在标「异常终止」、存在但接不回标「失联」，**绝不推定成功**（[[E-02]]、[[E-123]]）

## 必须处理的边界

- [[E-01]]　桌面 UI 关闭但任务在跑
    - 触发：用户关闭或桌面壳崩溃，agent 会话仍在运行
    - 期望：daemon 继续收流并在完成时发通知；桌面重开后从 daemon 全量拉状态，不重放本地缓存
- [[E-02]]　daemon 重启后会话失联
    - 触发：daemon 被杀或机器重启，agent 会话还活着
    - 期望：按持久化的 task↔session 映射重新 attach；attach 不上的标「失联」而非「失败」，由用户决定是否重派
- [[E-123]]　daemon 重启后的孤儿记录
    - 触发：重启后仍有状态为「运行中」的记录
    - 期望：启动时按 PID 对账，进程不存在则标「异常终止（daemon 重启）」，绝不推定成功
- [[E-210]]　注册被系统拒绝
    - 触发：UAC 拒绝或无权限写计划任务
    - 期望：注册失败**必须非致命**：daemon 仍由壳当场拉起、功能完整，仅重启后需手动开壳；设置页提供「重试注册自启」按钮
- [[E-212]]　完全不装壳直接起 daemon
    - 触发：用命令行启动
    - 期望：自启从未注册，**全部功能照常**；M1 只提供机制不自动执行

## 代码位置

<!-- code:begin -->
_（**落地前必须回填**。一行一处，格式：`路径:行号` — 说明）_
<!-- code:end -->

## 实施沉淀

<!-- notes:begin -->
_（**落地前必须回填**：怎么实现的、为什么这么选、踩了什么坑）_
<!-- notes:end -->

---

栈分支 `task/M1-T6` @ `agent-scheduler`　落地进 `main`
