---
type: edge
id: E-328
module: M7
covered_by: [M7-T8]
---

# E-328　查 bug 运行的 agent 取值与不可用

| | |
|---|---|
| 触发条件 | 派查 bug 运行时 |
| 期望行为 | 逐字复制该任务最近一次审查判 pass 的 `kind='implement'` 运行（含 `origin=rework`／`wrapup-fix`）派发快照 `launch_spec_json` 里的 agent/model/effort，不读注册表当前默认（E-93）；model 或 effort 为空同样为空透传、显示「—」；推导出的 agent 不可用（已禁用／探测失败／未安装）→ 不派、不换家，实施行转 `awaiting_human` 并在闸门 comment 写 `bughunt_agent_unavailable`；查 bug 会话恒为该任务新建的会话、`vendor_session_ref` 不得等于该任务实施／审查运行的引用；该 agent 并发已满 → 查 bug 运行 `queued`（`agent_limit_reached`），任务继续占原泳道、实施行仍 `reviewing`；实施 agent 是 dsh 照派，运行轨三段（E-253） |
| 归属模块 | [[M7]] |

## 被这些任务兜底

- [[M7-T8]]　查 bug 阶段运行与结果解析

## 验证记录

<!-- notes:begin -->
_（这条边界实测怎么验的、验过没有）_
<!-- notes:end -->
