---
type: edge
id: E-312
module: M8
covered_by: [M8-T8, M9-T22]
---

# E-312　收口模式为手动

| | |
|---|---|
| 触发条件 | `pipeline.wrapupMode='manual'` 且本批全部 landed 并进 HEAD |
| 期望行为 | tick 第 2 步不调 `triggerWrapup`；批次留在 `running`（分支未全进 HEAD 时是 `awaiting_landing`），`canWrapup=true` 且标题追加「可收口」暖色；只有 `POST /batches/:id/wrapup` 能进 `wrapping`；下一批仍等本批 `done`（E-281）；切回自动后下一次 tick 立即按 E-283 自动派 |
| 归属模块 | [[M8]] |

## 被这些任务兜底

- [[M8-T8]]　流水线槽位、补位与阶段设置
- [[M9-T22]]　顶栏流水线开关与流水线设置页

## 验证记录

<!-- notes:begin -->
_（这条边界实测怎么验的、验过没有）_
<!-- notes:end -->
