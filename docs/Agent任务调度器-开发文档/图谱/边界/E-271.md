---
type: edge
id: E-271
module: M1
covered_by: [M1-T3]
---

# E-271　平台数据目录输入缺失或不是绝对路径

| | |
|---|---|
| 触发条件 | Windows `APPDATA` 缺失/相对，Linux `XDG_DATA_HOME` 缺失/相对，或主目录含空格与 Unicode |
| 期望行为 | boot 注入冻结的宿主输入；Windows 只采用绝对 `APPDATA`，否则回落绝对 `homedir/AppData/Roaming`；macOS 用绝对 `homedir/Library/Application Support`；Linux 只采用绝对 `XDG_DATA_HOME`，否则回落绝对 `homedir/.local/share`，再追加应用目录。不得把 `~`、`${...}`、`%...%` 当路径交给文件系统；主目录本身不绝对则返回 `E_DATA_DIR_UNRESOLVABLE`。**`~` 禁令只针对词首的波浪号展开符**（`~`、`~user`、`~/x`）——shell 的波浪号展开也只在这种情况下发生；出现在路径中间的 `~` 是字面文本，最典型的是 Windows 8.3 短名（`C:\Users\RUNNER~1\…`、`C:\PROGRA~1\…`），把这类绝对路径判为未展开会让 daemon 在普通 Windows 机器上直接拒绝启动。实现按 `/^~/` 判定，`${...}` 与 `%...%` 两条规则不变 |
| 归属模块 | [[M1]] |

## 被这些任务兜底

- [[M1-T3]]　三平台路径、数据目录与可执行文件解析

## 验证记录

<!-- notes:begin -->
_（这条边界实测怎么验的、验过没有）_
<!-- notes:end -->
