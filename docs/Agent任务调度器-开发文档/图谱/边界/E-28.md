---
type: edge
id: E-28
module: M4
covered_by: [M4-T12]
---

# E-28　DeepSeek Harness 的破坏性变更

| | |
|---|---|
| 触发条件 | 官方声明会有破坏性变更且 `SESSION_FORMAT_VERSION = 0`。**2026-09-01 实测**：`--profile headless` 是唯一可用通道；`--profile acp` **不存在**——`@deepseek-ai/dsh-acp` 各版本均无 `dsh.bundle` 字段（是库不是 profile 层），`latest` 指向的 `0.0.1-rc.1` 依赖 404 装不上 |
| 期望行为 | 走 `--profile headless` 原生适配（第一版内置的第五家），**版本钉死**；启用前必须先跑探测 + 冒烟任务，通过才允许进批次派发；启动失败或非 0 退出即标该 agent 不可用并移出选择器，绝不影响其余四家与整批推进 |
| 归属模块 | [[M4]] |

## 被这些任务兜底

- [[M4-T12]]　dsh headless 适配器与通用 ACP 扩展槽

## 验证记录

<!-- notes:begin -->
_（这条边界实测怎么验的、验过没有）_
<!-- notes:end -->
