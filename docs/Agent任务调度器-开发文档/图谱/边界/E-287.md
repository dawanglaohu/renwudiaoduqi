---
type: edge
id: E-287
module: M8
covered_by: [M8-T6]
---

# E-287　自动收口的 agent 取值

| | |
|---|---|
| 触发条件 | tick 自动触发收口，无人在场选 agent |
| 期望行为 | 第 1 轮取本批 `ended_at` 最晚的 landed 实施运行快照里的 agent/model/effort（并列取 run id 字典序最大）；第 2 轮起沿用上一轮收口运行的取值；推导出的 agent 不可用则不派、批次 `needs_attention`（原因 `agent_unavailable`），人用手动触发带 `agentId` 覆盖；禁止静默换家 |
| 归属模块 | [[M8]] |

## 被这些任务兜底

- [[M8-T6]]　批次收口触发、轮次与批次级闸门

## 验证记录

<!-- notes:begin -->
_（这条边界实测怎么验的、验过没有）_
<!-- notes:end -->
