---
type: edge
id: E-327
module: M8
covered_by: [M8-T8]
---

# E-327　人打回后重新入道

| | |
|---|---|
| 触发条件 | 停靠在 `awaiting_human` 的任务被人打回（附意见） |
| 期望行为 | 当场 decide 闸门、意见落 `rework_text`（source=human）、运行迁 `reworking`，计数照加不受 E-55 拦截；返工进入可放行集合并**排在全部新任务之前**（多个返工按打回时间先后），有空泳道当场入道（泳道号可与原先不同）再按 M7-T5 三分支投递；等泳道期间 `lanes[]` 不含它、批次树该行显示「返工排队 · 等泳道」静态点不呼吸、审批卡已关闭；入道时进程已死或 daemon 重启过则按快照能力位恢复（E-112）或新开 `origin=rework`（E-279），worktree 已清理走 E-277；批次 `paused` 时不入道并显示「批次已暂停 · 返工排队」，恢复后先于新任务入道；返工再次退出后续接同一审查会话第 N+1 轮（未归档） |
| 归属模块 | [[M8]] |

## 被这些任务兜底

- [[M8-T8]]　流水线槽位、补位与阶段设置

## 验证记录

<!-- notes:begin -->
- 2026-09-23，分支 `fix/136-rework-delivery`（#136 生产返工投递接线落地后）：真 `createContainer` + 公开 `POST /api/v1/gates/:id/decide{decision:'reject'}` + `dispatch.tick()` + 可观察假进程，五条端到端断言全绿 —— `packages/daemon/test/integration/container-wiring.test.ts`：
  1) 有空槽当场入道并恢复已结束的原生 codex 会话：新运行 `origin=rework`、`state='running'`、继承 `vendor_session_ref` 与 `rework_count=1`，假进程 argv 为 `exec --json … resume <vendor_session_ref> <打回意见>`；
  2) 快照能力位为 `generic-acp`（不能回灌也不能恢复）时新开实施运行，自包含提示词（返工原文 + 「收到返工指令时」段 + 工作区指针 + 不 commit/push）落在 argv 末位；
  3) 满槽时排队不投递（`tasks.lane_no` NULL、运行 `reworking` 且 `queued_reason='lane_full'`、无新进程），槽位空出后下一次 tick 先于新任务把返工投递出去；
  4) 批次 `paused` 时排队（`queued_reason='batch_paused'`），批次恢复后由 tick 投递；
  5) 返工运行退出码 0 后出现 `kind='review'`、`parent_run_id` = 返工运行的新审查行（第 N+1 轮续接）。
- 计数语义（人工决定当场只加一次、补位不重复加、新运行继承该值）另由 `packages/daemon/test/unit/rework-dispatch.test.ts` 与 `packages/daemon/test/integration/pipeline-lanes.test.ts` 的公开闸门入口回归覆盖；无投递器、投递未确认两种失败都返回类型化 `undeliverable` 而不是无人消费的 `handover`。
<!-- notes:end -->
