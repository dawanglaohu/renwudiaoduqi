---
type: edge
id: E-213
module: M1
covered_by: [M1-T1]
---

# E-213　未 await 的 Promise 静默失败

| | |
|---|---|
| 触发条件 | Biome 无类型感知规则，查不出 `no-floating-promises` 这类问题 |
| 期望行为 | 兜底走运行期：daemon 注册 `process.on('unhandledRejection')` 落运行日志；**所有 fire-and-forget 必须显式写 `void fn()`**。被这类 bug 咬过两次才单独加 typescript-eslint 的这一条规则，而不是引入整套 ESLint |
| 归属模块 | [[M1]] |

## 被这些任务兜底

- [[M1-T1]]　仓库骨架、进程入口与最小常驻 daemon 闭环

## 验证记录

<!-- notes:begin -->
_（这条边界实测怎么验的、验过没有）_
<!-- notes:end -->
