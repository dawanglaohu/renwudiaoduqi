---
type: edge
id: E-318
module: M8
covered_by: [M8-T8, M9-T22]
---

# E-318　流水线设置存储缺行或损坏

| | |
|---|---|
| 触发条件 | `settings` 表无 key=pipeline 行，或 `value_json` 非法／含未知值 |
| 期望行为 | 缺行返回内置默认 `{bughunt: 0, wrapupMode: 'auto'}` 不插行；非法回落默认并 warn，不退出、不覆盖坏值直到下一次 PATCH；PATCH 两字段整体写入，缺字段或值非法返回 `E_VALIDATION`；写成功发 `settings.pipeline_changed` 让另一端刷新 |
| 归属模块 | [[M8]] |

## 被这些任务兜底

- [[M8-T8]]　流水线槽位、补位与阶段设置
- [[M9-T22]]　顶栏流水线开关与流水线设置页

## 验证记录

<!-- notes:begin -->
_（这条边界实测怎么验的、验过没有）_
<!-- notes:end -->
