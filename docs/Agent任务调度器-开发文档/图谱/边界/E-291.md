---
type: edge
id: E-291
module: M1
covered_by: [M8-T6]
---

# E-291　迁移需要整表重建

| | |
|---|---|
| 触发条件 | 扩枚举 CHECK 或放宽 NOT NULL，SQLite 只能 12 步法重建，且外键开着时 DROP 会失败 |
| 期望行为 | 首行标 `-- requires: foreign_keys=off` 的迁移在事务外先关外键、事务内重建、执行后 `PRAGMA foreign_key_check` 必须零行否则回滚并以非 0 退出、finally 开回；标记不在首行按普通迁移执行；重建不算改已应用文件 |
| 归属模块 | [[M1]] |

## 被这些任务兜底

- [[M8-T6]]　批次收口触发、轮次与批次级闸门

## 验证记录

<!-- notes:begin -->
_（这条边界实测怎么验的、验过没有）_
<!-- notes:end -->
