---
type: edge
id: E-338
module: M4
covered_by: [M4-T14, M9-T23]
---

# E-338　`live` 成功但清单不含配置当前值

| | |
|---|---|
| 触发条件 | `grok models`／`pi --list-models`／codex `model/list` 返回成功，但 `config.toml`／`settings.json` 里的当前模型名不在其中（用户手填了别名或已下线模型） |
| 期望行为 | 仍以 `source:'config'` 单独列出该项、标「当前配置 · 清单未列」并作默认选中；不改写、不做别名归并；选它派发原样透传，失败按 E-36 |
| 归属模块 | [[M4]] |

## 被这些任务兜底

- [[M4-T14]]　实时清单来源、来源标注与思考强度取值域
- [[M9-T23]]　指派面板的登录态、清单来源与刷新

## 验证记录

<!-- notes:begin -->
_（这条边界实测怎么验的、验过没有）_
<!-- notes:end -->
