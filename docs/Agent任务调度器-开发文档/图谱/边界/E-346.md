---
type: edge
id: E-346
module: M4
covered_by: [M4-T13]
---

# E-346　厂商改了列模型或登录状态命令的输出格式

| | |
|---|---|
| 触发条件 | `grok models` 文本、`pi --list-models` 表头、codex `model/list` 字段、`codex login status` 文本、`claude auth status` JSON 键、`pi auth check` 的 `status` 取值任一变化 |
| 期望行为 | 清单侧沿用 E-39：解析不了即 `live` 缺失、`rawStdout`（脱敏后）保留在可展开日志里不静默吞掉；登录侧输出不匹配一律 `unknown`（reason `unparsable`）而不是 `logged_out`，不禁用派发；探测命令、匹配模式与 `loginCommandHint` 在注册表数据层，改数据不改代码；codex／grok 的未登录形态本机未实测，对应 fixture 标 `synthetic` |
| 归属模块 | [[M4]] |

## 被这些任务兜底

- [[M4-T13]]　登录态探测与刷新

## 验证记录

<!-- notes:begin -->
_（这条边界实测怎么验的、验过没有）_
<!-- notes:end -->
