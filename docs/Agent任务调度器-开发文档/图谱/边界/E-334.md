---
type: edge
id: E-334
module: null, runId, reason}`、`lane.released{docId, laneNo, taskId
covered_by: [M8-T8]
---

# E-334　事件 scope 与过滤

| | |
|---|---|
| 触发条件 | 发本产品组事件、`GET /events?docId` 过滤 |
| 期望行为 | 信封 `scope` 八值 `run` / `task` / `batch` / `agent` / `system` / `lane` / `settings` / `document`，规则钉死为「本产品组 kind 的前缀就是信封 scope」并由 TS 类型断言机检（kind 的前缀字面量类型必须等于 scope）；`lane.assigned{docId, laneNo, taskId |
| 归属模块 | null, runId, reason}`、`lane.released{docId, laneNo, taskId |

## 被这些任务兜底

- [[M8-T8]]　流水线槽位、补位与阶段设置

## 验证记录

<!-- notes:begin -->
_（这条边界实测怎么验的、验过没有）_
<!-- notes:end -->
