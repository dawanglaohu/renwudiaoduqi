---
type: edge
id: E-203
module: M4
covered_by: [M1-T7, M4-T10]
---

# E-203　pi 的 RPC 行读取

| | |
|---|---|
| 触发条件 | 输出中出现 `U+2028` / `U+2029` |
| 期望行为 | **必须自写只按 `\n` 切的读取器，禁用 Node `readline`**——那两个字符在 JSON 字符串里合法但会被 readline 断行；须配一条含 `U+2028` 输出的回归用例 |
| 归属模块 | [[M4]] |

## 被这些任务兜底

- [[M1-T7]]　子进程 spawn 基座与行读取器
- [[M4-T10]]　pi 原生适配器（RPC 模式）

## 验证记录

<!-- notes:begin -->
_（这条边界实测怎么验的、验过没有）_
<!-- notes:end -->
