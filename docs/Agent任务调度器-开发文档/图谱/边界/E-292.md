---
type: edge
id: E-292
module: M8
covered_by: [M8-T4]
---

# E-292　闸门设置存储缺行或损坏

| | |
|---|---|
| 触发条件 | `settings` 表无 key=gates 行，或 `value_json` 非法/含未知值 |
| 期望行为 | 缺行返回内置默认 `{dispatch:auto, review:manual, landing:manual}` 不插行；非法值回落默认并 warn，不退出、不覆盖坏值直到下一次 PATCH；PATCH 三字段整体写入，缺字段或值不在 auto／manual 返回 `E_VALIDATION`；写成功发 `settings.gates_changed` 让另一端刷新 |
| 归属模块 | [[M8]] |

## 被这些任务兜底

- [[M8-T4]]　三个闸门与预设组合

## 验证记录

<!-- notes:begin -->
_（这条边界实测怎么验的、验过没有）_
<!-- notes:end -->
