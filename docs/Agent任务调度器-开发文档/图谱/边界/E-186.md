---
type: edge
id: E-186
module: M4
covered_by: [M4-T8]
---

# E-186　原生通道破坏性变更

| | |
|---|---|
| 触发条件 | codex app-server 官方明说 may change without notice |
| 期望行为 | 在数据层注册表把该 agent 的适配器种类由 `native` 切为 `generic-acp`，**不改代码不重打包**；能力位随之收窄并由 UI 灰掉对应按钮；在途任务按 E-02 语义标「失联」而非「失败」 |
| 归属模块 | [[M4]] |

## 被这些任务兜底

- [[M4-T8]]　codex 原生适配器

## 验证记录

<!-- notes:begin -->
在 `packages/daemon/test/unit/codex-adapter.test.ts` 中通过 `AC 4 & AC 3: 能力位与破坏性变更切 generic-acp (E-186)` 验证：仅需将注册表数据条目的 `adapterKind` 由 `'native'` 改为 `'generic-acp'`，`getCodexCapabilities` 立即将 `canReply` 置 false、`canResume` 置 false 并将 `sessionHistory` 降为 `'current-run-only'`，同时 `buildLaunchSpec` 自动附加 `isAcp: true`，无需任何代码重编译或打包修改即可完成降级。
<!-- notes:end -->
