---
type: edge
id: E-289
module: M5
covered_by: [M5-T5]
---

# E-289　分支本地已不存在时判进 HEAD

| | |
|---|---|
| 触发条件 | landed 运行的分支被 `git branch -d`/`-D` 删除 |
| 期望行为 | 用 tick 记录的 `branch_tip_sha` 做同一套祖先/无差异判定；都否 → 未进（method=branch_gone_unmerged）并给出 `git branch 〈原分支〉 〈sha〉` 可复制命令；无记录 SHA 且 worktree 已清理 → 视为进 HEAD（method=branch_gone）并暴露 method；worktree 仍在则用其 HEAD SHA 判定 |
| 归属模块 | [[M5]] |

## 被这些任务兜底

- [[M5-T5]]　分支进 HEAD 判定与分支尖 SHA 记录

## 验证记录

<!-- notes:begin -->
_（这条边界实测怎么验的、验过没有）_
<!-- notes:end -->
