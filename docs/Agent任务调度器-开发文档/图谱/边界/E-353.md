---
type: edge
id: E-353
module: M4
covered_by: [M4-T13]
---

# E-353　登录态探测不读凭据

| | |
|---|---|
| 触发条件 | 注册表 `loginProbe.args` 被填成输出凭据的命令；代码里有人读凭据文件 |
| 期望行为 | 注册表校验：`loginProbe.args` 含 `print-api-key`／`print-bearer-token`／`--credentials` 任一子串 → `invalid-config` 拒绝该条并 warn；`test/arch/registry-forbidden-args.test.ts` 扫 `defaults.ts` 兜底；arch 测试扫全仓禁止出现 `auth.json`／`credentials.json`／`.credentials` 路径串（`config/registry.ts` 读 `agents.json` 除外）；`LoginState` 只含枚举、时间、`loginCommand` 与 `providers` 子表，禁止带 stdout／stderr 原文；全仓除 `adapters/login-probe.ts` 与 `adapters/grok/login-patterns.ts` 外禁止出现 `logged in`／`loggedIn`／`not_ready`／`auth check` 字面量 |
| 归属模块 | [[M4]] |

## 被这些任务兜底

- [[M4-T13]]　登录态探测与刷新

## 验证记录

<!-- notes:begin -->
_（这条边界实测怎么验的、验过没有）_
<!-- notes:end -->
