---
type: edge
id: E-337
module: M4
covered_by: [M4-T13]
---

# E-337　claude `loggedIn:false` 但 API key 或网关可用

| | |
|---|---|
| 触发条件 | `claude auth status` 返回 `loggedIn:false`（本机实况），而 `ANTHROPIC_API_KEY` 或网关让 Claude Code 正常工作 |
| 期望行为 | 徽标照显示「未登录」+ 登录命令，派发不禁用；**不做「运行成功即改判 `logged_in`」的推断**（登录态只由 CLI 判定，缓存只由 `refreshLogin()` 写）；`apiProvider` 字段原样进 `LoginState.vendor` 供 title 显示，不参与判定 |
| 归属模块 | [[M4]] |

## 被这些任务兜底

- [[M4-T13]]　登录态探测与刷新

## 验证记录

<!-- notes:begin -->
_（这条边界实测怎么验的、验过没有）_
<!-- notes:end -->
