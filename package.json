{
	"name": "agent-scheduler",
	"private": true,
	"packageManager": "pnpm@9.15.9",
	"engines": {
		"node": ">=22"
	},
	"scripts": {
		"check": "biome ci . && tsc --noEmit -p tsconfig.json && vitest run",
		"test": "vitest run",
		"format": "biome format --write .",
		"lint": "biome lint ."
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4",
		"@types/node": "^22.10.2",
		"typescript": "^5.7.2",
		"vitest": "^2.1.8"
	}
}
